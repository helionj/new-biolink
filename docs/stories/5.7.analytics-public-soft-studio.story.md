# Story 5.7: Analytics + Public — Soft Studio Refresh

## Status

Done

## Executor Assignment

```yaml
executor: '@dev' # Tasks 1-12 (audit 1 page-level Analytics `app/dashboard/analytics/page.tsx` + 5 components `components/analytics/{MetricsCards,TimeSeriesChart,LinksTable,EmptyAnalyticsState,AnalyticsLoadingSkeleton}.tsx` + 2 components `components/public/{PublicPage,PublicLinkCard}.tsx` consumindo tokens Story 5.2 + primitives Story 5.3 + insights Stories 5.4/5.5/5.6)
quality_gate: '@qa'
quality_gate_tools: [typecheck, lint, build, test:components]
complexity: 'M' # Frontend-primary. Superfície: 1 page-level (`app/dashboard/analytics/page.tsx` H1 + subtitle) + 5 analytics components (MetricsCards typescale + plum number; TimeSeriesChart radius + peach gradient under line via `<AreaChart>` switch; LinksTable radius + opcional inline bar; EmptyAnalyticsState radius + typescale + copy reconcile; AnalyticsLoadingSkeleton radius alignment) + 2 public components (PublicPage h1 `text-h1 text-primary text-center` + bio `text-body text-center`; PublicLinkCard `hover:shadow-md transition-shadow`). Zero migration, zero RLS, zero schema (analytics SQL views/RPC Stories 4.1-4.3 intactos; public ISR + ViewBeacon + TrackedLinkCard Stories 2.7/4.1/4.2 intactos). Zero Server Action novo (analytics page Server Component reads Stories 4.1-4.4 preservados; `revalidateUserSurface` preservado). Tokens canônicos já em `app/globals.css` pós-5.2 (`--text-h1` 32px + `--text-h2` 24px + `--text-body` + `--radius-xl` 16 + `--primary` #5b3a8c plum + `--accent` #ffb5a7 peach) + primitives shadcn refreshed pós-5.3 (Card +`shadow-sm warm-tinted` consumido em MetricsCards automatic). Mudanças localizadas: Analytics page H1 + subtitle (linha 140); MetricsCards big number `text-3xl` → `text-h2 text-primary` (linha 26); TimeSeriesChart `<LineChart>` → `<AreaChart>` + `<defs><linearGradient>` peach under + figcaption `text-sm` → `text-h3 font-medium` + figure `rounded-lg` → `rounded-xl`; LinksTable `rounded-lg` → `rounded-xl` (+ inline bar CONDITIONAL DEV-A); EmptyAnalyticsState `rounded-lg` → `rounded-xl` + H2 `text-lg font-semibold` → `text-h3 font-medium` + body `text-sm` → `text-body` + copy reconcile spec L801 verbatim; AnalyticsLoadingSkeleton `rounded-md` → `rounded-xl`; PublicPage h1 `text-2xl font-semibold tracking-tight` → `text-h1 text-primary text-center` + bio `text-center text-muted-foreground` → `text-body text-center text-muted-foreground`; PublicLinkCard add `hover:shadow-md transition-shadow`. Risco material isolado: (a) **PublicPage snapshot regenerate obrigatório** (`tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — 3 snapshots light/dark/brand assertam className strings exatos em h1 + bio; mudança força `pnpm test:components -u` + commit do `.snap` atualizado); (b) **TimeSeriesChart switch `<LineChart>` → `<AreaChart>` + import delta** (recharts re-exports `AreaChart`/`Area`; behavior preservado: stroke plum + dot=false + isAnimationActive=false + gap-fill client-side + sr-only table; risco zero em behavior tests porque queries são por `getByRole('heading')`/`getByRole('link', { name: /7d|30d/ })` + `screen.getByText` no sr-only table); (c) Lighthouse CI **`/[username]` é gate path canônico** (`.github/workflows/lighthouse.yml` L72 — diferente das dashboard sub-pages que são authenticated routes fora dos gates) → mudanças em PublicPage/PublicLinkCard estão sob scrutiny direto; bundle delta esperado neutral; (d) lógica de negócio Stories 4.1-4.4 (analytics SQL aggregations + RPC) + Stories 2.7/4.1/4.2 (public SSR + ViewBeacon + TrackedLinkCard click tracking) NÃO tocadas — apenas refresh visual.
```

> **Rationale:** Story **Frontend-primary** — última das 4 stories page-level paralelizáveis (5.4-5.7) do refresh Soft Studio. Materializa **Phase 3 final** de `docs/frontend-spec.md` §5.3 ratificada em **PRD v0.5 §UX/Branding** (Change Log 2026-05-29). Tokens canônicos já estão em `app/globals.css` (Story 5.2 Done — paleta Soft Studio + radius scale + shadows + typescale 9 tokens) e primitives shadcn já estão alinhados (Story 5.3 Done — Card +shadow-sm warm-tinted consumido em MetricsCards automatic; Button outline preserve em EmptyAnalyticsState CTA). Esta story **consome** ambos nas 2 surfaces restantes do refresh: (1) `/dashboard/analytics` page autenticada — completa o full-funnel dashboard refreshed (Layout + Links 5.5 → Profile + Theme + Account 5.6 → Analytics 5.7); (2) `/@username` página pública não-autenticada — o **único surface diferenciador "warm creator studio"** visível para visitantes (gate path Lighthouse canônico). Conforme `.claude/rules/agent-authority.md`, ajustes em pages + components são escopo do **@dev (Dex)** com gate **@qa (Quinn)** — mesmo padrão das Stories 3.2/3.3/5.2/5.3/5.4/5.5/5.6. `git push`/`gh pr create` permanecem **exclusivos de @devops** (Gage).

> **Backlog crossover:** `[EPIC-5-S7]` 🟡 MEDIUM (este item — `docs/STORY-BACKLOG.md` L29-38) será movido para ✅ DONE após merge (Task 12.1 — pattern Stories 5.2/5.3/5.4/5.5/5.6). **Não bloqueia 5.8** (Motion + polish — paralelizável) **nem 5.9** (Logo + favicon — paralelizável). Após 5.7 mergeada, refresh visual **Phase 3 completo** (5 surfaces page-level alinhadas: Landing/Auth 5.4 + Dashboard Core 5.5 + Profile/Theme/Account 5.6 + Analytics/Public 5.7) — restam apenas 5.8 (motion polish) e 5.9 (wordmark + favicon) para closure total do Epic 5 refresh. `[STORY-5.3-F1]` LOW e `[STORY-5.2-F1]` LOW (amends spec warning/destructive ratios) **não tocados** — Story 5.7 não introduz uso novo de `--warning` em bg/text components nem `text-destructive` (Analytics/Public não têm surfaces destructive).

> **TimeSeriesChart decisão (DEV-A anticipated):** spec §2.8 L772-781 wireframe descreve _"Chart card com legenda em hover... Linha smooth, primary color... gradiente sutil bg under"_. Implementação atual (`components/analytics/TimeSeriesChart.tsx:90-117` Story 4.4) usa `<LineChart>` + `<Line>` recharts sem fill area underneath — só plot da linha. Para materializar "gradiente sutil bg under" da spec, recharts requer trocar para `<AreaChart>` + `<Area>` (Area pode ter stroke + fill simultaneamente; Line não suporta fill). **Decisão:** switch `<LineChart>/<Line>` → `<AreaChart>/<Area>` + `<defs><linearGradient id="analytics-area-gradient" .../></defs>` com peach (`var(--accent)`) `stopOpacity: 0.35 → 0`. Preserva: stroke plum (`var(--primary)`) + `dot={false}` + `isAnimationActive={false}` + gap-fill client-side + sr-only data table + tooltip formatter pt-BR + ResponsiveContainer 300px. Imports: `LineChart, Line` → `AreaChart, Area`. Comportamento accessibility (sr-only table) e responsive intactos. Behavior tests `TimeSeriesChart.test.tsx` (verificam toggle navigation + sr-only data table) zero-impact (queries semânticas).

> **PublicPage snapshot decisão (DEV-B anticipated):** `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` (Story 3.2 AC5) contém 3 snapshots HTML-only (light/dark/brand) que assertam className strings exatos. Mudanças em PublicPage.tsx (AC8 h1 + AC9 bio) afetam 2 elementos no snapshot. **Decisão:** regenerate via `pnpm test:components -u tests/components/features/public/PublicPage.test.tsx` + verificar diff que apenas 2 strings mudaram (h1 className + p className em 3 snapshots = 6 line changes total). Padrão consciente — snapshots são intencionalmente fragile para detectar regressões visuais não-intencionais; mudança aqui é INTENCIONAL e rastreada para spec §2.10 L878-880. Não há LinksTable.test.tsx ou MetricsCards.test.tsx snapshots — apenas behavior tests (zero-impact em radius/typescale changes).

> **LinksTable inline bar decisão (DEV-C condicional — ratificar durante implementação):** spec §2.8 L785-794 wireframe mostra _"Bar inline + count right"_ como visualização primária (e.g., `████████████████ 89`). Implementação atual usa 3 colunas numéricas (7d / 30d / Total). Wireframe é ilustrativo — a tabela atual provê info mais rica. **Decisão:** OPT-IN — @dev decide durante audit. ADD: nova coluna ou substituição de Total column por `<div className="flex items-center gap-2"><div className="h-2 flex-1 rounded-full bg-primary/20"><div className="h-full rounded-full bg-primary" style={{ width: \`${(row.clicksTotal / maxClicks) \* 100}%\` }} /></div><span className="tabular-nums">{row.clicksTotal}</span></div>` (bar proportional ao max clicks). SKIP: preserva 3 colunas numéricas funcional. Trade-off: bar adiciona signal visual mas tira info precisa de 7d/30d se substituir; ADD nova coluna duplica info. Documentar em Dev Agent Record qualquer que seja a escolha.

## Story

**As a** dono do BioLink autenticado (visualizando seu próprio analytics em `/dashboard/analytics` — Stories 4.1-4.4) **e** visitante anônimo de qualquer perfil público (`/@username` — Stories 2.7/3.2/4.1/4.2),
**I want** que as 2 surfaces restantes do refresh Soft Studio (Analytics dashboard com 4 metric cards + chart plum line + peach gradient + per-link table; Public page com h1 plum 32px centered + bio body 16px + link cards radius 16 hover shadow) renderizem **com os specs visuais e estruturais Soft Studio** (per `docs/frontend-spec.md` §2.8 + §2.10 + §1.2 paleta + §1.3.2 typescale + §1.4.2 radius scale + §4.2 keyboard nav + §4.6 touch targets) — Analytics page H1 32px `text-h1` em vez de `text-xl` legacy shipped Story 4.4, ADD subtitle "Como sua página tá indo." (spec §2.8 L754 verbatim), MetricsCards big number `text-h2 text-primary` (plum) em vez de `text-3xl` neutral (spec §2.8 L759 "H2 big number, plum"), TimeSeriesChart switch `<LineChart>` → `<AreaChart>` com peach gradient under line (spec §2.8 L776-777 "Linha smooth, primary color / gradiente sutil bg under" — preserva stroke plum), LinksTable + EmptyAnalyticsState + AnalyticsLoadingSkeleton radius `rounded-lg`/`rounded-md` → `rounded-xl` (radius scale §1.4.2 canon 16px), EmptyAnalyticsState copy reconcile "Compartilhe sua página para começar a ver analytics." → "Compartilhe sua página em /@<username> para começar a ver dados aqui." (spec §2.8 L801 verbatim), PublicPage h1 `text-2xl font-semibold tracking-tight` → `text-h1 text-primary text-center` (spec §2.10 L878 "H1 32, plum, centered") + bio `<p>` → `<p className="text-body text-center text-muted-foreground">` (spec §2.10 L880 "Body 16, muted-fg, centered"), PublicLinkCard `hover:shadow-md transition-shadow` (spec §2.10 L887 "hover: shadow-md") preservando radius 16 (shipped Story 2.5) e hover:bg-accent (peach feedback tema brand),

**so that** as 2 surfaces restantes parem de exibir typescale legacy + radius inconsistente + cores neutras em vez de plum primary, e passem a expressar a identidade "warm creator studio" canônica do PRD v0.5 §UX/Branding **completando o refresh visual full-funnel Phase 3** (Landing/Auth 5.4 → Dashboard Core 5.5 → Profile/Theme/Account 5.6 → Analytics/Public esta 5.7) — desbloqueando coerência total das 8 surfaces page-level (Landing + 3 Auth + Dashboard Core + Profile + Theme + Account + Analytics + Public) e materializando o **único surface diferenciador visível para visitantes** (Public `/@username` — gate path Lighthouse canônico) em paleta refreshed (após 5.2 tokens + 5.3 primitives + 5.4-5.6 dashboard authenticated + esta 5.7 analytics + public).

## Acceptance Criteria

> Todas as AC abaixo rastreiam para fontes canônicas — `docs/frontend-spec.md` v0.3 §2.8 (Analytics L746-803) + §2.10 (Public L867-927) + §1.2 (paleta L122 plum `#5B3A8C` / L138 peach `#FFB5A7`) + §1.3.2 (typescale tokens L232-243) + §1.4.2 (radius scale Card 16) + §4.2 (a11y keyboard nav) + §4.6 (touch targets ≥44px). Cada AC cita seção exata. NENHUMA invenção (Constitution Art. IV). Tokens canônicos estão em `app/globals.css` pós-5.2 + primitives refreshed em `components/ui/` pós-5.3 — esta story **apenas consome em pages + components/{analytics,public}**. Estado pré-existente, deviations descobertas durante audit e racional de cada AC estão isolados em **Dev Notes** — leia antes de implementar.

1. **Analytics page H1 + subtitle — "Analytics" + typescale `text-h1` 32px + ADD body subtitle** (per `docs/frontend-spec.md` §2.8 L753-754 _"Analytics ← H1 / Como sua página tá indo ← Body muted"_ + §1.3.2 typescale `--text-h1` 32px). Auditar `app/dashboard/analytics/page.tsx:138-141`. Mudanças: (a) **L140 H1** — `<h1 className="text-xl font-semibold tracking-tight">Analytics</h1>` (shipped Story 4.4) → `<h1 className="text-h1">Analytics</h1>` (spec verbatim §2.8 L753 — copy preservada). Remover classes Tailwind redundantes `text-xl font-semibold tracking-tight` (text-h1 token já carrega weight 500 + tracking -0.02em + line-height 40 per Story 5.2). (b) **ADD subtitle nova linha** — após H1 (L140) adicionar `<p className="text-body text-muted-foreground">Como sua página tá indo.</p>` (spec §2.8 L754 verbatim). (c) **Header tightness** — `<header>` (L139) ADD `className="space-y-1"` para gap apertado entre H1 e subtitle (pattern Stories 5.4/5.5/5.6).

2. **MetricsCards big number — `text-h2` 24px + `text-primary` plum** (per `docs/frontend-spec.md` §2.8 L759 _"H2 big number, plum"_ + §1.2 L122 _"Deep plum primary #5B3A8C"_ + §1.3.2 typescale `--text-h2` 24px). Auditar `components/analytics/MetricsCards.tsx:26`. Mudança: `<CardTitle className="text-3xl tabular-nums">` (shipped Story 4.4) → `<CardTitle className="text-h2 tabular-nums text-primary">` (spec verbatim §2.8 L759). Plum semântico via `--primary: #5b3a8c` (light/brand) + `#B8A1E8` (dark — lavender brighter, preserva contraste WCAG 2.1 AA per §1.2 L120-121). CardDescription label (L25 — "Total Page Views"/"Total Clicks"/"Page Views (30d)"/"Clicks (30d)") preservada — mix PT/EN intencional ratificado DEV-10 da Story 4.4 (@po). Card primitive (`<Card>`) consume automatic `+shadow-sm warm-tinted` ratificado Story 5.3 — zero mudança em primitive.

3. **TimeSeriesChart switch `<LineChart>` → `<AreaChart>` + peach gradient under + plum stroke preservado** (per `docs/frontend-spec.md` §2.8 L775-777 _"Linha smooth, primary color / gradiente sutil bg under"_ + §1.2 L138 _"Peach accent #FFB5A7"_). Auditar `components/analytics/TimeSeriesChart.tsx:4-12, 90-117`. Mudanças (DEV-A): (a) **Imports** (L4-12) — substituir `LineChart, Line` por `AreaChart, Area` (preserve `CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis`). (b) **Chart wrapper** (L92) — `<LineChart data={dense} margin={...}>` → `<AreaChart data={dense} margin={...}>` (props idênticas). (c) **ADD `<defs>`** — primeira tag dentro de `<AreaChart>`: `<defs><linearGradient id="analytics-area-gradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--accent)" stopOpacity={0.35} /><stop offset="100%" stopColor="var(--accent)" stopOpacity={0} /></linearGradient></defs>`. (d) **Substituir `<Line>`** (L106-113) por `<Area type="monotone" dataKey="count" stroke="var(--primary)" strokeWidth={2} fill="url(#analytics-area-gradient)" dot={false} isAnimationActive={false} />` (preserva: type monotone smooth + stroke plum + dot=false + isAnimationActive=false; adiciona fill via gradient peach `0.35 → 0` opacity). Preserva: CartesianGrid strokeDasharray + XAxis tickFormatter + YAxis allowDecimals=false + Tooltip formatter pt-BR + ResponsiveContainer 300px + sr-only data table (L118-134) intacto.

4. **TimeSeriesChart figure radius + figcaption typescale** (per `docs/frontend-spec.md` §1.4.2 radius scale Card 16 + §1.3.2 typescale `--text-h3` 20px). Auditar `components/analytics/TimeSeriesChart.tsx:77-87`. Mudanças: (a) **L77 figure** — `<figure className="space-y-3 rounded-lg border border-border p-4">` → `<figure className="space-y-3 rounded-xl border border-border p-4">` (radius scale alignment §1.4.2 canon 16px). (b) **L79 figcaption H2** — `<h2 className="text-sm font-medium">Page views — últimos {days} dias</h2>` → `<h2 className="text-h3 font-medium">Page views — últimos {days} dias</h2>` (subsection level §1.3.2 typescale; copy preservada). (c) **L61 ToggleLink classes** — `'rounded-md border border-border px-3 py-1 text-sm font-medium transition-colors'` → `'rounded-lg border border-border px-3 py-1 text-sm font-medium transition-colors'` (radius alignment intermediate — toggle é interactive button menor que figure card, `rounded-lg` (≈10px) é o intermediate stop entre `rounded-md` (6px) e `rounded-xl` (16px) per design system implícito; alternativa `rounded-md` é aceitável se @dev preferir consistency com Button shadcn variants). Touch target ≥44px preservado (`px-3 py-1` + text-sm = ~28px height — abaixo de 44px AC §4.6 mas dentro de tab/secondary control acceptance shipped Story 4.4 ratificado @po).

5. **LinksTable radius + (CONDITIONAL DEV-C) inline bar visualization** (per `docs/frontend-spec.md` §2.8 L785-794 _"Bar inline + count right"_ + §1.4.2 radius scale Card 16). Auditar `components/analytics/LinksTable.tsx:18`. Mudança mandatória: (a) **L18** — `<div className="overflow-x-auto rounded-lg border border-border">` → `<div className="overflow-x-auto rounded-xl border border-border">` (radius scale alignment §1.4.2). Mudança opcional (DEV-C condicional — ratificar durante audit): (b) **ADD inline bar column** OU **substituir Total column por bar+count** — pattern: `<div className="flex items-center gap-2"><div className="h-2 flex-1 rounded-full bg-primary/20"><div className="h-full rounded-full bg-primary" style={{ width: \`${maxClicks > 0 ? (row.clicksTotal / maxClicks) \* 100 : 0}%\` }} aria-hidden="true" /></div><span className="tabular-nums">{row.clicksTotal.toLocaleString('pt-BR')}</span></div>`(max clicks =`Math.max(...rows.map(r => r.clicksTotal), 1)` antes do map — evita divide-by-zero). Trade-off: bar adiciona signal visual mas tira info precisa de 7d/30d se substituir Total; ADD nova coluna duplica info Total. Se SKIP: preserva tabela atual (3 colunas numéricas) funcional + documentar em Dev Agent Record com racional ("wireframe ilustrativo, tabela atual mais rica").

6. **EmptyAnalyticsState radius + typescale + copy reconcile** (per `docs/frontend-spec.md` §2.8 L801 _"Compartilhe sua página em /@demo para começar a ver dados aqui."_ + §1.4.2 radius scale + §1.3.2 typescale). Auditar `components/analytics/EmptyAnalyticsState.tsx:13-29`. Mudanças: (a) **L13 outer div** — `<div className="rounded-lg border border-dashed border-border bg-muted/30 p-12 text-center">` → `<div className="rounded-xl border border-dashed border-border bg-muted/30 p-12 text-center">` (radius scale §1.4.2). (b) **L15 H2** — `<h2 className="mt-4 text-lg font-semibold">Sem analytics ainda</h2>` → `<h2 className="mt-4 text-h3 font-medium">Sem analytics ainda</h2>` (subsection level §1.3.2 typescale; copy preservada — spec L801 não prescreve título específico, "Sem analytics ainda" shipped Story 4.4 ratificado preserva tone). (c) **L16-18 body** — `<p className="mx-auto mt-2 max-w-md text-sm text-muted-foreground">Compartilhe sua página para começar a ver analytics.</p>` → `<p className="mx-auto mt-2 max-w-md text-body text-muted-foreground">{username ? \`Compartilhe sua página em /@${username} para começar a ver dados aqui.\` : 'Compartilhe sua página para começar a ver dados aqui.'}</p>`(text-body 16px + copy verbatim spec §2.8 L801 com fallback se username vazio per defense-in-depth current behavior L9). (d) **L20-27 CTA link** — preservar copy + href; mudar`text-sm font-medium`→`text-body-sm font-medium` (token alignment).

7. **AnalyticsLoadingSkeleton radius alignment** (per `docs/frontend-spec.md` §1.4.2 radius scale Card 16). Auditar `components/analytics/AnalyticsLoadingSkeleton.tsx:4, 16-23`. Mudança: (a) **L4 SkeletonBar base** — `<div className={cn('animate-pulse rounded-md bg-muted', className)} aria-hidden="true" />` → `<div className={cn('animate-pulse rounded-xl bg-muted', className)} aria-hidden="true" />` (radius scale §1.4.2 — todos os skeletons usam mesmo radius canonical). (b) **L16 H1 placeholder** — opcional `h-7 w-32` → `h-8 w-32` (8 = 32px alinha visualmente com `text-h1` 32px line-height 40, mas approxima sem exato match; @dev decide). Preservar aria-busy + aria-live + sr-only "Carregando analytics…" + grid 2×4 desktop / 2×2 mobile shipped Story 4.4.

8. **PublicPage H1 — `text-h1` 32px + `text-primary` plum + `text-center`** (per `docs/frontend-spec.md` §2.10 L878 _"H1 32, plum, centered"_ + §1.2 L122 _"Deep plum primary #5B3A8C"_ + §1.3.2 typescale `--text-h1` 32px). Auditar `components/public/PublicPage.tsx:61`. Mudança: `<h1 className="text-2xl font-semibold tracking-tight">{displayName}</h1>` (shipped Story 2.7/3.2) → `<h1 className="text-h1 text-primary text-center">{displayName}</h1>` (spec verbatim §2.10 L878). Remover classes Tailwind redundantes `text-2xl font-semibold tracking-tight` (text-h1 token já carrega weight 500 + tracking -0.02em + line-height 40). `text-primary` semântico: light/brand `#5b3a8c` + dark `#b8a1e8` — plum em todos os 3 temas (WCAG 2.1 AA validated per §1.2 L120 + Story 3.2 contrast tests). `text-center` explicit (parent `<main>` tem `items-center` flex column → centra como item, mas text-align dentro do `<h1>` é redundante para single-line text mas semântico para multi-line bio names — defense-in-depth).

9. **PublicPage bio — `text-body` 16px + `text-center` + `text-muted-foreground`** (per `docs/frontend-spec.md` §2.10 L880 _"Body 16, muted-fg, centered"_ + §1.3.2 typescale `--text-body` 16px). Auditar `components/public/PublicPage.tsx:63`. Mudança: `{profile.bio && <p className="text-center text-muted-foreground">{profile.bio}</p>}` (shipped Story 5.1 — display_name + bio) → `{profile.bio && <p className="text-body text-center text-muted-foreground">{profile.bio}</p>}` (text-body token explicit 16px per §1.3.2; text-muted-foreground + text-center preservados). Spec §2.10 L880 mostra "meu nome é demo" exemplo — copy do user-input (profile.bio) preservada.

10. **PublicLinkCard hover shadow-md + transition** (per `docs/frontend-spec.md` §2.10 L886-887 _"border 1px lavender; hover: shadow-md"_). Auditar `components/public/PublicLinkCard.tsx:26`. Mudança: `className="flex w-full items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 transition-colors hover:bg-accent"` (shipped Story 2.5) → `className="flex w-full items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 transition-all hover:bg-accent hover:shadow-md"` (preserva radius xl 16px + border + bg-card + bg-accent peach hover Story 5.3; ADD hover:shadow-md per spec L887; switch `transition-colors` → `transition-all` para incluir shadow no animate spec §1.5 default duration 150-200ms inherited). Shadow `--shadow-md` token consumido via Tailwind utility (mapeado via theme em globals.css L307 ou similar — se ausente, default Tailwind `shadow-md` é aceitável e shipped). Border 1px "lavender" — spec L887 refere `--border` token shipped Story 5.2 (light: `#EFE7FA` lavender-mist; dark: `#3D2F5C` plum-darker; brand: `#E0CFFA` lavender-soft) — já correto via `border-border`.

11. **PublicPage snapshot regenerate obrigatório** (per `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — Story 3.2 AC5). Snapshots HTML-only (light/dark/brand × 3 = 3 snapshots) assertam className strings exatos em h1 (L40 do snap atual: `class="text-2xl font-semibold tracking-tight"`) + bio paragraph (L45 do snap atual: `class="text-center text-muted-foreground"`). Mudanças AC8 (h1) + AC9 (bio) afetam **6 line strings total** (2 elementos × 3 snapshots). **Decisão (DEV-B):** regenerate via `pnpm test:components -u tests/components/features/public/PublicPage.test.tsx` + verificar diff que apenas 2 strings mudaram + commit `.snap` atualizado. Patterns esperados pós-update: h1 `class="text-h1 text-primary text-center"`; bio `class="text-body text-center text-muted-foreground"`. Snapshots de avatar fallback (`<div aria-hidden="true" class="flex h-24 w-24 items-center justify-center rounded-full bg-muted">`) + `<svg>` lucide user + link card (`rounded-xl bg-card hover:bg-accent`) **devem ser preservados intactos** — qualquer mudança não-prevista é regressão. PublicLinkCard className mudança (AC10 — add `hover:shadow-md` + `transition-all`) **TAMBÉM** afeta o snapshot (line ~56 do snap atual: `class="flex w-full items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 transition-colors hover:bg-accent"` → `class="flex w-full items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 transition-all hover:bg-accent hover:shadow-md"`). Total: 3 strings × 3 snapshots = 9 line diffs no `.snap` (h1 + p + a). Validar via `git diff tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` antes de commit — só essas 3 className strings devem ter mudado.

12. **Analytics + Public behavior tests preservados** (per `tests/components/features/{analytics,public}/*.test.tsx`). Tests existentes Stories 4.4/2.7/3.2/4.1/4.2 cobrem: (a) `MetricsCards.test.tsx` — renderiza 4 cards com data-testid + valores localizados pt-BR; (b) `TimeSeriesChart.test.tsx` — toggle navigation 7d/30d + sr-only data table accessibility; (c) `LinksTable.test.tsx` — renderiza linhas + null se rows vazio + cliques counts; (d) `EmptyAnalyticsState.test.tsx` — copy + CTA link condicional username; (e) `AnalyticsLoadingSkeleton.test.tsx` — aria-busy + aria-live + sr-only loading message; (f) `PublicPage.test.tsx` — data-theme wrapper + .dark class condicional (Story 3.2 AC5) + 3 snapshots; (g) `PublicLinkCard.test.tsx` — href + target="\_blank" + rel + icon + title; (h) `TrackedLinkCard.test.tsx` — beacon dispatch sendBeacon/fetch fallback; (i) `ViewBeacon.test.tsx` — beacon mount + body. **Mudanças são className/typescale/copy/import switch (LineChart→AreaChart) — zero impact em behavior queries semânticas.** AC11 cobre snapshot regenerate (PublicPage.test.tsx específico — `pnpm test:components -u` + commit `.snap`). Validação: rodar `pnpm test:components -t Analytics` + `-t Public` + suite full antes E depois — count PASS deve manter **195/195 verde por contagem** (baseline pós-Story 5.6 — 194 original + 1 novo Vibrante teste Story 5.6 = 195). Sem snapshot updates esperados além do PublicPage.test.tsx.snap (AC11).

13. **Suíte completa de qualidade local PASS sem regressão funcional**: `pnpm typecheck` exit 0 (recharts AreaChart/Area types preservam — mesmo namespace), `pnpm lint` zero warnings (sem novos imports unused; React 19 Server/Client Component boundaries preservados — TimeSeriesChart já `'use client'` shipped, AreaChart switch zero-impact), `pnpm build` (Next 16.2.5 / Turbopack) **16/16 static pages PASS** (16 routes — 15 static pré-5.6 + `/[username]` dynamic já contado; build deve completar sem erro; `app/dashboard/analytics/page.tsx` é Server Component dynamic — não conta para static), `pnpm test:components` mantém **195/195 verde por contagem pós-snapshot update** — único snapshot file atualizado: `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` (3 snapshots × 3 strings = 9 line diffs esperados). **Behavior tests** (não-snapshot): MetricsCards/TimeSeriesChart/LinksTable/EmptyAnalyticsState/AnalyticsLoadingSkeleton + PublicLinkCard/TrackedLinkCard/ViewBeacon todos PASS sem mudança. Todos devem CONTINUAR PASSING — quebra = regressão funcional.

14. **Lighthouse CI `/[username]` ≥ 0.85 + bundle ≤ 180 KB gz**: `.github/workflows/lighthouse.yml:70-73` gate paths inclui `/` (landing) + `/@demo` (public) + `/dashboard` (links). **`/@demo` é o path Lighthouse direto afetado por esta story** (AC8-AC11). Mudanças PublicPage.tsx + PublicLinkCard.tsx estão sob scrutiny direto. `/dashboard/analytics` é authenticated route — **fora do gate paths** (Lighthouse não navega para sub-pages dashboard em CI). Esperado: bundle delta **neutral** (mudanças são className + 1 import switch recharts AreaChart + 1 inline `<defs><linearGradient>` SVG — zero novo dependency, zero novo lucide-react import). LCP `/@demo` baseline pós-5.6 esperado ~1.0-1.2s; LCP target spec §2.10 L918 < 2.0s (margem folgada). Lighthouse audit pode flagar A11y issues novos se: (a) PublicPage h1 `text-primary` quebra contrast em algum tema — não aplicável (WCAG 2.1 AA validated per §1.2 L120 + Story 3.2 contrast tests); (b) TimeSeriesChart Area gradient quebra LCP em /[username] — não aplicável (analytics page é authenticated, /[username] não consome TimeSeriesChart); (c) PublicLinkCard hover:shadow-md transition adiciona CLS — não aplicável (shadow não afeta layout, apenas paint). Validar via Lighthouse no PR. Qualquer queda > 5 pontos exige investigação antes do merge (mesmo gate Stories 5.2/5.3/5.4/5.5/5.6 — runs:3 + median absorve flake per [STORY-3.5-F3]).

15. **Constitution Art. IV compliance + housekeeping**: cada AC1-AC14 cita `[Source: docs/frontend-spec.md §{n}.{m} L{linha}]` em Dev Notes. Zero invenção de glyphs/copies/comportamentos não-spec'd. "Plum" rastreado para `--primary: #5b3a8c` (= deep plum primary canonical per spec §1.2 L122 + L151). "Peach gradient" rastreado para `--accent: #ffb5a7` (= peach accent per spec §1.2 L138 + L157). Empty state copy verbatim spec §2.8 L801. Subtitle Analytics verbatim spec §2.8 L754 ("Como sua página tá indo."). Backlog crossover: `docs/STORY-BACKLOG.md` entrada `[EPIC-5-S7]` MEDIUM movida para Change Log DONE post-merge (Task 12.1 — pattern Stories 5.2/5.3/5.4/5.5/5.6). Story file mantém estrutura template-v2 (Status / Executor Assignment / Story / AC / 🤖 CodeRabbit Integration / Tasks / Dev Notes / Change Log / Dev Agent Record). Frontmatter ClickUp pulado (`mcp.enabled: false` per `core-config.yaml`). QA gate manual via QA-001 known-issues (CodeRabbit/WSL incompat. em macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`), padrão validado em 1.4-5.6.

> **No-Invention reconciliation:** O backlog `[EPIC-5-S7]` (`docs/STORY-BACKLOG.md` L29-38) lista como acceptance line: _"LCP `/@username` < 2.0s; bundle público ≤ 180 KB gz; Lighthouse CI público ≥ 90; smoke `/@demo` × 3 temas"_. Reconcilio: (a) **LCP `/@username` < 2.0s** — AC14 rastreia + spec §2.10 L918 verbatim; (b) **bundle público ≤ 180 KB gz** — AC14 rastreia + spec §2.10 L919 verbatim; (c) **Lighthouse CI público ≥ 90** — gate atual é **0.85** (`.lighthouserc.json` `minScore: 0.85`); 0.85 é menos rigoroso que 0.90 do backlog mas é o threshold ratificado pós-[STORY-3.5-F3] (banding fix). Reconcilio para AC14 com **`≥ 0.85` actual gate** (não inventar 0.90 não-vigente); risco de queda > 5 pontos investigada antes merge; (d) **smoke `/@demo` × 3 temas** — Task condicional 11 (não AC formal) — per MEMORY `feedback_skip_manual_smoke.md`, @dev pergunta antes e se skip pode marcar `[x]` com nota `_(SKIPPED ...)_` + risco residual. AC1-AC11 derivam **verbatim** de spec §2.8 + §2.10 + §1.4.2 + §1.3.2 (1 AC por elemento estrutural). AC12-AC15 cobrem preservação behavior + gates locais/CI/housekeeping.

> **Out-of-scope (explicitamente deferido):**
>
> - \*\*Refactor `<LineChart>` para `<AreaChart>` + adicionar Area types ao recharts custom — Story 4.4 escopo fechado; AC3 usa import switch pattern (LineChart, Line → AreaChart, Area do mesmo namespace recharts); zero custom types adicionados.
> - **Tracking server-side direto no PublicPage RSC (sem ViewBeacon client)** — viola ISR `revalidate=60` Story 2.7; ViewBeacon shipped Story 4.2 preservado intacto (zero mudança em `components/public/ViewBeacon.tsx`).
> - **Footer "feito com biolink ★" + share button** — spec §2.10 L899-905; "Compartilhe ↗" marcado spec L899 "Phase 2"; wordmark `biolink ★` é Story 5.9 escopo (logo + favicon Phase 5). Defer integral.
> - **Avatar 96×96 fallback differentiation por tema brand** — spec §2.10 L913 mostra "tema brand: background #F0E8FF, cards #FFFFFF puros". Tokens já fazem isso via `--background` + `--card` tema-aware (Stories 5.2 + 3.2); zero mudança em PublicPage.tsx além do h1 + bio (AC8/AC9).
> - **Analytics page mobile grid `1×4` vs `2×2`** — spec §2.8 L756 wireframe shows "Grid 2×2 mobile / 1×4 desktop"; implementação atual `grid-cols-2 gap-4 md:grid-cols-4` (page.tsx L143) já cumpre — zero mudança necessária.
> - **TimeSeriesChart per-link section** — spec §2.8 L783-794 "─── Cliques por link ───" com bar inline é a LinksTable AC5 (não nova section dentro do TimeSeriesChart).
> - **Recharts `<Brush>` ou navegação temporal expandida** — fora de escopo (toggle 7d/30d shipped Story 4.4 cobre AC1 da 4.4).
> - **Empty state customizado por tema (brand peach blob illustration vs light plum)** — spec §2.8 L801-802 + Story 4.4 shipped genérica `<BarChart3>` lucide icon; tema-specific blob illustration é over-engineering para esta story (potential backlog `[STORY-5.7-F1]` LOW se @dev julgar valor adicional).
> - **PublicPage avatar shape variations (square, rounded-2xl, etc.)** — spec §2.10 L874 mostra circle 96×96 — shipped Story 2.7 já cumpre; sem mudança.
> - **Spring transitions / motion polish (chart enter, link card spring hover)** → Story 5.8 (Motion + polish).
> - **Wordmark `biolink ★` component + favicon ★** → Story 5.9.
> - **Mudança em /dashboard (Links) ou /dashboard/{profile,theme,account}** → Stories 5.5/5.6 Done.
> - **Mudança em landing/auth** → Story 5.4 Done.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

> **Nota (@sm):** `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` **e** `mcp.enabled: false` → skip notice é o comportamento correto do template-v2 (idêntico às Stories 1.4-5.6, todas Done). ClickUp sync pulado (`mcp.enabled: false`). Gate de qualidade segue o **workaround manual QA-001** (CodeRabbit/WSL incompat. em macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`), padrão validado em 1.4-5.6.

## Tasks / Subtasks

- [x] **Task 1 — Audit Analytics Page (`app/dashboard/analytics/page.tsx`)** (AC: 1) — _executor: @dev_
  - [x] 1.1 Abrir `app/dashboard/analytics/page.tsx`. Mudar L140 H1 `text-xl font-semibold tracking-tight` → `text-h1` (copy "Analytics" preservada per spec §2.8 L753).
  - [x] 1.2 ADD nova `<p>` após L140 H1: `<p className="text-body text-muted-foreground">Como sua página tá indo.</p>` (spec §2.8 L754 verbatim).
  - [x] 1.3 ADD `className="space-y-1"` ao `<header>` parent L139 (tightness pattern Stories 5.4/5.5/5.6).
  - [x] 1.4 Rodar `pnpm typecheck` (sanity).

- [x] **Task 2 — Refresh MetricsCards (`components/analytics/MetricsCards.tsx`)** (AC: 2) — _executor: @dev_
  - [x] 2.1 Abrir `components/analytics/MetricsCards.tsx`. Mudar L26 `<CardTitle className="text-3xl tabular-nums">` → `<CardTitle className="text-h2 tabular-nums text-primary">` (plum per spec §2.8 L759).
  - [x] 2.2 Rodar `pnpm test:components -t MetricsCards` — confirmar 0 regressões (4 cards renderizam + valores pt-BR + data-testid intactos).

- [x] **Task 3 — Refresh TimeSeriesChart (`components/analytics/TimeSeriesChart.tsx`)** (AC: 3, 4) — _executor: @dev_
  - [x] 3.1 Abrir `components/analytics/TimeSeriesChart.tsx`. Imports L4-12: substituir `LineChart, Line` por `AreaChart, Area` (mesmo namespace recharts; preserve outros imports).
  - [x] 3.2 L92 chart wrapper: `<LineChart data={dense} margin={...}>` → `<AreaChart data={dense} margin={...}>` (props idênticas).
  - [x] 3.3 ADD `<defs>` primeira tag dentro de `<AreaChart>`: `<defs><linearGradient id="analytics-area-gradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--accent)" stopOpacity={0.35} /><stop offset="100%" stopColor="var(--accent)" stopOpacity={0} /></linearGradient></defs>`.
  - [x] 3.4 Substituir `<Line>` L106-113 por `<Area type="monotone" dataKey="count" stroke="var(--primary)" strokeWidth={2} fill="url(#analytics-area-gradient)" dot={false} isAnimationActive={false} />`.
  - [x] 3.5 L77 figure: `rounded-lg` → `rounded-xl` (radius scale §1.4.2).
  - [x] 3.6 L79 figcaption H2: `text-sm font-medium` → `text-h3 font-medium` (subsection level §1.3.2; copy preservada).
  - [x] 3.7 L61 ToggleLink: `rounded-md` → `rounded-lg` (radius alignment intermediate); aceitável manter `rounded-md` se @dev preferir consistency com Button shadcn — documentar decisão.
  - [x] 3.8 Rodar `pnpm test:components -t TimeSeriesChart` — confirmar 0 regressões (toggle navigation + sr-only data table queries semânticas intactas).

- [x] **Task 4 — Refresh LinksTable (`components/analytics/LinksTable.tsx`)** (AC: 5) — _executor: @dev_
  - [x] 4.1 Abrir `components/analytics/LinksTable.tsx`. L18: `rounded-lg` → `rounded-xl` (radius scale §1.4.2).
  - [x] 4.2 (DEV-C SKIP ratified user opt-in pre-implementation) _(SKIPPED — wireframe spec L787 é ilustrativo; tabela atual 3 colunas numéricas (7d / 30d / Total) provê info mais rica para dono do BioLink analisar próprio analytics. ADD bar+count duplicaria info de Total; substituir Total perderia precisão. Risco residual: ø — apenas refresh radius mantido per AC5 mandatory.)_
  - [x] 4.3 Rodar `pnpm test:components -t LinksTable` — confirmar 0 regressões.

- [x] **Task 5 — Refresh EmptyAnalyticsState (`components/analytics/EmptyAnalyticsState.tsx`)** (AC: 6) — _executor: @dev_
  - [x] 5.1 Abrir `components/analytics/EmptyAnalyticsState.tsx`. L13 outer div: `rounded-lg` → `rounded-xl`.
  - [x] 5.2 L15 H2: `text-lg font-semibold` → `text-h3 font-medium` (subsection level §1.3.2).
  - [x] 5.3 L16-18 body `<p>`: `text-sm` → `text-body` + copy reconcile per AC6 (com fallback se username vazio): `{username ? \`Compartilhe sua página em /@${username} para começar a ver dados aqui.\` : 'Compartilhe sua página para começar a ver dados aqui.'}` (spec §2.8 L801 verbatim).
  - [x] 5.4 L20-27 CTA link: `text-sm font-medium` → `text-body-sm font-medium` (token alignment).
  - [x] 5.5 Rodar `pnpm test:components -t EmptyAnalyticsState` — confirmar 0 regressões (queries por copy + link href tolerantes).

- [x] **Task 6 — Refresh AnalyticsLoadingSkeleton (`components/analytics/AnalyticsLoadingSkeleton.tsx`)** (AC: 7) — _executor: @dev_
  - [x] 6.1 Abrir `components/analytics/AnalyticsLoadingSkeleton.tsx`. L4 SkeletonBar base: `rounded-md` → `rounded-xl` (radius alignment).
  - [x] 6.2 (OPCIONAL) L16 H1 placeholder: `h-7 w-32` → `h-8 w-32` (approxima text-h1 32px) — @dev decide; preservar aria-busy + aria-live + sr-only loading message.
  - [x] 6.3 Rodar `pnpm test:components -t AnalyticsLoadingSkeleton` — confirmar 0 regressões.

- [x] **Task 7 — Refresh PublicPage (`components/public/PublicPage.tsx`)** (AC: 8, 9) — _executor: @dev_
  - [x] 7.1 Abrir `components/public/PublicPage.tsx`. L61 H1: `<h1 className="text-2xl font-semibold tracking-tight">{displayName}</h1>` → `<h1 className="text-h1 text-primary text-center">{displayName}</h1>` (spec verbatim §2.10 L878 "H1 32, plum, centered").
  - [x] 7.2 L63 bio paragraph: `<p className="text-center text-muted-foreground">{profile.bio}</p>` → `<p className="text-body text-center text-muted-foreground">{profile.bio}</p>` (spec verbatim §2.10 L880 "Body 16, muted-fg, centered").
  - [x] 7.3 Rodar `pnpm typecheck` (sanity; nenhum import change).

- [x] **Task 8 — Refresh PublicLinkCard (`components/public/PublicLinkCard.tsx`)** (AC: 10) — _executor: @dev_
  - [x] 8.1 Abrir `components/public/PublicLinkCard.tsx`. L26: substituir `transition-colors` por `transition-all` + ADD `hover:shadow-md` ao className do `<a>`. Final esperado: `className="flex w-full items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 transition-all hover:bg-accent hover:shadow-md"`.
  - [x] 8.2 Rodar `pnpm test:components -t PublicLinkCard` — confirmar 0 regressões.

- [x] **Task 9 — Regenerate PublicPage snapshot (`tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap`)** (AC: 11) — _executor: @dev_
  - [x] 9.1 Rodar `pnpm test:components -u tests/components/features/public/PublicPage.test.tsx` (3 snapshots light/dark/brand atualizados).
  - [x] 9.2 Validar via `git diff tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — apenas 3 className strings × 3 snapshots = 9 line diffs esperados: (a) h1 `text-2xl font-semibold tracking-tight` → `text-h1 text-primary text-center`; (b) bio p `text-center text-muted-foreground` → `text-body text-center text-muted-foreground`; (c) link `<a>` `transition-colors hover:bg-accent` → `transition-all hover:bg-accent hover:shadow-md`.
  - [x] 9.3 Verificar que nenhuma outra mudança estrutural ocorreu (avatar fallback + svg lucide user + main flex layout preservados intactos).
  - [x] 9.4 Stage `.snap` para commit junto com mudanças em PublicPage.tsx + PublicLinkCard.tsx.

- [x] **Task 10 — Quality Gates Locais** (AC: 13) — _executor: @dev_
  - [x] 10.1 Rodar `pnpm typecheck` — exit 0 esperado.
  - [x] 10.2 Rodar `pnpm lint` — zero warnings esperado.
  - [x] 10.3 Rodar `pnpm build` — Next 16.2.5 Turbopack; 16/16 routes intactos esperado; build deve completar sem erro.
  - [x] 10.4 Rodar `pnpm test:components` — 195/195 PASS esperado (1 snapshot update via Task 9; behavior tests intactos).
  - [x] 10.5 Confirmar `git diff tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` apenas com 9 line diffs esperados (className strings AC8/AC9/AC10).

- [x] **Task 11 — Smoke Runtime Manual × 3 temas /@demo + /dashboard/analytics (CONDITIONAL)** (AC: 13, 14) — _executor: @dev_
  - [x] 11.1 _(SKIPPED user opt-in pre-implementation — cobertura indireta via behavior tests 195/195 PASS + Lighthouse CI `/@demo` gate path canônico no PR. Pattern Stories 5.4/5.5/5.6 per MEMORY `feedback_skip_manual_smoke.md`. Risco residual: visual regressions sub-perceptuais não capturadas por snapshot HTML-only ou behavior tests; mitigation via Lighthouse a11y + perf gates ≥0.85 + manual visual review @qa pré-merge.)_
  - [x] 11.2 _(SKIPPED — n/a se 11.1 SKIPPED; mesma justificativa aplica para `/dashboard/analytics` authenticated route — fora de Lighthouse CI gates default; cobertura via MetricsCards/TimeSeriesChart/LinksTable behavior tests + manual review @qa pré-merge.)_
  - [x] 11.3 _(executada — marcação 11.1/11.2 `[x]` com notas SKIPPED feita per AC pattern e ratificação user opt-in.)_

- [x] **Task 12 — Housekeeping `[EPIC-5-S7]` MEDIUM → DONE** (AC: 15) — _executor: @dev_
  - [x] 12.1 Editar `docs/STORY-BACKLOG.md`: remover bloco `#### [EPIC-5-S7] Story 5.7 — Analytics + Public page` da seção 🟡 MEDIUM (L29-38). Atualizar stats (Open MEDIUM count -1; DONE count +1).
  - [x] 12.2 Adicionar entrada Change Log linha após Story 5.6 MERGE: `| 2026-06-02 | DONE | [EPIC-5-S7] → Story 5.7 implementada (Analytics + Public Soft Studio refresh — H1 plum + peach gradient chart + radius 16 + hover shadow link cards)... | Dex (dev) |`.
  - [x] 12.3 Atualizar frontmatter `last_updated:` com nota Story 5.7 implementada.
  - [x] 12.4 Atualizar narrative Epic 5 (L27) incluindo `+ ✅ 5.7 DONE (analytics + public page-level refresh + peach gradient chart + plum h1)`.

## Dev Notes

### Previous Story Insights

**Story 5.6 (Profile + Theme + Account — Soft Studio Refresh + rebrand "Brand" → "Vibrante"):**

- ✅ Pattern H1 refresh `text-xl font-semibold tracking-tight` → `text-h1` consolidado (Profile/Theme L30, L47). **REUSE direto** em Analytics page (AC1) — mesmo padrão.
- ✅ Pattern H1 `text-2xl font-semibold` → `text-h1` consolidado (Account L36). **REUSE direto** em PublicPage h1 (AC8) — mesmo padrão (preserva acrescimo `text-primary text-center` per spec §2.10 L878).
- ✅ Subtitle refresh `text-sm` → `text-body` consolidado (Profile/Theme/Account). **REUSE direto** em Analytics subtitle (AC1) — mesma transformação. Header tightness `space-y-1` pattern consolidado.
- ✅ Subsection H2 typescale `text-lg font-medium`/`text-lg font-semibold` → `text-h3 font-medium` consolidado (Profile subsection labels + Account export/delete). **REUSE direto** em TimeSeriesChart figcaption H2 (AC4) + EmptyAnalyticsState H2 (AC6) — mesma transformação.
- ✅ Radius alignment `rounded-lg` → `rounded-xl` consolidado (Account delete section). **REUSE direto** em TimeSeriesChart figure (AC4) + LinksTable (AC5) + EmptyAnalyticsState (AC6) — mesma transformação.
- ✅ DEV-A pattern (className override em primitive — Story 5.3 escopo fechado) → DEV-A desta story (`<LineChart>` → `<AreaChart>` recharts namespace switch; mesma natureza de "swap consciente sem refactor de primitive").
- ✅ DEV-B pattern (LABELS const rebrand UI-only preservando enum/DB) — análogo a DEV-B desta story (snapshot regenerate intencional preservando data-theme + .dark class assertions).
- ✅ Pattern test:components baseline 194 + 1 novo Vibrante = **195/195** ratificado Story 5.6 Dev Agent Record L379. Baseline para 5.7 = 195.
- ✅ Smoke runtime manual SKIPPED user opt-in per MEMORY `feedback_skip_manual_smoke.md` — pattern repetido em Task 11.
- ✅ Backlog housekeeping `[EPIC-5-S6]` MEDIUM → DONE entry + stats update + narrative — pattern repetido em Task 12.

**Story 5.5 (Dashboard Core — Layout + Links Soft Studio):**

- ✅ Sidebar/UserMenu/MobileDrawer audit NO-OP — pattern repetido aplicável em components shipped Stories 4.4/2.7 que já consomem primitives refreshed Story 5.3 (Card +shadow-sm em MetricsCards automatic).
- ✅ Lighthouse CI gate `/` + `/dashboard` afetado por shared bundle changes em `app/**` ou `components/**` — pattern repetido: `/@demo` é gate path canônico afetado por AC8-AC11 (PublicPage + PublicLinkCard); `/dashboard/analytics` é authenticated route fora dos gates.

**Story 5.4 (Landing + Auth Soft Studio):**

- ✅ Pattern h-12 className override em primitives Input/Button — **DEV-A1 da 5.4 "NÃO mexer em primitives Story 5.3 escopo fechado"** → análogo a DEV-A desta story (TimeSeriesChart Area switch consciente sem refactor de primitive recharts).
- ✅ H1 `text-2xl` → `text-h1` consolidado em 4 auth pages — **REUSE direto** em PublicPage h1 (AC8).

**Story 5.3 (Primitives Audit Soft Studio):**

- ✅ Card primitive `+shadow-sm` warm-tinted ratified — MetricsCards (Task 2) consume essa decision automaticamente via `<Card>` primitive shipped.
- ✅ Avatar primitive `data-[size=lg]:size-10` (40px max) — PublicPage avatar fallback usa `h-24 w-24` (96px) raw div (não primitive Avatar), zero impact aqui (diferente de Story 5.6 AvatarUpload override).

**Story 4.4 (UI Dashboard Analytics):**

- ✅ `app/dashboard/analytics/page.tsx` Server Component shipped com SQL aggregations Stories 4.1/4.2/4.3 + RPC `get_page_views_series` + 3 phases parallel queries (page + profile / links + total PV / per-link aggregations). **Preservar 100% — esta story é refresh visual apenas (className changes em components).**
- ✅ `MetricsCards.tsx` shipped com 4 cards (totalPageViews/totalClicks/pageViews30d/clicks30d) + `data-testid` per card + pt-BR localized + mix PT/EN labels DEV-10 ratificado (@po). Preservar 100% em behavior; apenas className `text-3xl` → `text-h2 text-primary` (AC2).
- ✅ `TimeSeriesChart.tsx` shipped com gap-fill client-side (RPC sparse → dense N dias) + ResponsiveContainer 300px + sr-only data table accessibility + ToggleLink 7d/30d via URL searchParams. Preservar 100% behavior; mudanças localizadas em imports + chart wrapper + gradient defs (AC3) + figcaption typescale + radius (AC4).
- ✅ `LinksTable.tsx` shipped com 3 colunas numéricas (7d/30d/Total) + null se rows vazio + truncate URLs + sort by clicksTotal DESC. Preservar 100% em behavior; mudança mandatória radius `rounded-lg` → `rounded-xl` + opcional inline bar (AC5).
- ✅ `EmptyAnalyticsState.tsx` shipped com `<BarChart3>` lucide icon + copy "Sem analytics ainda" + CTA "Ver minha página pública" condicional username. Preservar 100% behavior; mudanças className typescale/radius + copy reconcile per spec §2.8 L801 (AC6).
- ✅ `AnalyticsLoadingSkeleton.tsx` shipped com aria-busy + aria-live + sr-only message + grid 2×4 desktop/2×2 mobile + SkeletonBar primitive. Preservar 100% behavior; mudança radius `rounded-md` → `rounded-xl` (AC7).

**Story 2.7 (Página pública SSR):**

- ✅ `app/[username]/page.tsx` shipped com `parseUsername` validation + `getPublicPage` query + ISR `revalidate=60` + `generateMetadata` OG + `notFound()` fallback. Preservar 100% — esta story é refresh visual apenas em `components/public/{PublicPage,PublicLinkCard}.tsx`.
- ✅ `PublicPage.tsx` shipped com `data-theme={page.theme}` wrapper + `.dark` class condicional + ViewBeacon mount + 96×96 avatar Image (`priority` quando avatar_url presente) + 96×96 avatar fallback `<User/>` lucide div (`h-24 w-24 bg-muted`). Preservar 100% — mudança localizada em h1 className L61 (AC8) + bio p className L63 (AC9).

**Story 3.2 (Três presets de tema):**

- ✅ `PublicPage.test.tsx` (3 snapshots light/dark/brand via `it.each(THEMES)`) shipped — `data-theme` wrapper + `.dark` class assertions. Snapshots HTML-only (DEV-1 da 3.1 — jsdom 29 + Tailwind 4 não resolve CSS vars de stylesheets externas). **Mudanças AC8/AC9/AC10 forçam snapshot regenerate (AC11) — patterns intencional + rastreado para spec §2.10 L878-880 + L886-887.**

**Story 4.1 (Schema click events + tracking) + Story 4.2 (Page views tracking):**

- ✅ `TrackedLinkCard.tsx` Client wrapper + `ViewBeacon.tsx` Client mount-only — composição preservada (PublicPage usa TrackedLinkCard que wraps PublicLinkCard Server Component). Esta story não toca tracking layer; apenas className em PublicLinkCard (AC10).

### Data Models

`[Source: server/page/queries.ts, lib/supabase/types.ts]`

**PublicPageData shape (preserved Story 2.7):**

```typescript
// server/page/queries.ts — type inferred from getPublicPage return
export type PublicPageData = {
  profile: {
    username: string;
    display_name: string | null;
    bio: string | null;
    avatar_url: string | null;
  };
  page: {
    id: string;
    theme: 'light' | 'dark' | 'brand'; // Database['public']['Enums']['theme_preset']
  };
  links: Array<{
    id: string;
    title: string;
    url: string;
    icon: string | null;
    position: number;
  }>;
};
```

**Analytics aggregations (preserved Stories 4.1-4.3):**

- `page_views` table (count head: 'exact' lifetime) — total page views
- `click_events` table (count head: 'exact' on link_ids) — total clicks
- `page_views_30d` view (SUM count) — 30d page views
- `link_clicks_7d` + `link_clicks_30d` views — per-link aggregations
- `get_page_views_series` RPC — time series sparse (only days with count > 0)

Esta story **NÃO** toca aggregations/RPC/queries — apenas UI rendering (className + typescale + Area chart switch).

### API Specifications

`[Source: app/api/track/view/route.ts, app/api/track/click/route.ts — Stories 4.1/4.2]`

**Tracking endpoints (preserved):**

- `POST /api/track/view` — ViewBeacon dispatches on PublicPage mount (1 per pageId per ~30min dedup window)
- `POST /api/track/click` — TrackedLinkCard dispatches on link `<a>` click capture (sendBeacon primary + fetch keepalive fallback)

Esta story **NÃO** toca tracking endpoints — apenas client-side className em PublicLinkCard (AC10 hover shadow).

### Component Specifications

`[Source: docs/frontend-spec.md §2.8 + §2.10, components/analytics/*.tsx, components/public/*.tsx]`

**Analytics page flow (preserved Story 4.4):**

```
app/dashboard/analytics/page.tsx (Server Component)
├── Header (H1 + subtitle — AC1 refresh)
├── MetricsCards (4 stat cards — AC2 plum number)
├── TimeSeriesChart (Area + gradient — AC3/AC4 refresh)
└── LinksTable (3 cols + opcional bar — AC5 radius + DEV-C)
```

**Public page flow (preserved Story 2.7):**

```
app/[username]/page.tsx (Server Component)
└── PublicPage (Server Component — theme wrapper)
    ├── ViewBeacon (Client — mount-only)
    ├── Avatar Image OR User icon fallback (96×96)
    ├── h1 displayName (AC8 — text-h1 text-primary text-center)
    ├── bio paragraph (AC9 — text-body text-center)
    └── TrackedLinkCard (Client wrapper)
        └── PublicLinkCard (Server — AC10 hover:shadow-md)
```

### File Locations

`[Source: app/dashboard/analytics/, components/analytics/, components/public/, tests/components/features/]`

**Files modified (this story):**

```text
app/dashboard/analytics/page.tsx                              # AC1 — H1 + subtitle + header tightness (4-line delta)
components/analytics/MetricsCards.tsx                         # AC2 — CardTitle className (1-line delta)
components/analytics/TimeSeriesChart.tsx                      # AC3, AC4 — imports + AreaChart + defs + Area + figure radius + figcaption + ToggleLink radius (~10-line delta)
components/analytics/LinksTable.tsx                           # AC5 — outer div radius (1-line delta) + opcional inline bar (DEV-C ~5-line delta if ADD)
components/analytics/EmptyAnalyticsState.tsx                  # AC6 — outer div + H2 + body p + CTA className (4-line delta)
components/analytics/AnalyticsLoadingSkeleton.tsx             # AC7 — SkeletonBar base radius (1-line delta) + opcional H1 height (1-line delta)
components/public/PublicPage.tsx                              # AC8, AC9 — h1 + bio p className (2-line delta)
components/public/PublicLinkCard.tsx                          # AC10 — <a> className transition + hover:shadow-md (1-line delta)
tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap   # AC11 — 3 snapshots × 3 className strings = 9 line diffs (regenerate via -u)
docs/STORY-BACKLOG.md                                         # AC15 Task 12 — remove [EPIC-5-S7] MEDIUM + add DONE Change Log + stats update + narrative
```

**Files NOT modified (preserve 100% per AC12 negative ACs):**

```text
app/[username]/page.tsx                                       # Server Component routing — Story 2.7 preserve
server/page/queries.ts                                        # PublicPageData query — Story 2.7 preserve
components/public/TrackedLinkCard.tsx                         # Client beacon dispatcher — Story 4.1 preserve
components/public/ViewBeacon.tsx                              # Client mount beacon — Story 4.2 preserve
app/api/track/view/route.ts + app/api/track/click/route.ts    # Tracking endpoints — Stories 4.1/4.2 preserve
components/ui/card.tsx + button.tsx + (all 13 shadcn primitives)  # Primitives — Story 5.3 audit fechado
app/globals.css                                               # Tokens — Story 5.2 fechado
lib/theme.ts + supabase/migrations/0002_profiles.sql + 0003_pages.sql  # Theme enum + RLS preserve
```

### Testing Requirements

`[Source: docs/architecture/* (não-sharded — usar docs/architecture.md mono), MEMORY project_qa_review_hang.md, vitest.config.ts]`

**Test:components baseline:** 195/195 PASS (194 original + 1 novo Vibrante teste Story 5.6 — confirmado Dev Agent Record 5.6 L379 + L404).

**Snapshot impact:**

- `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — **OBRIGATÓRIO update via `pnpm test:components -u`** (3 snapshots × 3 className strings = 9 line diffs esperados: h1 + bio p + link `<a>`).
- Nenhum outro snapshot file existe em `tests/components/features/{analytics,public}/__snapshots__/` (validar via `find tests/components/features/{analytics,public} -name "*.snap"`).

**Behavior tests preserved (negative AC12):**

- `MetricsCards.test.tsx` — 4 cards data-testid + valores pt-BR
- `TimeSeriesChart.test.tsx` — toggle 7d/30d navigation + sr-only data table accessibility
- `LinksTable.test.tsx` — renderiza linhas + null se rows vazio + cliques
- `EmptyAnalyticsState.test.tsx` — copy + CTA link condicional username
- `AnalyticsLoadingSkeleton.test.tsx` — aria-busy + aria-live + sr-only
- `PublicPage.test.tsx` — data-theme wrapper + .dark class condicional + 3 snapshots (AC11)
- `PublicLinkCard.test.tsx` — href + target + rel + icon + title
- `TrackedLinkCard.test.tsx` — beacon dispatch sendBeacon/fetch fallback
- `ViewBeacon.test.tsx` — beacon mount + body

**QA gate execução manual** (per QA-001 known-issues + MEMORY `project_qa_review_hang.md`):

```yaml
qa_gate_manual:
  reason: CodeRabbit/WSL incompat. macOS — `@qa *review` trava Seção 0
  workaround: `docs/qa/known-issues.md` QA-001 manual checklist
  scope:
    - typecheck: pnpm typecheck → exit 0
    - lint: pnpm lint → zero warnings
    - build: pnpm build → 16/16 routes intactos
    - tests: pnpm test:components → 195/195 PASS pós-snapshot update
    - lighthouse_pr: Lighthouse CI ≥ 0.85 em /, /@demo, /dashboard (workflow auto)
```

### Technical Constraints

`[Source: package.json, next.config.mjs, .lighthouserc.json, .github/workflows/lighthouse.yml]`

**Stack constraints (preserved):**

- Next.js 16.2.5 / Turbopack — build deve completar sem erro
- React 19 — Server Components / Client Components boundaries preservados (TimeSeriesChart `'use client'` já shipped; AreaChart switch zero-impact em boundary)
- Tailwind CSS 4 — tokens via `@theme` directive em globals.css; mudanças aqui são apenas utility class consumption
- recharts — `AreaChart, Area` mesmo namespace `LineChart, Line`; switch zero-impact em bundle size esperado
- shadcn/ui — Card primitive consumed via composition (no changes)

**Performance constraints (preserved Story 2.7/3.5 + [STORY-3.5-F3]):**

- LCP `/@demo` < 2.0s — AC14 (spec §2.10 L918)
- Bundle `/@demo` ≤ 180 KB gz — AC14 (spec §2.10 L919)
- Lighthouse CI ≥ 0.85 Performance/Accessibility/Best Practices/SEO em `/` + `/@demo` + `/dashboard` (`.github/workflows/lighthouse.yml`)
- runs:3 + median absorve flake (`.lighthouserc.json` numberOfRuns: 3, aggregationMethod: median)

**Accessibility constraints (preserved per `docs/frontend-spec.md` §4 + `docs/a11y-audit.md` 2026-05-31):**

- WCAG 2.1 AA contrast — text-primary plum em h1 PublicPage validated em 3 temas (Story 3.2 contrast tests)
- Keyboard navigation — TimeSeriesChart ToggleLink (7d/30d) preserve `<Link href>` (native focus + Enter)
- Screen reader — sr-only data table TimeSeriesChart preserved; sr-only loading message AnalyticsLoadingSkeleton preserved
- Touch targets — ToggleLink ≥44px target (AC4 doc — `px-3 py-1` shipped ratified)
- `prefers-reduced-motion` — chart `isAnimationActive={false}` preserved (zero-impact em motion AC)

### Project Structure Notes

- Configuração `architectureSharded: true` em `core-config.yaml` L36 indica architecture sharded em `docs/architecture/` mas atual diretório só contém `schema-design.md` (validado via `ls docs/architecture/`). Story 5.7 usa fallback canônico `docs/frontend-spec.md` (source-of-truth do refresh per Story 5.6 pattern) + `docs/architecture.md` (mono) quando referência arquitetural ampla necessária. Spec frontend é canonical para esta story (frontend-primary refresh).
- Configuração `prdSharded: true` em `core-config.yaml` L29 mas `ls docs/prd/` retorna empty. Story 5.7 referencia `docs/prd.md` (mono) + Change Log v0.5 (2026-05-29 — ratification §UX/Branding Soft Studio).
- Story file path segue convenção `docs/stories/{epicNum}.{storyNum}.{kebab-case-title}.story.md` per `devStoryLocation: docs/stories` (core-config.yaml L48). Path final: `docs/stories/5.7.analytics-public-soft-studio.story.md` ✓.
- Frontmatter ClickUp omitido per `mcp.enabled: false` (core-config.yaml L22) — padrão consolidado Stories 1.4-5.6 todas Done sem ClickUp sync.
- `[Note: Using fallback file 'docs/frontend-spec.md' instead of sharded architecture/{frontend-architecture,components,core-workflows,data-models}.md — sharded files não existem; spec frontend é canonical per refresh Soft Studio ratificação PRD v0.5]`.
- `[Note: devLoadAlwaysFiles path 'docs/framework/{coding-standards,tech-stack,source-tree}.md' não existe; primary mono path é `docs/architecture.md` per Story 5.6 + 5.5 + 5.4 Dev Notes precedente]`.

### Suggested Files (Code Intelligence)

`[Source: skill execution — code intelligence opt-in graceful fallback]`

Code intelligence (`isCodeIntelAvailable()`) likely unavailable (provider não configurado per `aiox doctor` Story 5.6 implicit — sem flag explicito). Fallback canônico via cross-reference manual:

- `components/analytics/{MetricsCards,TimeSeriesChart,LinksTable,EmptyAnalyticsState,AnalyticsLoadingSkeleton}.tsx` — 5 components touched (AC2-AC7)
- `components/public/{PublicPage,PublicLinkCard}.tsx` — 2 components touched (AC8-AC10)
- `app/dashboard/analytics/page.tsx` — 1 page touched (AC1)
- `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — 1 snapshot regenerate (AC11)
- `tests/components/features/{analytics,public}/*.test.tsx` — 9 behavior tests preservados (AC12)
- `docs/STORY-BACKLOG.md` — housekeeping post-merge (AC15 Task 12)

Sem duplicação esperada — todos os components-alvo já existem (shipped Stories 4.4/2.7/3.2/4.1/4.2 + refreshed primitives 5.3) e a story refresha className/typescale/copy em superfícies bem delimitadas.

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Author      |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- |
| 2026-06-02 | 0.1     | **@sm `*draft 5.7` Interactive mode → Status Draft.** Story criada seguindo padrão template-v2 Stories 5.4/5.5/5.6 (Status / Executor Assignment / Story / 15 ACs / 🤖 CodeRabbit Integration / 12 Tasks / Dev Notes / Change Log / Dev Agent Record). **AC1-AC11 verbatim refresh:** Analytics page H1 + subtitle (AC1); MetricsCards plum + text-h2 (AC2); TimeSeriesChart switch `<LineChart>` → `<AreaChart>` + peach gradient under + plum stroke preservado (AC3); TimeSeriesChart figure radius + figcaption typescale + ToggleLink radius (AC4); LinksTable radius + opcional inline bar DEV-C (AC5); EmptyAnalyticsState radius + typescale + copy reconcile spec L801 verbatim (AC6); AnalyticsLoadingSkeleton radius alignment (AC7); PublicPage h1 `text-h1 text-primary text-center` (AC8); PublicPage bio `text-body text-center` (AC9); PublicLinkCard `hover:shadow-md transition-all` (AC10); PublicPage snapshot regenerate obrigatório 3 snapshots × 3 strings = 9 line diffs (AC11). **AC12-AC15 gates:** behavior tests preserved 195/195 baseline pós-5.6 + snapshot single update via -u; suite local typecheck/lint/build/test:components; Lighthouse CI `/@demo` gate path canônico ≥ 0.85 (não 0.90 backlog — reconcile gate atual `.lighthouserc.json`); housekeeping `[EPIC-5-S7]` MEDIUM → DONE pattern Stories 5.2-5.6. **DEV-A anticipated:** recharts `<LineChart>` → `<AreaChart>` switch + `<defs><linearGradient>` peach `--accent` `0.35 → 0` opacity stops; preserva stroke plum `--primary` + dot=false + isAnimationActive=false + gap-fill + sr-only table. **DEV-B anticipated:** snapshot regenerate intencional rastreado para spec §2.10 L878-880 + L886-887; validate via `git diff` apenas 9 line diffs esperados. **DEV-C conditional:** LinksTable inline bar visualization opt-in (@dev decide durante audit; ADD ou SKIP com racional documentado). **Out-of-scope explicitamente deferido:** footer "feito com biolink ★" (Story 5.9); share button "Phase 2" spec L899; motion polish (Story 5.8); avatar tema-specific bg (tokens já fazem via §1.2 brand `--background #F0E8FF`). Pendente `@po *validate-story-draft 5.7`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | River (@sm) |
| 2026-06-03 | 2.0     | **`@po *close-story 5.7` post-merge closure.** Story 5.7 mergeada em main via **PR #37** (squash commit `41c5f3d`). CI **12/12 SUCCESS** (install + lint + typecheck + test-unit + test-components 195/195 + test-integration + audit + build + **lighthouse 2m10s** + gitleaks + Vercel preview + Vercel Preview Comments; health-check skipping não-bloqueante). REL-001 LOW observation **RESOLVED** (Lighthouse pass confirmou bundle/a11y/perf gates ≥0.85 mantidos em `/` + `/@demo` + `/dashboard`). Branch `feat/story-5.7-analytics-public-soft-studio` deletada (remote + local via `gh pr merge --delete-branch`). Status `Ready for Review → Done` per `.claude/rules/story-lifecycle.md` (@devops merge → @po close). Working tree limpo post-discard `next-env.d.ts` auto-regen (pattern Stories 5.6/5.7 — Next auto-regenera cosmetic single → double quote durante build, descartado). `STORY-BACKLOG.md` atualizado: CLOSE entry adicionada (Change Log) + frontmatter `last_updated` reconciliado (remove "pendente push @devops" + adiciona PR #37 squash `41c5f3d` + CI 12/12 SUCCESS). Stats unchanged 11/0/2/9/7 (housekeeping `[EPIC-5-S7]` já aplicado por @dev pre-merge). Pattern Stories 5.2/5.3/5.4/5.5/5.6 close imediato post-merge. **Phase 3 Epic 5 COMPLETA** — 5 surfaces page-level Soft Studio alinhadas (Landing/Auth 5.4 + Dashboard Core 5.5 + Profile/Theme/Account 5.6 + Analytics/Public 5.7). Stories 5.8 (Motion + polish) + 5.9 (Logo + favicon) paralelizáveis prontas para `@sm *draft`. Próximo handoff: `@sm *draft 5.8` OU `@sm *draft 5.9` (ordem indiferente per Phase 4/5 não-bloqueantes entre si).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Pax (@po)   |
| 2026-06-03 | 1.0     | **@dev `*develop 5.7` YOLO mode → Status `Ready → Ready for Review`.** Implementação completa de 12 Tasks + 15 ACs verbatim. **DEV decisões ratificadas user opt-in pre-implementation (AskUserQuestion):** Mode YOLO + DEV-C SKIP (LinksTable inline bar — wireframe ilustrativo, tabela 3 colunas mais rica) + Task 11 SKIP smoke runtime manual (pattern Stories 5.4/5.5/5.6 per MEMORY `feedback_skip_manual_smoke.md`). **Mudanças mandatórias entregues:** Analytics page H1 `text-xl font-semibold tracking-tight` → `text-h1` + ADD subtitle "Como sua página tá indo." (spec §2.8 L754 verbatim) + `<header space-y-1>`; MetricsCards `<CardTitle text-3xl>` → `text-h2 text-primary` (plum §2.8 L759); TimeSeriesChart (DEV-A ratified) switch `<LineChart>/<Line>` → `<AreaChart>/<Area>` + ADD `<defs><linearGradient analytics-area-gradient>` peach `--accent` 0.35→0 opacity + preserva stroke plum `--primary` + dot=false + isAnimationActive=false + gap-fill + sr-only table + figure `rounded-lg` → `rounded-xl` (§1.4.2) + figcaption H2 `text-sm` → `text-h3 font-medium` (§1.3.2) + ToggleLink `rounded-md` → `rounded-lg`; LinksTable `rounded-lg` → `rounded-xl`; EmptyAnalyticsState outer `rounded-lg` → `rounded-xl` + H2 `text-lg` → `text-h3 font-medium` + body `text-sm` → `text-body` + copy reconcile spec §2.8 L801 verbatim com fallback condicional username + CTA `text-sm` → `text-body-sm`; AnalyticsLoadingSkeleton SkeletonBar `rounded-md` → `rounded-xl` + H1 placeholder `h-7` → `h-8`; PublicPage h1 `text-2xl font-semibold tracking-tight` → `text-h1 text-primary text-center` (§2.10 L878) + bio `text-center text-muted-foreground` → `text-body text-center text-muted-foreground` (§2.10 L880); PublicLinkCard `transition-colors hover:bg-accent` → `transition-all hover:bg-accent hover:shadow-md` (§2.10 L887); snapshot PublicPage regenerate (DEV-B ratified) 3 snapshots × 3 className strings = **18 line diffs exatos conforme predição AC11**, zero outras mudanças estruturais. **Test rewrites mandatórios (descobertos durante implementação contradizendo "zero-impact" da story — pattern idêntico Story 5.6 ThemeSelector test rewrite):** (1) `TimeSeriesChart.test.tsx` recharts mock ADD `AreaChart: Passthrough, Area: () => null` (5 testes 0/5 FAIL → 5/5 PASS após fix); (2) `EmptyAnalyticsState.test.tsx` 2 assertions copy `"...ver analytics."` → `"...em /@alice para começar a ver dados aqui."` (com username) + `"...para começar a ver dados aqui."` (sem username) (3 testes 1/3 FAIL → 3/3 PASS após fix). **Gates locais:** `pnpm typecheck` exit 0 ✓ + `pnpm lint` zero warnings ✓ + `pnpm build` Next 16.2.5 Turbopack 15/15 static pages PASS (16 routes intactos) ✓ + `pnpm test:components` final **195/195 PASS** (12m47s; baseline pós-5.6 preservado) ✓. **Bundle delta esperado neutral** (sem novo dependency; mudanças são className + 1 import switch recharts + inline `<defs>` SVG). **Backlog housekeeping:** `[EPIC-5-S7]` MEDIUM block removido + Change Log DONE entry + stats 12/0/3/9/6 → 11/0/2/9/7 + narrative Epic 5 update ✅ 5.7 DONE + last_updated reconcile. **Pré-existing chore commit:** `4b90a18` chore(story-5.6): close Done post-merge (@po close-story 5.6 da sessão anterior — STORY-BACKLOG.md + 5.6 story Done — commit autônomo ANTES de iniciar work 5.7 para manter commits limpos). **Pendente push @devops** (Constitution Art. II — `git push`/`gh pr create` exclusivos de @devops). **Phase 3 Epic 5 completa** — 5.8 (motion polish) + 5.9 (logo + favicon) paralelizáveis para closure total.                                                                                                                                                                                                                                                                                                                                                                        | Dex (dev)   |
| 2026-06-02 | 0.2     | **@po `*validate-story-draft 5.7` Interactive mode → Status `Draft → Ready`. Verdict: GO (10/10).** 10-point validation checklist (`.claude/rules/story-lifecycle.md` Phase 2) integralmente PASS: (1) título objetivo "Analytics + Public — Soft Studio Refresh" ✓; (2) descrição completa com Story statement + Rationale + Backlog crossover + 3 DEV decisões anticipadas ✓; (3) 15 AC testáveis com [Source: ...] verbatim + métricas mensuráveis (typecheck/lint/build/195 tests/Lighthouse ≥0.85/9-line diff snapshot) ✓; (4) escopo IN (15 ACs explícitos) + OUT (12 itens deferred para 5.8/5.9) ✓; (5) dependencies mapeadas (5.2/5.3 base + 5.4-5.6 page precedent + 4.4 analytics + 2.7/3.2/4.1/4.2 public) ✓; (6) complexity M frontend-primary justificada por 1 page + 7 components + 1 snapshot regenerate ✓; (7) business value claro (Public `/@username` = único surface visitor visible = gate path Lighthouse canônico `.github/workflows/lighthouse.yml:72`) ✓; (8) risks documented via DEV-A (recharts AreaChart switch) + DEV-B (snapshot 9-line diff esperados validáveis via `git diff`) + DEV-C condicional (LinksTable inline bar) ✓; (9) criteria of Done explícito (195/195 tests + Lighthouse ≥0.85 `/@demo` + bundle ≤180 KB gz + snapshot regenerate single file) ✓; (10) alignment PRD v0.5 §UX/Branding + Epic 5 + spec §5.3 Phase 3 ratificado 2026-05-29 ✓. **Validation Steps (task `validate-next-story.md`):** Step 1 Template Completeness PASS (todas seções template-v2 presentes; placeholders Dev Agent Record + QA Results intencionais para preenchimento pós-implementação); Step 1.1 Executor Assignment PASS (`executor: '@dev'` + `quality_gate: '@qa'` + tools array; `executor != quality_gate`; frontend-primary @dev convention precedent Stories 5.4-5.6); Step 2 File Structure PASS (9 files modified + files NOT modified explícitos com paths verificados); Step 3 UI/Frontend Completeness PASS (tokens semânticos cited + WCAG AA preserved 3 temas + sr-only data table preserved + Server/Client boundaries claros); Step 4 AC Satisfaction PASS (Task-AC mapping 1:1 explícito; edge cases AC5 max divide-by-zero + AC6 username fallback + AC11 9-line diff validation); Step 5 Testing Guidance PASS (9 test files + baseline 195/195 pós-5.6 + 1 snapshot regenerate AC11 + QA-001 manual workaround documentado); Step 6 Security N/A (UI-only refresh; tracking endpoints + ISR preserved); Step 7 Tasks Sequence PASS (Task 9 snapshot AFTER Tasks 7+8 dep clara; Task 10 gates AFTER all changes; Task 11 smoke CONDITIONAL; Task 12 housekeeping last); Step 8 CodeRabbit N/A (`coderabbit_integration` ausente em core-config.yaml → skip notice rendered corretamente conforme template-v2 pattern Stories 1.4-5.6); Step 8.1 Code Intel SKIP (graceful fallback — Suggested Files manual cross-reference fornecido); Step 9 Anti-Hallucination PASS (cada AC cita `[Source: docs/frontend-spec.md §X.Y LZZ]`; plum/peach rastreados para tokens `--primary #5b3a8c` / `--accent #ffb5a7`; subtitle "Como sua página tá indo." verbatim §2.8 L754; empty state copy verbatim §2.8 L801; h1 plum centered verbatim §2.10 L878; bio body verbatim §2.10 L880; Lighthouse threshold reconciled de `≥0.90 backlog` para `≥0.85 actual gate .lighthouserc.json` — reconciliação consciente, não invenção); Step 10 Dev Implementation Readiness PASS (self-contained — cada AC inline current code + expected change + spec ref; sem context switching externo necessário). **Implementation Readiness Score: 9.5/10** (small deduction for DEV-C conditional decision-point — mirrors successful Stories 5.5/5.6 DEV-H/DEV-C/DEV-D pattern). **Confidence Level: HIGH.** Status transição `Draft → Ready` per `.claude/rules/story-lifecycle.md` Phase 2 (CRITICAL — PO MUST update status field when verdict GO). **Próximo handoff:** `@dev *develop 5.7` (Interactive mode default; YOLO se @dev preferir per Story 5.6 precedent). | Pax (@po)   |

## Dev Agent Record

### Agent Model Used

- **Agent:** @dev (Dex — Builder)
- **Model:** claude-opus-4-7 (Anthropic Opus 4.7)
- **Mode:** YOLO (autônomo) ratificado user opt-in pre-implementation
- **Date:** 2026-06-02 → 2026-06-03 (cross-midnight; macOS slow environment full suite ~12-13min per run)

### Debug Log References

- **Branch:** `feat/story-5.7-analytics-public-soft-studio` (criada de `main`)
- **Pre-existing chore commit:** `4b90a18` — `chore(story-5.6): close Done post-merge [Story 5.6]` (housekeeping @po close-story 5.6 da sessão anterior — STORY-BACKLOG.md last_updated + 5.6 story Status `Ready for Review → Done` + Change Log v2.0 entry; commit autônomo ANTES de iniciar work 5.7 para manter commits 5.7 limpos)
- **Test failures encountered + mandatory fixes:**
  1. `tests/components/features/analytics/TimeSeriesChart.test.tsx` — 5 testes 0/5 FAIL após switch `<LineChart>/<Line>` → `<AreaChart>/<Area>` (AC3); root cause: `vi.mock('recharts')` mock leve com lista hardcoded sem `AreaChart`/`Area`. **Fix:** ADD `AreaChart: Passthrough, Area: () => null,` ao mock (preserva pattern Passthrough existente; `LineChart`/`Line` preservados para tolerância). Resultado pós-fix: 5/5 PASS isolated. Pattern análogo ao Story 5.6 test rewrite mandatório (ThemeSelector `name: /brand/i` → `/vibrante/i` regex update — também descoberto durante implementação contradizendo "zero-impact" da story original).
  2. `tests/components/features/analytics/EmptyAnalyticsState.test.tsx` — 2/3 testes FAIL após copy reconcile (AC6); root cause: testes assertam copy verbatim antiga `"Compartilhe sua página para começar a ver analytics."` (shipped Story 4.4); spec §2.8 L801 verbatim novo = `"Compartilhe sua página em /@${username} para começar a ver dados aqui."` (com username) + fallback genérico (sem username). **Fix:** atualizar 2 assertions: (a) test 1 (com username "alice") → `"Compartilhe sua página em /@alice para começar a ver dados aqui."`; (b) test 3 (sem username) → `"Compartilhe sua página para começar a ver dados aqui."`. JSDoc header atualizado para refletir Story 5.7 AC6 copy refresh + spec §2.8 L801 verbatim source. Resultado pós-fix: 3/3 PASS isolated.
- **Snapshot regenerate output:**
  - Comando: `pnpm test:components -u tests/components/features/public/PublicPage.test.tsx`
  - 1ª run: `Snapshots 3 updated` ✓ (PublicPage 3/3 PASS) MAS unrelated test files dialog.test.tsx + form.test.tsx flaked com "Timeout waiting for worker to respond" (macOS slow environment pattern — idêntico à Story 5.6 LoginForm flake; 7 testes total flaked across 3 unrelated files).
  - Validação `git diff`: 18 line diffs no `.snap` (9 +/- 9) exatamente conforme predição AC11: 3 snapshots × 3 className strings (h1 + bio p + link `<a>`); zero outras mudanças estruturais.
- **Full test:components final run:** **34/34 test files PASS, 195/195 tests PASS, exit 0, duration 12m47s.** Baseline pós-5.6 (195/195) preservado integralmente.
- **macOS slow environment observation:** test:components full suite varia entre ~5-16min por run em macOS dependendo de carga de fundo. Worker timeout flakes em dialog/form/PublicPage test files são environment-bound (não regressões 5.7) — sempre re-run isolated para validar. Pattern documentado em Story 5.6 Dev Agent Record.

### Completion Notes List

**DEV decisões ratificadas (user opt-in pre-implementation via AskUserQuestion):**

1. **Mode: YOLO** — Decisões tomadas autonomously com logging em commits + Change Log. ACs bem definidas justificaram baixa interatividade.
2. **DEV-C: SKIP** (LinksTable inline bar) — Preserva 3 colunas numéricas (7d / 30d / Total); wireframe ilustrativo cobre o conceito mas tabela atual é UX-richer para dono do BioLink. Risco zero (apenas radius `rounded-lg` → `rounded-xl` aplicado per AC5 mandatory).
3. **Task 11 SKIP** (smoke runtime manual × 3 temas) — Cobertura indireta via behavior tests 195/195 + Lighthouse CI `/@demo` gate no PR. Pattern Stories 5.4/5.5/5.6.

**Mudanças mandatórias entregues (AC1-AC11):**

- **AC1 — Analytics page header:** H1 `text-xl font-semibold tracking-tight` → `text-h1` + ADD `<p text-body text-muted-foreground>Como sua página tá indo.</p>` (spec §2.8 L754 verbatim) + `<header className="space-y-1">` tightness (pattern Stories 5.4/5.5/5.6).
- **AC2 — MetricsCards big number plum:** `<CardTitle text-3xl tabular-nums>` → `<CardTitle text-h2 tabular-nums text-primary>` (spec §2.8 L759 "H2 big number, plum").
- **AC3 — TimeSeriesChart Area + peach gradient (DEV-A ratified):** imports `LineChart, Line` → `AreaChart, Area` (mesmo namespace recharts); `<LineChart>` → `<AreaChart>` (props idênticas); ADD `<defs><linearGradient id="analytics-area-gradient" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--accent)" stopOpacity={0.35} /><stop offset="100%" stopColor="var(--accent)" stopOpacity={0} /></linearGradient></defs>`; `<Line>` → `<Area type="monotone" dataKey="count" stroke="var(--primary)" strokeWidth={2} fill="url(#analytics-area-gradient)" dot={false} isAnimationActive={false} />`. Preservado: stroke plum + dot=false + isAnimationActive=false + gap-fill client-side + sr-only data table + tooltip pt-BR + ResponsiveContainer 300px.
- **AC4 — TimeSeriesChart figure + figcaption typescale:** figure `rounded-lg` → `rounded-xl` (radius scale §1.4.2); figcaption H2 `text-sm font-medium` → `text-h3 font-medium` (subsection level §1.3.2); ToggleLink `rounded-md` → `rounded-lg` (radius alignment intermediate; documentei em commit message).
- **AC5 — LinksTable radius:** `rounded-lg` → `rounded-xl`. DEV-C SKIP (mandatory only).
- **AC6 — EmptyAnalyticsState refresh:** outer div `rounded-lg` → `rounded-xl`; H2 `mt-4 text-lg font-semibold` → `mt-4 text-h3 font-medium`; body `mt-2 text-sm` → `mt-2 text-body` + copy reconcile spec §2.8 L801 verbatim com fallback condicional username; CTA `text-sm font-medium` → `text-body-sm font-medium`.
- **AC7 — AnalyticsLoadingSkeleton radius:** SkeletonBar base `rounded-md` → `rounded-xl`; H1 placeholder `h-7 w-32` → `h-8 w-32` (approxima text-h1 32px visual).
- **AC8 — PublicPage h1 plum centered:** `<h1 className="text-2xl font-semibold tracking-tight">` → `<h1 className="text-h1 text-primary text-center">` (spec §2.10 L878).
- **AC9 — PublicPage bio body:** `<p className="text-center text-muted-foreground">` → `<p className="text-body text-center text-muted-foreground">` + reformat com newline (Prettier auto-reformat preservou estrutura).
- **AC10 — PublicLinkCard hover shadow:** `transition-colors hover:bg-accent` → `transition-all hover:bg-accent hover:shadow-md` (spec §2.10 L887; preserva radius-xl + bg-card + border-border shipped Story 2.5/5.3).
- **AC11 — Snapshot regenerate (DEV-B ratified):** `pnpm test:components -u tests/components/features/public/PublicPage.test.tsx`; 3 snapshots × 3 className strings = 18 line diffs no `.snap` exatos: (a) h1 `text-2xl font-semibold tracking-tight` → `text-h1 text-primary text-center`; (b) bio p `text-center text-muted-foreground` → `text-body text-center text-muted-foreground`; (c) link `<a>` `transition-colors hover:bg-accent` → `transition-all hover:bg-accent hover:shadow-md`. Zero outras mudanças estruturais (avatar fallback + svg lucide user + main flex layout intactos).

**Gates AC12-AC15:**

- AC12: 9 test files preservados; 2 test rewrites mandatórios (TimeSeriesChart mock + EmptyAnalyticsState copy) — rastreados aqui.
- AC13: `pnpm typecheck` exit 0 ✓ + `pnpm lint` zero warnings ✓ + `pnpm build` Next 16.2.5 Turbopack 15/15 static pages + 16 routes ✓ + `pnpm test:components` **195/195 PASS** (12m47s) ✓.
- AC14: Lighthouse CI `/@demo` gate path canônico será validado no PR (workflow `.github/workflows/lighthouse.yml:72`). Bundle delta esperado neutral (mudanças são className + 1 import switch recharts + inline `<defs>` SVG; zero novo dependency; zero novo lucide-react import).
- AC15: housekeeping `[EPIC-5-S7]` MEDIUM bloco removido + stats 12/0/3/9/6 → 11/0/2/9/7 + narrative Epic 5 update ✅ 5.7 DONE + Change Log DONE entry + frontmatter `last_updated` reconciled.

**Risco residual documentado:**

- Smoke manual SKIPPED — visual regressions sub-perceptuais não capturadas por snapshot HTML-only ou behavior tests; mitigation via Lighthouse a11y/perf gates ≥0.85 + manual visual review @qa pré-merge no PR.
- DEV-C SKIP — wireframe inline bar diferente da tabela 3-coluna shipped; aceito como UX tradeoff (info-richer prevails).
- Worker timeout flakes em dialog.test.tsx + form.test.tsx + (1ª run snapshot regenerate) — environment-bound (macOS slow), não regressão 5.7; full suite final run PASS confirma.

**Pendente push @devops** (Constitution Art. II — `git push`/`gh pr create` exclusivos de @devops). Próximo handoff: `@qa *qa-gate 5.7` OU `@devops *push` (depende preferência do user para QA gate pré-push ou pós-push).

### File List

**Modified (8 source files + 2 test files + 2 docs):**

```text
app/dashboard/analytics/page.tsx                                      # AC1 — header H1 + subtitle + space-y-1 (3-line delta)
components/analytics/MetricsCards.tsx                                 # AC2 — CardTitle text-h2 text-primary (1-line delta)
components/analytics/TimeSeriesChart.tsx                              # AC3 + AC4 — imports + AreaChart + defs + Area + figure radius + figcaption typescale + ToggleLink radius (~15-line delta)
components/analytics/LinksTable.tsx                                   # AC5 — outer div radius (1-line delta)
components/analytics/EmptyAnalyticsState.tsx                          # AC6 — outer + H2 + body + CTA + copy reconcile (~10-line delta)
components/analytics/AnalyticsLoadingSkeleton.tsx                     # AC7 — SkeletonBar radius + H1 placeholder height (2-line delta)
components/public/PublicPage.tsx                                      # AC8 + AC9 — h1 className + bio p className (3-line delta com reformat)
components/public/PublicLinkCard.tsx                                  # AC10 — <a> className transition + hover:shadow-md (1-line delta)
tests/components/features/analytics/TimeSeriesChart.test.tsx          # AC12 mandatory test fix — recharts mock ADD AreaChart + Area (2-line delta + comment update)
tests/components/features/analytics/EmptyAnalyticsState.test.tsx      # AC12 mandatory test fix — 2 assertions copy reconcile spec §2.8 L801 verbatim (4-line delta + JSDoc header update)
tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap  # AC11 snapshot regenerate via -u (18 line diffs 9+/9-)
docs/STORY-BACKLOG.md                                                 # AC15 Task 12 — remove [EPIC-5-S7] block + stats 12/0/3/9/6 → 11/0/2/9/7 + narrative Epic 5 update + Change Log DONE entry + last_updated reconcile
docs/stories/5.7.analytics-public-soft-studio.story.md                # Status InProgress → Ready for Review + Tasks/Subtasks [ ] → [x] + Dev Agent Record + Change Log v1.0 entry
```

**Not modified (preservado per AC12 negative ACs):**

```text
app/[username]/page.tsx                                               # Server Component routing — Story 2.7 preserve
server/page/queries.ts                                                # PublicPageData query — Story 2.7 preserve
components/public/TrackedLinkCard.tsx                                 # Client beacon dispatcher — Story 4.1 preserve
components/public/ViewBeacon.tsx                                      # Client mount beacon — Story 4.2 preserve
app/api/track/view/route.ts + app/api/track/click/route.ts            # Tracking endpoints — Stories 4.1/4.2 preserve
components/ui/*.tsx (13 shadcn primitives)                            # Primitives — Story 5.3 audit fechado
app/globals.css                                                       # Tokens — Story 5.2 fechado
lib/theme.ts + supabase/migrations/0002_profiles.sql + 0003_pages.sql # Theme enum + RLS preserve
```

### Story Draft Checklist Results

**Executado por:** @sm (River) — `*draft 5.7` auto-checklist pós-criação (2026-06-02)
**Checklist source:** `.aiox-core/product/checklists/story-draft-checklist.md`
**Story readiness:** **READY** (Clarity score: **9/10**)

| Category                             | Status   | Issues                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1. Goal & Context Clarity            | **PASS** | Story statement (As a / I want / so that) + Rationale callout cobrem goal, epic relationship (Phase 3 final 5.4-5.7), system flow (dashboard authenticated + public visitor surface), dependencies explícitas (5.2/5.3 base + 5.4/5.5/5.6 page-level + 4.4 analytics + 2.7/3.2/4.1/4.2 public), business value (Public = único surface diferenciador visível visitantes = gate path Lighthouse). |
| 2. Technical Implementation Guidance | **PASS** | File Locations section lista 9 files modified + files NOT modified (preserve negative ACs); imports/APIs descritos (recharts AreaChart switch, tracking endpoints preserved, PublicPageData shape quoted); deviations consciously noted via DEV-A (recharts switch), DEV-B (snapshot regenerate intencional), DEV-C condicional (LinksTable inline bar opt-in).                                  |
| 3. Reference Effectiveness           | **PASS** | Cada AC1-AC15 cita seção verbatim spec `[Source: docs/frontend-spec.md §X.Y LZZ]`; Previous Story Insights summary cobre 5.6/5.5/5.4/5.3/4.4/2.7/3.2/4.1/4.2 (não apenas refs externos); formato consistente.                                                                                                                                                                                    |
| 4. Self-Containment Assessment       | **PASS** | Snippets de código inline (linhas exatas current + esperadas pós-mudança); plum/peach explicados com hex (`#5B3A8C` / `#FFB5A7`) + token names; edge cases addressed (empty username fallback AC6 + max clicks divide-by-zero AC5 + 3 temas contrast preserved AC8); out-of-scope (12 items) explicitamente deferred.                                                                            |
| 5. Testing Guidance                  | **PASS** | Test approach outlined (typecheck + lint + build + test:components 195/195 baseline + 1 snapshot regen + Lighthouse CI `/@demo`); 9 test files referenciados em Testing Requirements; success criteria mensurável (195/195 PASS + 9 line diffs no `.snap`); special considerations (QA-001 manual workaround per MEMORY `project_qa_review_hang.md`).                                            |
| 6. CodeRabbit Integration            | **N/A**  | `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` + `mcp.enabled: false` → skip notice presente conforme template-v2 (idêntico Stories 1.4-5.6 todas Done). QA gate manual via QA-001 known-issues.                                                                                                                                                                              |

**Specific Issues:** Nenhum crítico identificado.

- _Minor:_ DEV-C (LinksTable inline bar) é condicional opt-in — @dev decide durante audit. Pattern idêntico ao DEV-C/D da Story 5.6 (subsection labels Profile + Card wrap Account export) que foi navegado com sucesso (ambos ratificados ADD por @dev na 5.6 Done).
- _Minor:_ Story tem 15 ACs (vs 14 na 5.6 e 13 na 5.5) — densidade maior justificada pelo escopo: 1 page + 7 components + 1 snapshot regenerate obrigatório forçam ACs separadas para rastreabilidade. Tasks correspondentes (12 Tasks) mantém granularidade.

**Developer Perspective:**

- **Pode @dev implementar como escrito?** Sim. Cada AC tem linha exata + transformação esperada + source spec. Padrão idêntico ao bem-sucedido Story 5.6 (Done).
- **Questões esperadas:** Apenas a decisão DEV-C (inline bar ADD vs SKIP) — perguntar antes via `feedback_skip_manual_smoke.md` pattern. Demais ACs determinísticas.
- **Causas potenciais de delay:** (a) recharts AreaChart switch (AC3) — verificar se `<Area>` props são idênticas a `<Line>` props no recharts 2.x shipped — quick sanity via `pnpm typecheck`; (b) PublicPage snapshot regenerate (AC11) — usar `-u` flag não interativo + `git diff` review é trivial.
- **Risco de rework:** BAIXO. Snapshot regenerate é tipicamente fonte de rework em PRs grandes, mas aqui AC11 explicita as 9 line diffs esperadas para validação por @dev pré-commit.

**Final Assessment:** ✅ **READY** — A story provê extreme context para @dev implementar sem ambiguidade. Próximo passo: `@po *validate-story-draft 5.7` (10-point validation checklist).

## QA Results

### Review Date: 2026-06-03

### Reviewed By: Quinn (Test Architect)

**Review mode:** QA Gate Manual (workaround QA-001 — `docs/qa/known-issues.md` + MEMORY `project_qa_review_hang.md`). Padrão validado em Stories 5.2/5.3/5.4/5.5 todas mergeadas com gate manual. CodeRabbit/WSL bypass per `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` e ambiente macOS sem WSL.

**Sanity gates re-rodados durante QA:**

- ✅ `pnpm typecheck` exit 0
- ✅ `pnpm lint` exit 0 zero warnings
- ✅ Branch state: `feat/story-5.7-analytics-public-soft-studio` 2 commits ahead de main (chore close 5.6 + feat 5.7), working tree limpo
- ✅ Diff stats batem com File List da story: 13 files (619+/58−), 1 NEW story file

**AC traceability (15/15 cobertas):**

- AC1-AC10 → 8 source files modificados con git diff verificável + spec §2.8 + §2.10 verbatim refs
- AC11 → 18 line diffs (9+/9−) exatos no `PublicPage.test.tsx.snap` (3 snapshots × 3 className strings) ratificado via `git diff`
- AC12 → 195/195 PASS final (baseline pós-5.6 preservado) com 2 test rewrites mandatórios documentados (`MNT-001` LOW)
- AC13 → suite local PASS (typecheck/lint/build/test:components)
- AC14 → Lighthouse CI `/@demo` pendente validação no PR (`REL-001` LOW observation)
- AC15 → Constitution Art. IV + housekeeping `[EPIC-5-S7]` MEDIUM → DONE + stats 12/0/3/9/6 → 11/0/2/9/7 ratificados

**NFR assessment:**

| NFR           | Status       | Notes                                                                                                                                                                                                                                                       |
| ------------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Security      | ✅ Preserved | UI-only refresh; zero auth/schema/RLS/tracking endpoint changes. Tracking endpoints (`/api/track/view` + `/api/track/click`) intactos.                                                                                                                      |
| Performance   | ✅ Preserved | Bundle delta neutral esperado (className changes + recharts namespace switch + inline `<defs>` SVG; zero novo dependency). LCP `/@demo` target < 2.0s preserved.                                                                                            |
| Reliability   | ✅ Preserved | ISR `revalidate=60` preserved. ViewBeacon mount-only + TrackedLinkCard sendBeacon/fetch fallback intactos.                                                                                                                                                  |
| Accessibility | ✅ Preserved | WCAG 2.1 AA: `text-primary` plum validated 3 themes (Story 3.2 contrast tests); sr-only data table TimeSeriesChart preserved; sr-only loading message AnalyticsLoadingSkeleton preserved; `prefers-reduced-motion` preserved (`isAnimationActive={false}`). |
| Testability   | ✅ Preserved | 195/195 baseline pós-5.6 mantido; 2 test rewrites mandatórios (TimeSeriesChart mock + EmptyAnalyticsState copy) classificados em `MNT-001` learning para futuras stories.                                                                                   |

**Risk profile:**

- 🟢 LOW `REL-001` — Lighthouse CI `/@demo` validação pendente no PR (gate canônico `.github/workflows/lighthouse.yml:72`); bundle delta esperado neutral; investigar se queda > 5 pontos.
- 🟢 LOW `RISK-SMOKE-SKIP` — Smoke runtime manual × 3 temas SKIPPED user opt-in (pattern Stories 5.4/5.5/5.6); mitigation via behavior tests + Lighthouse CI no PR.
- 🟢 LOW `MNT-001` — 2 test rewrites mandatórios contradizendo "zero-impact" da story (pattern Story 5.6 ThemeSelector); learning emergente para drafts 5.8/5.9.

**DEV decisions ratificadas:** DEV-A (TimeSeriesChart Area switch + peach gradient) ✓ DEV-B (snapshot regenerate intencional) ✓ DEV-C (LinksTable inline bar SKIP) ✓ DEV-MODE-YOLO ✓ DEV-TASK-11-SKIP ✓ DEV-CHORE-PRE ✓ DEV-NEXT-ENV-DISCARD ✓.

### Gate Status

Gate: PASS → `docs/qa/gates/5.7-analytics-public-soft-studio.yml`

**Recommendation:** @devops `*push` (pattern Stories 5.5/5.6 — feat squash merge + Vercel preview + Lighthouse CI gate ≥0.85 + close-story @po post-merge).
