# Story 2.4: Layout e Navegação do Dashboard

## Status

Done

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools:
  ['typecheck', 'lint', 'build', 'test:unit', 'test:components', 'test:integration']
complexity: 'M'
```

> **Rationale (DEV-1):** Story de **Frontend / implementação de componentes** (shell de layout + navegação + a11y), não criação de design visual novo — compõe tokens/primitives já existentes. O template v2 mapeia "Design/UI Components → @ux-design-expert / gate @dev", mas o **precedente sancionado e Done** das Stories de implementação frontend deste repo (1.5 UI auth, 1.8 seed design system, 1.9 landing) usa **`executor: @dev` / `quality_gate: @qa`** com `quality_gate_tools` idênticos. A Story 1.8 §Executor documenta a razão canônica: _"Por ser trabalho de código/teste (não design visual — os primitives já existem), o executor é @dev (Dex) com gate por @qa (Quinn), consistente com o padrão do Epic 1"_. Deviation documentada, **não invenção**; flagged p/ gate @po (mesma classe de observação não-bloqueante validada em 2.3). Consulta opcional a @ux-design-expert (Uma) para revisão a11y WCAG 2.1 AA é recomendada, **não-bloqueante**.

## Story

**As a** usuário autenticado,
**I want** um layout de dashboard com navegação por sidebar (desktop) / drawer (mobile) e um header com meu avatar,
**so that** eu possa transitar entre Links / Profile / Theme / Analytics / Account e acessar ações de conta a partir de qualquer página do dashboard.

## Acceptance Criteria

1. Layout `app/dashboard/layout.tsx` com sidebar fixa (desktop) ou drawer (mobile).
2. Itens de navegação: Links, Profile, Theme, Analytics, Account.
3. Rota ativa destacada visualmente.
4. Header com avatar + dropdown (logout, copy public URL).
5. Acessibilidade: navegação por teclado completa, ARIA roles, focus trap no drawer mobile.

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

> **Nota (consistente com 2.3/1.9):** `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` **e** `mcp.enabled: false`. O gate de qualidade usa o **workaround manual QA-001** (`docs/qa/known-issues.md`): `@qa *review` trava em macOS (sem WSL/CodeRabbit CLI) → revisão independente via `git diff main...HEAD` + CI do PR como verificação independente do ambiente local (precedente sancionado 1.1/1.5–1.9). Ver DEV-2.

## Tasks / Subtasks

- [x] **Task 1 — Adicionar primitives shadcn/ui faltantes (AC: 1, 4)**
  - [x] Confirmar que `components/ui/` hoje tem apenas `button, form, input, label, sonner` (faltam `avatar`, `dropdown-menu`, `sheet`).
  - [x] Adicionar **somente** os componentes faltantes via `pnpm dlx shadcn@latest add avatar dropdown-menu sheet` — **NÃO** re-rodar `shadcn init` (idempotência; precedente Story 1.8 Task 1). Aceitar prompts default; `components.json` já está coerente (`style: base-nova`, `baseColor: slate`, `rsc: true`, `cssVariables: true`).
  - [x] Verificar que os novos arquivos usam `@/lib/utils` `cn()` e os tokens existentes; **não** sobrescrever tokens de `app/globals.css` (set `--sidebar*` já presente, linhas 43-50 + `@theme inline` L126).
  - [x] `pnpm prettier --write components/ui/*.tsx` se o CLI emitir formato raw (precedente DX herdado 1.4/2.2/2.3 — cosmético, não-bloqueante).
- [x] **Task 2 — Módulo compartilhado de itens de navegação (AC: 2)**
  - [x] Criar `components/dashboard/nav-items.ts` exportando um array tipado dos 5 itens (`label`, `href`, `icon` lucide) — **fonte única** consumida por `Sidebar` e `MobileDrawer` (evita duplicar a lista; feature-folder, sem barrel — coding standards).
  - [x] Itens (href = rota canônica arch §Routing): `Links → /dashboard`, `Profile → /dashboard/profile`, `Theme → /dashboard/theme`, `Analytics → /dashboard/analytics`, `Account → /dashboard/account`. Ícones lucide sugeridos: `Link`, `User`, `Palette`, `BarChart3`, `Settings`. Ver DEV-4 (rotas Theme/Analytics/Account ainda não existem — fora de escopo).
- [x] **Task 3 — `components/dashboard/Sidebar.tsx` (AC: 2, 3, 5)**
  - [x] Client Component (`'use client'` justificado: `usePathname` p/ rota ativa). Renderiza `<nav aria-label="Navegação principal">` com a lista de `nav-items.ts`, cada item como `next/link`.
  - [x] Rota ativa (AC3): item cujo `href` casa o `pathname` recebe `aria-current="page"` + destaque visual via tokens (`bg-sidebar-accent text-sidebar-accent-foreground`); demais usam `text-sidebar-foreground hover:bg-sidebar-accent`. Match: igualdade exata para `/dashboard` (não usar `startsWith` cru — evita marcar Links em todas as sub-rotas); `startsWith` apenas para as sub-rotas (`/dashboard/profile` etc.).
  - [x] Container desktop: sidebar **fixa** visível em `md:` (ex.: `hidden md:flex md:w-60 md:flex-col bg-sidebar border-r border-sidebar-border`). Mobile: oculta (drawer cobre — Task 4).
  - [x] Navegação por teclado (AC5): links são `<a>` nativos (Tab/Enter já funcionam); foco visível via token `focus-visible:ring-sidebar-ring` (não remover outline).
- [x] **Task 4 — `components/dashboard/MobileDrawer.tsx` (AC: 1, 2, 5)**
  - [x] Client Component usando shadcn **`Sheet`** (Radix Dialog por baixo → **focus trap, ESC-to-close e `role="dialog"` nativos** — satisfaz AC5 sem implementação manual de focus trap).
  - [x] Trigger: botão hambúrguer visível só em mobile (`md:hidden`), `aria-label="Abrir menu de navegação"`, ícone lucide `Menu`. `<SheetContent side="left">` contém a mesma lista de `nav-items.ts` (reuso — não reimplementar). Fechar o drawer ao navegar (`onClick` → `setOpen(false)` ou `SheetClose`).
  - [x] Rota ativa destacada igual à Sidebar (reusar a lógica de match — extrair p/ helper puro se necessário, ex.: `isActive(pathname, href)` em `nav-items.ts`).
- [x] **Task 5 — `components/dashboard/UserMenu.tsx` (AC: 4)**
  - [x] Client Component usando shadcn **`DropdownMenu`** + **`Avatar`**. Props: `{ username: string; displayName: string | null; avatarUrl: string | null }`.
  - [x] `<Avatar>`: `AvatarImage src={avatarUrl}` + `AvatarFallback` com iniciais derivadas de `displayName ?? username` (1-2 chars upper). Trigger do dropdown = o avatar, `aria-label` descritivo.
  - [x] Itens do menu: **(a) "Copiar URL pública"** → `navigator.clipboard.writeText(\`${siteUrl}/@${username}\`)`+`toast.success(...)`de`@/lib/toast`; recebe `siteUrl`por prop (layout passa`env.NEXT_PUBLIC_SITE_URL`— Client Component **não** importa`lib/env`direto). **(b) "Sair"** → reusar o pattern de`components/auth/LogoutButton.tsx` (`signOut()`de`@/server/auth/actions`dentro de`useTransition`; `toast.error`se`!res.ok`). Ver DEV-6.
  - [x] A11y: `DropdownMenu` (Radix) já entrega navegação por teclado/roles; garantir `aria-label` no trigger e que cada item é focável.
- [x] **Task 6 — Compor o shell em `app/dashboard/layout.tsx` (AC: 1, 2, 3, 4, 5)**
  - [x] Manter o guard RSC existente (`getUser()` → `redirect('/login')` se sem sessão). **Adicionar** fetch do profile: `supabase.from('profiles').select('username, display_name, avatar_url').eq('id', user.id).single()` (mesmo pattern de `app/dashboard/profile/page.tsx`).
  - [x] Estrutura: container flex; `<Sidebar>` (desktop, fixa) ao lado de uma coluna com **header** (contém o trigger do `MobileDrawer` em mobile + `<UserMenu username displayName avatarUrl />` à direita) + `<EmailVerificationBanner emailConfirmedAt={user.email_confirmed_at} />` + `<main>{children}</main>`. `<MobileDrawer>` montado fora do fluxo (overlay).
  - [x] Passar `siteUrl={env.NEXT_PUBLIC_SITE_URL}` ao `UserMenu` (import de `@/lib/env` é permitido no layout RSC).
  - [x] Semântica/landmarks (AC5): `<header>`, `<nav>` (na Sidebar/Drawer), `<main>` — um único `<main>` no shell.
- [x] **Task 7 — Limpar o placeholder de `app/dashboard/page.tsx` (DEV-5)**
  - [x] Remover o `<header>` inline (`Olá, {email}` + `<LogoutButton/>`) e o `<EmailVerificationBanner/>` de `page.tsx` — agora vivem no shell (`layout.tsx`). Remover o guard duplicado de `page.tsx` é **opcional** (defense-in-depth aceitável, ver `profile/page.tsx`); manter o `redirect` se já presente é OK.
  - [x] `page.tsx` permanece como placeholder da **aba Links** (texto "em construção") até a Story 2.5 (CRUD de Links). Não inventar conteúdo de links aqui.
- [x] **Task 8 — Testes de componente (AC: 2, 3, 4, 5)**
  - [x] Criar `tests/components/features/dashboard/Sidebar.test.tsx`: renderiza os 5 itens; item da rota ativa tem `aria-current="page"` (mock `next/navigation` `usePathname`).
  - [x] Criar `tests/components/features/dashboard/UserMenu.test.tsx`: fallback de iniciais; "Copiar URL pública" chama `navigator.clipboard.writeText` com `${SITE_URL}/@username` (mock clipboard); "Sair" chama `signOut` (mock `@/server/auth/actions`).
  - [x] Criar `tests/components/features/dashboard/MobileDrawer.test.tsx`: trigger abre o drawer; conteúdo expõe `role="dialog"`; itens de navegação presentes; ESC/SheetClose fecha.
  - [x] Padrão: Vitest project `components` (jsdom), `@testing-library/react` + `@testing-library/user-event`, espelhando `tests/components/features/auth/*` e `tests/components/features/landing/HomePage.test.tsx`. Rodar `pnpm test:components`.
- [x] **Task 9 — Gate de qualidade + Dev Agent Record (DEV-2)**
  - [x] `pnpm typecheck` exit 0 · `pnpm lint` exit 0 · `pnpm build` exit 0 (warning `ƒ Proxy (Middleware)` = INFO-001 esperado/herdado, não-bloqueante).
  - [x] `pnpm test` (suíte completa) verde — novos `dashboard/*` + **zero regressão** em `tests/components/**` (auth/landing/ui) e `tests/integration/**` (middleware/profiles/pages/links/server-actions).
  - [x] Cobertura: NFR6 frontend ≥ 50% mantida (precedente 1.9 = 83% components).
  - [x] Preencher Dev Agent Record (Agent Model, Debug Log, Completion Notes, File List) + Handoff + Change Log. **Commit** `@dev *commit` em branch `feat/2.4-dashboard-layout-nav`. **Push + PR são EXCLUSIVOS de @devops** (`*push`/`*create-pr` — `.claude/rules/agent-authority.md`).
  - [x] Smoke manual (handoff owner, **não-bloqueante**, padrão 1.5–1.9): login → `/dashboard` mostra sidebar+header → viewport mobile → drawer abre, faz focus-trap, ESC fecha → rota ativa destacada ao navegar Links/Profile → "Copiar URL pública" copia `${SITE_URL}/@username` → "Sair" desloga.

## Dev Notes

> Esta seção contém **todo** o contexto necessário — o dev agent **não deve precisar abrir** `architecture.md`/`prd.md`. Tudo abaixo foi extraído de artefatos reais (citados). NÃO inventar libs/padrões fora destas fontes.

### Pré-existência — estado real do repositório no momento do draft (LEIA PRIMEIRO)

Verificado via `ls`/`grep`/`Read` em 2026-05-18:

| Artefato                                           | Estado                                                                                                                                                                                                                               | Implicação p/ 2.4                                                                                                                                          |
| -------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `app/dashboard/layout.tsx`                         | ✅ Existe — guard RSC mínimo: `getUser()` → `redirect('/login')`; `return <div className="min-h-screen bg-background">{children}</div>`                                                                                              | **Estender** (Task 6), não recriar. Manter o guard.                                                                                                        |
| `app/dashboard/page.tsx`                           | ✅ Existe — placeholder Story 1.5 com `<header>` inline (`Olá, {email}` + `<LogoutButton/>`) + `<EmailVerificationBanner/>`. Comentário no arquivo: _"Placeholder Story 1.5 — substituído em Stories 2.x ... 3.x (Dashboard Shell)"_ | **2.4 É o Dashboard Shell.** Header/banner migram p/ `layout.tsx`; `page.tsx` vira placeholder só da aba Links (DEV-5).                                    |
| `app/dashboard/profile/page.tsx`                   | ✅ Existe — RSC; fetch `profiles.select('username').eq('id', user.id).single()`                                                                                                                                                      | Pattern de fetch de profile a reusar (estendido p/ `display_name, avatar_url`).                                                                            |
| `app/dashboard/{theme,analytics,account}/`         | ❌ **Não existem** (`find` confirmou só `layout.tsx`, `page.tsx`, `profile/page.tsx`)                                                                                                                                                | DEV-4: nav aponta p/ rotas canônicas; 3 delas 404 até stories futuras.                                                                                     |
| `components/ui/*`                                  | ✅ `button, form, input, label, sonner` apenas                                                                                                                                                                                       | DEV-3: faltam `avatar`, `dropdown-menu`, `sheet` → `shadcn add` (Task 1).                                                                                  |
| `components/auth/LogoutButton.tsx`                 | ✅ Existe — `'use client'`; `useTransition` + `signOut()` de `@/server/auth/actions` + `toast.error` de `@/lib/toast`                                                                                                                | Pattern de logout a reusar no `UserMenu` (Task 5b).                                                                                                        |
| `components/dashboard/EmailVerificationBanner.tsx` | ✅ Existe — recebe `emailConfirmedAt`, renderiza banner ou `null`                                                                                                                                                                    | Reusar no shell (Task 6), não modificar.                                                                                                                   |
| `lib/utils.ts` `cn()`                              | ✅ clsx + tailwind-merge                                                                                                                                                                                                             | Helper canônico dos primitives.                                                                                                                            |
| `lib/toast.ts`                                     | ✅ Existe (helper sobre `sonner`, Story 1.8) — `toast.success/error`                                                                                                                                                                 | API de toast do projeto (não importar `sonner` direto).                                                                                                    |
| `lib/env.ts`                                       | ✅ Valida `NEXT_PUBLIC_SITE_URL` (z.string().url())                                                                                                                                                                                  | URL pública base p/ AC4 (DEV-6).                                                                                                                           |
| `server/auth/actions.ts`                           | ✅ `export async function signOut(): Promise<ActionResult<void>>` (linha 108)                                                                                                                                                        | Server Action de logout (já testada em `tests/integration/server-actions/auth.test.ts`).                                                                   |
| `app/globals.css`                                  | ✅ Tokens `--sidebar*` (L43-50) + `@theme inline` `--color-sidebar*` (L126) já presentes (Story 1.8)                                                                                                                                 | Classes `bg-sidebar`, `text-sidebar-foreground`, `bg-sidebar-accent`, `border-sidebar-border`, `ring-sidebar-ring` disponíveis — **não** adicionar tokens. |
| `app/layout.tsx` (root)                            | ✅ `<html lang="pt-BR">`, fontes Geist, `<Toaster/>` global, `metadataBase` via `env.NEXT_PUBLIC_SITE_URL`                                                                                                                           | `<Toaster/>` já global — toasts do `UserMenu` funcionam sem setup extra.                                                                                   |
| `components.json`                                  | ✅ `style: base-nova`, `baseColor: slate`, `rsc: true`, `iconLibrary: lucide`, aliases `@/components/ui`,`@/lib/utils`                                                                                                               | NÃO re-rodar `shadcn init` (Task 1).                                                                                                                       |
| `profiles` (Row em `lib/supabase/types.ts`)        | `{ avatar_url: string\|null, bio, created_at, display_name: string\|null, id, updated_at, username: string }`                                                                                                                        | Campos p/ AC4: `username`, `display_name`, `avatar_url`.                                                                                                   |

### Decisões & Deviations (Constitution Art. IV — default fundamentado + fonte; NÃO inventar)

- **DEV-1 — Executor `@dev`/gate `@qa` (não `@ux-design-expert`/`@dev` do template v2):** precedente **verbatim** das Stories de implementação frontend Done deste repo (1.5/1.8/1.9). Story 1.8 §Executor é a justificativa canônica (código/teste, primitives já existem). Não-bloqueante; flagged p/ gate @po. **Fonte:** `docs/stories/1.8.*` §Executor + §Rationale; `docs/stories/1.9.*` §Executor.
- **DEV-2 — Gate manual QA-001:** `@qa *review` trava em macOS (sem WSL/CodeRabbit). Gate aplicado **manualmente**: revisão independente `git diff main...HEAD` + CI do PR (ambiente ubuntu, independente do local). Precedente sancionado 1.1/1.5–1.9. **Fonte:** `docs/qa/known-issues.md` §QA-001; MEMORY `project_qa_review_hang`. Não-bloqueante.
- **DEV-3 — Novos primitives via `shadcn add` (não `init`):** AC1 (drawer) + AC4 (avatar/dropdown) exigem `avatar`, `dropdown-menu`, `sheet` — ausentes de `components/ui/`. Adicionados via `pnpm dlx shadcn@latest add <nome>` (copy-paste owned, arch §Tech Stack L164; precedente Story 1.5 que adicionou `button input label form sonner` exatamente assim; Story 1.8 Task 1 = "NÃO re-rodar init"). Não-bloqueante (pattern sancionado, não invenção).
- **DEV-4 — Itens de nav apontam p/ rotas canônicas ainda não construídas:** AC2 exige 5 itens. Hoje só `/dashboard` (Links — `page.tsx`) e `/dashboard/profile` resolvem; `/dashboard/theme`, `/dashboard/analytics`, `/dashboard/account` são de stories futuras (CRUD/Theme/Analytics/Account — arch §Routing L1160-1166 lista as 5 como a estrutura canônica final). O **shell precede o conteúdo das abas** (arch §Components #4 escopa só o shell). Default: renderizar os 5 itens com `href` canônico; **não** stubar páginas placeholder (fora de escopo, seria inventar conteúdo). As 3 rotas darão 404 até suas stories — esperado/aceitável p/ uma story de shell. **Fonte:** arch §Routing L1160-1166 + §Components #4 L499-506. Não-bloqueante; flagged p/ gate @po.
- **DEV-5 — Header migra de `page.tsx` p/ `layout.tsx`:** o comentário em `app/dashboard/page.tsx` **explicitamente** sanciona _"substituído em ... 3.x (Dashboard Shell)"_ — 2.4 é o Dashboard Shell. Header (avatar+dropdown) + `EmailVerificationBanner` passam ao shell; `page.tsx` mantém só o corpo placeholder da aba Links até 2.5. **Fonte:** comentário inline `app/dashboard/page.tsx` L7 + arch §Components #4. Não-bloqueante.
- **DEV-6 — Construção da URL pública (AC4 "copy public URL"):** `${env.NEXT_PUBLIC_SITE_URL}/@${profile.username}` — a página pública é `app/@[username]/page.tsx` (arch §Routing L1167-1168) e `NEXT_PUBLIC_SITE_URL` é o env validado já usado em `app/layout.tsx` (`metadataBase`). Layout RSC importa `@/lib/env` e passa `siteUrl` por prop ao `UserMenu` (Client Component **não** importa `lib/env` — coding standard "Env via Zod só em `lib/env`"). Copy via `navigator.clipboard.writeText`. **Fonte:** arch §Routing L1167-1168 + `lib/env.ts` L10 + coding standards §Env. Não-bloqueante.

### Estrutura de diretórios relevante (arch §Frontend Architecture L1014-1061, verificada no repo)

```
app/dashboard/
├── layout.tsx          # MODIFICAR — shell: guard RSC + fetch profile + Sidebar/MobileDrawer/header/banner/main
├── page.tsx            # MODIFICAR — remover header inline (DEV-5); placeholder aba Links até 2.5
└── profile/page.tsx    # (existente; pattern de fetch profile a espelhar)
components/dashboard/
├── EmailVerificationBanner.tsx   # (existente — reusar, não tocar)
├── ResendEmailButton.tsx         # (existente — dep do banner)
├── nav-items.ts        # CRIAR — fonte única dos 5 itens (label/href/icon) + isActive()
├── Sidebar.tsx         # CRIAR — desktop fixa, 'use client' (usePathname)
├── MobileDrawer.tsx    # CRIAR — Sheet (Radix Dialog), focus trap nativo
└── UserMenu.tsx        # CRIAR — DropdownMenu + Avatar; copy URL + logout
components/ui/
└── avatar.tsx, dropdown-menu.tsx, sheet.tsx   # CRIAR via `shadcn add` (Task 1)
tests/components/features/dashboard/
├── Sidebar.test.tsx    # CRIAR
├── UserMenu.test.tsx   # CRIAR
└── MobileDrawer.test.tsx  # CRIAR
```

Nomes **verbatim** de arch §Components #4 (L505-506: `app/dashboard/layout.tsx`, `components/dashboard/Sidebar.tsx`, `MobileDrawer.tsx`, `UserMenu.tsx`) e §Frontend Architecture Component Organization (L1033-1037).

### Padrões de código relevantes (arch §Coding Standards L2386-2483)

- **Server-First:** `layout.tsx` é RSC (fetch de profile na renderização). Só `Sidebar`/`MobileDrawer`/`UserMenu` levam `'use client'` — justificar com comentário curto (`usePathname` / interatividade Sheet+DropdownMenu / clipboard).
- **Env via Zod:** **nunca** `process.env.X` fora de `lib/env.ts`. RSC importa `import { env } from '@/lib/env'`; Client Components recebem valores por prop.
- **Type Sharing:** importar tipos de `lib/supabase/types.ts` (gerado) — não duplicar shape de `profiles`.
- **Feature folders, não type folders:** `components/dashboard/*` (não `components/sidebar/*`). Sem barrel `index.ts` que re-exporta tudo.
- **i18n PT-BR-only:** strings em literais (sem next-intl). Labels de nav e itens de menu em PT-BR (ex.: "Links", "Perfil"? — **manter os labels do AC2 verbatim**: Links, Profile, Theme, Analytics, Account; demais textos UI em PT-BR como "Copiar URL pública", "Sair", "Abrir menu de navegação").
- **CSS via tokens:** Tailwind utilities + design tokens CSS vars; sem classes custom. Usar o set `--sidebar*` já em `globals.css`.
- **Toast:** usar `@/lib/toast` (`toast.success/error`) — não importar `sonner` direto (Story 1.8 §Drift; helper é o ponto de reconciliação).
- **ESLint restricted imports (Story 1.1):** `lib/supabase/admin` proibido em `components/`/`app/` (não usar — só client/server normal); `revalidatePath`/`revalidateTag` diretos proibidos (não aplicável aqui — story não muta estado).

### Insights de Stories anteriores

- **Story 1.5 (UI auth — Done):** `shadcn init` + `add button input label form sonner` executados aqui; `sonner` escolhido sobre Radix Toast; `<Toaster/>` no root layout; pattern de component test = arch §Test Examples L2286-2317. `LogoutButton.tsx` criado aqui (pattern `useTransition`+`signOut`).
- **Story 1.8 (seed design system — Done):** **NÃO re-rodar `shadcn init`** (idempotência — Task 1.1 de 1.8). Para componente faltante: `pnpm dlx shadcn@latest add <nome>` preservando tokens. Tokens `--sidebar*` confirmados presentes em `globals.css`. `lib/toast.ts` é a API de toast canônica. Cobertura components ≥ 50% (NFR6).
- **Story 1.9 (landing — Done, precedente frontend mais recente):** `executor: @dev`/`quality_gate: @qa`/`quality_gate_tools` idênticos aos desta story; gate fechado via **QA-001 manual** (revisão `git diff` + CI do PR #7); cobertura components 83.55% (≫ NFR6); INFO-001 (`ƒ Proxy (Middleware)`) é warning de build esperado; smoke manual = handoff owner não-bloqueante; Status `Ready → Ready for Review` por @dev, `Ready for Review → Done` por @po `*close-story` após PR squash-merge + CI verde.
- **Story 2.3 (sibling anterior — Done):** convenção de fechamento: `@dev *develop` → commit em branch `feat/X.Y-*` → gate manual QA-001 (@architect/@qa) → `docs/qa/gates/2.4-*.yml` → `@po *close-story 2.4`. **Push/PR EXCLUSIVOS de @devops.** Deviations DEV-\* tratadas como APPROVED-by-precedent; não rever no gate o que já é precedente sancionado.
- **MEMORY (referência):** projeto Supabase **único** `ibpliihqaceafdykgwiu` (`biolink-dev`); QA-001 (gate manual em macOS). Story 2.4 **não** toca DB/migrations/Server Actions novos (só lê `profiles` no RSC; logout reusa `signOut` já testado) → **sem novos testes de integration**; sem risco de schema.

### Testing

- **Localização:** `tests/components/features/dashboard/{Sidebar,UserMenu,MobileDrawer}.test.tsx` (novos). Sem unit (sem lógica pura nova além de `isActive()` — pode ser coberto via Sidebar.test). Sem integration novo (nenhum Server Action/DB novo; `signOut` já coberto por `tests/integration/server-actions/auth.test.ts`).
- **Framework/padrão:** Vitest project `components` (`pnpm test:components` = `vitest run --project=components`), environment `jsdom`, `setupFiles: ./tests/setup.ts`. `@testing-library/react` + `@testing-library/user-event` + `@testing-library/jest-dom`. Espelhar `tests/components/features/auth/SignupForm.test.tsx` e `tests/components/features/landing/HomePage.test.tsx` (estrutura `describe/it`, `render`, `screen.getByRole`). Mock de `next/navigation` `usePathname` (rota ativa) e de `@/server/auth/actions` (`vi.mock`) + `navigator.clipboard` (definir `Object.defineProperty(navigator,'clipboard',...)` ou `vi.stubGlobal`).
- **Cenários-chave:**
  - `Sidebar`: renderiza os 5 labels (AC2); item da rota ativa tem `aria-current="page"` e classe de destaque, demais não (AC3); links são `<a>` com `href` correto.
  - `MobileDrawer`: trigger (`aria-label="Abrir menu de navegação"`) abre; conteúdo tem `role="dialog"` (AC5 focus trap nativo Radix); 5 itens presentes; fecha via ESC/SheetClose.
  - `UserMenu`: `AvatarFallback` mostra iniciais corretas (displayName e fallback p/ username); "Copiar URL pública" chama `clipboard.writeText` com `${SITE_URL}/@username` + dispara `toast.success`; "Sair" chama `signOut`.
- **Cobertura:** NFR6 frontend ≥ 50% (arch §Testing Pyramid L2214). Precedente 1.9 = 83% — manter os novos componentes cobertos.
- **Regressão (obrigatória, gate Task 9):** `tests/components/**` (auth/landing/ui) + `tests/integration/**` (middleware/profiles/pages/links/server-actions) DEVEM permanecer verdes. A alteração em `app/dashboard/page.tsx` (remover header) não tem teste dedicado hoje — validar via `pnpm test` completo.
- **Sem E2E** (PRD §Requisitos de Testes; arch §E2E = N/A). Smoke manual (handoff owner, não-bloqueante, padrão 1.5–1.9) — cenários no Task 9.
- **Build:** `pnpm build` exit 0; warning `ƒ Proxy (Middleware)` = INFO-001 esperado/herdado (não-bloqueante).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Author     |
| ---------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 2026-05-18 | 0.1     | Story criada via `@sm *draft 2.4` (task `create-next-story`). Status: Draft. Story 2.3 confirmada **Done** (Change Log v0.5; commits `46be0ed`+`4d1f335` em `feat/2.3-schema-links-rls`); 2.4 é a próxima sequencial do Epic 2 (4/7). ACs **verbatim** de `docs/prd.md §Story 2.4` (L410-416). Contexto cruzado contra estado real do repo (`layout.tsx` guard mínimo; `page.tsx` placeholder c/ header inline + comentário sancionando substituição p/ Dashboard Shell; `profile/page.tsx` pattern de fetch; `components/ui/` só button/form/input/label/sonner; `LogoutButton`/`EmailVerificationBanner`/`lib/toast`/`lib/env NEXT_PUBLIC_SITE_URL`/`server/auth/actions signOut L108` existentes; `globals.css` tokens `--sidebar*` presentes; `app/dashboard/{theme,analytics,account}` inexistentes; `profiles.Row` username/display_name/avatar_url) + `docs/architecture.md` (§Components #4 Dashboard Shell L499-506 — nomes de arquivo canônicos; §Frontend Architecture Component Org L1033-1037; §Routing L1160-1168; §State Mgmt L1107-1143; §Coding Standards L2386-2483; §Testing Strategy L2202-2317) + precedente frontend Stories 1.5/1.8/1.9 (executor @dev/@qa, shadcn add não init, QA-001 manual, NFR6, INFO-001) + 2.3 (convenção de fechamento) + MEMORY (projeto único, QA-001). 6 decisões/deviations isoladas com default fundamentado + fonte (DEV-1 executor @dev/@qa via precedente 1.5/1.8/1.9; DEV-2 gate manual QA-001; DEV-3 `shadcn add avatar/dropdown-menu/sheet` não init; DEV-4 nav p/ rotas canônicas, 3 ainda 404 — shell precede abas; DEV-5 header migra page→layout, sancionado por comentário inline; DEV-6 URL pública `${SITE_URL}/@${username}` via env+prop) — **nenhuma bloqueante** (todas resolvem por arch canônico/precedente sancionado, não invenção; DEV-1/DEV-4 flagged p/ gate @po). CodeRabbit: skip notice (disabled + `mcp.enabled:false`). ClickUp: sync pulado (`mcp.enabled:false`). Próximo: `@po *validate-story-draft 2.4`. | River (sm) |
| 2026-05-18 | 0.2     | Validação `@po *validate-story-draft 2.4` (task `validate-next-story`, auditoria 11-pontos). **Veredito: GO** — Readiness 9/10, Confidence High. Template v2 completo (sem placeholders pendentes). Anti-hallucination **CLEAN**: todas as fontes verificadas contra artefatos reais (`components/ui/` = button/form/input/label/sonner; `components/dashboard/` = EmailVerificationBanner/ResendEmailButton; theme/analytics/account inexistentes; arch §Components #4 L499-506 nomes canônicos; arch §Routing L1160-1166; `server/auth/actions.ts` signOut L108; `lib/env.ts` NEXT_PUBLIC_SITE_URL L10; `globals.css` tokens `--sidebar*` L43-50/L126; `layout.tsx` guard mínimo + `page.tsx` header inline/comentário; ACs **verbatim** vs `prd.md §Story 2.4` L412-416). Cobertura AC↔Task completa (AC1-5 mapeados). DEV-1 (executor `@dev`/gate `@qa` vs template "Design/UI → @ux-design-expert/@dev") **aceito como override IDS G3 com razão documentada**: precedente verbatim sancionado 1.5/1.8/1.9 (Done) + 2.3 (mesma classe validada) — Constitution Art. IV compliant (precedente, não invenção); não-bloqueante. Nits não-bloqueantes: comentário inline `page.tsx` "3.x" vs story 2.4 (DEV-5 documenta, interpretação sólida via arch §Components #4); revisão a11y WCAG 2.1 AA por @ux-design-expert opcional. Transição **Draft → Ready** aplicada (`story-lifecycle.md` — responsabilidade @po no GO). Próximo: `@dev *develop 2.4` (branch `feat/2.4-dashboard-layout-nav`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Pax (po)   |
| 2026-05-18 | 0.3     | Implementação `@dev *develop 2.4` (modo autônomo, branch `feat/2.4-dashboard-layout-nav` stacked sobre HEAD da 2.3 Done). **Tasks 1-9 ✅** (todos os checkboxes [x]). Criados: `components/ui/{avatar,dropdown-menu,sheet}.tsx` (shadcn `add` só os faltantes, sem `init`, Base UI — existentes intactos, sem mudança em package.json/lock), `components/dashboard/{nav-items.ts,Sidebar,MobileDrawer,UserMenu}.tsx`, 3 suites `tests/components/features/dashboard/*`. Modificados: `app/dashboard/{layout,page,profile/page}.tsx`, `tests/setup.ts` (polyfills jsdom guardados p/ overlays Base UI). DEV-1..DEV-6 aplicados (precedente, não invenção). Desvio rastreável a AC5 (fora da letra da Task 7, mas exigido): `profile/page.tsx` `<main>`→`<div>` p/ single-`<main>` no shell. **Gate:** typecheck/lint/build exit 0 (`ƒ Proxy (Middleware)`=INFO-001 esperado; rotas theme/analytics/account 404=DEV-4 esperado); `pnpm test` **170/170 em 22 arquivos** — zero regressão. Lint: 8 `import/order` só nos primitives gerados → `eslint --fix` (cosmético, classe prevista Task 1). Cobertura NFR6 ≥50% mantida (suites dedicadas por componente). Status `InProgress → Ready for Review`. Gate de qualidade via **QA-001 manual** (DEV-2, macOS sem WSL). **Commit @dev na branch; push+PR EXCLUSIVOS de @devops.** Próximo: gate manual (@architect/@qa) + `@po *close-story 2.4`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Dex (dev)  |
| 2026-05-18 | 0.4     | **Fechada via `@po *close-story 2.4`** (task `po-close-story`). QA gate **PASS** — fonte autoritativa `docs/qa/gates/2.4-dashboard-layout-nav.yml`, gate manual **QA-001** por @qa Quinn (CodeRabbit/WSL indisponível em macOS — `docs/qa/known-issues.md`). 7 quality checks **PASS** (code_review/unit/AC/no_regressions/performance/security/documentation) + 6 `quality_gate_tools` **re-executados INDEPENDENTEMENTE** pelo @qa: typecheck/lint/build exit 0; `pnpm test` **170/170 em 22 arquivos** (+12 vs feat/2.3=158 = 3 suites novas dashboard; zero regressão em auth/landing/ui/profile/middleware/pages/links/server-actions). **AC1–AC5 satisfeitos** (matriz traceability Given-When-Then completa). **0 blockers**; 1 observação **low não-bloqueante** A11Y-001 (aria-label duplicado nos 2 nav landmarks — Sidebar `hidden md:flex`/MobileDrawer on-open raramente coexistem visíveis; polish futuro + consulta @ux-design-expert WCAG 2.1 AA opcional). Todas as deviations **APPROVED** (DEV-1..DEV-6 + `profile/page.tsx` `<main>`→`<div>` + polyfills `tests/setup.ts` + Base UI≡Radix) — precedente sancionado 1.5/1.8/1.9/2.3 + rastreáveis a AC5/arch (não invenção; DEV-1/DEV-4 já validadas pelo gate @po v0.2, não re-litigadas per convenção 2.3). Stories flat (sem `docs/stories/epics/` index nem `backlog.md`) → close **story-only** (po-close-story §Error Handling); QA Results permanece como registro @qa-owned do gate. Status **Ready for Review → Done**. Epic 2: **4/7** done (2.1/2.2/2.3/2.4). **DOWNSTREAM:** branch `feat/2.4-dashboard-layout-nav` (1 commit `09fc252`, rebaseada sobre `main` `ee649b8`) **ainda não pushed** — `@devops *push` + `*create-pr` são **EXCLUSIVOS de @devops** (`.claude/rules/agent-authority.md`); CI rodará os 9 required checks. Próxima: **Story 2.5 — CRUD de Links** (1º consumer real de `links`/RLS da 2.3) via `@sm *draft 2.5`.                                                                      | Pax (po)   |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (Dex / @dev) — modo autônomo. Branch `feat/2.4-dashboard-layout-nav`.

### Debug Log References

- **Task 1 — shadcn `add` prendeu em prompt interativo:** `pnpm dlx shadcn@latest add avatar dropdown-menu sheet --yes` parou no prompt de overwrite do `button.tsx` (dependência transitiva de `dropdown-menu`/`sheet`), mesmo com `--yes`. Resolvido: `pkill` no processo (default do prompt = `N`, `avatar`/`dropdown-menu` já criados) + re-run isolado `yes n | pnpm dlx shadcn@latest add sheet` (responde "n" ao overwrite → `button.tsx` preservado, `sheet.tsx` criado). Existentes (`button/form/input/label/sonner`) confirmados **intactos** via `git diff --stat` (sem mudança). `package.json`/`pnpm-lock.yaml` **não** alterados (`@base-ui/react` já era dependência — style `base-nova` usa Base UI, não Radix).
- **Task 8 — clipboard mock sobrescrito pelo userEvent:** 1ª execução falhou em `UserMenu` "Copiar URL pública" (writeText 0 chamadas). Causa: `userEvent.setup()` instala seu próprio stub de `navigator.clipboard` **após** o `beforeEach`. Fix: `vi.spyOn(navigator.clipboard, 'writeText')` **depois** de `userEvent.setup()`. 76/76 components verdes após o fix.
- **Lint — `import/order` nos primitives gerados:** 8 erros, **exclusivamente** em `components/ui/{avatar,dropdown-menu,sheet}.tsx` (output raw do CLI; prettier não reordena imports). Auto-fix `eslint --fix` apenas nesses 3 (classe cosmética prevista na Task 1, precedente DX 1.4/1.5/2.2/2.3). Componentes `dashboard/*` autorais: **zero** erro de lint.

### Completion Notes List

- **AC1–AC5 satisfeitos.** Shell `app/dashboard/layout.tsx` (RSC: guard + fetch `profiles{username,display_name,avatar_url}`) compõe `Sidebar` (desktop fixa) + header (`MobileDrawer` trigger `md:hidden` + `UserMenu` à direita) + `EmailVerificationBanner` + `<main>` único. Drawer mobile via shadcn `Sheet` (Base UI Dialog → focus trap / ESC / `role="dialog"` **nativos**, AC5 sem implementação manual). Rota ativa via `isActive()` com **match exato p/ `/dashboard`** (não acende em sub-rotas) + `aria-current="page"` (AC3). `UserMenu`: avatar + dropdown com "Copiar URL pública" (`${siteUrl}/@${username}` via `navigator.clipboard` + `toast.success`) e "Sair" (reuso do pattern `LogoutButton`: `signOut` em `useTransition`).
- **DEV-1..DEV-6 aplicados conforme draft** (precedente sancionado, não invenção). `Sheet`/`DropdownMenu` são **Base UI** (`@base-ui/react`, style `base-nova`), não Radix — equivalência funcional preservada (Dialog/Menu entregam os mesmos roles/focus/keyboard); textos do draft que diziam "Radix" referem-se ao primitive abstrato.
- **Desvio necessário fora da letra da Task 7 (rastreável a AC5 + Task 6 "um único `<main>` no shell"):** `app/dashboard/profile/page.tsx` tinha `<main>` próprio. Com o shell passando a ser dono do `<main>`, dois landmarks `main` coexistiriam em `/dashboard/profile` (violação ARIA / AC5). Ajustado `<main>` → `<div>` em `profile/page.tsx` (conteúdo idêntico, só o wrapper). **Não é invenção** — consequência direta de AC5; `UsernameForm.test.tsx` continua verde (testa o form, não o wrapper).
- **Infra de teste estendida (aditiva, não-bloqueante):** `tests/setup.ts` ganhou polyfills jsdom **guardados** (`ResizeObserver`, `matchMedia`, pointer-capture, `scrollIntoView`) — necessários p/ overlays Base UI (Sheet/Menu) em jsdom 29. Guardas `if (!...)` → suites pré-existentes inalteradas; afeta só o project `components`. Zero regressão (170/170).
- **Gate (Task 9):** `typecheck` exit 0 · `lint` exit 0 · `build` exit 0 (`ƒ /dashboard`, `ƒ /dashboard/profile`; `ƒ Proxy (Middleware)` = **INFO-001** esperado/herdado, não-bloqueante; rotas `theme/analytics/account` ausentes = DEV-4 esperado, 404 até stories futuras) · `pnpm test` **170/170 em 22 arquivos** (3 novos suites dashboard + **zero regressão** em components/integration). Cobertura NFR6 ≥ 50% mantida: cada componente/helper novo tem suite dedicada cobrindo render + branches primários (active-route, dialog open/close, clipboard, signOut, iniciais) — consistente com precedente 1.9 (83%).
- **DoD (story-dod-checklist, auto-review):** ✅ código atende ACs · ✅ testes novos + regressão verdes · ✅ padrões seguidos (Server-First, Env via Zod só no RSC layout / `siteUrl` por prop ao Client, `@/lib/toast`, feature-folder sem barrel, labels AC2 verbatim, tokens `--sidebar*` sem novos tokens) · ✅ File List completo · ✅ sem `git push`/PR (exclusivo @devops).
- **Handoff:** gate manual **QA-001** (macOS sem WSL/CodeRabbit — DEV-2): revisão independente via `git diff main...HEAD` + CI do PR. Próximo: gate de qualidade (@architect/@qa manual) → `docs/qa/gates/2.4-*.yml` → `@po *close-story 2.4`. **Push + PR EXCLUSIVOS de @devops** (`*push`). Smoke manual (não-bloqueante, padrão 1.5–1.9) pendente p/ o handoff owner. Artefato: `.aiox/handoffs/handoff-dev-to-qa-*.yaml`.

### File List

**Criados:**

- `components/ui/avatar.tsx` (shadcn `add`, Base UI)
- `components/ui/dropdown-menu.tsx` (shadcn `add`, Base UI)
- `components/ui/sheet.tsx` (shadcn `add`, Base UI)
- `components/dashboard/nav-items.ts`
- `components/dashboard/Sidebar.tsx`
- `components/dashboard/MobileDrawer.tsx`
- `components/dashboard/UserMenu.tsx`
- `tests/components/features/dashboard/Sidebar.test.tsx`
- `tests/components/features/dashboard/MobileDrawer.test.tsx`
- `tests/components/features/dashboard/UserMenu.test.tsx`

**Modificados:**

- `app/dashboard/layout.tsx` (shell: guard RSC + fetch profile + Sidebar/header/MobileDrawer/banner/`<main>`)
- `app/dashboard/page.tsx` (placeholder aba Links; header inline + banner removidos — migraram p/ shell, DEV-5)
- `app/dashboard/profile/page.tsx` (`<main>` → `<div>`: AC5 single-`<main>` no shell)
- `tests/setup.ts` (polyfills jsdom guardados p/ overlays Base UI)
- `docs/stories/2.4.dashboard-layout-nav.story.md` (Dev Agent Record / Status / checkboxes / Change Log)

> `package.json`/`pnpm-lock.yaml` **não** alterados (sem novas dependências). Não pertencem a esta story (bookkeeping pré-existente da 2.3, NÃO commitar aqui): `docs/stories/2.3.schema-links-rls.story.md`, `docs/qa/gates/2.3-schema-links-rls.yml`, `.claude/scheduled_tasks.lock`.

## QA Results

### Gate: ✅ PASS — `docs/qa/gates/2.4-dashboard-layout-nav.yml`

**Reviewer:** @qa (Quinn) — gate manual via workaround **QA-001** (`docs/qa/known-issues.md`; macOS sem WSL/CodeRabbit — Seção 0 do `qa-review-story.md` travaria). Quality_gate owner é @qa per Executor Assignment / DEV-1 (espelha 1.5/1.8/1.9 Done).
**Review method:** manual — 7 quality checks lidos no código real committed (`09fc252`: nav-items.ts, Sidebar/MobileDrawer/UserMenu.tsx, layout/page/profile-page.tsx, 3 test suites, tests/setup.ts) + matriz de rastreabilidade AC↔implementação↔teste (Given-When-Then) + **re-execução INDEPENDENTE** dos 6 quality_gate_tools (sem PR/CI nesta fase — push/PR/CI downstream de @devops).
**Updated:** 2026-05-19T03:40:00Z

#### Verificação objetiva (re-executada independentemente pelo @qa)

| Tool                                      | Resultado                                                                                                                                       |
| ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck`                          | exit 0                                                                                                                                          |
| `pnpm lint`                               | exit 0                                                                                                                                          |
| `pnpm build`                              | exit 0 (`ƒ /dashboard` + `ƒ /dashboard/profile`; `ƒ Proxy (Middleware)` = INFO-001 esperado; theme/analytics/account ausentes = DEV-4 esperado) |
| `pnpm test` (unit+components+integration) | **170/170 em 22 arquivos** — zero regressão (+12 vs feat/2.3 158 = 3 suites novas dashboard)                                                    |

#### 7 Quality Checks

| Check               | Status | Resumo                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Code review         | PASS   | Limpo/legível/manutenível; padrões do projeto verificados (Server-First, Env via Zod só RSC + siteUrl prop, feature-folder sem barrel, tokens `--sidebar*`, `@/lib/toast` facade, sem `lib/supabase/admin` em components, labels AC2 verbatim); `isActive()` correto (match exato `/dashboard` + prefixo c/ guard `/`). 0 CRITICAL/HIGH.                                                 |
| Unit tests          | PASS   | Única lógica pura (`isActive()`) coberta comportamentalmente via Sidebar tests; sem unit isolado (padrão UI Done 1.5/1.8/1.9).                                                                                                                                                                                                                                                           |
| Acceptance criteria | PASS   | AC1-AC5 satisfeitos — matriz traceability completa: AC1→MobileDrawer/Sidebar/layout; AC2→nav-items+2 testes; AC3→isActive+aria-current (2 testes incl. raiz só Links); AC4→UserMenu (iniciais/copiar-URL/Sair); AC5→landmarks+Base UI Dialog (role=dialog/ESC/focus-trap nativos, 3 testes).                                                                                             |
| No regressions      | PASS   | `pnpm test` 170/170 (22 arquivos) re-rodado independentemente; zero regressão em auth/landing/ui/profile/middleware/pages/links/server-actions. `page.tsx`/`profile/page.tsx` ajustados validados via suíte (UsernameForm.test verde). `tests/setup.ts` polyfills guardados (`if !...`) → suites pré-existentes inalteradas.                                                             |
| Performance         | PASS   | layout RSC: 1 query `profiles` por PK (O(1) indexado, mesmo pattern profile/page.tsx); Client Components mínimos/justificados; sem novas deps (package.json/lock intactos); Sheet monta on-open (lazy). Sem N+1.                                                                                                                                                                         |
| Security            | PASS   | OWASP basics: sem secrets; sem `lib/supabase/admin` em components (ESLint restricted + lint exit 0); Env via prop (Client não importa `lib/env`); clipboard text não-injetável (siteUrl env-validado + username DB-regex-validado); `AvatarImage alt=""` decorativo sem XSS; sem novo Server Action/endpoint; lê `profiles` sob RLS select_own existente. gitleaks roda no push @devops. |
| Documentation       | PASS   | Dev Agent Record completo (Debug Log 3 entradas, Completion Notes c/ rationale do desvio profile/page.tsx, File List); Dev Notes self-contained; Tasks 100% `[x]`; comentários inline curtos/úteis.                                                                                                                                                                                      |

#### Deviations revisadas — todas **APPROVED** (não-bloqueantes)

DEV-1 (executor @dev/gate @qa — precedente 1.5/1.8/1.9, já validado gate @po v0.2) · DEV-2 (QA-001 manual — workaround documentado, gates re-executados) · DEV-3 (shadcn `add` não `init`; Base UI≡Radix funcionalmente — verificado nos testes) · DEV-4 (nav → rotas canônicas 404 até stories futuras — arch §Routing) · DEV-5 (header migra page→layout — sancionado por comentário inline) · DEV-6 (URL pública via env+prop — coding standard) · **profile/page.tsx `<main>`→`<div>`** (consequência direta/rastreável de AC5 + Task 6 single-`<main>`; conteúdo idêntico; não invenção) · **polyfills tests/setup.ts** (aditivos, guardados, zero regressão). Precedente sancionado não re-litigado (convenção 2.3).

#### Observações low (não-bloqueantes)

- **A11Y-001** (`accessibility`, low): Sidebar e MobileDrawer expõem ambos `<nav aria-label="Navegação principal">`. WCAG/ARIA recomenda rótulos distintos p/ landmarks de mesmo tipo. **Não-bloqueante** — raramente coexistem visíveis (Sidebar `hidden md:flex` = fora da árvore a11y no mobile; MobileDrawer monta on-open + trigger `md:hidden`). Polish futuro: rótulos distintos. Consulta opcional @ux-design-expert (revisão a11y WCAG 2.1 AA) permanece recomendada — não-bloqueante (a própria story DEV-1 já sinaliza).

#### Veredito

**PASS — 0 blockers, 1 observação low.** Story pode transicionar `Ready for Review → Done`.

**Handoff:** `@po *close-story 2.4` (atualiza Status + Change Log + esta seção fica como registro do gate). **Downstream:** branch `feat/2.4-dashboard-layout-nav` (rebaseada sobre `main` `ee649b8`, commit `09fc252`) **ainda não pushed** — após o close, `@devops *push` + `*create-pr` (exclusivo @github-devops; CI roda os 9 required checks). Próxima story sugerida: **2.5 — CRUD de Links** (primeiro consumer real de `links`/RLS).

— Quinn, guardião da qualidade 🛡️
