# Story 5.5: Dashboard Core (Layout + Links) — Soft Studio Refresh

## Status

Done

## Executor Assignment

```yaml
executor: '@dev' # Tasks 1-9 (audit dashboard chrome `app/dashboard/layout.tsx` + Links page `app/dashboard/page.tsx` + 4 link components `components/links/{LinkRow,LinkList,EmptyState,AddLinkModal}.tsx` + 3 dashboard chrome `components/dashboard/{Sidebar,MobileDrawer,UserMenu}.tsx` consumindo primitives refreshed Story 5.3 + tokens Story 5.2)
quality_gate: '@qa'
quality_gate_tools: [typecheck, lint, build, test:components]
complexity: 'M' # Frontend-primary. Superfície: 2 page-level (`app/dashboard/layout.tsx` + `app/dashboard/page.tsx`) + 4 components/links (`LinkRow.tsx`, `LinkList.tsx` (no edit esperado), `EmptyState.tsx`, `AddLinkModal.tsx`) + 0-N edits em `components/dashboard/{Sidebar,MobileDrawer,UserMenu}.tsx` (apenas se audit revelar tokens hard-coded ou typescale shipped genérica) + 1-2 component novos pequenos (`components/dashboard/PublicUrlCopy.tsx` Client para AC3 + opcional `components/links/AddLinkButton.tsx` para AC6 dashed wrapper) + 0-N snapshot updates em `tests/components/features/{dashboard,links}/__snapshots__/` (esperados mas inexistentes pré-5.5 — confirmar via `ls`). Zero migration, zero RLS, zero schema, zero Server Action novo. Tokens canônicos já em `app/globals.css` pós-5.2 + primitives shadcn já alinhados pós-5.3 (Card +shadow-sm +ring-1 + Switch peach ON + Button ghost peach hover + Input destructive ring /30 etc.). Mudanças localizadas: layout (wordmark left + theme quick-link palette right + UserMenu intact), page (H1 typescale + URL pública + inline copy button), LinkRow (Card-like wrapper rounded-xl shadow-sm + drag handle group-hover desktop only), EmptyState (soft blob illustration replacing lucide Link2 + CTA bump), AddLinkModal trigger (dashed border accent hover via wrapping component). Risco material isolado: (a) snapshot files podem inexistir pré-5.5 — não regredir behavior tests; (b) drag handle CSS group-hover pode interferir com a11y focus-visible (validar); (c) Lighthouse CI `/dashboard` (authenticated route — fica fora dos gate paths default `/` + `/[username]`); (d) `useUsernameAvailability` + reorder-links + create-link Server Actions NÃO tocadas (lógica de negócio Story 2.5/2.6 + 5.1 intacta).
```

> **Rationale:** Story **Frontend-primary** — segunda das 4 stories page-level paralelizáveis (5.4-5.7) do refresh Soft Studio. Materializa **Phase 3** de `docs/frontend-spec.md` §5.3 ratificada em **PRD v0.5 §UX/Branding** (Change Log 2026-05-29). Tokens canônicos já estão em `app/globals.css` (Story 5.2 Done — paleta Soft Studio + radius scale + shadows + typescale 9 tokens) e primitives shadcn já estão alinhados (Story 5.3 Done — Button ghost peach hover, Card +shadow-sm warm-tinted, Input destructive ring /30, Switch peach ON state, Dialog/AlertDialog rounded-2xl, FormMessage AlertCircle, DropdownMenu refreshed). Esta story **consome** ambos no dashboard autenticado: ajusta layout (wordmark "biolink" left no header + theme palette quick-link `/dashboard/theme` next to UserMenu), Links page (H1 32px "Meus links" + URL pública inline + copy button), LinkRow (Card-visual rounded-xl + shadow-sm + ring-1 substituindo o `border border-border` shipped Story 2.5/2.6; drag handle hover-only desktop / always mobile), EmptyState (soft blob illustration peach + lavender substituindo `<Link2>` plain lucide + CTA larger), AddLinkButton (dashed border accent-hover wrapper para AddLinkModal trigger per spec §2.5 L597). Conforme `.claude/rules/agent-authority.md`, ajustes em pages + components são escopo do **@dev (Dex)** com gate **@qa (Quinn)** — mesmo padrão das Stories 3.2/3.3/5.2/5.3/5.4. `git push`/`gh pr create` permanecem **exclusivos de @devops** (Gage).

> **Backlog crossover:** `[EPIC-5-S5]` 🟡 MEDIUM (este item — `docs/STORY-BACKLOG.md`) será movido para ✅ DONE após merge (Task 9.1). **Não bloqueia 5.6/5.7** (paralelizáveis — Profile/Theme/Account e Analytics/Public). **Bloqueia visualmente** a superfície principal autenticada (`/dashboard` é o primeiro view pós-login Story 1.5 + 1.7 middleware shipped) — usuários logados ainda veem dashboard em paleta seed pré-5.2. `[STORY-5.9]` (Wordmark "biolink ★" + favicon) é **independente** desta story — dashboard layout aqui usa wordmark `biolink` text-only (sem ★ + sem logomark), Story 5.9 substituirá pelo wordmark component dedicated `<Wordmark />`. `[STORY-5.3-F1]` LOW e `[STORY-5.2-F1]` LOW (amends spec warning/destructive ratios) **não tocados** — Story 5.5 não introduz uso de `--warning` em bg/text components nem `text-destructive` sobre brand bg.

> **Theme palette quick-link decisão (DEV-A1 anticipated, ratified via user elicitation):** spec §2.5 wireframe L569 mostra `[⊙]` (theme-switch glyph) no header global do dashboard. A IA real do projeto (Story 3.3 + 5.1 + 5.2 + 5.3 Done) tem persistência de tema em DB (`pages.theme` enum `'light' | 'dark' | 'brand'` Story 2.2 + ThemeSelector em `/dashboard/theme` Story 3.3 + label rebrand "Brand → Vibrante" planejado Story 5.6 Q3 §6 PRD). NÃO há runtime toggle global (não usa `next-themes` lib; tema é per-user persisted). **Decisão:** adicionar icon-only ghost button `<Link href="/dashboard/theme">` com lucide `PaletteIcon` (sem JS runtime toggle, sem refactor de ThemeSelector existente). Glyph spec `[⊙]` materializado como **quick-link palette**, não como instant toggle. Justificativa: (a) preserva IA estável e persistência DB single-source-of-truth (Story 3.3); (b) custo mínimo (~5 linhas no layout); (c) materializa visualmente o glyph spec sem inventar runtime UI. Tradeoff aceito: usuário clica → navega para /dashboard/theme (1 extra click vs runtime toggle), mas runtime toggle exigiria refactor maior de ThemeSelector + decidir source-of-truth (local storage vs DB sync) e está fora do escopo refresh visual.

## Story

**As a** dono do BioLink autenticado (e por extensão, todo usuário logado que pousa em `/dashboard` pós-login Story 1.5/1.7 ou navega entre as tabs Links/Profile/Theme/Analytics/Account Story 2.4/3.4/4.4/4.5/5.1),
**I want** que o dashboard core (chrome compartilhado `app/dashboard/layout.tsx` com sidebar 240px desktop + sheet mobile + header user dropdown + theme quick-link palette + a Links page `/dashboard` `app/dashboard/page.tsx` com H1 32px "Meus links" + URL pública inline copiável + link cards Soft Studio visual + empty state soft blob illustration + dashed-border "Adicionar link" CTA) renderize **com os specs visuais e estruturais Soft Studio** (per `docs/frontend-spec.md` §2.5 + §3.1 drag-drop + §3.2 inline edit + §3.3 toggle visibility + §0.5.3 IA — sidebar/header navigation) — Links cards consumindo Card primitive Story 5.3 (rounded-xl + shadow-sm warm-tinted plum + ring-1) em vez do `border border-border` plain shipped Story 2.5/2.6, drag handle revelado on row :hover desktop + sempre visível mobile (touch UX preservada), AddLinkButton com dashed border + accent peach on hover per spec §2.5 L597, EmptyState com soft blob illustration peach + lavender em vez do lucide Link2 plain (per spec §2.5 estado Empty L605),
**so that** a superfície principal autenticada pare de exibir o dashboard shipped Story 2.4/2.5/2.6 em paleta violet-seed legacy + cards plain border + bullet-style empty state, e passe a expressar a identidade "warm creator studio" canônica do PRD v0.5 §UX/Branding **na primeira tela que o usuário logado vê** — desbloqueando coerência visual full-funnel (landing 5.4 Done → signup 5.4 Done → login 5.4 Done → dashboard 5.5 esta) e materializando a quinta superfície shipped em paleta refreshed (após 5.2 tokens + 5.3 primitives + 5.4 pre-auth + esta 5.5 dashboard core).

## Acceptance Criteria

> Todas as AC abaixo rastreiam para fontes canônicas — `docs/frontend-spec.md` v0.3 §2.5 (Dashboard/Links L565-621) + §3.1 (drag-drop L932-949) + §3.2 (edição inline L951-987) + §3.3 (toggle visibility L989-1006) + §3.5 (copy-to-clipboard L1019-1034) + §0.5.3 (IA L82-87 — sidebar+header structure) + §1.3.2 (typescale tokens consumed) + §1.4.2 (radius scale Card 16) + §4.2 (a11y keyboard nav) + §4.6 (touch targets ≥44px). Cada AC cita seção exata. NENHUMA invenção (Constitution Art. IV). Tokens canônicos estão em `app/globals.css` pós-5.2 + primitives refreshed em `components/ui/` pós-5.3 — esta story **apenas consome em pages + components/dashboard + components/links**. Estado pré-existente, deviations descobertas durante audit e racional de cada AC estão isolados em **Dev Notes** — leia antes de implementar.

1. **Dashboard layout header — wordmark "biolink" left + theme palette quick-link right + UserMenu preservado** (per `docs/frontend-spec.md` §2.5 L569 _"[≡] BIOLINK [@demo ▾] [⊙]"_ + §0.5.3 L85 _"Header global: logo (link /dashboard), user dropdown (@username + logout), theme switch global"_ + §1.6 wordmark all-lowercase). Auditar `app/dashboard/layout.tsx`. Mudanças: (a) **Wordmark left**: adicionar `<Link href="/dashboard" className="text-h3 lowercase">biolink</Link>` entre o `<MobileDrawer />` e o spacer `ml-auto`. Pattern idêntico à Auth layout 5.4 e Landing 5.4 header — wordmark text-only lowercase, sem ★ (asterisco prefix DEFERIDO para Story 5.9 wordmark component); (b) **Theme palette quick-link**: adicionar `<Link href="/dashboard/theme" aria-label="Mudar tema" className={buttonVariants({ variant: 'ghost', size: 'icon' })}><PaletteIcon className="size-4" /></Link>` antes do `<UserMenu />` (right side, sequência: theme-link → user-menu). Ghost variant Story 5.3 consome `hover:bg-accent` peach automatic. NÃO implementar runtime toggle light/dark/brand (per DEV-A1 anticipated — preserva IA + ThemeSelector existente Story 3.3 single-source-of-truth); (c) **UserMenu preservado** — sem alteração (já consome `<Avatar>` + `<DropdownMenu>` refreshed Story 5.3); (d) Container header já tem `flex items-center gap-2 border-b border-border px-4 py-3` (shipped Story 2.4) — manter. Resultado: `[wordmark biolink] [MobileDrawer ≡] ... [PaletteIcon ⊙] [UserMenu Avatar ▾]` em mobile, e no desktop o `MobileDrawer` é `hidden md:hidden` (shipped CSS) então fica `[wordmark biolink] ... [PaletteIcon ⊙] [UserMenu Avatar ▾]`.

2. **Dashboard Links page H1 — "Meus links" copy + typescale `text-h1` 32px** (per `docs/frontend-spec.md` §2.5 L572 _"Meus links ← H1 32"_ + §1.3.2 typescale `--text-h1` 32px). Auditar `app/dashboard/page.tsx`. Mudar L50 H1 de **`<h1 className="text-xl font-semibold tracking-tight">Seus links</h1>`** (shipped Story 2.5) para **`<h1 className="text-h1">Meus links</h1>`** (spec verbatim §2.5 L572 — "Meus" not "Seus"; pessoalidade aumentada). Aplicar classe `text-h1` (= 32px + line-height 40 + weight 500 + tracking -0.02em per Story 5.2 typescale tokens em `@theme inline`). Remover classes Tailwind redundantes `text-xl font-semibold tracking-tight` (encapsuladas em `text-h1` via suffix convention DEV-7 5.2). Cor herda `text-foreground` deep plum-tinted automatic.

3. **Dashboard Links page subtitle — URL pública inline + copy-to-clipboard button** (per `docs/frontend-spec.md` §2.5 L573 _"biolink.app/@demo [copiar] ← Body + copy-to-clipboard button"_ + §3.5 copy-to-clipboard pattern L1019-1034). Auditar `app/dashboard/page.tsx`. Mudar L51-53 subtitle de **`<p className="text-sm text-muted-foreground">Gerencie os links que aparecem na sua página pública.</p>`** (shipped descriptive) para uma linha com URL pública + inline copy button: **`{siteUrl}/@{username} [copy]`**. Implementação: criar novo Client Component pequeno `components/dashboard/PublicUrlCopy.tsx` que recebe props `{ username: string; siteUrl: string }` e renderiza `<div className="text-body text-muted-foreground flex items-center gap-2"><span className="font-mono text-sm">{siteUrl}/@{username}</span><Button variant="ghost" size="icon-sm" aria-label="Copiar URL pública" onClick={handleCopy}><CopyIcon className="size-3.5" /></Button></div>` com handler `navigator.clipboard.writeText(...)` + `toast.success('URL pública copiada')` + `toast.error('Não foi possível copiar a URL')` no catch (pattern verbatim de `components/dashboard/UserMenu.tsx:37-44`). Em `app/dashboard/page.tsx` (Server Component): query `username` adicionalmente em `profiles` (já tem user via `auth.getUser()`; adicionar `.select('username')` ao query existente OU criar novo query) → passar para `<PublicUrlCopy username={profile.username} siteUrl={env.NEXT_PUBLIC_SITE_URL} />`. **NOTA:** `UserMenu` já tem essa mesma funcionalidade no dropdown — manter (defense-in-depth UX; usuário pode copiar do header OU inline da Links page).

4. **Link card visual — Card primitive consumido em vez de `<li>` plain border** (per `docs/frontend-spec.md` §2.5 L575-594 _"Card stack — drag handle visible on hover"_ + §1.4.2 Card radius 16 + Story 5.3 AC2 Card +shadow-sm + DEV-B anticipated). Auditar `components/links/LinkRow.tsx:135-139`. Mudar wrapper `<li>` de **`<li ref={setNodeRef} style={style} className="flex items-center gap-3 rounded-md border border-border bg-card p-3">`** (shipped Story 2.5/2.6 — `rounded-md` 6px + plain border) para **`<li ref={setNodeRef} style={style} className="group flex items-center gap-3 rounded-xl border border-border bg-card p-4 shadow-sm ring-1 ring-foreground/5 transition-shadow hover:shadow-md">`**. Justificativas: (a) `rounded-xl` (=16px) matches Card primitive default per §1.4.2 + Story 5.3 audit; (b) `shadow-sm` warm-tinted plum (Story 5.2 tokens `--shadow-sm`); (c) `ring-1 ring-foreground/5` subtle border interno per Story 5.3 Card primitive ratified; (d) `hover:shadow-md` ratified em §2.5 L607 "drag em progresso tem `--shadow-lg`" — hover state é intermediate `--shadow-md` (mais sutil que drag state); (e) `group` class habilita `group-hover:` no drag handle (AC5); (f) `p-4` (16px) mais generoso que `p-3` (12px) — alinha com Card primitive `gap-4 py-4 px-4` Story 5.3 ratified. **NÃO** refatorar para usar `<Card>` primitive diretamente (LinkRow precisa ser `<li>` por `SortableContext` accessibility + ref forwarding to `setNodeRef` from useSortable — Card primitive não suporta `as` prop facilmente; manter `<li>` mas aplicar classes equivalentes via Tailwind — DEV-B anticipated).

5. **LinkRow drag handle visibility — hover-only desktop / always mobile via CSS group-hover** (per `docs/frontend-spec.md` §2.5 L612 _"Drag handle (⋮⋮) sempre visível em mobile (não hover-only)"_ + L619 _"Drag handle aparece on `:hover` no row (revela espacial)"_ + ratified via user elicitation). Auditar `components/links/LinkRow.tsx:144-152` (botão drag handle wrapping `<GripVertical>`). Mudança: adicionar classes responsive opacity ao botão drag handle: substituir className shipped `-ml-1 touch-none cursor-grab text-muted-foreground hover:text-foreground active:cursor-grabbing disabled:cursor-not-allowed disabled:opacity-50` por `-ml-1 touch-none cursor-grab text-muted-foreground transition-opacity hover:text-foreground active:cursor-grabbing disabled:cursor-not-allowed disabled:opacity-50 md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100` — mobile sempre vísible (touch target ≥44px preservado via tap area do button + GripVertical), desktop opacity-0 default + opacity-100 quando o group (`<li>`) está hovered OU quando o próprio handle está focused (a11y keyboard). **NÃO regredir** focus-visible (keyboard users via Tab → handle revela mesmo sem hover do mouse — via `md:focus-visible:opacity-100`). NÃO mudar a11y attributes (`aria-label="Reordenar link"` + `{...attributes}` + `{...listeners}` do dnd-kit) — comportamento dnd-kit preservado intacto.

6. **AddLinkButton — dashed border + accent peach on hover wrapping AddLinkModal trigger** (per `docs/frontend-spec.md` §2.5 L596-598 _"[⊕] Adicionar link ← BUTTON dashed border, accent on hover"_ + §1.4.2 radius scale `--radius-xl` 16). Auditar `app/dashboard/page.tsx:56` e `components/links/EmptyState.tsx:19`. Atualmente AddLinkModal recebe `trigger={<Button type="button">Adicionar link</Button>}` (default primary plum, h-9). Mudança: criar novo component `components/links/AddLinkButton.tsx` Client Component (ou Server Component se não precisar de state) que renderiza `<AddLinkModal trigger={<button type="button" className="flex w-full items-center justify-center gap-2 rounded-xl border-2 border-dashed border-border bg-transparent px-4 py-4 text-body-sm font-medium text-muted-foreground transition-colors hover:border-accent hover:bg-accent/10 hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"><PlusIcon className="size-4" /> Adicionar link</button>} />` (border-dashed 2px border-border default → border-accent peach + bg-accent/10 on hover per spec L597; rounded-xl 16 per radius scale; lucide PlusIcon = ⊕ glyph spec; "Adicionar link" copy verbatim spec L598). Em `app/dashboard/page.tsx:56`: substituir `<AddLinkModal trigger={<Button type="button">Adicionar link</Button>} />` por `<AddLinkButton />` (rendered como bottom-of-list CTA per wireframe — abaixo do `<LinkList>` quando há links). DEV-C anticipated: posição — wireframe §2.5 mostra o AddLinkButton abaixo do último card (não no header). Mudar `<header className="flex items-center justify-between gap-4">` para SEM o AddLinkModal nele (remover linha 55-57 condicional). Adicionar `<AddLinkButton />` após o `<LinkList>` ou após o `<EmptyState>` (sempre visível, regardless de estado vazio/preenchido). **EmptyState** (AC7) também recebe AddLinkButton style para CTA larger.

7. **EmptyState soft blob illustration + larger CTA** (per `docs/frontend-spec.md` §2.5 L605 _"Empty (0 links): ilustração soft (peach + lavender blob) + texto 'Adicione seu primeiro link →' + BUTTON CTA grande"_). Auditar `components/links/EmptyState.tsx`. Mudança ampla: substituir o ícone `<Link2 aria-hidden className="size-10 text-muted-foreground" />` por uma SVG inline soft blob (peach + lavender gradient) renderizada via JSX. Pattern: criar SVG dentro do component (ou novo arquivo `components/links/SoftBlobIllustration.tsx` se preferir isolar): `<svg viewBox="0 0 200 200" aria-hidden="true" className="h-32 w-32"><defs><linearGradient id="blob-gradient" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" stopColor="var(--accent)" /><stop offset="100%" stopColor="var(--primary)" stopOpacity="0.3" /></linearGradient></defs><path d="M40,100 Q40,40 100,40 Q160,40 160,100 Q160,160 100,160 Q40,160 40,100 Z" fill="url(#blob-gradient)" opacity="0.6" /></svg>` (path = blob orgânico com 4 quadratic curves; gradient peach `--accent` → plum `--primary` 30% opacity; opacity overall 0.6 para softness). Mudar texto + estrutura: substituir `<p className="text-sm font-medium">Nenhum link ainda</p>` por `<p className="text-h2 font-medium">Adicione seu primeiro link →</p>` (per spec L605 verbatim — H2 24px), substituir `<p className="text-sm text-muted-foreground">Adicione seu primeiro link para começar a montar sua página.</p>` por `<p className="text-body text-muted-foreground max-w-md">Seus links aparecerão aqui para o público em /@username. Comece adicionando o primeiro.</p>` (helper context, sem repetir CTA copy do H2). Substituir o Button shipped `<AddLinkModal trigger={<Button type="button">Adicione seu primeiro link</Button>} />` por `<AddLinkButton />` (AC6 — dashed border component reused; consistência visual + DRY). Wrapper: ajustar `<section className="flex flex-col items-center gap-4 rounded-md border border-border bg-card p-6 text-center">` para `<section className="flex flex-col items-center gap-6 rounded-xl border border-border bg-card p-12 text-center">` (rounded-xl matches Card 16; p-12 generoso per "BUTTON CTA grande" spec L605; gap-6 spacing between blob + texts + CTA).

8. **MobileDrawer header — adicionar wordmark "biolink" + back link consistency com auth/landing layout** (per `docs/frontend-spec.md` §2.5 L569 + §0.5.3 L85 + consistency com Story 5.4 auth layout). Auditar `components/dashboard/MobileDrawer.tsx`. Mudança opcional + consistência: adicionar wordmark "biolink" no topo do SheetContent (acima da `<nav>`) — atualmente apenas `<SheetTitle className="sr-only">` (a11y label) + `<nav>` (nav items). Após audit, se sheet visual em mobile (clicando ≡ icon) parece "vazio no topo" sem branding, adicionar `<div className="px-3 pt-6 pb-3"><Link href="/dashboard" onClick={() => setOpen(false)} className="text-h3 lowercase">biolink</Link></div>` antes da `<nav>`. **CONDITIONAL** task — se @dev julgar que o wordmark no header global (AC1) já cobre branding mobile, ESKIP esta mudança e documentar. NÃO regredir SheetTitle sr-only (a11y obrigatório para Sheet/Dialog primitives).

9. **Sidebar + Sidebar tokens consumed audit — confirm Story 5.2 tokens are refreshed (no change esperado)** (per `docs/frontend-spec.md` §1.2 sidebar tokens + Story 5.2 globals.css). Auditar `components/dashboard/Sidebar.tsx` + `globals.css` para sidebar-\* tokens. Verificar que `--sidebar`, `--sidebar-foreground`, `--sidebar-accent`, `--sidebar-accent-foreground`, `--sidebar-border`, `--sidebar-ring` estão presentes em `globals.css` pós-5.2 com valores Soft Studio (light: lavender mist tinted; dark: deep plum surface tinted; brand: deeper lavender). Se ABSENT (não foram adicionados em 5.2 — verificar `:root`/`.dark`/`.brand` blocks), abrir `[STORY-5.5-F1]` LOW e DEFERIR para Story 5.6 ou adicionar inline com 1-liner per tema (DEV-D anticipated). Se PRESENT, esta AC é **no-op** (Sidebar.tsx + MobileDrawer.tsx já consomem `bg-sidebar`, `text-sidebar-foreground`, `bg-sidebar-accent`, `text-sidebar-accent-foreground`, `ring-sidebar-ring` per shipped Story 2.4 — tokens swap automatic via 5.2). **Validação visual:** sidebar background deve aparecer com tint lavender em light theme (não `#FFFFFF` plain), active nav item bg deve aparecer peach `--accent` (não plum bg primary). Smoke runtime manual opcional (Task 8).

10. **UserMenu visual audit — confirm primitives consumed are 5.3-refreshed (no change esperado)** (per Story 5.3 audit DropdownMenu + Avatar primitives Done). Auditar `components/dashboard/UserMenu.tsx`. Verificar que `DropdownMenu` + `DropdownMenuContent` + `DropdownMenuItem` + `Avatar` + `AvatarImage` + `AvatarFallback` primitives consumidos estão refreshed Story 5.3 (hover peach, focus ring 3px @ 0.5 alpha, rounded-xl, etc.). UserMenu não tem typescale hardcoded explícito (consome defaults de DropdownMenuItem). **Esta AC é no-op esperada** (Story 5.3 audit fechado e UserMenu shipped Story 2.4 sem typescale legacy). Se @dev encontrar `text-{n}xl` ou `font-semibold tracking-tight` hardcoded em UserMenu durante audit, refresh para token canônico (`text-body`/`text-h3` etc.); caso contrário, marcar `[x]` no-op.

11. **dnd-kit drag-drop + inline edit + toggle visibility behaviors preservados** (per `docs/frontend-spec.md` §3.1 drag-drop + §3.2 inline edit + §3.3 toggle visibility + Story 2.6 Done). LinkRow shipped Story 2.5/2.6 já implementa: (a) drag-drop via `useSortable` + `SortableContext` (LinkList.tsx) — handle aria-label + Keyboard sensor + Pointer/Touch sensors com activation constraints; (b) inline title edit + URL edit — `editingTitle`/`editingUrl` state + `Input` + Enter/Escape/onBlur handlers + `updateLink` Server Action; (c) toggle visibility via `<Switch>` primitive (Story 5.3 refreshed — peach ON state) + `toggleLinkVisibility` Server Action com rollback optimistic. **Esta AC é negativa: NÃO regredir nenhum desses comportamentos.** Verificação: rodar `pnpm test:components -t LinkRow` e `pnpm test:components -t LinkList` — todos os tests behavior devem continuar passing pós-mudanças visuais (AC4 + AC5). Snapshot pode mudar (esperado por className update), mas behavior tests (`fireEvent.keyDown(input, { key: 'Enter' })`, `fireEvent.click(switch)`, etc.) devem preservar resultado verde.

12. **Suíte completa de qualidade local PASS sem regressão funcional**: `pnpm typecheck` exit 0, `pnpm lint` zero warnings, `pnpm build` (Next 16.2.5 / Turbopack) 15/15 static pages PASS (note: `/dashboard` é dynamic route, não conta para static — mas build deve completar sem erro), `pnpm test:components` mantém **194/194 verde por contagem** — snapshots em `tests/components/features/links/__snapshots__/` + `tests/components/features/dashboard/__snapshots__/` podem inexistir pré-5.5 (test files usam `screen.getByRole`/`getByText` queries, não `toMatchSnapshot()` — confirmar via `ls`). Se test:components inclui any snapshot:**Processo obrigatório:** rodar `pnpm test:components` SEM `-u` primeiro → revisar diff → confirmar mudança é estrutural/intencional → só então rodar `pnpm test:components -u`. Documentar **quantos snapshots foram atualizados e quais** no Dev Agent Record (mesmo padrão Story 5.3/5.4). **Behavior tests** (não-snapshot): LinkRow tests verificam edit/toggle/delete flows; LinkList tests verificam drag-drop reorder; Sidebar/UserMenu/MobileDrawer tests verificam nav rendering + dropdown interactions + drawer toggle. Todos devem CONTINUAR PASSING — quebra = regressão funcional.

13. **Lighthouse CI mantido ≥ 0.85 em paths gated + bundle delta neutral**: `.lighthouserc.json` threshold (≥ 0.85 Performance/Accessibility/Best Practices/SEO em `/` + `/[username]` per Story 3.5 + estabilizado [STORY-3.5-F3]). `/dashboard` é authenticated route — **fora do gate paths default** (Lighthouse não navega para `/dashboard` em CI). Mas paths trigger inclui `app/**` e `components/**` — mudanças aqui podem afetar `/` (landing também consome `Card` primitive 5.3 + tokens 5.2) via shared bundle. Esperado: bundle delta **neutral** (mudanças são layout/className + 1-2 ícones lucide novos `Palette` + `Plus` + `Copy` se não estavam — Copy já em UserMenu shipped; Palette + Plus tree-shakeable ~1-2 KB max). LCP `/` baseline pós-5.2/5.3/5.4 ~180-185 KB First Load JS. Lighthouse audit pode flagar A11y issues novos se: (a) drag handle `opacity-0` no desktop sem `focus-visible:opacity-100` (mas AC5 obriga focus-visible behavior); (b) soft blob SVG sem `aria-hidden="true"` (AC7 obriga); (c) AddLinkButton dashed sem `focus-visible:ring` (AC6 obriga). Validar via Lighthouse no PR. Qualquer queda > 5 pontos exige investigação antes do merge (mesmo gate Story 5.2/5.3/5.4 — runs:3 + median absorve flake per [STORY-3.5-F3]).

14. **No regression em UserMenu copy URL pública behavior (single-source-of-truth check)**: AC3 adiciona inline copy button em `app/dashboard/page.tsx` via `<PublicUrlCopy>` component novo. UserMenu shipped também tem `handleCopyUrl` (linhas 37-44 — `navigator.clipboard.writeText(`${siteUrl}/@${username}`)` + toast). **Ambos devem coexistir** — não remover do UserMenu (defense-in-depth UX; usuário pode preferir copiar do header dropdown). **Esta AC é negativa: NÃO regredir UserMenu copy behavior.** Validar via `pnpm test:components -t UserMenu` — test `clicking copy URL fires clipboard.writeText` deve continuar passing.

15. **Constitution Art. IV compliance + housekeeping**: cada AC1-AC14 cita `[Source: docs/frontend-spec.md §{n}.{m} L{linha}]` em Dev Notes. Zero invenção de glyphs/copies/comportamentos não-spec'd. Backlog crossover: `docs/STORY-BACKLOG.md` entrada `[EPIC-5-S5]` MEDIUM movida para Change Log DONE post-merge (Task 9.1 — pattern Story 5.2/5.3/5.4). Story file mantém estrutura template-v2 (Status / Executor Assignment / Story / AC / 🤖 CodeRabbit Integration / Tasks / Dev Notes / Change Log / Dev Agent Record). Frontmatter ClickUp pulado (`mcp.enabled: false` per `core-config.yaml`). QA gate manual via QA-001 known-issues (CodeRabbit/WSL incompat. macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`).

> **No-Invention reconciliation:** O backlog `[EPIC-5-S5]` (`docs/STORY-BACKLOG.md`) lista como acceptance line: _"`pnpm test:components` PASS; component tests de `LinkRow`/`AddLinkButton` atualizados; smoke `/dashboard` × 3 temas"_. Reconcilio: backlog menciona `AddLinkButton` como component esperado — Story 5.5 **cria** (AC6) `components/links/AddLinkButton.tsx` como novo file (não existe pré-5.5; verificar via `ls components/links/`). Tests para AddLinkButton serão criados em Task 7 OU implicitamente cobertos pelo render via `app/dashboard/page.tsx` + `EmptyState.tsx` consumidores. Smoke × 3 temas é **Task condicional 8** (não AC formal) — per MEMORY `feedback_skip_manual_smoke.md`, @dev pergunta antes de pedir, e se skip pode marcar `[x]` com nota `_(SKIPPED ...)_` + risco residual documentado. AC1-AC10 derivam **verbatim** de spec §2.5 L565-621 (1 AC por elemento estrutural). AC11-AC14 cobrem preservação behavior + gates + no-regression. AC15 housekeeping.

> **Out-of-scope (explicitamente deferido):**
>
> - **Runtime theme toggle (light/dark/brand instant switch)** — preserva IA ThemeSelector Story 3.3 (DB persistence); AC1 implementa **palette quick-link** apenas. Toggle real seria refactor maior → Story 5.6 OU pós-v1.x.
> - **Spring transitions** (drag-drop spring overshoot, Switch flip spring, Card lift-on-hover scale) → Story 5.8 (Motion + polish).
> - **Wordmark `biolink ★` component + favicon ★** → Story 5.9 (esta story usa text-only "biolink" lowercase como placeholder em layout AC1 + opcional MobileDrawer AC8).
> - **Soft blob illustration upgrade** (SVG inline básico em AC7 vs ilustração real produzida por designer) — Story 5.5 ship com SVG inline pragmático; designer-grade illustration pode substituir em iteração futura.
> - **Mudança em /dashboard/profile/theme/account/analytics** → Stories 5.6/5.7.
> - **Mudança em /@username (PublicPage)** → Story 5.7.
> - **Refactor LinkRow para usar Card primitive como wrapper `<Card>`** (em vez de `<li>` com classes equivalentes) → fora de escopo (precisaria de `as` prop em Card primitive — Story 5.3 escopo fechou primitives). LinkRow continua sendo `<li>` por SortableContext + setNodeRef ref forwarding necessidade.
> - **Sidebar-\* tokens add ao globals.css** se ausentes (AC9 verifica + ESKIP/F1 LOW se ausentes) → Story 5.6 escopo melhor.
> - **Email/username live validation refresh visual** → Story 5.1 implementou + 5.4 ratificou; out desta story.
> - **Refactor `check-contrast.mjs` para parser CSS automatizado** → `[STORY-3.2-F1]` LOW (continua aberto).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

> **Nota (@sm):** `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` **e** `mcp.enabled: false` → skip notice é o comportamento correto do template-v2 (idêntico às Stories 1.4-5.4, todas Done). ClickUp sync pulado (`mcp.enabled: false`). Gate de qualidade segue o **workaround manual QA-001** (CodeRabbit/WSL incompat. em macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`), padrão validado em 1.4-5.4.

## Tasks / Subtasks

- [x] **Task 1 — Audit Dashboard Layout (`app/dashboard/layout.tsx`)** (AC: 1) — _executor: @dev_
  - [x] 1.1 Abrir `app/dashboard/layout.tsx`. Imports atuais (L1-9): `redirect`, `ReactNode`, `EmailVerificationBanner`, `MobileDrawer`, `Sidebar`, `UserMenu`, `env`, `createClient`. Adicionar imports faltantes: `import Link from 'next/link'`, `import { Palette } from 'lucide-react'`, `import { buttonVariants } from '@/components/ui/button'`.
  - [x] 1.2 Modificar header (L31-41). Atual:
    ```tsx
    <header className="flex items-center gap-2 border-b border-border px-4 py-3">
      <MobileDrawer />
      <div className="ml-auto">
        <UserMenu ... />
      </div>
    </header>
    ```
    Novo (wordmark left + palette quick-link before UserMenu):
    ```tsx
    <header className="flex items-center gap-3 border-b border-border px-4 py-3">
      <MobileDrawer />
      <Link href="/dashboard" className="text-h3 lowercase">
        biolink
      </Link>
      <div className="ml-auto flex items-center gap-1">
        <Link
          href="/dashboard/theme"
          aria-label="Mudar tema"
          className={buttonVariants({ variant: 'ghost', size: 'icon' })}
        >
          <Palette className="size-4" aria-hidden="true" />
        </Link>
        <UserMenu
          username={profile?.username ?? ''}
          displayName={profile?.display_name ?? null}
          avatarUrl={profile?.avatar_url ?? null}
          siteUrl={env.NEXT_PUBLIC_SITE_URL}
        />
      </div>
    </header>
    ```
    (gap-3 entre MobileDrawer + wordmark; flex gap-1 wrap para palette-link + UserMenu lado a lado right).
  - [x] 1.3 `text-h3` no wordmark = 20px per Story 5.2 typescale. Lowercase per spec §1.6. `<Link href="/dashboard">` (não `/`) — dashboard wordmark leva ao home autenticado, não landing.
  - [x] 1.4 `Palette` é named export de `lucide-react` (confirmar via `grep PaletteIcon` ou `Palette` em package.json `lucide-react` v0.x catalog — esperado present; se ausente, usar `Sparkles` ou `Brush` como fallback — DEV-A2 anticipated).
  - [x] 1.5 Rodar `pnpm typecheck` exit 0 após edit (sanity check imports).

- [x] **Task 2 — Audit Dashboard Links page (`app/dashboard/page.tsx`)** (AC: 2, 3, 6) — _executor: @dev_
  - [x] 2.1 Abrir `app/dashboard/page.tsx`. Imports atuais (L1-8). Adicionar imports faltantes: `import { env } from '@/lib/env'`, `import { PublicUrlCopy } from '@/components/dashboard/PublicUrlCopy'` (criado Task 4), `import { AddLinkButton } from '@/components/links/AddLinkButton'` (criado Task 3). Remover `import { Button } from '@/components/ui/button'` se não restar uso após Task 2.4.
  - [x] 2.2 Adicionar query para `username` (DEV-C). Atual L26-30 query `pages` apenas. Adicionar query `profiles` para extrair username:
    ```tsx
    const { data: profile } = await supabase
      .from('profiles')
      .select('username')
      .eq('id', user.id)
      .single();
    ```
    (single query, RLS já garante owner-only access — Story 1.4). Se `profile?.username` for falsy (edge case durante onboarding antes do trigger 1.4), passar `''` para `<PublicUrlCopy>` que renderiza fallback `siteUrl/@` placeholder.
  - [x] 2.3 Modificar H1 (L50). Atual: `<h1 className="text-xl font-semibold tracking-tight">Seus links</h1>`. Novo: `<h1 className="text-h1">Meus links</h1>` (text-h1 = 32px per typescale Story 5.2; copy "Meus" not "Seus" per spec §2.5 L572).
  - [x] 2.4 Modificar subtitle (L51-53). Atual: `<p className="text-sm text-muted-foreground">Gerencie os links que aparecem na sua página pública.</p>`. Novo: `<PublicUrlCopy username={profile?.username ?? ''} siteUrl={env.NEXT_PUBLIC_SITE_URL} />` (component Client criado Task 4).
  - [x] 2.5 Modificar header da page (L48-58). Atual structure tem `<header>` com h1+subtitle left + AddLinkModal condicional right. Novo: separar header simples (h1+subtitle, sem AddLinkModal) + posicionar `<AddLinkButton />` ABAIXO do LinkList/EmptyState:

    ```tsx
    return (
      <div className="mx-auto max-w-2xl space-y-6">
        <header className="space-y-2">
          <h1 className="text-h1">Meus links</h1>
          <PublicUrlCopy username={profile?.username ?? ''} siteUrl={env.NEXT_PUBLIC_SITE_URL} />
        </header>

        {links.length === 0 ? <EmptyState /> : <LinkList links={links} />}

        {links.length > 0 && <AddLinkButton />}
      </div>
    );
    ```

    (AddLinkButton aparece após LinkList quando há links; EmptyState contém o próprio AddLinkButton internally per Task 6 — AC7).

  - [x] 2.6 Rodar `pnpm typecheck` exit 0 (sanity check imports + types).

- [x] **Task 3 — Create `components/links/AddLinkButton.tsx`** (AC: 6) — _executor: @dev_
  - [x] 3.1 Criar novo file `components/links/AddLinkButton.tsx` com Client Component que wrapa `<AddLinkModal>` com trigger dashed-border per AC6:

    ```tsx
    'use client';

    import { Plus } from 'lucide-react';

    import { AddLinkModal } from '@/components/links/AddLinkModal';

    export function AddLinkButton() {
      return (
        <AddLinkModal
          trigger={
            <button
              type="button"
              className="flex w-full items-center justify-center gap-2 rounded-xl border-2 border-dashed border-border bg-transparent px-4 py-4 text-body-sm font-medium text-muted-foreground transition-colors hover:border-accent hover:bg-accent/10 hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2"
            >
              <Plus className="size-4" aria-hidden="true" /> Adicionar link
            </button>
          }
        />
      );
    }
    ```

    Notas: (a) `<button>` element raw (não Button primitive) — Button primitive não suporta border-dashed nativo + custom hover; (b) `border-2 border-dashed border-border` per spec L597 "dashed border"; (c) `hover:border-accent hover:bg-accent/10 hover:text-accent-foreground` per spec L598 "accent on hover" — peach `--accent` Story 5.2 token; (d) `rounded-xl` = 16 per radius scale §1.4.2; (e) `Plus` lucide = ⊕ glyph spec L597.

  - [x] 3.2 Verificar que `AddLinkModal` props (`trigger: ReactElement`) aceita `<button>` raw — confirmar olhando `AddLinkModal.tsx:35` (`trigger`: ReactElement type) e `DialogTrigger render={trigger}` (Base UI Dialog pattern, suporta any ReactElement). Sem mudança necessária no `AddLinkModal`.
  - [x] 3.3 Documentar choice de NÃO usar `<Button variant="outline" className="border-dashed">` (que falharia: Button primitive `outline` variant é solid border, não há border-dashed nativo + custom hover styling required).

- [x] **Task 4 — Create `components/dashboard/PublicUrlCopy.tsx`** (AC: 3) — _executor: @dev_
  - [x] 4.1 Criar novo file `components/dashboard/PublicUrlCopy.tsx` Client Component:

    ```tsx
    'use client';

    import { Copy } from 'lucide-react';

    import { Button } from '@/components/ui/button';
    import { toast } from '@/lib/toast';

    interface Props {
      username: string;
      siteUrl: string;
    }

    export function PublicUrlCopy({ username, siteUrl }: Props) {
      const url = username ? `${siteUrl}/@${username}` : `${siteUrl}/@`;

      async function handleCopy() {
        if (!username) return;
        try {
          await navigator.clipboard.writeText(url);
          toast.success('URL pública copiada');
        } catch {
          toast.error('Não foi possível copiar a URL');
        }
      }

      return (
        <div className="text-body text-muted-foreground flex items-center gap-2">
          <span className="font-mono text-sm">{url}</span>
          <Button
            variant="ghost"
            size="icon-sm"
            aria-label="Copiar URL pública"
            onClick={handleCopy}
            disabled={!username}
          >
            <Copy className="size-3.5" aria-hidden="true" />
          </Button>
        </div>
      );
    }
    ```

    Notas: (a) handler pattern verbatim `UserMenu.tsx:37-44` (não regredir AC14); (b) fallback URL quando `username = ''` previne TypeError no Server Component → Client transition; (c) `size="icon-sm"` Button — verificar que essa variant existe (Story 5.3 audit; se ausente, usar `size="icon"` h-8 default + reduzir CopyIcon size para `size-3`); (d) `text-body` = 16px / `font-mono text-sm` = 14px monospace (URL é code-like — monospace ratificada por convention; se preferir não-mono, usar `text-body-sm` default).

- [x] **Task 5 — Audit LinkRow (`components/links/LinkRow.tsx`)** (AC: 4, 5, 11) — _executor: @dev_
  - [x] 5.1 Abrir `components/links/LinkRow.tsx`. Localizar wrapper `<li>` (L136-140).
  - [x] 5.2 Modificar className do `<li>` per AC4: atual `flex items-center gap-3 rounded-md border border-border bg-card p-3` → novo `group flex items-center gap-3 rounded-xl border border-border bg-card p-4 shadow-sm ring-1 ring-foreground/5 transition-shadow hover:shadow-md`. Adicionar `group` para habilitar group-hover no drag handle.
  - [x] 5.3 Modificar className do drag handle button (L147) per AC5: atual `-ml-1 touch-none cursor-grab text-muted-foreground hover:text-foreground active:cursor-grabbing disabled:cursor-not-allowed disabled:opacity-50` → novo `-ml-1 touch-none cursor-grab text-muted-foreground transition-opacity hover:text-foreground active:cursor-grabbing disabled:cursor-not-allowed disabled:opacity-50 md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100`. Justificativa: opacity-0 default desktop + opacity-100 quando row hovered OU handle focused; mobile (`< md`) sempre opacity-100 (sem classes responsive).
  - [x] 5.4 NÃO mudar: a11y attributes (`aria-label="Reordenar link"`), `{...attributes}` `{...listeners}` dnd-kit, ref forwarding (`ref={setNodeRef}`), style prop (`style={style}`), edit handlers (commitTitle/commitUrl), toggle visibility handler (onToggleVisibility), delete dialog (AlertDialog), Switch (visibility), ChevronUp/ChevronDown (keyboard fallback move).
  - [x] 5.5 Rodar `pnpm test:components -t LinkRow` para validar behavior tests não regrediram (edit/toggle/delete flows).

- [x] **Task 6 — Audit EmptyState (`components/links/EmptyState.tsx`)** (AC: 6, 7) — _executor: @dev_
  - [x] 6.1 Abrir `components/links/EmptyState.tsx`. Reescrever ampla per AC7.
  - [x] 6.2 Imports: substituir `import { Link2 } from 'lucide-react'` (removido — substituído por SVG inline soft blob). Manter `import { AddLinkModal } from '@/components/links/AddLinkModal'` SE ainda usado (após AC6 muda para `<AddLinkButton />` — remover AddLinkModal import + Button import; adicionar `import { AddLinkButton } from '@/components/links/AddLinkButton'`).
  - [x] 6.3 Reescrever component body:

    ```tsx
    import { AddLinkButton } from '@/components/links/AddLinkButton';

    export function EmptyState() {
      return (
        <section className="flex flex-col items-center gap-6 rounded-xl border border-border bg-card p-12 text-center">
          <svg viewBox="0 0 200 200" aria-hidden="true" className="h-32 w-32">
            <defs>
              <linearGradient id="empty-blob-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stopColor="var(--accent)" />
                <stop offset="100%" stopColor="var(--primary)" stopOpacity="0.3" />
              </linearGradient>
            </defs>
            <path
              d="M40,100 Q40,40 100,40 Q160,40 160,100 Q160,160 100,160 Q40,160 40,100 Z"
              fill="url(#empty-blob-gradient)"
              opacity="0.6"
            />
          </svg>
          <div className="space-y-2">
            <p className="text-h2 font-medium">Adicione seu primeiro link →</p>
            <p className="text-body max-w-md text-muted-foreground">
              Seus links aparecerão aqui para o público em /@username. Comece adicionando o
              primeiro.
            </p>
          </div>
          <AddLinkButton />
        </section>
      );
    }
    ```

    Justificativas: (a) wrapper rounded-xl + p-12 + gap-6 generoso per spec L605 "BUTTON CTA grande"; (b) SVG inline blob peach (`--accent`) → plum (`--primary` 30% opacity) gradient com 60% overall opacity per spec L605 "ilustração soft"; (c) H2 (24px) "Adicione seu primeiro link →" copy verbatim spec L605 (arrow → mantida per spec); (d) helper text com max-w-md (para não estender em telas wide).

  - [x] 6.4 Documentar grep count consumidores de `<EmptyState>` (`grep -rn EmptyState app/ components/`) — esperado: apenas `app/dashboard/page.tsx:60`.
  - [x] 6.5 Confirmar que `'use client'` directive NÃO é necessária no EmptyState (component é puro JSX/SVG estático, sem hooks/event handlers). Apenas `<AddLinkButton>` (filho) é Client. Padrão Server Component → Client Component permitido em RSC tree.

- [x] **Task 7 — Audit dashboard chrome components (`components/dashboard/{Sidebar,MobileDrawer,UserMenu}.tsx`)** (AC: 8, 9, 10) — _executor: @dev_
  - [x] 7.1 _(no-op confirmed — Sidebar.tsx consome `bg-sidebar/border-sidebar-border/text-sidebar-foreground/bg-sidebar-accent/text-sidebar-accent-foreground/ring-sidebar-ring`; `app/globals.css` confirmou `--sidebar*` tokens em :root L72-79 + .dark L132-139 + .brand L192-199. F1 LOW NÃO materializa.)_ Abrir `components/dashboard/Sidebar.tsx` (AC9). Audit: verificar classes `bg-sidebar`, `text-sidebar-foreground`, `bg-sidebar-accent`, `text-sidebar-accent-foreground`, `ring-sidebar-ring` continuam consumidos (esperado: SIM, sem refactor). Validar que `globals.css` :root/.dark/.brand têm `--sidebar*` tokens (`grep "^\s*--sidebar" app/globals.css`). Se ABSENT em algum tema, criar `[STORY-5.5-F1]` LOW (out-of-scope; default tem fallback via `--background`/`--foreground` Tailwind v4 — no visual regression). Sem mudança esperada no arquivo `Sidebar.tsx`.
  - [x] 7.2 _(SKIPPED — DEV-E: wordmark do header global AC1 já visível em mobile (Link `text-h3 lowercase` sem `md:hidden`); adicionar wordmark dentro do Sheet seria redundância visual. SheetContent intact.)_ Abrir `components/dashboard/MobileDrawer.tsx` (AC8). Audit: SheetContent atual renderiza apenas `<SheetTitle sr-only>` + `<nav>`. Considerar adicionar wordmark inline (opcional per AC8 conditional):
    ```tsx
    <SheetTitle className="sr-only">Menu de navegação</SheetTitle>
    <div className="px-3 pt-6 pb-3">
      <Link
        href="/dashboard"
        onClick={() => setOpen(false)}
        className="text-h3 lowercase"
      >
        biolink
      </Link>
    </div>
    <nav aria-label="Navegação principal" className="flex flex-col gap-1 p-3 pt-0">
      ...
    </nav>
    ```
    DEV-E anticipated: se @dev julga que wordmark no header global (AC1) cobre mobile branding suficientemente, SKIP esta mudança e marcar `[x] _(SKIPPED — wordmark layout header cobre mobile per AC1 ratified)_`.
  - [x] 7.3 _(no-op confirmed — UserMenu sem typescale legacy hardcoded; consome DropdownMenuItem defaults refreshed via Story 5.3.)_ Abrir `components/dashboard/UserMenu.tsx` (AC10). Audit: verificar typescale + classes consumed estão refreshed Story 5.3. Esperado no-op (DropdownMenu refreshed via 5.3; sem `text-{n}xl font-semibold tracking-tight` hardcoded). Se encontrar legacy classes, refresh para tokens canônicos. Marcar `[x] _(no-op confirmed)_` se sem mudança.
  - [x] 7.4 Verificar que `<UserMenu>` ainda consome `<Copy>` lucide icon (linha 4 `import { Copy, LogOut }`) — necessário para defense-in-depth UX (AC14). Sem mudança.

- [x] **Task 8 — Quality gates locais + (eventual) snapshot review** (AC: 12) — _executor: @dev_
  - [x] 8.1 Rodar SEQUENCIAL (em foreground, capturar exit code per MEMORY `feedback_avoid_tail_pipe_in_background.md`):
    ```bash
    pnpm typecheck
    pnpm lint
    pnpm build
    pnpm test:components
    ```
    Capturar output final no Dev Agent Record `Debug Log References`. Exit code 0 obrigatório.
  - [x] 8.2 **Snapshot review preventivo:** se `test:components` reportar snapshots (verificar via `find tests/components -name "*.snap"`), revisar diffs intencionais para LinkRow/EmptyState (mudanças className AC4/AC5/AC7) e qualquer test que use `toMatchSnapshot()`. Se inexistentes (esperado per current convention), TASK no-op aqui.
  - [x] 8.3 Behavior tests críticos a confirmar PASSING:
    - `tests/components/features/dashboard/Sidebar.test.tsx` — renders nav items + isActive logic
    - `tests/components/features/dashboard/MobileDrawer.test.tsx` — toggle drawer + nav items + onClick close
    - `tests/components/features/dashboard/UserMenu.test.tsx` — dropdown items + clipboard handler + signOut handler
    - `tests/components/features/links/LinkList.test.tsx` — dnd-kit reorder + onMove ↑/↓ + persistence
    - `tests/components/features/links/LinkRow.test.tsx` — edit title/URL + toggle visibility + delete dialog
    - `tests/components/features/links/AddLinkModal.test.tsx` — open modal + submit form + validation errors
      Se alguma test file usa snapshot, listar quais foram atualizados em Completion Notes.
  - [x] 8.4 _(no-op — Task 8.2 confirmou ausência de snapshots em `tests/components/features/{links,dashboard}/__snapshots__/`; behavior tests usam `screen.getByRole/getByText` queries. 1 test source file atualizado em `LinkList.test.tsx` (não snapshot): copy "Adicione seu primeiro link" → regex + button name "Adicionar link" reconciliado com nova estrutura EmptyState per DEV-H. test:components 194/194 PASS sem `-u`.)_ Rodar `pnpm test:components -u` SOMENTE se Task 8.2 confirmar snapshots desatualizados e diff revisado intencional. Re-rodar sem `-u` para confirmar 194/194 green.

- [x] **Task 9 — Smoke runtime manual (CONDICIONAL — perguntar antes per MEMORY `feedback_skip_manual_smoke.md`)** (AC: 13) — _executor: @dev_
  - [x] 9.1 Perguntar ao usuário: "Quer smoke manual browser para validar (a) Dashboard layout header com wordmark biolink + Palette icon link + UserMenu, (b) /dashboard H1 Meus links + URL pública + copy button, (c) LinkRow Card visual rounded-xl + shadow-sm + drag handle hover-only desktop / always mobile, (d) AddLinkButton dashed border accent peach on hover, (e) EmptyState soft blob gradient + larger CTA, (f) Sidebar tokens lavender mist tinted, (g) cycle nos 3 temas (light/dark/brand) via Theme palette quick-link?"
  - [x] 9.2 _(N/A — user respondeu SKIP em 9.1.)_ Se **SIM** → rodar `pnpm dev` em background, abrir `http://localhost:3000/login` → autenticar → navegar `/dashboard` → cycle pelos 3 temas via header palette icon → ver Empty state primeiro (sem links) + adicionar 1 link → ver LinkRow card + hover drag handle. Documentar resultados.
  - [x] 9.3 _(SKIPPED — cobertura indireta via test:components 194/194 PASS + behavior tests dashboard/links + Lighthouse CI no PR; risco residual: regressões visuais responsive mobile vs desktop, drag handle group-hover behavior CSS, palette quick-link hover state, soft blob SVG renders cross-browser — não detectáveis 100% por test:components quando classes condicionais via group-hover/responsive. Aceito por @dev — @qa pode solicitar smoke pontual se necessário.)_
  - [x] 9.4 Lighthouse CI vai validar Performance/Accessibility automaticamente em PR (gate ≥ 0.85, paths `app/**` + `components/**`). `/dashboard` é authenticated route — fora dos gate URLs default `/` + `/[username]`, mas mudanças aqui podem afetar bundle shared via Card/Button primitives consumidos (delta esperado neutral). Não rodar local.

- [x] **Task 10 — Housekeeping (Status + Backlog + Change Log + File List + Dev Agent Record)** (AC: 15) — _executor: @dev_
  - [x] 10.1 Atualizar `docs/STORY-BACKLOG.md`:
    - Remover entrada `[EPIC-5-S5]` da seção MEDIUM (padrão Story 5.2/5.3/5.4 — remove ao Done).
    - Adicionar linha no Change Log do backlog: `| 2026-{MM}-{DD} | DONE | [EPIC-5-S5] → Story 5.5 implementada (dashboard core layout + links Soft Studio refresh — wordmark biolink + Palette quick-link + H1 Meus links + URL copy inline + LinkRow Card visual + drag handle hover-only desktop + AddLinkButton dashed + EmptyState soft blob) | Dex (dev) |`.
    - Decrementar statistics (Total e MEDIUM).
    - Atualizar nota Epic 5 Refresh header narrative: incluir 5.5 como DONE.
    - Atualizar `last_updated` frontmatter.
    - Se `[STORY-5.5-F1]` LOW (sidebar-\* tokens absent — AC9 condicional) materializar, adicionar entrada em LOW Priority + Change Log ADD.
  - [x] 10.2 Mudar Status desta story (linha 5) de `Draft` para `InProgress` no início da implementação e depois `Ready for Review` antes do handoff @qa.
  - [x] 10.3 Adicionar linhas em **Change Log** desta story (versões 0.2 post-validate-po, 0.3 implementação).
  - [x] 10.4 Atualizar **Dev Agent Record** (Model Used, Debug Log References, Completion Notes, File List).
  - [x] 10.5 Não commitar com `git push` (delegar a @devops per agent-authority). Commit local OK com mensagem `feat: dashboard core layout + links Soft Studio refresh [Story 5.5]`.

## Dev Notes

> Esta seção **substitui** a necessidade de @dev abrir `docs/frontend-spec.md` ou `docs/architecture/*` durante implementação. Tudo o que importa para implementar 5.5 está aqui, com `[Source: ...]` rastreando cada decisão.

### Previous Story Insights (5.2 + 5.3 + 5.4)

**Story 5.2 Done — tokens canônicos em `app/globals.css`:**

- **Cores** (light/dark/brand): `--primary` deep plum `#5B3A8C` (light/brand) / lavender `#B8A1E8` (dark); `--accent` peach `#FFB5A7` (light/brand) / warm peach `#FF9B8A` (dark); `--background` lavender mist `#FAF8FF` (light) / plum-night `#14102A` (dark) / deeper lavender `#F0E8FF` (brand); `--foreground` deep ink `#1B1530` (light/brand) / cream `#F4EFFB` (dark); `--card`/`--popover` white (light/brand) / `#2A2244` (dark); `--destructive` `#C84141` (light/brand DEV-9) / `#FF7878` (dark); `--ring` deep plum (matches primary).
- **Typescale 9 tokens** via Tailwind v4 suffix convention DEV-7: `text-display` (40px / 48 lh / weight 500 / tracking -0.02em), `text-h1` (32 / 40 / 500 / -0.02), `text-h2` (24 / 32 / 500 / -0.01), `text-h3` (20 / 28 / 500 / -0.01), `text-body-lg` (18 / 28 / 400 / 0), `text-body` (16 / 24 / 400 / 0), `text-body-sm` (14 / 20 / 400 / 0), `text-caption` (12 / 16 / 400 / +0.01), `text-button` (14 / 20 / 500 / 0). **Aplicação Story 5.5: substituir `text-xl font-semibold tracking-tight` shipped em DashboardPage H1 por `text-h1` canônico; aplicar `text-h2` no EmptyState H1; aplicar `text-h3` no wordmark layout header per spec §1.6 + §2.5 + §1.3.2.**
- **Radius scale**: `--radius-sm` 4 / `--radius-md` 8 (DEPRECATED uso default em LinkRow shipped) / `--radius-lg` 12 (default Input/Button shipped) / `--radius-xl` 16 (Card default Story 5.3, target Story 5.5 LinkRow/EmptyState wrappers) / `--radius-2xl` 24 (Dialog Story 5.3) / `--radius-full` 9999. **Story 5.5 swap `rounded-md` → `rounded-xl` em LinkRow wrapper + EmptyState section + AddLinkButton.**
- **Shadows warm-tinted plum** (light/brand): `--shadow-sm` `0 1px 2px 0 rgba(91, 58, 140, 0.05)` (Card default Story 5.3; target LinkRow Story 5.5 hover state) / `--shadow-md` (hover state intermediate; target LinkRow `:hover`) / `--shadow-lg` (Dialog Story 5.3; drag state per §2.5 L607 mas drag não implementado nesta story — Story 5.8 motion). **Story 5.5 aplica `shadow-sm` em LinkRow default + `shadow-md` em LinkRow hover.**
- **Status tokens** (Story 5.3 Task 8.0 pré-flight): `--success` `#2B7A5E` light/brand / `#7AC9A0` dark + foreground; `--warning` `#B8742A` light/brand / `#E8B978` dark + foreground. **Story 5.5 NÃO consome status tokens** (single consumer atual é Sonner border-left via Story 5.3 AC7; LinkRow toggle visibility usa Switch peach ON state, não success/warning tokens).
- **Sidebar tokens** (provavelmente em globals.css, AC9 verifica): `--sidebar`, `--sidebar-foreground`, `--sidebar-accent`, `--sidebar-accent-foreground`, `--sidebar-border`, `--sidebar-ring`. **Story 5.5 NÃO mexe em sidebar tokens — apenas valida presença (se absent, abre F1 LOW).**

**Story 5.3 Done — primitives audit:**

- **Button**: ghost variant consome `hover:bg-accent hover:text-accent-foreground` (peach hover automatic — DEV-A1 5.3); focus ring 3px @ 0.5 alpha; default `rounded-lg` = 12px. Size variants: xs/sm/default(h-8)/lg(h-9)/icon/icon-sm. **NÃO tem h-12 nativo** (Story 5.4 usou className override pattern para forms; Story 5.5 NÃO precisa h-12 — usa size="icon" para PaletteIcon header link (h-8 default) e size="icon-sm" para CopyIcon inline). `buttonVariants({ variant: 'ghost', size: 'icon' })` consumido em layout header (AC1).
- **Card**: `rounded-xl` (16) + `shadow-sm` warm-tinted plum + `ring-1 ring-foreground/10` (subtle border) + `bg-card text-card-foreground`. **Story 5.5 NÃO usa Card primitive diretamente em LinkRow (precisa `<li>` por SortableContext), mas REPLICA via classes Tailwind equivalentes (`rounded-xl border border-border bg-card shadow-sm ring-1 ring-foreground/5 hover:shadow-md`)** — AC4 DEV-B.
- **Input/Textarea**: `rounded-lg` (12) + h-8 default + `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50`. **Story 5.5 NÃO modifica Input inline edit LinkRow** (Story 2.6 shipped; inline edit consome Input default — sem refactor escolhido).
- **Switch**: peach ON state. **Story 5.5 usa via LinkRow visibility toggle (já shipped Story 2.6) — sem refactor; consome refreshed peach automatic via 5.3**.
- **DropdownMenu/Avatar**: refreshed Story 5.3 — consumido por UserMenu (AC10 no-op confirm).
- **AlertDialog**: rounded-2xl + shadow-lg — consumido por LinkRow delete confirm (Story 2.6 shipped; sem refactor).
- **Sheet**: rounded-r-xl (mobile drawer) — consumido por MobileDrawer (AC8 conditional add wordmark; sem refactor primitive).
- **FormMessage/Sonner**: irrelevantes (Story 5.5 não cria forms novos; AddLinkModal já existing).
- **Label**: irrelevante (não há label new inputs nesta story).

**Story 5.4 Done — landing + auth pages page-level refresh:**

- **Pattern wordmark** "biolink" lowercase + `text-h3` confirmado em `app/(auth)/layout.tsx` + `app/page.tsx` header. **Story 5.5 replica idêntico em `app/dashboard/layout.tsx` AC1** — consistência cross-surface (não-autenticada landing/auth + autenticada dashboard).
- **Pattern h-12 override** em forms (DEV-A1 5.4) — **Story 5.5 NÃO precisa h-12 escopo** (sem forms novos; AddLinkModal já shipped com defaults).
- **Pattern Card** consumido em landing "3 motivos" — Story 5.5 NÃO usa `<Card>` direto em LinkRow (precisa `<li>`), mas REPLICA classes equivalentes em LinkRow wrapper.
- **HomePage test rewrite** (DEV-E 5.4) — Story 5.5 pode precisar test rewrite similar se LinkRow tests dependerem de `<li>` structure rígida (esperado: tests usam `screen.getByRole`/`getByText`, não `<li>` queries diretas — verificar via grep antes de edit).

### Decisões & Deviations potenciais (a confirmar durante implementação)

- **DEV-A1 (anticipated, ratified via user elicitation):** Theme switch glyph `[⊙]` no header dashboard (spec §2.5 L569 + §0.5.3 L85). IA real tem persistência DB em /dashboard/theme (Story 3.3); não há runtime toggle. Decisão: implementar como **palette quick-link** (icon-only ghost Link → `/dashboard/theme`) usando `PaletteIcon` lucide-react. Custo: ~5 linhas no layout. NÃO runtime toggle (preserva ThemeSelector single-source-of-truth Story 3.3).
- **DEV-A2 (anticipated):** lucide-react `Palette` icon export. Esperado present em lucide-react v0.x (named export padrão `Palette`). Se ABSENT (improbable — verificar via `grep '^export.*Palette' node_modules/lucide-react/dist/esm/icons/`), usar `Sparkles` ou `Brush` como fallback. Pattern fallback: já materializado em Story 5.4 (DEV-A2-extra `GithubIcon` → `ExternalLinkIcon`). Documentar choice em Dev Agent Record.
- **DEV-B (anticipated, ratified via user elicitation):** LinkRow drag handle visibility — hover-only desktop / always mobile via CSS group-hover. Pattern: `md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100` no handle button + `group` na wrapper `<li>`. Preserva touch UX mobile (sempre visível para tap) + desktop limpo (revelado on row hover per spec L619) + a11y (keyboard via focus-visible reveals). DEV-A2 5.4 ratificou pattern responsive opacity em primitives — Story 5.5 replica.
- **DEV-C (anticipated):** Query username em DashboardPage. Shipped `app/dashboard/page.tsx` queries apenas `pages` (L26-30) para `pageId`. AC3 precisa de `username` para `<PublicUrlCopy>`. Adicionar query separada (`.from('profiles').select('username')...`) OU consolidar via `.select('username, pages(id)')` (embed). Recomendo query separada — embed traz array typed por PostgREST mesmo para 1:1 (já documentado em comentário shipped L23-25). Custo: +1 query (RLS-protected, ~5ms).
- **DEV-D (anticipated):** Sidebar tokens `--sidebar*` em globals.css (AC9). Esperado present desde Story 2.4 ou 5.2 (refresh). Validar: `grep "^\s*--sidebar" app/globals.css` deve retornar ≥6 lines per tema × 3 temas = 18+ matches. Se ABSENT: marcar AC9 como condicional `[STORY-5.5-F1]` LOW + DEFER para Story 5.6 (Profile + Theme + Account — natural fit). NÃO criar tokens novos nesta story (out-of-scope Story 5.2/5.6).
- **DEV-E (anticipated):** MobileDrawer wordmark add (AC8). Conditional task — se @dev julgar header global (AC1) cobre mobile branding via desktop sticky/mobile aware layout (header é always visible em mobile + desktop), SKIP MobileDrawer addition. Justificativa para SKIP: header global já tem wordmark em mobile (CSS `flex` não esconde wordmark em < md); SheetContent é overlay drawer, redundante adicionar wordmark dentro. Justificativa para ADD: spec §2.5 wireframe L569 mostra `[≡] BIOLINK` — `[≡]` é o trigger do sheet, "BIOLINK" pode ser interpretado como wordmark dentro do sheet content (alternativa visual). DEV escolhe + documenta.
- **DEV-F (anticipated):** AddLinkButton positioning (AC6, Task 2.5). Shipped `app/dashboard/page.tsx:55-57` tem AddLinkModal trigger no header da page (`<header><h1>...</h1>{links.length > 0 && <AddLinkModal trigger={<Button>Adicionar link</Button>} />}</header>`). AC6 + spec §2.5 L596-598 sugere AddLinkButton como bottom-of-list CTA (wireframe mostra após último Card no stack). Decisão: mover AddLinkButton para abaixo do LinkList (visível quando há ≥1 link); EmptyState contém o AddLinkButton internally quando 0 links. UX consistente: sempre visível mas posição muda conforme estado. Alternative: keep AddLinkModal trigger no header como duplicação (mas remove duplicidade visual desnecessária).
- **DEV-G (anticipated):** EmptyState soft blob SVG inline. Spec L605 sugere "ilustração soft (peach + lavender blob)". Story 5.5 ship com SVG inline 4-quadratic-curve blob + linear gradient. Designer-grade illustration upgrade fica fora de escopo (designer asset pipeline + future iteration). Pragmatic ship: SVG ~30 linhas inline, zero asset external, theme-aware (consome `var(--accent)` + `var(--primary)`).
- **DEV-H (anticipated):** Test rewrites. Esperado **0-N** test rewrites em LinkRow.test.tsx + EmptyState.test.tsx (if exists) + AddLinkButton.test.tsx (new). LinkRow test pode falhar se assert depender de `rounded-md` ou `border border-border` exato (mudou para `rounded-xl shadow-sm`). EmptyState test pode falhar se assert `Link2` icon (mudou para SVG inline). Pattern Story 5.4 (HomePage rewrite DEV-E) — se behavior tests rígidos, reescrever para `getByRole('listitem')` + count + `getByText` queries. Tests usam Testing Library best practices (queries por accessible name, não por CSS classes) — esperado **resilient a className changes**.

### Source Tree (arquivos relevantes a esta story)

**Modificáveis (target da implementação — 2 page-level + 3 components/links + 1 condicional components/dashboard + 2 components/{links,dashboard} NEW):**

```
app/dashboard/
├── layout.tsx                              # Task 1 — wordmark left + theme palette quick-link right
└── page.tsx                                # Task 2 — H1 typescale + URL copy inline + AddLinkButton positioning

components/links/
├── LinkRow.tsx                             # Task 5 — Card visual (rounded-xl + shadow-sm + ring-1) + drag handle group-hover desktop
├── LinkList.tsx                            # (no edit esperado — wrapper DnD context já correto)
├── EmptyState.tsx                          # Task 6 — soft blob SVG + larger CTA + AddLinkButton consumed
├── AddLinkModal.tsx                        # (no edit esperado — trigger prop aceita ReactElement)
└── AddLinkButton.tsx                       # NEW Task 3 — dashed border accent peach hover wrapper for AddLinkModal trigger

components/dashboard/
├── Sidebar.tsx                             # Task 7.1 — audit no-op esperado (sidebar-* tokens shipped)
├── MobileDrawer.tsx                        # Task 7.2 — conditional add wordmark inside Sheet
├── UserMenu.tsx                            # Task 7.3 — audit no-op esperado (DropdownMenu refreshed via 5.3)
├── nav-items.ts                            # (no edit — navItems source-of-truth shipped Story 2.4)
└── PublicUrlCopy.tsx                       # NEW Task 4 — Client Component for URL + copy button inline
```

**Snapshot files (potencialmente afetados — review obrigatório se existirem):**

```
tests/components/features/links/__snapshots__/                 # (inexistente esperado — confirmar via ls)
tests/components/features/dashboard/__snapshots__/             # (inexistente esperado — confirmar via ls)
```

**Apenas leitura (referência durante implementação):**

```
app/globals.css                                                    # Tokens canônicos pós-5.2 + status tokens pós-5.3
components/ui/{button,card,input,switch,dropdown-menu,sheet,alert-dialog}.tsx   # Primitives refreshed pós-5.3 (consumidos)
docs/frontend-spec.md (§2.5 L565-621, §3.1 L932-949, §3.2 L951-987, §3.3 L989-1006, §3.5 L1019-1034, §0.5.3 L82-87, §1.3.2 typescale, §1.4.2 radius) # Source of truth
docs/stories/5.4.landing-auth-soft-studio.story.md                 # Insights anteriores (wordmark + Card composition)
docs/stories/5.3.primitives-audit-13-shadcn.story.md               # Insights primitives consumed
docs/qa/gates/5.4-landing-auth-soft-studio.yml                     # Gate 5.4 PASS — confirma primitives + tokens ready
docs/STORY-BACKLOG.md                                              # Entrada [EPIC-5-S5] (housekeeping Task 10.1)
```

**NÃO tocar:**

- `app/globals.css` (Story 5.2 + Story 5.3 escopo — esta story apenas consome tokens em pages/components)
- `components/ui/*` (Story 5.3 audit fechado — esta story consome primitives via composition/className override)
- `app/layout.tsx` (root layout — Story 5.2 escopo)
- `app/(auth)/**` (Story 5.4 escopo)
- `app/page.tsx` (Story 5.4 escopo)
- `app/[username]/**` (Story 5.7 escopo)
- `app/dashboard/{profile,theme,analytics,account}/` (Stories 5.6/5.7 escopo)
- `components/links/LinkList.tsx` (Story 2.6 DnD container intact — sem refactor escolhido)
- `components/links/AddLinkModal.tsx` (Story 2.5 form modal intact — apenas trigger consumer muda via AddLinkButton wrap)
- `components/dashboard/nav-items.ts` (Story 2.4 source-of-truth shipped — sem refactor)
- `components/profile/`, `components/public/`, `components/theme/` (Stories 5.6/5.7 escopo)
- `lib/**` (lógica de negócio — Story 5.5 é apenas refresh visual + 1 query username adicional em RSC)
- `server/**` (Server Actions — Story 5.5 não toca em data layer; createLink/updateLink/toggleLinkVisibility/reorderLinks/deleteLink shipped Story 2.5/2.6 intactos)
- `scripts/check-contrast.mjs` ([STORY-3.2-F1] LOW)
- `tests/integration/**` (RLS tests + Server Action tests — não afetam UI refresh)

### Component Specifications

Já documentadas exaustivamente em **Acceptance Criteria 1-15** (1 AC por elemento estrutural ou tipo de mudança) + **Tasks 1-7** (1 task por arquivo modificado ou criado). Não repetir aqui — fonte canônica é a AC + Task combinada.

`[Source: docs/frontend-spec.md §2.5 L565-621, §3.1 L932-949, §3.2 L951-987, §3.3 L989-1006, §3.5 L1019-1034, §0.5.3 L82-87, §1.3.2 typescale, §1.4.2 radius]`

### API Specification

N/A — Story frontend-only, zero mudança de API/Server Actions. DashboardPage adiciona 1 query a `profiles` (RLS-protected via Story 1.4 schema; `select('username')` em pageId-equivalent query). Server Actions `createLink`/`updateLink`/`toggleLinkVisibility`/`reorderLinks`/`deleteLink` (Story 2.5/2.6) consumidas via LinkRow/AddLinkModal sem mudança.

### File Locations

Files modificados estão em `app/dashboard/` (page-level routes) e `components/{links,dashboard}/` (Server + Client Components). Convenção Next 16 App Router + Tailwind v4 + shadcn 4.7.0 + @base-ui/react v1.4.1 (consumed via primitives Story 5.3) + lucide-react (icons consumed: Palette, Plus, Copy NEW; GripVertical, ChevronUp/Down, Pencil, Trash2, Menu, LogOut shipped). Path alias: `@/*` → `./` (tsconfig.json).

### Testing Requirements

`[Source: tests/components/features/, package.json scripts]`

**Framework:** Vitest 3 + @testing-library/react 16 + jsdom. Script `pnpm test:components` = `vitest run --project=components` (confirmado package.json L17).

**Baseline obrigatória:** 194/194 PASS (mantida desde Story 5.1 + confirmada em 5.2/5.3/5.4 Dev Agent Records).

**Tipos de teste relevantes:**

- **dashboard/Sidebar.test.tsx** — renders nav items + isActive logic. **NÃO esperado quebrar** (Story 5.5 não modifica Sidebar.tsx — apenas audit no-op).
- **dashboard/MobileDrawer.test.tsx** — toggle drawer + nav items + onClick close. **NÃO esperado quebrar** se Task 7.2 SKIPPED (no MobileDrawer edit); pode precisar atualizar se Task 7.2 ADD wordmark inside Sheet (adiciona Link element to query results).
- **dashboard/UserMenu.test.tsx** — dropdown items + clipboard handler + signOut handler. **NÃO esperado quebrar** (Story 5.5 não modifica UserMenu.tsx — apenas audit no-op).
- **links/LinkList.test.tsx** — dnd-kit reorder + onMove ↑/↓ + persistence. **NÃO esperado quebrar** (Story 5.5 não modifica LinkList.tsx).
- **links/LinkRow.test.tsx** — edit title/URL + toggle visibility + delete dialog. **Pode precisar update** se asserts dependem de `rounded-md` exato ou `border border-border` no className (mudou para `rounded-xl shadow-sm ring-1 hover:shadow-md`). Behavior tests (edit/toggle/delete) **NÃO devem quebrar** (lógica preserved per AC11).
- **links/AddLinkModal.test.tsx** — open modal + submit form + validation errors. **NÃO esperado quebrar** (Story 5.5 não modifica AddLinkModal.tsx; trigger prop consumer muda mas modal interna intact).
- **(NEW) links/AddLinkButton.test.tsx** — opcional novo test em Task 3 (verificar render + click opens modal). Não obrigatório se cobertura indireta via consumers (DashboardPage + EmptyState).
- **(NEW) dashboard/PublicUrlCopy.test.tsx** — opcional novo test em Task 4 (verificar render URL + click fires clipboard.writeText + toast). Não obrigatório se cobertura indireta via DashboardPage render.
- **EmptyState** — pode não ter test file shipped; se existir e usar `Link2` icon assertion, atualizar para SVG presence assertion ou `getByRole('img', { hidden: true })`.

**Behavior tests** (não-snapshot) — testam render condicional, event handlers, ARIA. Estes **NÃO devem quebrar** por mudanças visuais. Se quebrar → investigar regressão.

**Smoke runtime manual:** condicional (Task 9), perguntar antes per MEMORY `feedback_skip_manual_smoke.md`.

### Technical Constraints

- **Zero token novo** em `globals.css` (Story 5.2 + 5.3 fecharam token surface). AC9 valida `--sidebar*` tokens; se absent, abre F1 LOW + DEFER para Story 5.6.
- **Zero mudança em primitives** (`components/ui/*` — Story 5.3 audit fechado; usar composition + className override pattern em consumers).
- **Zero mudança de API pública** — Server Actions Story 2.5/2.6 (createLink/updateLink/etc.) consumidas intactas. DashboardPage adiciona 1 query a `profiles` (RLS-protected).
- **Zero nova dependência** — todos os imports lucide-react já estão no bundle (Copy + GripVertical em LinkRow shipped; Palette + Plus podem ser novos mas tree-shakeable ~1 KB cada). Card/Button/Switch/DropdownMenu primitives já no bundle pós-5.3.
- **Constitution Art. IV (No Invention):** todas as mudanças derivam de `frontend-spec.md` §2.5 + §3.1-3.5 + §0.5.3 + §1.3.2 + §1.4.2 verbatim. Qualquer choice arquitetural (DEV-A1..H) deve ser documentada no Dev Agent Record.
- **Constitution Art. III (Story-Driven):** mudanças fora dos 2 page-level + 4 components/links + 3 components/dashboard + 2 NEW components + `STORY-BACKLOG.md` (housekeeping) requerem amend desta story.
- **Backward compat:** consumers existentes de DashboardLayout (4 sub-pages: profile/theme/analytics/account + Links page index) continuam funcionando — props inchanged. LinkRow API inchanged (props inchanged; only className update). EmptyState API inchanged. UserMenu props inchanged.
- **a11y WCAG 2.1 AA** mantida (§4) — focus rings preservados via Story 5.3 primitives, drag handle keyboard accessible via `md:focus-visible:opacity-100` (AC5), soft blob SVG `aria-hidden="true"` (AC7), AddLinkButton focus-visible:ring-2 (AC6), PaletteIcon Link `aria-label="Mudar tema"` (AC1), CopyIcon button `aria-label="Copiar URL pública"` (AC3).
- **Performance:** mudanças são layout/className/copy + 2 new small Client Components (PublicUrlCopy ~30 lines, AddLinkButton ~15 lines). Bundle delta esperado **neutral** (~1-2 KB max para 2-3 ícones lucide novos Palette + Plus + Copy se Copy não estava em bundle — Copy já em UserMenu shipped). LCP `/` mantido per NFR1 (`<` 2.0s baseline pós-5.4). `/dashboard` não tem Lighthouse gate (authenticated), mas paths trigger `app/**` + `components/**` em CI workflow.
- **Drag handle responsive opacity** (AC5 DEV-B) deve preservar a11y: keyboard users via Tab → handle revelado via `focus-visible` (não apenas hover). Validar via Lighthouse a11y no PR + opcional smoke teclado (Task 9).
- **Theme palette quick-link** (AC1 DEV-A1) é `<Link>` Next (não `<a>`) — Next routing client-side sem full page reload. Custo navigation: ~50ms (cached route shell pós primeira visita /dashboard/theme).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Author        |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------- |
| 2026-06-01 | 0.1     | Story draft criada pela `*draft 5.5` do @sm (River). Cobertura: **15 ACs** (10 visuais dashboard layout + Links page + LinkRow + EmptyState + AddLinkButton + chrome audit + 5 behavior preservation + gates locais/CI + housekeeping); **10 Tasks** (1 layout + 1 page + 1 AddLinkButton NEW + 1 PublicUrlCopy NEW + 1 LinkRow + 1 EmptyState + 1 chrome audit (3 sub-tasks) + 1 quality gates + 1 smoke condicional + 1 housekeeping); **Dev Notes** completos com tokens 5.2 + primitives 5.3 + insights 5.4 + tabela arquivos + **DEV-A1..H antecipadas** (theme palette quick-link via user elicitation; Palette lucide fallback; drag handle group-hover responsive via user elicitation; username query separada; sidebar tokens audit conditional F1 LOW; MobileDrawer wordmark conditional add; AddLinkButton positioning bottom-of-list; soft blob SVG inline pragmatic; test rewrites antecipados); Source-of-truth: `frontend-spec.md` §2.5 L565-621 (Dashboard/Links wireframe + states + mobile/desktop specs) + §3.1 L932-949 (drag-drop preserved) + §3.2 L951-987 (inline edit preserved) + §3.3 L989-1006 (toggle visibility preserved) + §3.5 L1019-1034 (copy-to-clipboard pattern) + §0.5.3 L82-87 (IA dashboard header global) + §1.3.2 typescale + §1.4.2 radius + insights Stories 5.2/5.3/5.4 Done. Status: Draft. Próximo: `@po *validate-story-draft 5.5` antes do handoff para `@dev *develop`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | River (sm)    |
| 2026-06-01 | 0.3     | **`@dev *develop 5.5` → 10 Tasks executadas (5 modified + 2 NEW + 1 housekeeping + 3 audited no-op).** Mode Interactive (1 elicitation Task 9 smoke → user SKIP per MEMORY `feedback_skip_manual_smoke`). Gates: typecheck/lint/build (15/15 static pages, 16 routes) PASS; test:components 194/194 PASS (1 test source file atualizado em `LinkList.test.tsx` describe `<EmptyState>` per DEV-H — regex + button name reconciliado, NÃO snapshot). DEV decisions ratificadas: A1 (palette quick-link Link, não runtime toggle), A2 (Palette lucide PRESENT, fallback não materializou), B (drag handle group-hover responsive opacity), C (query profiles.username separada), D (sidebar-* tokens NO-OP confirmed L72-79/L132-139/L192-199), E (MobileDrawer wordmark SKIPPED — header global cobre mobile), F (AddLinkButton bottom-of-list positioning), G (soft blob SVG inline pragmatic), H (LinkList.test.tsx EmptyState describe atualizado). `[STORY-5.5-F1]` LOW (sidebar tokens) NÃO materializa. Smoke manual SKIPPED (cobertura indireta + Lighthouse CI PR). Status: `InProgress → Ready for Review`. Próximo: `@qa *qa-gate 5.5`(workaround manual QA-001 macOS via`docs/qa/known-issues.md`) ou `@devops \*push` se gate manual aprovado.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Dex (dev)     |
| 2026-06-01 | 0.5     | **Story 5.5 MERGED em main via PR #34 (squash commit `fc20749`).** CI 12/12 PASS (lighthouse 2m02s + test-integration 3m51s + test-components 194/194 + build + audit + gitleaks + lint + typecheck + test-unit + install + Vercel). Status `Ready for Review → Done`. Observations resolvidas: REL-001 LOW (Lighthouse bundle delta neutral confirmado em `/` + `/[username]`); RISK-SMOKE-SKIP LOW (cobertura indireta validada via 194 behavior tests + Vercel preview deploy). Phase 3 Epic 5 continua destravada — Stories 5.6/5.7 paralelizáveis. Pattern Stories 5.2/5.3/5.4: feat squash mergeado → chore close PR posterior. Branch `feat/story-5.5-dashboard-soft-studio` deletada via `--delete-branch`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Gage (devops) |
| 2026-06-01 | 0.4     | **`@qa *qa-gate 5.5` → PASS.** Gate manual via workaround QA-001 (CodeRabbit/WSL incompat. macOS — `mcp.enabled: false` no `core-config.yaml`; pattern Stories 1.4-5.4 todas mergeadas via gate manual). 15/15 ACs cobertas (AC8 SKIP ratificado per DEV-E; AC9/AC10 audit no-op confirmados; AC13 Lighthouse validável no PR). Gates re-verificados pelo QA: typecheck/lint exit 0 zero warnings; build/test:components Dev Agent Record (deterministic — código inalterado). Constitution: Art. IV (No Invention) ✓ — cada AC cita §section verbatim de `frontend-spec.md`; Art. III (Story-Driven) ✓ — File List bate gitStatus inicial; Art. II (Agent Authority) ✓ — push delegado @devops. 2 LOW observations: REL-001 (Lighthouse `/dashboard` fora gate paths default — validável via bundle-share no PR) + RISK-SMOKE-SKIP (smoke runtime manual SKIPPED per MEMORY `feedback_skip_manual_smoke`, cobertura indireta). Gate file: `docs/qa/gates/5.5-dashboard-core-soft-studio.yml`. Próximo: `@devops *push`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Quinn (qa)    |
| 2026-06-01 | 0.2     | **`@po *validate-story-draft 5.5` → GO incondicional.** **10-point validation: 10/10 PASS.** **Implementation Readiness Score: 9.5/10.** **Confidence: HIGH.** Cross-validation técnica (anti-hallucination Section 9): (a) `Palette` icon CONFIRMADO present em lucide-react (`index_Palette as Palette` named export — DEV-A2 fallback Sparkles/Brush não precisará materializar); (b) `--sidebar*` tokens CONFIRMADOS present em `app/globals.css` light L72-79 / dark L132-139 / brand L192-199 — **AC9 condicional `[STORY-5.5-F1]` LOW NÃO materializará** (DEV-D anticipated resolves to no-op confirmed); (c) Typescale tokens `--text-display/h1/h2/h3` confirmados L230-237 + radius `--radius-xl: 1rem` (=16px) L43/L108/L167 + shadows `--shadow-sm/md` L211-219 + colors `--primary/--accent` cross-themes; (d) `Switch` primitive confirmado (`SwitchPrimitive.Root`/`Thumb` em `components/ui/switch.tsx:15,24` — Story 5.3 peach ON state ratificado); (e) `size="icon-sm"` variant confirmado em `components/ui/button.tsx:31` — AC3 PublicUrlCopy pode consumir sem fallback; (f) Spec line refs L569 (header) / L572-573 (H1+URL) / L596-598 (AddLinkButton dashed) / L605 (Empty soft blob) / L612 (mobile handle visible) / L619 (desktop handle hover) / L82-87 (IA header global) / L241-244 (typescale) — TODOS verificados accurate verbatim. **Executor Assignment** (Section 1.1 validate-next-story.md): `executor: @dev` + `quality_gate: @qa` + `quality_gate_tools: [typecheck, lint, build, test:components]` — convenção do projeto (Stories 5.2/5.3/5.4 todas ratificadas GO+PASS com mesmo pattern). **Should-Fix minor (non-blocker):** SF-1 — AC9 wording menciona `[STORY-5.5-F1] LOW` conditional para sidebar tokens absent, mas tokens estão present pre-impl. Task 7.1 audit confirmará no-op direto sem abrir F1 — texto pode ficar como está (defensive). SF-2 — template default L249 lista `quality_gate ∈ [@architect, @dev, @pm]`; story usa `@qa` matching project convention ratified Stories 5.2-5.4 (não-blocker; override documentado pelo padrão histórico). **Nice-to-Have:** NH-1 PublicUrlCopy `font-mono text-sm` mantém semântica code/URL (alternativa `text-body-sm` válida também); NH-2 SoftBlob SVG `aria-hidden="true"` is canonical pattern para decorative; NH-3 Lighthouse `/dashboard` fora dos gate paths default — risco residual aceito + mitigado via behavior tests Task 8.3 + smoke condicional Task 9. **CodeRabbit Integration** (Section 8): `mcp.enabled: false` em core-config.yaml — skip notice renderizado corretamente (mesmo pattern Stories 5.2-5.4 Done). **Status transition: `Draft → Ready`** per `.claude/rules/story-lifecycle.md` (responsabilidade @po post-GO). Próximo: `@dev *develop 5.5` (modo Interactive recomendado dada complexity M com 2 components NEW + 1 query Supabase nova em RSC + drag handle responsive CSS + EmptyState SVG inline). | Pax (po)      |

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (Dex / @dev) — mode Interactive (1 elicitation @ Task 9 smoke per MEMORY `feedback_skip_manual_smoke.md`).

### Debug Log References

- `pnpm typecheck` — exit 0 (rodado 2× — após Task 1 layout edit + após Task 4 PublicUrlCopy / page edit).
- `pnpm lint` — exit 0 zero warnings.
- `pnpm build` — exit 0; Next.js 16.2.5 Turbopack; Compiled successfully in 111s; TypeScript 2.6min; 15/15 static pages generated; route map intacto (16 routes — `/`, `/login`, `/signup`, `/reset-password`, `/dashboard` dynamic + 4 sub-pages, `/[username]`, `/dev/themes`, `/auth/callback`, 2 API routes, `_not-found`).
- `pnpm test:components` — exit 0; **34 test files passed; 194/194 tests passed**; duration 553.74s (macOS slow per known baseline). 1 test atualizado em `tests/components/features/links/LinkList.test.tsx` linha 64-68 (EmptyState describe block — copy "Adicione seu primeiro link" via regex + button name "Adicionar link" reconciliado per DEV-H após reescrita do EmptyState).
- Nenhum snapshot atualizado (`find tests/components/features/{links,dashboard} -name "*.snap"` → empty; behavior tests usam queries semânticas).

### Completion Notes

**AC1-AC15 mapping → implementation:**

- **AC1 Dashboard layout header** ✅ — `app/dashboard/layout.tsx`: `+ Palette` import lucide-react / `+ Link` next / `+ buttonVariants` Button; wordmark `<Link href="/dashboard" className="text-h3 lowercase">biolink</Link>` entre MobileDrawer e ml-auto; Palette ghost link icon antes do UserMenu (sequência: wordmark → MobileDrawer → ml-auto → PaletteIcon → UserMenu); `gap-2` → `gap-3` no header container.
- **AC2 Links page H1** ✅ — `app/dashboard/page.tsx:55`: `text-xl font-semibold tracking-tight` "Seus links" → `text-h1` "Meus links" (spec §2.5 L572 verbatim).
- **AC3 PublicUrlCopy inline** ✅ — NEW `components/dashboard/PublicUrlCopy.tsx` Client Component (Copy lucide + Button ghost size="icon-sm" + toast.success/error); query `username` em `profiles` adicionada ao DashboardPage server component; fallback `${siteUrl}/@` quando username falsy + handler short-circuit `if (!username) return` previne TypeError.
- **AC4 LinkRow Card visual** ✅ — `components/links/LinkRow.tsx:139`: `rounded-md border border-border bg-card p-3` → `group flex items-center gap-3 rounded-xl border border-border bg-card p-4 shadow-sm ring-1 ring-foreground/5 transition-shadow hover:shadow-md`. DEV-B confirmado: mantido `<li>` (SortableContext + setNodeRef ref forwarding); Card primitive class equivalents via Tailwind.
- **AC5 LinkRow drag handle** ✅ — `components/links/LinkRow.tsx:147`: `+ transition-opacity md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100` (mobile sempre visible touch UX; desktop hidden default + revealed on row hover OU handle focus a11y). `aria-label="Reordenar link"` + attributes/listeners dnd-kit + ref preservados.
- **AC6 AddLinkButton dashed** ✅ — NEW `components/links/AddLinkButton.tsx` Client Component; `<button>` raw (não Button primitive) com `border-2 border-dashed border-border` + `hover:border-accent hover:bg-accent/10 hover:text-accent-foreground` peach; `Plus` lucide ⊕ + "Adicionar link" copy; rounded-xl 16; focus-visible:ring-2 ring-ring ring-offset-2.
- **AC7 EmptyState soft blob** ✅ — `components/links/EmptyState.tsx` reescrito: `<Link2>` lucide removido, substituído por SVG inline `viewBox="0 0 200 200"` + `<linearGradient>` accent→primary 30% opacity + path 4-quadratic-curve blob + overall opacity 0.6; H1 "Nenhum link ainda" → H2 `text-h2 font-medium` "Adicione seu primeiro link →"; helper text `text-body max-w-md`; CTA Button → `<AddLinkButton />` (DRY); wrapper `rounded-md p-6 gap-4` → `rounded-xl p-12 gap-6` per spec L605 "BUTTON CTA grande".
- **AC8 MobileDrawer wordmark** ⏭️ — SKIPPED per DEV-E (header global cobre mobile branding; wordmark text-h3 visível em mobile sem `md:hidden`; adicionar wordmark dentro do Sheet seria redundância visual).
- **AC9 Sidebar tokens audit** ✅ — `grep '^\s*--sidebar' app/globals.css` confirma 6 tokens × 3 temas = 18 matches (L72-79 :root, L132-139 .dark, L192-199 .brand + L390+ media query dark). Sidebar.tsx consome `bg-sidebar/border-sidebar-border/text-sidebar-foreground/bg-sidebar-accent/text-sidebar-accent-foreground/ring-sidebar-ring` — NO-OP confirmado; `[STORY-5.5-F1]` LOW NÃO materializa.
- **AC10 UserMenu audit** ✅ — UserMenu.tsx sem typescale legacy hardcoded; consome DropdownMenuItem refreshed Story 5.3 + Avatar; `handleCopyUrl` linhas 37-44 preservadas (AC14 defense-in-depth UX). NO-OP confirmado.
- **AC11 dnd-kit + inline edit + toggle preserved** ✅ — `tests/components/features/links/LinkList.test.tsx` + `LinkRow.test.tsx` PASS (8/8 + tests behavior). LinkRow useSortable + commitTitle/commitUrl + onToggleVisibility + onConfirmDelete + ChevronUp/Down + Switch + AlertDialog intactos.
- **AC12 suíte qualidade local** ✅ — typecheck/lint/build (15/15) PASS; test:components 194/194 PASS; 1 test source file atualizado (não snapshot) em LinkList.test.tsx per DEV-H anticipated.
- **AC13 Lighthouse CI** — Verificação no PR. `/dashboard` é authenticated (fora gate paths `/` + `/[username]`); mudanças tocam `app/dashboard/**` + `components/{links,dashboard}/**` + bundle compartilhado via Card/Button primitives. Delta esperado neutral (~1-2 KB para Palette + Plus lucide tree-shakeable; Copy já em bundle via UserMenu shipped).
- **AC14 UserMenu copy URL no regression** ✅ — UserMenu `handleCopyUrl` preservado (defense-in-depth UX). PublicUrlCopy inline adicionado coexiste com UserMenu dropdown copy. `tests/components/features/dashboard/UserMenu.test.tsx` PASS.
- **AC15 Constitution Art. IV + housekeeping** ✅ — `docs/STORY-BACKLOG.md` `[EPIC-5-S5]` removido + Change Log DONE entry adicionado + statistics 14/5 → 13/4 MEDIUM e DONE 4 → 5 + narrative Epic 5 atualizado + `last_updated` 2026-06-01.

**DEV decisions ratificadas durante implementação:**

- **DEV-A1** Palette quick-link materializado como icon-only ghost `<Link>` (não runtime toggle) — preserva ThemeSelector Story 3.3 single-source-of-truth DB.
- **DEV-A2** `Palette` lucide-react named export PRESENT (confirmado pelo PO validate Section 9 cross-check Story 5.5 v0.2); fallback Sparkles/Brush não materializou.
- **DEV-B** LinkRow drag handle group-hover responsive — `md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100` + `group` class no `<li>` wrapper.
- **DEV-C** Query `profiles.username` separada em DashboardPage (não embed); custo +1 query RLS-protected ~5ms.
- **DEV-D** Sidebar-\* tokens audit no-op (validate-po Section 9 cross-check confirmou present L72-79/L132-139/L192-199).
- **DEV-E** MobileDrawer wordmark SKIPPED (header global cobre mobile branding; redundância evitada).
- **DEV-F** AddLinkButton positioning: removido do header DashboardPage, posicionado bottom-of-list (`{links.length > 0 && <AddLinkButton />}` após LinkList); EmptyState contém AddLinkButton internally per AC7.
- **DEV-G** EmptyState soft blob SVG inline pragmatic ship; designer-grade asset deferido para iteração futura.
- **DEV-H** Test rewrite em `LinkList.test.tsx` describe `<EmptyState>` — getByText "Nenhum link ainda" → regex /Adicione seu primeiro link/ + button name "Adicionar link" (reconciliado com novo H2 + AddLinkButton wrapping AddLinkModal).

### File List

**Modificados (5):**

- `app/dashboard/layout.tsx` — wordmark biolink left + Palette ghost link `/dashboard/theme` right (AC1).
- `app/dashboard/page.tsx` — H1 `text-h1` "Meus links" + `<PublicUrlCopy>` subtitle + `<AddLinkButton>` bottom-of-list + query `profiles.username` (AC2, AC3, AC6, DEV-C, DEV-F).
- `components/links/LinkRow.tsx` — Card visual `rounded-xl shadow-sm ring-1 ring-foreground/5 hover:shadow-md` + drag handle `group-hover` responsive opacity (AC4, AC5).
- `components/links/EmptyState.tsx` — SVG inline soft blob gradient peach→plum + H2 CTA + AddLinkButton consumed + wrapper `rounded-xl p-12` (AC6, AC7).
- `tests/components/features/links/LinkList.test.tsx` — describe `<EmptyState>` test atualizado per DEV-H (regex + button name "Adicionar link").

**Criados (2):**

- `components/links/AddLinkButton.tsx` — NEW Client Component dashed border accent peach hover wrapper para AddLinkModal trigger (AC6).
- `components/dashboard/PublicUrlCopy.tsx` — NEW Client Component inline URL pública + ghost icon-sm copy button + clipboard handler + toast (AC3).

**Atualizados (1) — housekeeping:**

- `docs/STORY-BACKLOG.md` — `[EPIC-5-S5]` removido de MEDIUM + Change Log DONE entry + statistics 14/5 → 13/4 + narrative Epic 5 incluindo 5.5 DONE + `last_updated` 2026-06-01 (AC15, Task 10.1).

**Auditados sem mudança (3):**

- `components/dashboard/Sidebar.tsx` — NO-OP (`bg-sidebar/*` tokens consumed + globals.css confirma `--sidebar*` em 3 temas, AC9).
- `components/dashboard/MobileDrawer.tsx` — NO-OP (AC8 wordmark add SKIPPED per DEV-E).
- `components/dashboard/UserMenu.tsx` — NO-OP (sem typescale legacy; primitives refreshed Story 5.3, AC10/AC14).

## QA Results

### Review Date: 2026-06-01

### Reviewed By: Quinn (Test Architect)

**Modo:** QA gate manual (workaround QA-001 macOS — CodeRabbit/WSL incompatível; pattern Stories 1.4-5.4 todas mergeadas via gate manual).

**Evidence Re-Verification (durante QA gate):**

- `pnpm typecheck` → exit 0 ✅
- `pnpm lint` → exit 0 zero warnings ✅
- `pnpm build` / `pnpm test:components` → trust Dev Agent Record (deterministic, código inalterado desde rodada do @dev): 15/15 static pages + 194/194 tests PASS ✅

**AC Coverage:** 15/15 (AC8 skip ratificado per DEV-E; AC9/AC10 audit no-op confirmados; AC13 Lighthouse validável no PR).

**Code Review Findings:**

- `app/dashboard/layout.tsx` ✅ — wordmark `<Link href="/dashboard" className="text-h3 lowercase">biolink</Link>` + Palette ghost link `/dashboard/theme` com `aria-label="Mudar tema"` corretamente posicionado antes do UserMenu; `gap-3` consistente.
- `app/dashboard/page.tsx` ✅ — H1 `text-h1` "Meus links" verbatim spec §2.5 L572; `<PublicUrlCopy>` subtitle; `<AddLinkButton />` bottom-of-list conditional `{links.length > 0}` evita duplicação com EmptyState que já inclui AddLinkButton.
- `components/dashboard/PublicUrlCopy.tsx` ✅ — Client Component com fallback `${siteUrl}/@` (username falsy) + short-circuit `if (!username) return` no handler previne TypeError + `disabled={!username}` na Button (defense-in-depth). Toast success/error patterns idênticos a UserMenu (consistência).
- `components/links/AddLinkButton.tsx` ✅ — `<button>` raw (não Button primitive) intencional por causa do `border-2 border-dashed` style spec; focus-visible:ring-2 ring-ring ring-offset-2 a11y preservado.
- `components/links/LinkRow.tsx:139` ✅ — `group flex items-center gap-3 rounded-xl border border-border bg-card p-4 shadow-sm ring-1 ring-foreground/5 transition-shadow hover:shadow-md` matches AC4 verbatim; `<li>` preservado (SortableContext + setNodeRef necessidade, DEV-B).
- `components/links/LinkRow.tsx:147` ✅ — drag handle `md:opacity-0 md:group-hover:opacity-100 md:focus-visible:opacity-100` a11y preservada (keyboard users via Tab → handle revelado via focus-visible mesmo sem hover).
- `components/links/EmptyState.tsx` ✅ — SVG inline `viewBox="0 0 200 200"` + `<linearGradient>` accent→primary 30% opacity + path 4-quadratic-curve blob + `aria-hidden="true"` decorative; H2 CTA "Adicione seu primeiro link →" + helper text + AddLinkButton consumed (DRY com AC6).
- `tests/components/features/links/LinkList.test.tsx:63-70` ✅ — describe `<EmptyState>` atualizado para regex `/Adicione seu primeiro link/` + button name `/Adicionar link/` (DEV-H ratified, não snapshot).

**Constitution Compliance:**

- **Art. IV (No Invention):** Cada AC1-AC10 cita §section verbatim de `docs/frontend-spec.md`. AC11-AC14 são preservação behavior. AC15 housekeeping. Zero invenção. ✅
- **Art. III (Story-Driven):** File List bate exatamente com gitStatus inicial (5 mod + 2 NEW + 1 backlog). Zero out-of-scope. ✅
- **Art. II (Agent Authority):** Push/PR delegados a @devops (próximo step). ✅

**Observações:**

- **LOW · REL-001** — Lighthouse CI threshold ≥0.85 gated em `/` + `/[username]`; `/dashboard` é authenticated route fora dos gate paths. Bundle compartilhado via Card/Button/Switch primitives → run dispara no PR via paths trigger `app/**` + `components/**`. Esperado neutral (~1-2 KB para Palette+Plus lucide tree-shakeable; Copy já em bundle via UserMenu shipped). **Ação:** validar no PR; queda > 5 pontos exige investigação.
- **LOW · RISK-SMOKE-SKIP** — Smoke runtime manual (×3 temas /dashboard) SKIPPED per MEMORY `feedback_skip_manual_smoke`. Cobertura indireta via 194 behavior tests + Lighthouse CI no PR. Risco residual baixo (frontend-primary, zero mudança em Server Actions/Schema/RLS).

**No-regression checks:**

- ✅ UserMenu `handleCopyUrl` preservado (defense-in-depth UX vs novo PublicUrlCopy inline) — AC14.
- ✅ LinkRow useSortable + commitTitle/commitUrl + onToggleVisibility + onConfirmDelete + ChevronUp/Down + Switch + AlertDialog intactos — AC11.
- ✅ Sidebar.tsx / MobileDrawer.tsx / UserMenu.tsx audit NO-OP — sem regressão chrome dashboard — AC8/AC9/AC10.
- ✅ Server Actions Story 2.5/2.6/5.1 não tocadas — backward compat.

### Gate Status

Gate: PASS → docs/qa/gates/5.5-dashboard-core-soft-studio.yml

**Próximo:** `@devops *push` (recomendado). Lighthouse CI no PR validará LOW observation REL-001.

— Quinn, guardião da qualidade 🛡️
