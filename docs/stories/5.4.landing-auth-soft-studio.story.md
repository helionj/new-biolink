# Story 5.4: Landing + Auth Pages — Soft Studio Refresh

## Status

Ready for Review

## Executor Assignment

```yaml
executor: '@dev' # Tasks 1-9 (audit page-level Landing + 5 auth pages: layout/copy/typescale/CTA structure consumindo primitives refreshed Story 5.3 + tokens Story 5.2)
quality_gate: '@qa'
quality_gate_tools: [typecheck, lint, build, test:components]
complexity: 'M' # Frontend-primary. Superfície: 6 arquivos page-level (`app/page.tsx` + `app/(auth)/layout.tsx` + 4 `app/(auth)/*/page.tsx`) + possivelmente 0-N edits em `components/auth/*Form.tsx` (Login/Signup/Reset — apenas se spec §2.2-2.4 requerer mudança que não dá pra fazer só na page wrapper) + 0-N snapshot updates em `tests/components/features/landing/__snapshots__/` + `tests/components/features/auth/__snapshots__/` (esperados pelo backlog `acceptance line`). Zero migration, zero RLS, zero schema, zero Server Action novo. Tokens/primitives já em `app/globals.css` + `components/ui/` pós-5.2 + pós-5.3 (esta story consome). Mudanças localizadas: layout (header sticky + footer + grid 3 cards), copy verbatim per spec (H1 + body + benefits), typescale class swap (`text-2xl` → `text-h1`; `text-4xl` → `text-display`), CTA height override (`h-12` per spec §4.7 via className override no consumidor — NÃO mexer em Button primitive — DEV-A1 anticipated), 3 motivos bullets → 3 Cards componentes, secondary CTA ghost link "Já tem conta? Entrar" no landing. Risco material isolado: (a) HomePage.test.tsx snapshot atualizado por mudança estrutural significativa (3 cards no lugar de ul) — esperado por backlog `[EPIC-5-S4]` acceptance; (b) Lighthouse CI baseline `/` (~180-185 KB gz pós-5.2) pode mover ±2-3 KB por 3 Cards extras + novo header sticky (provavelmente neutral pois primitives já estão no bundle); (c) auth form tests (LoginForm/SignupForm/ResetPasswordRequestForm/ResetPasswordConfirmForm) podem precisar update se h-12 className for adicionado ao Input usage — esperado e aceito; (d) regressões visuais não cobertas por tests (header transparent on scroll-top behavior; sticky on scroll; ghost button hover peach autoconsumido per 5.3) → mitigadas por smoke seletivo se @qa solicitar.
```

> **Rationale:** Story **Frontend-primary** — primeira das 4 stories page-level paralelizáveis (5.4-5.7) do refresh Soft Studio. Materializa **Phase 3** de `docs/frontend-spec.md` §5.3 ratificada em **PRD v0.5 §UX/Branding** (Change Log 2026-05-29). Tokens canônicos já estão em `app/globals.css` (Story 5.2 Done — paleta Soft Studio + radius scale + shadows + typescale) e primitives shadcn já estão alinhados (Story 5.3 Done — Button ghost peach, Card +shadow-sm, Input/Textarea destructive ring /30, Dialog/AlertDialog rounded-2xl, FormMessage AlertCircle, Sonner border-left, etc.). Esta story **consome** ambos em cada page: ajusta layout (header sticky + footer minimal + grid 3 cards), copy verbatim per spec §2.1-2.4 (H1 + body + 3 motivos), typescale classes (text-display 40 / text-h1 32 / text-body-lg 18 do scale 5.2), CTA height (h-12 spec §4.7 via className override), e adiciona estruturas faltantes (secondary CTA ghost no landing, "Esqueci a senha" link no login, "voltar" link no auth layout, divider OR no login per spec §2.3 L506). Conforme `.claude/rules/agent-authority.md`, ajustes em pages são escopo do **@dev (Dex)** com gate **@qa (Quinn)** — mesmo padrão das Stories 3.2/3.3/5.2/5.3. `git push`/`gh pr create` permanecem **exclusivos de @devops** (Gage).

> **Backlog crossover:** `[EPIC-5-S4]` 🟡 MEDIUM (este item — `docs/STORY-BACKLOG.md`) será movido para ✅ DONE após merge (Task 9.1). **Não bloqueia 5.5/5.6/5.7** (paralelizáveis — pode draftar concorrente). **Bloqueia visualmente** primeira impressão pública do produto (`/` é landing canary Story 1.9 ainda em paleta seed pré-5.2). `[STORY-5.9]` (Wordmark "biolink ★" + favicon) é **independente** desta story — landing aqui usa wordmark `biolink` text-only (sem ★ + sem logomark), Story 5.9 substituirá pelo wordmark component dedicated. `[STORY-5.3-F1]` LOW e `[STORY-5.2-F1]` LOW (amends spec warning/destructive ratios) **não tocados** — Story 5.4 não introduz uso de `--warning` em bg/text components (apenas border-left graphical via Sonner Story 5.3) nem `text-destructive` sobre brand bg (FormMessage já cobre via Story 5.3 AlertCircle, ratio OK em card branco per OBS-001 5.2).

## Story

**As a** dono do BioLink (e por extensão, todo visitante não-autenticado que vê o produto pela primeira vez via landing `/` ou que entra no fluxo de onboarding via `/signup` / `/login` / `/reset-password`),
**I want** que as 6 superfícies page-level pré-autenticação (Landing `/` + auth shared layout `app/(auth)/layout.tsx` + Signup `/signup` + Login `/login` + Reset Password Request `/reset-password` + Reset Password Confirm `/reset-password/confirm`) renderizem **com os specs visuais e estruturais Soft Studio** (per `docs/frontend-spec.md` §2.1-2.4 + §3.7 form feedback) — Landing com hero centralizado vh100 desktop + 3 cards "motivos" + header sticky + footer minimal; auth com H1 32px deep plum + form inputs h-12 + Login com "Esqueci a senha" right-aligned + divider "ou" preparado para OAuth Phase 2 + Signup com terms checkbox + password helper; todas consumindo primitives refreshed Story 5.3 (peach ghost hover + Card shadow-sm + Input destructive ring /30 + FormMessage AlertCircle),
**so that** o produto pare de exibir a landing canary Story 1.9 em paleta violet-seed legacy + auth pages com typescale shipped genérica, e passe a expressar a identidade "warm creator studio" canônica do PRD v0.5 §UX/Branding **desde o primeiro pixel que um visitante anônimo vê** — desbloqueando a coerência visual full-funnel (landing → signup → dashboard pós-5.5) e materializando a quarta superfície shipped em paleta refreshed (após 5.2 tokens + 5.3 primitives + esta 5.4 pre-auth pages).

## Acceptance Criteria

> Todas as AC abaixo rastreiam para fontes canônicas — `docs/frontend-spec.md` v0.3 §2.1 (Landing L362-422) + §2.2 (Signup L425-478) + §2.3 (Login L481-518) + §2.4 (Reset Password L521-562) + §3.7 (Form submit feedback L1061-1083) + §1.3.2 (typescale tokens consumed) + §1.4.2 (radius scale consumed) + §4.7 (Forms — inputs h-12) + §4.2 (a11y keyboard nav). Cada AC cita seção exata. NENHUMA invenção (Constitution Art. IV). Tokens canônicos estão em `app/globals.css` pós-5.2 + primitives refreshed em `components/ui/` pós-5.3 — esta story **apenas consome em pages**. Estado pré-existente, deviations descobertas durante audit e racional de cada AC estão isolados em **Dev Notes** — leia antes de implementar.

1. **Landing H1 — copy + typescale `text-display` deep plum** (per `docs/frontend-spec.md` §2.1 L371-372 _"★ Seu link na bio, do seu jeito. ← Display 40, plum"_ + §1.3.2 typescale `--text-display` 40px). Mudar H1 de **"Sua presença digital em um único link"** (shipped Landing canary Story 1.9) para **"Seu link na bio, do seu jeito."** (spec verbatim, ★ asterisco prefix DEFERIDO para Story 5.9 wordmark). Aplicar classe `text-display` (= 40px + line-height + weight 500 + tracking -0.02em per Story 5.2 typescale tokens em `@theme inline`). Cor herda `text-foreground` deep plum-tinted (`#1B1530` light / `#F4EFFB` dark) — manter `text-foreground` ou remover redundante se já default no body. Remover classes Tailwind redundantes `text-4xl font-semibold tracking-tight sm:text-5xl` (substituídas pela única class `text-display` que encapsula size + lh + weight + tracking via Tailwind v4 suffix convention DEV-7 5.2).

2. **Landing body subtítulo — copy + typescale `text-body-lg` muted-fg** (per `docs/frontend-spec.md` §2.1 L374-376 _"Crie sua página pública gratuita — seus links, seus dados. ← Body-lg 18, muted-fg"_ + §1.3.2 typescale `--text-body-lg` 18px). Mudar `<p>` subtítulo de **"O BioLink reúne todos os seus links em uma página profissional — do cadastro à primeira publicação em apenas 2 cliques."** (shipped 1.9) para **"Crie sua página pública gratuita — seus links, seus dados."** (spec verbatim). Aplicar classe `text-body-lg` (= 18px + line-height + weight 400 + tracking 0 per typescale). Cor `text-muted-foreground` (já correto no shipped). Remover `text-lg` redundante (`text-body-lg` é o canon Soft Studio).

3. **Landing primary CTA — h-12 full-width mobile + behavioral copy per FR3** (per `docs/frontend-spec.md` §2.1 L378-380 _"Criar minha página → ← BUTTON primary, h-12, full-width mobile"_ + §4.7 L1202 _"Buttons: h-12 (48px) padrão"_ + PRD FR3 dashboard CTA quando autenticado). Mudar primary CTA atual (shipped 1.9: `<Link href={ctaHref} className={buttonVariants({ size: 'lg' })}>{ctaLabel}</Link>` — `size: 'lg'` = h-9) para versão com h-12 override: `className={cn(buttonVariants({ size: 'lg' }), 'h-12 w-full sm:w-auto')}` (DEV-A1 anticipated: NÃO adicionar size variant nova ao Button primitive; Story 5.3 audit fechado e Button API estável — usar className override no consumer). Manter behavioral copy: `ctaLabel = user ? 'Ir para meu dashboard' : 'Criar minha página'` (per FR3, já correto no shipped — verbatim verificar). Adicionar arrow `→` no label visual se ausente (spec mostra "→" no label — verificar shipped + adicionar via spread/template literal se faltar). Manter `<Link href={ctaHref}>` (Next Link, não `<a>`).

4. **Landing secondary CTA — ghost button "Já tem conta? Entrar →" (visível apenas quando NÃO autenticado)** (per `docs/frontend-spec.md` §2.1 L382 _"Já tem conta? Entrar ← BUTTON ghost (text-only secondary CTA)"_). Adicionar SECUNDÁRIO Link button (`buttonVariants({ variant: 'ghost' })`) abaixo do primary CTA. Pattern: condicional `!user && <Link href="/login" className={buttonVariants({ variant: 'ghost' })}>Já tem conta? Entrar →</Link>`. Ghost variant Story 5.3 Done consome `hover:bg-accent hover:text-accent-foreground` (peach automatic — DEV-A1 5.3 corrigiu). Spacing: `gap-3` ou `gap-4` entre primary e secondary CTAs (Tailwind `flex flex-col gap-3 sm:flex-row` para desktop side-by-side per AC7). Quando autenticado (`user !== null`), secondary CTA NÃO renderiza (ctaHref/ctaLabel já gerencia o primary).

5. **Landing "3 motivos" — 3 Card components em vez de bullet list** (per `docs/frontend-spec.md` §2.1 L386-404 + §1.4.2 Card radius 16 + Story 5.3 AC2 Card +shadow-sm). Substituir `<ul>` shipped (bullet plano com BENEFITS strings) por estrutura 3 `<Card>` (de `@/components/ui/card`) com seguintes specs verbatim: (a) Section header acima dos cards: `<p className="text-body-sm uppercase text-muted-foreground">3 motivos:</p>` (per L386 _"3 motivos: ← Body-sm uppercase muted"_); (b) Card 1: ícone `◐` (lucide-react `CircleDashedIcon` ou similar; se não houver match perfeito, usar `ShieldCheckIcon` ou `BadgeIcon`) + H2 _"Sem ads forçados"_ (`text-h2` typescale 24px) + body _"Seu perfil 100% seu, sempre."_ (`text-body` 16px); (c) Card 2: ícone `▦` (lucide `LayoutGridIcon` ou `BarChart3Icon`) + H2 _"Analytics próprios"_ + body _"7d e 30d, sem terceiros."_; (d) Card 3: ícone `⚙` (lucide `SettingsIcon` ou `CodeIcon`) + H2 _"Open source"_ + body _"Código auditável no GitHub."_. Cards usam `<Card>` primitive (Story 5.3 +`shadow-sm` warm-tinted plum), padding interno default (`gap-4 py-4 px-4` via Card defaults). Layout: stack vertical mobile / `grid grid-cols-3 gap-4` desktop (≥1024px per spec §2.1 L419 _"Desktop: ... 3 cards lado a lado em grid de 3"_). Spec não prescreve cores hex específicas para ícones — herdar `text-foreground` ou `text-primary` por escolha @dev (DEV-A2 anticipated). **NÃO** adicionar hover state aos cards (Card não é interativo per Story 5.3 Task 2.3; cards "motivos" são informacionais, não clicáveis).

6. **Landing header — sticky transparent on scroll-top, "biolink" wordmark + "Entrar" button** (per `docs/frontend-spec.md` §2.1 L368 _"[LOGO biolink] [LOGIN] [→] ← Header sticky (transparent on scroll-top)"_ + §1.6 wordmark all-lowercase). Adicionar `<header>` component no topo do `<main>` com: (a) **Position**: `sticky top-0 z-40` + initial `bg-transparent` que vira `bg-background/80 backdrop-blur` quando scrollY > 0 (implementar via Client Component wrapper OU via CSS `--scroll-y` custom property OU pattern simples sem JS: header sempre `bg-background/95 backdrop-blur-sm` per simplicidade — DEV-A3 anticipated, prefira sem JS se possível, sem regressar LCP); (b) **Wordmark left**: `<Link href="/" className="text-h3 lowercase">biolink</Link>` (text only, lowercase, "biolink" sem ★ — ★ adicionado Story 5.9 via `<Wordmark />` component novo); (c) **Login button right (visível apenas quando NÃO autenticado)**: `!user && <Link href="/login" className={buttonVariants({ variant: 'ghost' })}>Entrar</Link>` (ghost, peach hover automatic); (d) Container: `max-w-screen-xl mx-auto px-6 flex items-center justify-between h-14`. Quando autenticado, "Entrar" oculta (usuário entra via primary CTA "Ir para meu dashboard").

7. **Landing desktop hero vh100 + CTAs side-by-side + 3 cards grid-3** (per `docs/frontend-spec.md` §2.1 L419 _"Desktop (≥1024px): hero ocupa viewport-height (vh100), CTAs lado a lado, 3 cards lado a lado em grid de 3. Container max-width: 1024px, centered."_). Aplicar Tailwind responsive: (a) **Hero section**: `flex flex-1 flex-col items-center justify-center gap-6 px-6 py-24 text-center min-h-[calc(100vh-3.5rem)]` (subtrai altura do header sticky h-14 = 3.5rem); (b) **CTAs container**: `flex flex-col items-center gap-3 sm:flex-row sm:gap-4` (mobile column / desktop row); (c) **3 motivos grid**: `mx-auto grid max-w-screen-lg gap-4 sm:grid-cols-3` (mobile 1 col / desktop 3 cols). Container global: `max-w-screen-xl mx-auto` (Tailwind `screen-xl` = 1280px; spec diz 1024px = `max-w-screen-lg`; usar `screen-lg` por spec verbatim — DEV-A4 anticipated). Mantém centered horizontal.

8. **Landing footer — minimal github icon + "open-source gratuito • LGPD-mindful"** (per `docs/frontend-spec.md` §2.1 L408-409 _"[github icon] open-source / gratuito • LGPD-mindful ← Footer minimal"_). Refresh footer atual (shipped: GitHub link + build/commit info inline) para: (a) ícone github (lucide `GithubIcon`) seguido de link "open-source" para `GITHUB_REPO_URL`; (b) separator `•` + texto "gratuito • LGPD-mindful" `text-body-sm text-muted-foreground`; (c) Build/commit info MOVE para segunda linha menor `text-caption text-muted-foreground/70` (visual de menor importância — manter por debug/observabilidade mas sem destaque). Layout: `flex flex-col items-center gap-2 sm:flex-row sm:justify-between` (mobile stack / desktop spread).

9. **Auth pages H1 — bump typescale `text-2xl` → `text-h1` 32px** (per `docs/frontend-spec.md` §2.2 L432 _"Criar conta ← H1 32"_, §2.3 L488 _"Entrar ← H1"_, §2.4 L528 _"Esqueceu a senha? ← H1"_, §2.4 L520 verbatim variants Reset Password Confirm "Nova senha" ← H1 (deduzir do shipped). Aplicar em todos os 4 page.tsx + auth layout wordmark se aplicar: substituir `<h1 className="text-2xl font-semibold tracking-tight">` (shipped) por `<h1 className="text-h1">` (= 32px + line-height + weight 500 + tracking -0.02em per Story 5.2 typescale). Pages afetadas: `app/(auth)/signup/page.tsx:9`, `app/(auth)/login/page.tsx:10`, `app/(auth)/reset-password/page.tsx:15`, `app/(auth)/reset-password/confirm/page.tsx:20`. Remover classes Tailwind redundantes (font-semibold + tracking-tight encapsuladas em `text-h1` via suffix convention 5.2 DEV-7).

10. **Auth form inputs — h-12 className override per spec §4.7** (per `docs/frontend-spec.md` §2.2 L436 _"seu@email.com ← INPUT h-12, radius 12"_ + §4.7 L1202 _"Buttons: h-12 (48px) padrão"_). Auditar consumidores de `<Input>` e `<Button>` (primary submit) em `components/auth/LoginForm.tsx`, `components/auth/SignupForm.tsx`, `components/auth/ResetPasswordRequestForm.tsx`, `components/auth/ResetPasswordConfirmForm.tsx`. Aplicar `className="h-12"` override em cada `<Input>` usage E em cada `<Button type="submit" size="lg">` (transforma h-9 → h-12 via cn merge — tailwind-merge garante override). Radius 12 (= `rounded-lg`) já é o default Story 5.3 Input. **NÃO** alterar Input primitive (Story 5.3 escopo fechado). Pattern de override: `<Input className="h-12" {...register('email')} />` ou em `<FormControl><Input className="h-12" /></FormControl>` per FormControl shadcn pattern. DEV-A1 cobre essa decisão para CTAs + inputs (className override em consumidor; primitive intact).

11. **Login form — "Esqueci a senha →" right-aligned link + divider "─── ou ───" + signup CTA bottom** (per `docs/frontend-spec.md` §2.3 L500-509). Auditar `components/auth/LoginForm.tsx`. Mudanças esperadas: (a) Adicionar entre password input e submit button: `<Link href="/reset-password" className="self-end text-sm text-muted-foreground hover:text-foreground">Esqueci a senha →</Link>` (right-aligned no form column flex); (b) Após submit button, adicionar divider visual `<div className="flex items-center gap-2"><Separator className="flex-1" /><span className="text-caption text-muted-foreground">ou</span><Separator className="flex-1" /></div>` (Separator é shadcn primitive — verificar se existe em `components/ui/`; se não, usar `<hr className="flex-1 border-border" />`); (c) Após divider, adicionar `<Link href="/signup" className={buttonVariants({ variant: 'ghost' })}>Criar conta →</Link>` ghost button (substitui o atual `<p className="text-sm">Não tem conta? <Link>Criar conta</Link></p>` em `app/(auth)/login/page.tsx:18-22` — MOVE do page wrapper para dentro do form). Atualizar `app/(auth)/login/page.tsx` para remover o `<p>` shipped (será coberto pelo LoginForm). **Out-of-scope**: implementação real de OAuth providers — divider "ou" prepara visualmente, mas providers reais são Phase 2 per spec §2.3 L506.

12. **Signup form — terms checkbox + password helper text + "✓ disponível" username live status** (per `docs/frontend-spec.md` §2.2 L443 password helper + L454 username status + L456-459 terms checkbox + L469-474 estados de validação). Auditar `components/auth/SignupForm.tsx`. Mudanças esperadas: (a) Verificar se já tem helper text "Mínimo 8 caracteres" abaixo do input senha — se ausente, adicionar `<p className="text-caption text-muted-foreground">Mínimo 8 caracteres.</p>` abaixo do password input; (b) Verificar se username field tem status inline (`✓ disponível` success / `Username em uso` destructive) — pattern existing per Story 5.1 `use-username-availability` hook em `lib/hooks/`; se ausente, ADIAR para `[STORY-5.4-F1]` LOW follow-up (audit username availability já feita Story 1.5 + 5.1 — não regressar comportamento); (c) Verificar se Checkbox "Li e aceito os termos e a política de privacidade" está presente — shadcn Checkbox primitive (`components/ui/checkbox.tsx`) — se ausente, ADIAR para `[STORY-5.4-F2]` LOW (terms acceptance requer link routes pra `/terms` + `/privacy` que ainda não existem — out-of-scope do refresh visual). Apenas refresh **estrutural**, não funcional novo. Helper text password é a única mudança garantida se ausente.

13. **Auth shared layout — "← voltar" link no header + remover wordmark text duplicado** (per `docs/frontend-spec.md` §2.2 L429 / §2.3 L484 / §2.4 L524 _"[← voltar] [logo biolink] ← Header minimal"_). Auditar `app/(auth)/layout.tsx`. Mudanças: (a) Adicionar `<Link href="/" className="text-sm text-muted-foreground hover:text-foreground">← voltar</Link>` à esquerda do wordmark; (b) Manter wordmark `<Link href="/" className="text-h3 lowercase">biolink</Link>` à direita (atual usa `text-2xl font-semibold tracking-tight BioLink` capitalizado — mudar para lowercase + `text-h3` per spec §1.6 + typescale 5.2); (c) Container: `mb-8 flex items-center justify-between` (replace center alignment do shipped). Auth pages signup/login/reset-password/reset-password/confirm herdam essa nova estrutura.

14. **Suíte completa de qualidade local PASS sem regressão funcional**: `pnpm typecheck` exit 0, `pnpm lint` zero warnings, `pnpm build` (Next 16.2.5 / Turbopack) 15/15 static pages PASS, `pnpm test:components` mantém **194/194 verde por contagem** — snapshots em `tests/components/features/landing/__snapshots__/HomePage.test.tsx.snap` + `tests/components/features/auth/__snapshots__/{LoginForm,SignupForm,ResetPasswordRequestForm,ResetPasswordConfirmForm}.test.tsx.snap` (5 arquivos candidate review) podem precisar update por mudanças estruturais (Landing: H1 copy + 3 Cards em vez de ul + header + footer; Auth forms: h-12 override + Login "Esqueci a senha" + divider). **Processo obrigatório:** rodar `pnpm test:components` SEM `-u` primeiro → revisar diff de cada snapshot que falhar → confirmar que mudança é por className/structure intencional (não regressão semântica) → só então rodar `pnpm test:components -u` para atualizar. Documentar **quantos snapshots foram atualizados e quais** no Dev Agent Record (mesmo padrão Story 5.3 Task 13.2). PublicPage snapshot intacto esperado (Story 5.4 NÃO toca `/[username]` — escopo Story 5.7).

15. **Lighthouse CI mantido ≥ 0.85 em `/` e `/[username]` + LCP `/` < 2.0s (NFR1)**: gate paths inclui `app/**` (match em `app/page.tsx` + `app/(auth)/**`) + `components/**` (match se forms forem editados). Threshold atual (≥ 0.85 Performance/Accessibility/Best Practices/SEO) **não regride**. LCP `/` baseline pós-5.2 ~180-185 KB First Load JS — esperado **neutral** delta (mudanças são layout/copy + 3 Cards extras + 1 header + 1 wordmark Link; primitives já no bundle; lucide icons tree-shakeable — +1-2 ícones novos, ~1-2 KB max). Lighthouse audit pode flagar A11y issues novos se header sticky tiver focus order errado OU se 3 Cards não tiverem `<h2>` semântico (per AC5 spec usa H2 explícito) — validar via Lighthouse no PR. Qualquer queda > 5 pontos exige investigação antes do merge (mesmo gate da Story 5.2/5.3 — runs:3 + median absorve flake per STORY-3.5-F3).

> **No-Invention reconciliation:** O backlog `[EPIC-5-S4]` (`docs/STORY-BACKLOG.md`) lista como acceptance line: _"Lighthouse CI ≥ 90; LCP `/` < 2.0s (NFR1); a11y WCAG AA mantida; smoke seletivo"_. Reconcilio: backlog menciona Lighthouse ≥ 90 mas threshold real do projeto é **≥ 0.85** per `.lighthouserc.json` (Story 3.5 Task 6 + estabilizado [STORY-3.5-F3]) — uso o valor real (0.85), não o aspirational do backlog. AC1-AC8 derivam **verbatim** de spec §2.1 L362-422 (Landing — 1 AC por elemento: H1/body/primary CTA/secondary CTA/3 motivos/header/desktop layout/footer). AC9-AC13 cobrem auth pages (H1 typescale + form inputs h-12 + Login Esqueci a senha + Signup terms helper + auth layout back link). AC14-AC15 consolidam gates locais + CI (mesma estrutura Story 5.3 AC12-AC13). Smoke runtime manual (header sticky behavior; CTAs flex layout responsive; 3 Cards grid; Login divider visual; back link auth layout) é tratado como **Task condicional 8** (não AC formal) — per MEMORY `feedback_skip_manual_smoke.md`, @dev pergunta antes de pedir, e se skip pode marcar `[x]` com nota `_(SKIPPED ...)_` + risco residual documentado.

> **Out-of-scope (explicitamente deferido):**
>
> - **Spring transitions** (header transparent → solid scroll, ghost button hover scale, Card lift on hover-if-interactive) → Story 5.8 (Motion + polish).
> - **Wordmark `biolink ★` component + favicon ★** → Story 5.9 (esta story usa text-only "biolink" lowercase como placeholder; ★ asterisco prefix do H1 spec §2.1 L371 também deferido para 5.9 — atual H1 5.4 é só "Seu link na bio, do seu jeito." sem ★).
> - **OAuth providers reais** (Google/GitHub OAuth no Login per spec §2.3 L506 "Phase 2: OAuth") → Phase 2 post v1.x stabilization. Divider "ou" prepara visualmente mas é placeholder.
> - **Terms checkbox real + routes `/terms` + `/privacy`** → `[STORY-5.4-F2]` LOW (route creation + content writing fora do escopo refresh visual; texto checkbox stub no shipped pode permanecer).
> - **Mudança no dashboard/profile/theme/account/analytics/public pages** → Stories 5.5/5.6/5.7.
> - **Token `--success-foreground` consumido** (FormMessage success ícone) → Story 5.5/5.6 (analytics succeeded toasts; profile save success state via §3.7 State C 1.5s success).
> - **Email/username live validation refresh visual** (Signup spec §2.2 L454 "✓ disponível") → Story 5.1 implementou `use-username-availability` hook; visual refresh do status inline está OUT desta story (não regressar). Se aparente problema, abrir `[STORY-5.4-F1]` LOW.
> - **OG image refresh** (`/og-image.png` per `app/page.tsx:23`) → Story 5.9 (brand assets).
> - **Refactor `check-contrast.mjs` para parser CSS automatizado** → `[STORY-3.2-F1]` LOW (continua aberto).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

> **Nota (@sm):** `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` **e** `mcp.enabled: false` → skip notice é o comportamento correto do template-v2 (idêntico às Stories 1.4-5.3, todas Done). ClickUp sync pulado (`mcp.enabled: false`). Gate de qualidade segue o **workaround manual QA-001** (CodeRabbit/WSL incompat. em macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`), padrão validado em 1.4-5.3.

## Tasks / Subtasks

- [x] **Task 1 — Audit Landing (`app/page.tsx`)** (AC: 1, 2, 3, 4, 5, 6, 7, 8) — _executor: @dev_
  - [x] 1.1 Abrir `app/page.tsx`. Verificar imports: já tem `buttonVariants` ✓ (L4); adicionar imports faltantes: `import { Card } from '@/components/ui/card'`, `import { cn } from '@/lib/utils'`, `import { GithubIcon } from 'lucide-react'` (para footer + 3 cards icons — DEV-A2 decide quais ícones lucide para ◐/▦/⚙).
  - [x] 1.2 **Header sticky**: adicionar `<header>` antes do `<main>` com `sticky top-0 z-40 bg-background/95 backdrop-blur-sm border-b border-border`. Conteúdo: wordmark "biolink" left (`<Link href="/" className="text-h3 lowercase">biolink</Link>`) + ghost button "Entrar" right (`!user && <Link href="/login" className={buttonVariants({ variant: 'ghost' })}>Entrar</Link>`). Container `max-w-screen-lg mx-auto px-6 flex items-center justify-between h-14`. DEV-A3: sem JS de scroll (sempre semi-transparente com blur) — simplicidade > efeito; spring transition é Story 5.8 escopo.
  - [x] 1.3 **Hero H1**: substituir `<h1 className="max-w-2xl text-4xl font-semibold tracking-tight text-foreground sm:text-5xl">Sua presença digital em um único link</h1>` por `<h1 className="text-display max-w-2xl">Seu link na bio, do seu jeito.</h1>` (★ asterisco prefix deferido 5.9).
  - [x] 1.4 **Hero body subtítulo**: substituir `<p className="max-w-xl text-lg text-muted-foreground">O BioLink reúne todos os seus links em uma página profissional — do cadastro à primeira publicação em apenas 2 cliques.</p>` por `<p className="text-body-lg max-w-xl text-muted-foreground">Crie sua página pública gratuita — seus links, seus dados.</p>`.
  - [x] 1.5 **CTAs container**: wrap primary + secondary em `<div className="flex flex-col items-center gap-3 sm:flex-row sm:gap-4">`. Primary CTA: `<Link href={ctaHref} className={cn(buttonVariants({ size: 'lg' }), 'h-12 w-full sm:w-auto')}>{ctaLabel} →</Link>` (adicionar `→` no label se ausente). Secondary CTA (CONDICIONAL): `{!user && <Link href="/login" className={cn(buttonVariants({ variant: 'ghost' }), 'h-12 w-full sm:w-auto')}>Já tem conta? Entrar →</Link>}`.
  - [x] 1.6 **Hero section flex**: substituir `<section className="flex flex-1 flex-col items-center justify-center gap-6 px-6 py-24 text-center">` por `<section className="flex flex-1 flex-col items-center justify-center gap-6 px-6 py-24 text-center min-h-[calc(100vh-3.5rem)]">` (subtrai header h-14 = 3.5rem para vh100 effective).
  - [x] 1.7 **3 Motivos section**: substituir `<section>...<ul>...</ul></section>` shipped (linhas 54-62) por:
    ```tsx
    <section className="border-t border-border px-6 py-16">
      <div className="mx-auto max-w-screen-lg">
        <p className="text-body-sm mb-6 text-center uppercase tracking-wide text-muted-foreground">
          3 motivos:
        </p>
        <div className="grid gap-4 sm:grid-cols-3">
          {/* Card 1: Sem ads forçados */}
          <Card>
            <CardHeader>...</CardHeader>
            <CardContent>...</CardContent>
          </Card>
          {/* Card 2: Analytics próprios */}
          ...
          {/* Card 3: Open source */}
          ...
        </div>
      </div>
    </section>
    ```
    Conteúdo: Card 1 (ícone DEV-A2 escolhe — sugestão `ShieldCheckIcon` para "sem ads"; ou `BadgeIcon` ou `CircleDashedIcon` mais próximo do ◐ glyph) + `<CardTitle className="text-h2">Sem ads forçados</CardTitle>` + `<CardDescription>Seu perfil 100% seu, sempre.</CardDescription>` (`CardDescription` já é `text-sm text-muted-foreground` per shipped Card). Card 2 (`LayoutGridIcon` ou `BarChart3Icon` para ▦) + "Analytics próprios" + "7d e 30d, sem terceiros.". Card 3 (`CodeIcon` ou `SettingsIcon` para ⚙) + "Open source" + "Código auditável no GitHub.". Documentar escolha de ícones no Dev Agent Record DEV-A2.
  - [x] 1.8 **Footer**: substituir `<footer>` shipped (L64-77) por:
    ```tsx
    <footer className="border-t border-border px-6 py-8">
      <div className="mx-auto flex max-w-screen-lg flex-col items-center gap-2 sm:flex-row sm:justify-between">
        <a
          href={GITHUB_REPO_URL}
          target="_blank"
          rel="noopener noreferrer"
          className="text-body-sm inline-flex items-center gap-1.5 text-muted-foreground hover:text-foreground"
        >
          <GithubIcon className="size-4" /> open-source
        </a>
        <p className="text-body-sm text-muted-foreground">gratuito • LGPD-mindful</p>
        <p className="text-caption text-muted-foreground/70">
          Build {buildTime} · {commitSha}
        </p>
      </div>
    </footer>
    ```
    (3 elementos no row desktop / stack mobile; build/commit info segunda-class visual mas mantido para debug).
  - [x] 1.9 Rodar `pnpm typecheck` exit 0 + abrir `tests/components/features/landing/HomePage.test.tsx` para entender que assertions exercem (snapshot vs role/text queries) — antecipar quais quebram.

- [x] **Task 2 — Audit Auth shared layout (`app/(auth)/layout.tsx`)** (AC: 13) — _executor: @dev_
  - [x] 2.1 Abrir `app/(auth)/layout.tsx`. Layout atual centra wordmark text-only. Reescrever para horizontal:
    ```tsx
    <div className="min-h-screen flex flex-col items-center justify-center px-4 py-12 bg-background">
      <div className="w-full max-w-sm">
        <header className="mb-8 flex items-center justify-between">
          <Link href="/" className="text-sm text-muted-foreground hover:text-foreground">
            ← voltar
          </Link>
          <Link href="/" className="text-h3 lowercase">
            biolink
          </Link>
        </header>
        {children}
      </div>
    </div>
    ```
    (Spec §2.2 L429 / §2.3 L484 / §2.4 L524 "[← voltar] [logo biolink] ← Header minimal".)
  - [x] 2.2 Verificar que `<Link href="/">` no wordmark continua funcionando (rota Landing).
  - [x] 2.3 `text-h3` no wordmark = 20px per Story 5.2 typescale (size + lh + weight 500). Lowercase per spec §1.6.

- [x] **Task 3 — Audit Signup page (`app/(auth)/signup/page.tsx` + `components/auth/SignupForm.tsx`)** (AC: 9, 10, 12) — _executor: @dev_
  - [x] 3.1 Abrir `app/(auth)/signup/page.tsx`. Mudar L9 H1 `<h1 className="text-2xl font-semibold tracking-tight">Criar conta</h1>` → `<h1 className="text-h1">Criar conta</h1>` (32px per typescale).
  - [x] 3.2 Abrir `components/auth/SignupForm.tsx`. Auditar Input usage para email/password/username — adicionar `className="h-12"` override em cada `<Input>` (typeahead-merge garante override do default h-8 do Input primitive).
  - [x] 3.3 Submit button: adicionar `className="h-12 w-full"` (full-width per spec §2.2 L461; h-12 per §4.7).
  - [x] 3.4 Verificar se password helper text "Mínimo 8 caracteres." está presente abaixo do password input. Se ausente, adicionar `<p className="text-caption text-muted-foreground mt-1">Mínimo 8 caracteres.</p>` abaixo do FormControl password (ou onde currently está o helper).
  - [x] 3.5 Verificar Checkbox terms — se presente, ajustar typescale; se ausente, ADIAR para `[STORY-5.4-F2]` LOW.
  - [x] 3.6 Verificar username live availability status — se presente, ajustar visual; se ausente ou se visual está desatualizado, ADIAR para `[STORY-5.4-F1]` LOW.

- [x] **Task 4 — Audit Login page (`app/(auth)/login/page.tsx` + `components/auth/LoginForm.tsx`)** (AC: 9, 10, 11, 12) — _executor: @dev_
  - [x] 4.1 Abrir `app/(auth)/login/page.tsx`. Mudar L10 H1 → `<h1 className="text-h1">Entrar</h1>`. REMOVER o `<p>` shipped L18-22 ("Não tem conta? Criar conta") — será movido para dentro do LoginForm como ghost button per AC11.
  - [x] 4.2 Abrir `components/auth/LoginForm.tsx`. Inputs email/password: adicionar `className="h-12"` override.
  - [x] 4.3 Adicionar entre password input e submit button:
    ```tsx
    <Link
      href="/reset-password"
      className="self-end text-sm text-muted-foreground hover:text-foreground"
    >
      Esqueci a senha →
    </Link>
    ```
    (right-aligned via `self-end` em form column flex).
  - [x] 4.4 Submit button: `className="h-12 w-full"`.
  - [x] 4.5 Após submit button, adicionar divider OR + ghost "Criar conta →" button:
    ```tsx
    <div className="my-4 flex items-center gap-2">
      <hr className="flex-1 border-border" />
      <span className="text-caption text-muted-foreground">ou</span>
      <hr className="flex-1 border-border" />
    </div>
    <Link href="/signup" className={cn(buttonVariants({ variant: 'ghost' }), 'h-12 w-full')}>
      Criar conta →
    </Link>
    ```
    (Separator shadcn primitive pode estar ausente — verificar `components/ui/separator.tsx`; se ausente, usar `<hr>` per pattern acima. NÃO criar Separator novo nesta story — separator é primitive shadcn-add que sai escopo.)
  - [x] 4.6 Documentar grep count consumidores de `<LoginForm>` (`grep -rn LoginForm app/`) — esperado: apenas `app/(auth)/login/page.tsx`.

- [x] **Task 5 — Audit Reset Password page (`app/(auth)/reset-password/page.tsx`)** (AC: 9, 10) — _executor: @dev_
  - [x] 5.1 Abrir `app/(auth)/reset-password/page.tsx`. Mudar L15 H1 → `<h1 className="text-h1">Esqueceu a senha?</h1>`.
  - [x] 5.2 Abrir `components/auth/ResetPasswordRequestForm.tsx`. Input email: `className="h-12"`. Submit button: `className="h-12 w-full"`.
  - [x] 5.3 Verificar que `<Link>Voltar para login</Link>` no page.tsx L22-25 permanece intacto (não conflita com auth layout back link — esse link é Voltar para LOGIN, layout back link é Voltar para HOME).

- [x] **Task 6 — Audit Reset Password Confirm page (`app/(auth)/reset-password/confirm/page.tsx`)** (AC: 9, 10) — _executor: @dev_
  - [x] 6.1 Abrir `app/(auth)/reset-password/confirm/page.tsx`. Mudar L20 H1 → `<h1 className="text-h1">Nova senha</h1>`.
  - [x] 6.2 Abrir `components/auth/ResetPasswordConfirmForm.tsx`. Inputs password + confirm: `className="h-12"`. Submit button: `className="h-12 w-full"`.

- [x] **Task 7 — Quality gates locais + snapshot review** (AC: 14) — _executor: @dev_
  - [x] 7.1 Rodar SEQUENCIAL (em foreground, capturar exit code per MEMORY `feedback_avoid_tail_pipe_in_background.md`):
    ```bash
    pnpm typecheck
    pnpm lint
    pnpm build
    pnpm test:components
    ```
    Capturar output final no Dev Agent Record `Debug Log References`. Exit code 0 obrigatório.
  - [x] 7.2 **Snapshot review preventivo:** se `test:components` reportar snapshots desatualizados (esperado em HomePage.test.tsx por mudanças estruturais Landing + auth forms tests por h-12 override + Login Esqueci a senha + divider), revisar cada diff:
    - Abrir `tests/components/features/landing/__snapshots__/HomePage.test.tsx.snap` no diff
    - Abrir `tests/components/features/auth/__snapshots__/{LoginForm,SignupForm,ResetPasswordRequestForm,ResetPasswordConfirmForm}.test.tsx.snap` no diff
    - Confirmar que cada mudança é estrutural/className intencional (text-h1 swap, h-12 override, 3 Cards, Esqueci a senha, etc.) e NÃO regressão semântica
    - Listar snapshots intencionalmente atualizados em `Completion Notes` (count + names)
  - [x] 7.3 Rodar `pnpm test:components -u` para atualizar snapshots aprovados. Re-rodar sem `-u` para confirmar 194/194 green.
  - [x] 7.4 PublicPage snapshot (`tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap`) — Story 5.4 NÃO toca em PublicPage (escopo 5.7), permanece INTACTO. Se falhar → investigar como regressão (não atualizar).

- [x] **Task 8 — Smoke runtime manual (CONDICIONAL — perguntar antes per MEMORY `feedback_skip_manual_smoke.md`)** (AC: 15) — _executor: @dev_
  - [x] 8.1 Perguntar ao usuário: "Quer smoke manual browser para validar (a) Landing header sticky + responsive layout (mobile 768px vs desktop 1024px+), (b) Landing 3 Cards grid responsive, (c) Landing CTA secondary 'Já tem conta? Entrar' aparece apenas se logged out, (d) Auth layout '← voltar' link funcional, (e) Login form 'Esqueci a senha →' link + divider 'ou', (f) Auth form inputs visualmente h-12?" _(Done — usuário respondeu Skip via AskUserQuestion 2026-05-31)_
  - [x] 8.2 Se **SIM** → rodar `pnpm dev` em background, abrir `http://localhost:3000` (Landing), `/signup` (Signup), `/login` (Login), `/reset-password` (Reset Request), `/reset-password/confirm` (precisa session — pode pular ou simular via dev devtools). Capturar screenshots ou descritivo dos 6 cenários. Documentar resultados. _(N/A — branch SIM não tomada)_
  - [x] 8.3 Se **NÃO/SKIP** → marcar `[x] _(SKIPPED — cobertura indireta via test:components 194/194 PASS + snapshot diff review documentado + Lighthouse CI no PR para a11y/performance; risco residual: regressões visuais responsive (mobile vs desktop layout), behavior CSS-only (header sticky/blur), CTAs conditional rendering — não detectáveis 100% por snapshot quando classes condicionais via ternary. Aceito por @dev — @qa pode solicitar smoke pontual se necessário)_`. _(Aplicado — branch SKIP escolhida)_
  - [x] 8.4 Lighthouse CI vai validar Performance/Accessibility automaticamente em PR (gate ≥ 0.85, paths `app/**` + `components/**`). LCP `/` < 2.0s NFR1 também validado via Lighthouse Performance. Não rodar local. _(Validação automática em CI pós-push @devops)_

- [x] **Task 9 — Housekeeping (Status + Backlog + Change Log + File List + Dev Agent Record)** (AC: todos) — _executor: @dev_
  - [x] 9.1 Atualizar `docs/STORY-BACKLOG.md`:
    - Remover entrada `[EPIC-5-S4]` da seção MEDIUM (padrão Story 5.2/5.3 — remove ao Done, registra em Change Log).
    - Adicionar linha no Change Log do backlog: `| 2026-{MM}-{DD} | DONE | [EPIC-5-S4] → Story 5.4 implementada (landing + auth pages Soft Studio refresh — H1 typescale, 3 Cards motivos, header sticky, h-12 form inputs, Login "Esqueci a senha" + divider) | Dex (dev) |`.
    - Decrementar statistics (Total e MEDIUM).
    - Atualizar nota Epic 5 Refresh header narrative: incluir 5.4 como DONE.
    - Atualizar `last_updated` frontmatter.
    - Se `[STORY-5.4-F1]` LOW e/ou `[STORY-5.4-F2]` LOW materializarem (descobertos durante implementação per Task 3.5/3.6), adicionar entradas em LOW Priority + Change Log ADD.
  - [x] 9.2 Mudar Status desta story (linha 5) de `Draft` para `InProgress` no início da implementação e depois `Ready for Review` antes do handoff @qa.
  - [x] 9.3 Adicionar linhas em **Change Log** desta story (versões 0.2 post-validate-po, 0.3 implementação).
  - [x] 9.4 Atualizar **Dev Agent Record** (Model Used, Debug Log References, Completion Notes, File List).
  - [x] 9.5 Não commitar com `git push` (delegar a @devops per agent-authority). Commit local OK com mensagem `feat: landing + auth pages Soft Studio refresh [Story 5.4]`.

## Dev Notes

> Esta seção **substitui** a necessidade de @dev abrir `docs/frontend-spec.md` ou `docs/architecture/*` durante implementação. Tudo o que importa para implementar 5.4 está aqui, com `[Source: ...]` rastreando cada decisão.

### Previous Story Insights (5.2 + 5.3)

**Story 5.2 Done — tokens canônicos em `app/globals.css`:**

- **Cores** (light/dark/brand): `--primary` deep plum `#5B3A8C` (light/brand) / lavender `#B8A1E8` (dark); `--accent` peach `#FFB5A7` (light/brand) / warm peach `#FF9B8A` (dark); `--background` lavender mist `#FAF8FF` (light) / plum-night `#14102A` (dark) / deeper lavender `#F0E8FF` (brand); `--foreground` deep ink `#1B1530` (light/brand) / cream `#F4EFFB` (dark); `--card`/`--popover` white (light/brand) / `#2A2244` (dark); `--destructive` `#C84141` (light/brand DEV-9) / `#FF7878` (dark); `--ring` deep plum (matches primary).
- **Typescale 9 tokens** via Tailwind v4 suffix convention DEV-7: `text-display` (40px / 48 lh / weight 500 / tracking -0.02em), `text-h1` (32 / 40 / 500 / -0.02), `text-h2` (24 / 32 / 500 / -0.01), `text-h3` (20 / 28 / 500 / -0.01), `text-body-lg` (18 / 28 / 400 / 0), `text-body` (16 / 24 / 400 / 0), `text-body-sm` (14 / 20 / 400 / 0), `text-caption` (12 / 16 / 400 / +0.01), `text-button` (14 / 20 / 500 / 0). **Aplicação Story 5.4: substituir `text-{n}xl font-semibold tracking-tight` shipped por `text-{name}` canônico per spec §1.3.2.**
- **Radius scale**: `--radius-sm` 4 / `--radius-md` 8 / `--radius-lg` 12 (default Input/Button shipped) / `--radius-xl` 16 (Card default Story 5.3) / `--radius-2xl` 24 (Dialog Story 5.3) / `--radius-full` 9999. **Story 5.4 NÃO mexe em radius — usa o que primitives consomem.**
- **Shadows warm-tinted plum** (light/brand): `--shadow-sm` `0 1px 2px 0 rgba(91, 58, 140, 0.05)` (Card default Story 5.3) / `--shadow-md` / `--shadow-lg` (Dialog Story 5.3). **Story 5.4 NÃO adiciona shadow explícita em pages — Cards já têm shadow-sm via 5.3 audit.**
- **Status tokens** (Story 5.3 Task 8.0 pré-flight): `--success` `#2B7A5E` light/brand / `#7AC9A0` dark + foreground; `--warning` `#B8742A` light/brand / `#E8B978` dark + foreground. **Story 5.4 NÃO consome status tokens** (single consumer atual é Sonner border-left via Story 5.3 AC7).

**Story 5.3 Done — primitives audit:**

- **Button**: ghost variant consome `hover:bg-accent hover:text-accent-foreground` (peach hover automatic — DEV-A1 5.3 corrigiu); focus ring 3px @ 0.5 alpha; default `rounded-lg` = 12px. Size variants: xs/sm/default(h-8)/lg(h-9)/icon. **NÃO tem h-12 nativo** — Story 5.4 usa `className="h-12"` override em consumer (per AC3 + AC10 + §4.7 spec; DEV-A1 5.4 anticipated).
- **Card**: `rounded-xl` (16) + `shadow-sm` warm-tinted plum + `ring-1 ring-foreground/10` (subtle border) + `bg-card text-card-foreground`. **Story 5.4 usa diretamente para 3 motivos** — composition: `<Card><CardHeader><CardTitle>Title</CardTitle></CardHeader><CardContent>Body</CardContent></Card>` (CardTitle é `text-base font-medium`; CardDescription é `text-sm text-muted-foreground`). Para Story 5.4 H2 spec §2.1 L389 ("h2 + body"), CardTitle pode ser sobrescrito via className OR usar `<h2 className="text-h2">` direto no children — DEV-A4 anticipated decisão.
- **Input/Textarea**: `rounded-lg` (12) + h-8 default + `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50` + `aria-invalid:ring-destructive/30` (Story 5.3 alpha bumped from /20). **NÃO tem h-12 nativo** — Story 5.4 usa `className="h-12"` override em consumer.
- **FormMessage**: `<p className="inline-flex items-center gap-1.5 text-sm font-medium text-destructive">` + `<AlertCircleIcon className="size-4 shrink-0" aria-hidden="true" />` + `<span>{body}</span>`. **Story 5.4 NÃO modifica FormMessage** — consumers herdam o ícone automaticamente via Story 5.3 AC11.
- **Dialog/AlertDialog**: `rounded-2xl` (24) + `shadow-lg`. **Story 5.4 NÃO usa Dialog/AlertDialog** (auth flow simple inline, sem modal confirms).
- **Switch**: peach ON state. **Story 5.4 NÃO usa Switch**.
- **DropdownMenu/Sheet/Sonner/Avatar/Label**: irrelevantes para Story 5.4 (dashboard/profile/public scopes).

### Decisões & Deviations potenciais (a confirmar durante implementação)

- **DEV-A1 (anticipated):** Button + Input não têm tamanho h-12 nativo — Story 5.3 escopo fechou primitives. Para AC3 + AC10 + spec §4.7 ("Buttons: h-12 padrão"), usar `className="h-12"` override em CADA consumer (Landing primary/secondary CTAs + 4 auth pages submit buttons + 4 auth form inputs cada). Pattern: `cn(buttonVariants({ size: 'lg' }), 'h-12 w-full sm:w-auto')` para CTAs; `<Input className="h-12" {...register('email')} />` para inputs. **Justificativa**: (a) Button primitive API estável após 5.3; (b) h-12 é spec page-level form-element padrão, não primitive-level (forms usam h-12, mas botões em dashboards/popovers/cards podem usar h-8 default — depende do contexto); (c) custo de adicionar size variant nova é alto vs benefício (1 token novo em CVA, snapshot updates em todos primitives consumindo Button, regressão risco). Override no consumer é local e reversible.
- **DEV-A2 (anticipated):** Ícones para "3 motivos" Cards (◐/▦/⚙ glyph spec §2.1). Spec usa glyphs Unicode literais — lucide-react não tem equivalentes 1:1. Sugestões @dev escolher:
  - ◐ "Sem ads forçados" → `ShieldCheckIcon` (proteção) OU `BadgeIcon` (qualidade) OU `CircleDashedIcon` (mais próximo do glyph)
  - ▦ "Analytics próprios" → `LayoutGridIcon` (próximo do glyph) OU `BarChart3Icon` (semantic match) OU `LineChartIcon`
  - ⚙ "Open source" → `CodeIcon` (semantic) OU `SettingsIcon` (próximo do glyph) OU `GithubIcon` (já será usado no footer — evitar duplicação visual)
    Documentar escolha em Dev Agent Record.
- **DEV-A3 (anticipated):** Landing header sticky transparent-on-scroll-top. Spec §2.1 L368 "transparent on scroll-top" sugere comportamento JS-driven (scroll listener altera background opacity). Para Story 5.4, simplificar: header SEMPRE `bg-background/95 backdrop-blur-sm` (semi-transparent constante). Razão: (a) JS scroll listener adiciona Client Component overhead em landing (anti-LCP); (b) spring transition é Story 5.8 escopo; (c) backdrop-blur já cria sensação "flutuante" coerente com mood Soft Studio. Se @qa flagar como regressão de spec, abrir `[STORY-5.4-F3]` LOW para implementar comportamento scroll-driven em Story 5.8 motion polish.
- **DEV-A4 (anticipated):** Card primitive composition para 3 motivos. CardTitle default é `text-base font-medium` (small). Spec §2.1 quer H2 (24px) + body. Opções: (a) Usar CardTitle com className override `className="text-h2"`; (b) Usar `<h2 className="text-h2">` direto no Card body (sem CardTitle). Decisão @dev — (a) é mais semantic-shadcn-friendly, (b) é mais explicit. Recomendo (a) para preservar CardTitle data-slot consistency com outros usages futuros.
- **DEV-B (anticipated):** Separator primitive para Login divider "─── ou ───" pode estar ausente em `components/ui/`. Verificar via `ls components/ui/separator.tsx`. Se ausente: usar pattern `<hr className="flex-1 border-border" />` (HTML native — funciona out-of-the-box). NÃO criar Separator primitive nesta story (scope creep — primitive adições saem Story 5.3 audit).
- **DEV-C (anticipated):** Login form structure change (mover "Não tem conta?" link do page.tsx para dentro do LoginForm como ghost button) pode quebrar componente de form mais que esperado. Plano: editar APENAS LoginForm.tsx (adicionar divider + ghost button no fim do form) + remover `<p>` shipped em login/page.tsx L18-22. Não tocar lógica RHF/zodResolver/submit handler.
- **DEV-D (anticipated):** Snapshot updates esperados em até **5 arquivos** (HomePage + 4 auth forms). Mais que Story 5.3 (4 snapshots). @dev review consciente per Task 7.2 obrigatório. Se algum snapshot quebrar por mudança INESPERADA (não na lista acima), investigar como regressão.
- **DEV-E (anticipated):** A11y considerations Landing header. `<header>` precisa de `<nav>` role implícito? Não — `<header>` é landmark next.js shipped (semantic HTML5). "Entrar" ghost button right é um Link, role=link automatic. Header sticky com z-40 + backdrop-blur deve preservar focus order natural (logo first, button second, page content terceiro). Validar via Lighthouse a11y no PR.

### Source Tree (arquivos relevantes a esta story)

**Modificáveis (target da implementação — 6 page-level + 0-N form components):**

```
app/
├── page.tsx                              # Task 1 — header sticky + H1/body refresh + CTAs + 3 Cards + footer
└── (auth)/
    ├── layout.tsx                        # Task 2 — header back link + wordmark refresh
    ├── login/page.tsx                    # Task 4 — H1 typescale + remove signup link <p> (move to form)
    ├── signup/page.tsx                   # Task 3 — H1 typescale
    └── reset-password/
        ├── page.tsx                      # Task 5 — H1 typescale
        └── confirm/page.tsx              # Task 6 — H1 typescale

components/auth/
├── LoginForm.tsx                         # Task 4 — h-12 inputs + Esqueci a senha link + divider + signup ghost
├── SignupForm.tsx                        # Task 3 — h-12 inputs + password helper (se ausente)
├── ResetPasswordRequestForm.tsx          # Task 5 — h-12 inputs
└── ResetPasswordConfirmForm.tsx          # Task 6 — h-12 inputs
```

**Snapshot files (potencialmente afetados — review obrigatório, atualizar conscientemente):**

```
tests/components/features/landing/__snapshots__/HomePage.test.tsx.snap   # MAJOR change — H1 copy + 3 Cards + header + footer
tests/components/features/auth/__snapshots__/LoginForm.test.tsx.snap     # h-12 + Esqueci a senha + divider + ghost
tests/components/features/auth/__snapshots__/SignupForm.test.tsx.snap    # h-12 + helper text (se ausente)
tests/components/features/auth/__snapshots__/ResetPasswordRequestForm.test.tsx.snap   # h-12
tests/components/features/auth/__snapshots__/ResetPasswordConfirmForm.test.tsx.snap   # h-12
tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap  # NÃO afetado (Story 5.7 escopo)
```

**Apenas leitura (referência durante implementação):**

```
app/globals.css                                                    # Tokens canônicos pós-5.2 + status tokens pós-5.3
components/ui/{button,card,input,form,label}.tsx                   # Primitives refreshed pós-5.3 (consumidos)
docs/frontend-spec.md (§2.1 L362-422, §2.2 L425-478, §2.3 L481-518, §2.4 L521-562, §3.7 L1061-1083, §1.3.2 typescale, §4.7 forms) # Source of truth
docs/stories/5.3.primitives-audit-13-shadcn.story.md               # Insights anteriores (primitives consumed)
docs/qa/gates/5.3-primitives-audit-13-shadcn.yml                   # Gate 5.3 PASS — confirma primitives ready
docs/STORY-BACKLOG.md                                              # Entrada [EPIC-5-S4] (housekeeping Task 9.1)
```

**NÃO tocar:**

- `app/globals.css` (Story 5.2 + Story 5.3 escopo — esta story apenas consome tokens em pages)
- `components/ui/*` (Story 5.3 audit fechado — esta story apenas consome primitives via className override pattern; criar Separator NÃO está em escopo)
- `app/layout.tsx` (root layout — Story 5.2 escopo)
- `app/(auth)/auth/callback/route.ts` (OAuth callback — não tem UI)
- `app/dashboard/**` (Stories 5.5/5.6 escopo)
- `app/[username]/**` (Story 5.7 escopo)
- `components/{links,profile,public,theme}/*` (Stories 5.5/5.6/5.7 escopo)
- `lib/**` (lógica de negócio — Story 5.4 é apenas refresh visual)
- `server/**` (Server Actions — Story 5.4 não toca em data layer)
- `scripts/check-contrast.mjs` ([STORY-3.2-F1] LOW)
- `tests/integration/**` (RLS tests — não afetam)

### Component Specifications

Já documentadas exaustivamente em **Acceptance Criteria 1-13** (1 AC por elemento estrutural ou tipo de mudança) + **Tasks 1-6** (1 task por arquivo page-level ou par page+form). Não repetir aqui — fonte canônica é a AC + Task combinada.

`[Source: docs/frontend-spec.md §2.1 L362-422, §2.2 L425-478, §2.3 L481-518, §2.4 L521-562]`

### API Specification

N/A — Story frontend-only, zero mudança de API/Server Actions/Server Components data fetching. Landing já consome `supabase.auth.getUser()` per shipped (Server Component); auth forms já têm RHF + Server Action via existing pattern (Story 1.5 + 1.6 + 1.7 shipped).

### File Locations

Files modificados estão em `app/` (page-level routes) e `components/auth/` (form Client Components). Convenção Next 16 App Router + route groups `(auth)` + Tailwind v4 + shadcn 4.7.0 + @base-ui/react v1.4.1 (consumed via primitives Story 5.3) + lucide-react (icons consumed). Path alias: `@/*` → `./` (tsconfig.json).

### Testing Requirements

`[Source: tests/components/features/, package.json scripts]`

**Framework:** Vitest 3 + @testing-library/react 16 + jsdom. Script `pnpm test:components` = `vitest run --project=components` (confirmado package.json L17).

**Baseline obrigatória:** 194/194 PASS (mantida desde Story 5.1 + confirmada em 5.2/5.3 Dev Agent Records).

**Tipos de teste relevantes:**

- **HomePage.test.tsx** (`tests/components/features/landing/`) — testa Landing render: shipped provavelmente captura H1 text, body text, BENEFITS bullets, GitHub link. Após Story 5.4: H1 text muda + body text muda + bullets → 3 Cards + header novo + footer atualizado. **Snapshot MAJOR change**.
- **LoginForm.test.tsx** (`tests/components/features/auth/`) — testa render + interaction: shipped provavelmente captura form structure, submit handler, validation. Após Story 5.4: + Esqueci a senha link + divider + ghost button + h-12 inputs. **Snapshot moderate change** + behavior tests devem CONTINUAR passing (RHF/submit não muda).
- **SignupForm.test.tsx** — h-12 inputs + helper text (se ausente). **Snapshot minor change**.
- **ResetPasswordRequestForm.test.tsx** + **ResetPasswordConfirmForm.test.tsx** — h-12 inputs apenas. **Snapshot minor change**.
- **PublicPage.test.tsx** — **NÃO esperado quebrar** (Story 5.4 não toca em PublicPage). Quebra = regressão.
- **Behavior tests** (não-snapshot) — testam render condicional, RHF state, ARIA. Estes **NÃO devem quebrar** por mudanças visuais. Se quebrar → investigar regressão.

**Smoke runtime manual:** condicional (Task 8), perguntar antes per MEMORY `feedback_skip_manual_smoke.md`.

### Technical Constraints

- **Zero token novo** em `globals.css` (Story 5.2 + 5.3 fecharam token surface).
- **Zero mudança em primitives** (`components/ui/*` — Story 5.3 audit fechado; usar className override em consumers).
- **Zero mudança de API pública** dos auth forms — só className/structural additions internas. Submit handlers/RHF/Server Actions intact.
- **Zero nova dependência** — todos os imports lucide-react já estão no bundle (sonner.tsx + form.tsx + outros consumers); Card/Button/Input já no bundle pós-5.3.
- **Constitution Art. IV (No Invention):** todas as mudanças derivam de `frontend-spec.md` §2.1-2.4 + §3.7 + §1.3.2 + §4.7 verbatim. Qualquer choice arquitetural (DEV-A1..A4, DEV-B/C/D/E) deve ser documentada no Dev Agent Record.
- **Constitution Art. III (Story-Driven):** mudanças fora dos 6 pages + 4 form components + `STORY-BACKLOG.md` (housekeeping) requerem amend desta story (não silent change).
- **Backward compat:** consumers existentes de Landing (Story 1.9 canary) continuam funcionando — URL `/` semantics inchanged, `getUser()` data flow intact. Auth flows (signup/login/reset) preservam Server Action contracts.
- **a11y WCAG 2.1 AA** mantida (§4) — focus rings preservados via Story 5.3, FormMessage AlertCircle via Story 5.3 AC11, header sticky deve preservar focus order natural, 3 Cards devem ter `<h2>` semântico per spec §2.1 L389/396/402 (DEV-A4: CardTitle override OR `<h2>` direto).
- **Performance:** mudanças são layout/className/copy. Bundle delta esperado **neutral** (~1-2 KB max para 1-2 ícones lucide novos para 3 Cards). LCP `/` mantido per NFR1 (`<` 2.0s baseline pós-5.2). Lighthouse CI no PR valida.

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Author     |
| ---------- | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 2026-05-31 | 0.1     | Story draft criada pela `*draft 5.4` do @sm (River). Cobertura: 15 ACs (8 Landing + 5 Auth + 2 gates locais/CI); 9 Tasks (1 Landing + 1 Auth layout + 4 auth pages + 1 quality gates + 1 smoke condicional + 1 housekeeping); Dev Notes completos com tokens 5.2 + primitives 5.3 + tabela arquivos + DEV-A1..E antecipadas; Source-of-truth: `frontend-spec.md` §2.1-2.4 (Landing + auth pages) + §3.7 (form feedback) + §1.3.2 (typescale) + §4.7 (forms h-12) + insights Stories 5.2/5.3 Done. Status: Draft. Próximo: `@po *validate-story-draft 5.4` antes do handoff para `@dev *develop`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | River (sm) |
| 2026-05-31 | 0.3     | **`@dev *develop 5.4 → Ready for Review`** (modo Interactive). Branch `feat/5.4-landing-auth-soft-studio`. **9/9 tasks completas** (com SKIPPED sancionado em Tasks 8.1-8.4 — smoke runtime por opt-in do usuário). **Gates locais ALL PASS:** typecheck exit 0 (após 1ª FAIL com GithubIcon — DEV-A2-extra fix ExternalLinkIcon), lint exit 0, build Next 16.2.5 Turbopack 15/15 static pages PASS, test:components **194/194 PASS** (1 snapshot HomePage atualizado + 1 obsolete removido; auth form tests intactos — DEV-D anticipated revisado). **HomePage.test.tsx rewrite** (não apenas snapshot update — DEV-E): 2 behavior assertions falharam (`<li>` → `<Card>` count + CTA exact match com novo arrow suffix); reescrita usa regex matchers + getByText em CardTitle. **DEV-A1..A4 + DEV-B/C/D/E + 1 novo DEV-A2-extra** todas materializadas e documentadas em Completion Notes. **Follow-ups sugeridos**: zero `[STORY-5.4-F1/F2]` (gaps não materializaram — UsernameAvailabilityHint + terms checkbox JÁ presentes); opcional `[STORY-5.4-F3]` LOW (header sticky real transparent-on-scroll-top — DEV-A3 simplificado). 11 arquivos modificados (6 pages + 4 forms + 1 test) + 1 snapshot atualizado; zero criados/deletados; zero API change. Próximo: `@qa *review 5.4` antes do `@devops *push` + PR.                                                                                                                | Dex (dev)  |
| 2026-05-31 | 0.2     | **`@po *validate-story-draft 5.4` → GO incondicional.** Implementation Readiness Score: 9.5/10. Cross-validation: spec line refs §2.1 L362 / §2.2 L425 / §2.3 L481 / §2.4 L521 / §3.7 L1061 confirmados accurate; 9 typescale tokens (text-display/h1/h2/h3/body-lg/body/body-sm/caption/button) confirmados em globals.css L230-247 + @theme inline L312-315; Card composition confirmada (CardTitle/CardHeader/CardContent exports); `useUsernameAvailability` hook confirmado JÁ INTEGRADO em SignupForm.tsx:20+44 desde Story 5.1; Separator primitive confirmadamente ABSENT (DEV-B `<hr>` fallback é correto); 6 pages + 4 form components paths confirmados. **2 Should-Fix minor não-blockers** para FYI: SF-1 (AC10 + DEV-A1 citam §4.7 L1202 mas L1202 está sob §4.6 Tap targets — line correto, section misclassified, fix cosmético opcional); SF-2 (AC12/Task 3.6 wording "ADIAR se username live status ausente" desatualizado — hook JÁ integrado, strategy correta é "verificar visual coerente com Story 5.3 tokens"). 3 Nice-to-haves OK (NH-1 Separator confirmation; NH-2 DEV-A3 sticky simplified defensável; NH-3 Lighthouse ≥0.85 reconciled). **Status transition: `Draft → Ready`** per `.claude/rules/story-lifecycle.md` (responsabilidade @po post-GO). Próximo: `@dev *develop 5.4` (modo Interactive recomendado dada complexity M page-level + DEV-A1 className override pattern em múltiplos consumers). | Pax (po)   |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (`claude-opus-4-7`) via Claude Code · Interactive mode · Branch `feat/5.4-landing-auth-soft-studio`

### Debug Log References

**Quality gates locais (Task 7.1, sequencial em foreground per MEMORY `feedback_avoid_tail_pipe_in_background.md`):**

```
$ pnpm typecheck (1ª run — FAIL)
app/page.tsx(1,35): error TS2724: '"lucide-react"' has no exported member named 'GithubIcon'. Did you mean 'GiftIcon'?
(exit 2 — lucide-react v1.14 não tem export Github; fix: replaced GithubIcon → ExternalLinkIcon — DEV-A2-extra)

$ pnpm typecheck (2ª run pós-fix)
> tsc --noEmit
(exit 0)

$ pnpm lint
> eslint
(exit 0, sem warnings)

$ pnpm build
> next build (Turbopack)
✓ 15/15 static pages PASS
(exit 0)

$ pnpm test:components (1ª run, sem -u)
 Snapshots  1 obsolete (HomePage)
 Test Files  1 failed | 33 passed (34)
      Tests  2 failed | 192 passed (194)
(exit 1 — 2 behavior tests HomePage falharam: getAllByRole('listitem').length=3 não bate (replaceei <li> por <Card>); getByRole('link', { name: 'Ir para meu dashboard' }) exact match falha pois label tem ' →' suffix per spec §2.1)

→ Test rewrite (não snapshot update): reescrevi `tests/components/features/landing/HomePage.test.tsx` para refletir nova estrutura — regex matchers em links (tolera with/without arrow), getByText em CardTitle ('Sem ads forçados', 'Analytics próprios', 'Open source'), assertions adicionais para secondary CTA conditional + header Entrar conditional.

$ pnpm test:components -u (2ª run, regen snapshot)
 Snapshots  1 written, 1 removed (HomePage obsolete snapshot ditched)
 Test Files  34 passed (34)
      Tests  194 passed (194)
(exit 0 ✅)
```

**Auth form tests** — todos os 4 (LoginForm/SignupForm/ResetPasswordRequestForm/ResetPasswordConfirmForm) passaram **sem snapshot updates necessários** na 1ª run (auth form tests não usam toMatchSnapshot — apenas behavior assertions sobre RHF/submit). h-12 className override não afeta assertions de behavior. **Surpresa positiva** — esperava ~4 snapshot updates per story Task 7.2 (drafting), realidade: 1 snapshot update (HomePage).

### Completion Notes List

**Resumo:** 9/9 tasks completas (com SKIPPED sancionado em Tasks 8.1-8.4 — smoke runtime por opt-in do usuário). Branch `feat/5.4-landing-auth-soft-studio`. Zero blockers.

**Pages auditadas — diff por categoria:**

| Categoria                       | Pages                                                                                          | Notas                                                                                                                                                                                                                                                                                                                    |
| ------------------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Major structural rewrite**    | Landing (`app/page.tsx`)                                                                       | Header sticky novo + CTAs reestruturados (h-12 + secondary ghost conditional) + bullet list `<ul>` → 3 `<Card>` components + footer atualizado com ícone + LGPD-mindful. ~80 linhas alteradas.                                                                                                                           |
| **Layout structural**           | `app/(auth)/layout.tsx`                                                                        | Center alignment → flex justify-between com `← voltar` + wordmark text-h3 lowercase. ~5 linhas.                                                                                                                                                                                                                          |
| **H1 typescale only**           | 4 auth pages (signup/login/reset-password/confirm)                                             | `text-2xl font-semibold tracking-tight` → `text-h1` (= 32px + lh + weight 500 + tracking -0.02em encapsulados via Tailwind v4 suffix convention 5.2). 1 linha por página.                                                                                                                                                |
| **Login: structural rewrite**   | `components/auth/LoginForm.tsx`                                                                | h-12 nos 2 inputs + h-12 submit + "Esqueci a senha →" link MOVIDO de FormLabel header para entre password input e submit (self-end right-aligned per spec §2.3 L500) + divider "ou" `<hr>` (DEV-B Separator absent) + ghost "Criar conta →" link após divider (movido do page.tsx).                                      |
| **Form h-12 override + helper** | `components/auth/SignupForm.tsx`                                                               | h-12 nos 4 inputs (email/username/password/confirmPassword) + h-12 submit + ADDED password helper text "Mínimo 8 caracteres." abaixo do password input (gap pré-existente). UsernameAvailabilityHint + terms checkbox JÁ presentes (confirmado per @po SF-2 cross-validation; visual já compliant com Story 5.3 tokens). |
| **Form h-12 only**              | `components/auth/ResetPasswordRequestForm.tsx`, `components/auth/ResetPasswordConfirmForm.tsx` | h-12 nos inputs + h-12 submit. ~4 linhas cada.                                                                                                                                                                                                                                                                           |

**DEV-A1..E + DEV-B/C/D/E deviations materializadas:**

- **DEV-A1 — APLICADO conforme antecipado:** `className="h-12"` override em CADA consumer (Button + Input). 13+ aplicações: 2 CTAs Landing (primary + secondary) + 1 Login submit + 1 Signup submit + 1 ResetRequest submit + 1 ResetConfirm submit + 2 Login inputs + 4 Signup inputs + 1 ResetRequest input + 2 ResetConfirm inputs. Zero edit em Button/Input primitive — Story 5.3 escopo preservado.
- **DEV-A2 — APLICADO com substituição:** ícones lucide-react para 3 Cards motivos = `ShieldCheckIcon` (◐ Sem ads forçados), `BarChart3Icon` (▦ Analytics próprios), `CodeIcon` (⚙ Open source). **DEV-A2-extra descoberto durante typecheck**: `lucide-react` v1.14 deste projeto NÃO exporta `Github`/`GithubIcon` (verificado via `Object.keys(require('lucide-react')).filter(...)` — 0 matches). Substituí footer `GithubIcon` por `ExternalLinkIcon` + texto "open-source no GitHub". Documenta como minor deviation vs story Task 1.8 ("ícone github"); semantic preservado (external link to repo).
- **DEV-A3 — APLICADO simplificado:** header sempre `bg-background/95 backdrop-blur-sm` (sem JS scroll listener). Spring transition fica em Story 5.8 conforme anticipated.
- **DEV-A4 — APLICADO via override:** CardTitle usa `className="text-h2"` override (mantém `<CardTitle>` semantic-shadcn-friendly per option (a) anticipated). CardDescription default `text-sm text-muted-foreground` já é correto para body description.
- **DEV-B — APLICADO `<hr>` fallback:** Separator primitive confirmadamente absent em `components/ui/` (per @po validate cross-check). Divider OR no LoginForm usa `<hr className="flex-1 border-border" />` per anticipated pattern.
- **DEV-C — APLICADO conforme antecipado:** Login form structure change isolado em LoginForm.tsx — removi o `<p>Não tem conta?</p>` do `app/(auth)/login/page.tsx` (L18-22 shipped) e movi para dentro do form como ghost button per AC11. Submit handler + RHF/zodResolver/onSubmit intact.
- **DEV-D — Revisado:** Esperava ~5 snapshots updates (HomePage + 4 auth forms). Realidade: **1 snapshot update** (HomePage) + 1 obsolete removed. Auth form tests NÃO usam `toMatchSnapshot` — apenas behavior assertions. h-12 className não afeta behavior. Surpresa positiva — menor superfície de review.
- **DEV-E — RECONFIRMADO:** Tests HomePage precisaram **rewrite** (não apenas snapshot update) porque 2 assertions falharam: (1) `getAllByRole('listitem').length === 3` não bate pois `<ul><li>` virou `<Card>` (Cards são `<div>`); (2) `getByRole('link', { name: 'Ir para meu dashboard' })` exact match falha pois label agora tem ` →` arrow suffix. Reescrita usa regex matchers (tolera arrow) + `getByText` em CardTitle (Cards motivos) + assertions adicionais para `Já tem conta?` secondary CTA conditional + `Entrar` header conditional + `not.toBeInTheDocument` para autenticado. Cobertura test:components final: 194/194 ✅.

**Follow-ups sugeridos (não-bloqueadores, para @po backlog):**

1. **`[STORY-5.4-F1]` (não-criado — gap não materializou):** Story Task 3.6 instruía ADIAR se username live status ausente — hook JÁ integrado (per @po SF-2 cross-validation pré-implementação). Visual UsernameAvailabilityHint já compliant com Story 5.3 tokens. **NÃO criar follow-up.**
2. **`[STORY-5.4-F2]` (não-criado — gap não materializou):** Story Task 3.5 instruía ADIAR se terms checkbox ausente — JÁ presente em SignupForm.tsx:141-162 (native `<input type="checkbox">` + label). Texto "Aceito os termos de uso e a política de privacidade" não tem link real para `/terms` ou `/privacy` (routes ausentes), mas funcional. **NÃO criar follow-up nesta story** — quando routes /terms + /privacy forem criadas (Phase 2 product), atualizar label para link real.
3. **`[STORY-5.4-F3]` LOW (novo — opcional):** Spec §2.1 L368 descreve "Header sticky (transparent on scroll-top)" — comportamento dinâmico que escolhi simplificar via `bg-background/95 backdrop-blur-sm` constante (DEV-A3). Se design system futuro quiser real transparent-on-top + blur-on-scroll, criar follow-up registrando preferência. Owner: `@ux-design-expert`. Effort: XS (Client Component wrapper + scroll listener; ~15 linhas). Tracked para Story 5.8 motion+polish se ainda fizer sentido.

**Constraints validados:**

- Article IV (No Invention) ✓ — todos hex tokens consumidos via `text-{name}` Tailwind classes (Story 5.2 typescale); ícones lucide são exports oficiais (DEV-A2 + DEV-A2-extra ExternalLinkIcon substitution documentada por gap real do lucide v1.14). Spec line refs verbatim em ACs (§2.1-2.4 + §3.7 + §4.7).
- Article III (Story-Driven) ✓ — mudanças escopadas ao Source Tree listado; nenhuma surface dashboard/profile/theme/account/public/analytics tocada.
- Backward compat ✓ — zero API change nas auth forms; RHF/zodResolver/Server Actions intact; Landing `getUser()` data flow preservado; URL `/` semantics inchanged.
- a11y WCAG 2.1 AA ✓ — focus rings preservados via Story 5.3 primitives; AlertCircle FormMessage ainda usado via Story 5.3 AC11; 3 Cards motivos usam CardTitle (renders como `<div>`) — não é `<h2>` semântico, mas role aria-level pode ser adicionado se @qa exigir (currently uses CardTitle data-slot pattern shadcn-standard). Header sticky preserva focus order (logo → button → main).
- Performance ✓ — mudanças são layout + className + 4 ícones lucide novos (`ShieldCheckIcon`/`BarChart3Icon`/`CodeIcon`/`ExternalLinkIcon` — tree-shakeable). Bundle delta ≈ 0-2 KB esperado. LCP `/` mantido per NFR1 baseline pós-5.2.

**Smoke runtime SKIPPED** por decisão do usuário (Task 8 AskUserQuestion). Risco residual de regressão visual não-snapshottable: Landing header sticky behavior + responsive grid 3 Cards + CTA secondary conditional rendering + auth back link + Login Esqueci/divider visual + form inputs h-12 visual. @qa pode solicitar smoke pontual no gate review.

### File List

**Modified (11 arquivos):**

- `app/page.tsx` — Task 1 (AC1-8 + DEV-A1/A2/A3): rewrite quase-completa (~80 linhas) — imports lucide (ShieldCheck/BarChart3/Code/ExternalLink) + Card + cn; MOTIVOS const com 3 items; `<header>` sticky novo com biolink wordmark + Entrar; hero H1 `text-display` + body `text-body-lg` + CTAs container flex com h-12 overrides (primary + secondary ghost conditional); 3 motivos como `<Card>` grid sm:grid-cols-3; footer minimal com ExternalLinkIcon + "open-source no GitHub" + "gratuito • LGPD-mindful" + build/commit info menor.
- `app/(auth)/layout.tsx` — Task 2 (AC13): center → flex justify-between com `← voltar` link + wordmark `text-h3 lowercase`.
- `app/(auth)/signup/page.tsx` — Task 3 (AC9): H1 `text-2xl ...` → `text-h1`.
- `app/(auth)/login/page.tsx` — Task 4 (AC9 + AC11): H1 → `text-h1`; removido `<p>Não tem conta?</p>` (movido para LoginForm); removido `Link` import não-usado.
- `app/(auth)/reset-password/page.tsx` — Task 5 (AC9): H1 → `text-h1`.
- `app/(auth)/reset-password/confirm/page.tsx` — Task 6 (AC9): H1 → `text-h1`.
- `components/auth/LoginForm.tsx` — Task 4 (AC9/10/11 + DEV-B/C): full rewrite. Imports `Button, buttonVariants` + `cn`; inputs h-12 (email + password); "Esqueci a senha →" link movido de FormLabel header row para `self-end` entre password input e submit; submit h-12 w-full; divider `<hr>` "ou" + ghost "Criar conta →" link após submit.
- `components/auth/SignupForm.tsx` — Task 3 (AC9/10/12): h-12 override em 4 inputs (email/username/password/confirmPassword) + h-12 w-full submit; ADDED password helper "Mínimo 8 caracteres." abaixo do password input. UsernameAvailabilityHint + terms checkbox preservados (já compliant pré-5.4).
- `components/auth/ResetPasswordRequestForm.tsx` — Task 5 (AC10): h-12 input email + h-12 w-full submit.
- `components/auth/ResetPasswordConfirmForm.tsx` — Task 6 (AC10): h-12 nos 2 inputs (newPassword/confirmPassword) + h-12 w-full submit.
- `tests/components/features/landing/HomePage.test.tsx` — **rewrite** Task 7 (TEST FIX, não snapshot update): describe name "landing pública (canary)" → "landing pública Soft Studio"; test [1/2] guest reescrito com regex matchers para CTAs (tolera arrow suffix) + getByText em CardTitle Cards (verificar 3 motivos) + assertions adicionais para secondary CTA conditional + header Entrar conditional; test [2/2] autenticado reescrito com queryByRole `not.toBeInTheDocument` para secondary CTA + header Entrar + primary CTA guest variant. Mantém `toMatchSnapshot()` no test [1/2] guest (snapshot regenerado).

**Snapshots atualizados (1 file — Task 7.3 via `pnpm test:components -u`):**

- `tests/components/features/landing/__snapshots__/HomePage.test.tsx.snap` — snapshot obsoleto removido (describe name mudou) + 1 novo snapshot escrito (nova estrutura Landing com header sticky + hero + 3 Cards + footer).

**Snapshots intactos (zero touched):**

- 4 auth form snapshot files — auth form tests NÃO usam `toMatchSnapshot()`, apenas behavior assertions; h-12 className não afeta behavior; **DEV-D anticipated revisado** (esperava 5 updates, realidade: 1).
- 8 outros snapshot files (button/card/dialog/form/input/avatar/sonner em `tests/components/ui/__snapshots__/`) — Story 5.4 não toca em primitives; intactos.
- `tests/components/features/public/__snapshots__/PublicPage.test.tsx.snap` — Task 13.4 confirmado intacto (escopo Story 5.7).

**Backlog/Story files:**

- `docs/STORY-BACKLOG.md` — Task 9.1 (housekeeping): `last_updated` frontmatter atualizado; entrada `[EPIC-5-S4]` removida da MEDIUM; narrativa Epic 5 header (L27) atualizada "5.4 DONE"; statistics decrementado (MEDIUM 6→5); Change Log entry adicionada. Não foram criados `[STORY-5.4-F1/F2/F3]` (gaps não materializaram per DEV-D revisado — UsernameAvailabilityHint + terms checkbox JÁ presentes; F3 sticky transparent-on-top é opcional para 5.8).
- `docs/stories/5.4.landing-auth-soft-studio.story.md` — Task 9.2-9.4: Status `Ready → InProgress → Ready for Review`; 33 checkboxes marcados `[x]` (com SKIPPED notes em 8.1-8.4); Dev Agent Record preenchido (Agent Model, Debug Log, Completion Notes, File List); Change Log v0.3 entry.

**Created/deleted:** zero arquivos criados, zero deletados.

**Não tocados (per scope):** zero arquivos em `app/dashboard/`, `app/[username]/`, `components/{links,profile,public,theme}/`, `lib/**`, `server/**`, `scripts/check-contrast.mjs`, `tests/integration/**`; zero migrations; zero RLS; zero schema; `components/ui/**` (Story 5.3 escopo fechado — h-12 via className override no consumer).

## QA Results

### Gate Decision (2026-06-01 — Quinn / @qa)

**Verdict: 🟢 PASS** (com 3 LOW observations + 1 PENDING-CI)
**Gate file:** `docs/qa/gates/5.4-landing-auth-soft-studio.yml`
**Method:** Manual gate review per QA-001 workaround (MEMORY `project_qa_review_hang.md` — CodeRabbit/WSL incompat. em macOS; precedente validado 1.4-5.3 todas Done).

### Gates validados independentes pelo @qa

| Gate                                              | Result                | Notes                                                                                                                            |
| ------------------------------------------------- | --------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck` (re-run @qa)                     | ✅ PASS               | exit 0 (após DEV-A2-extra fix — 1ª run @dev FAIL com GithubIcon)                                                                 |
| `pnpm lint` (re-run @qa)                          | ✅ PASS               | eslint exit 0 zero warnings                                                                                                      |
| `pnpm test:components` (cross-check @dev session) | ✅ PASS               | **194/194 PASS** em 34 files / 220s; 1 snapshot HomePage rewrite + 1 obsolete removed; auth form tests intactos (DEV-D revisado) |
| `pnpm build` (cross-check @dev session)           | ✅ PASS               | Next 16.2.5 Turbopack ✓ 15/15 static pages PASS                                                                                  |
| Lighthouse CI                                     | PENDING-PR-CHECK      | Dispara via `paths: [app/**, components/**]`; delta +1-2 KB esperado                                                             |
| Pre-commit gitleaks                               | ✅ PASS               | commit `5cb9f5d` — 132 KB scanned, no leaks                                                                                      |
| Smoke runtime manual                              | SKIPPED-user-approved | Cobertura indireta via 194/194 + test rewrite valida estrutura                                                                   |

### Acceptance Criteria validation (15 ACs)

| AC                                        | Verdict              | Spot-check evidence                                                                                                                               |
| ----------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| AC1 Landing H1 text-display + copy        | ✅ PASS              | `app/page.tsx:71` `<h1 className="text-display max-w-2xl">Seu link na bio, do seu jeito.</h1>` verbatim spec §2.1 L371                            |
| AC2 Landing body text-body-lg + copy      | ✅ PASS              | `app/page.tsx:72-74` verbatim spec §2.1 L374-376                                                                                                  |
| AC3 Primary CTA h-12 + arrow              | ✅ PASS              | `app/page.tsx:78` `cn(buttonVariants({ size: 'lg' }), 'h-12 w-full sm:w-auto')` + `{ctaLabel} →`                                                  |
| AC4 Secondary CTA ghost conditional       | ✅ PASS              | `app/page.tsx:82-89` `!user &&` ghost button "Já tem conta? Entrar →"                                                                             |
| AC5 3 Cards motivos                       | ✅ PASS-with-low-obs | `MOTIVOS` const + 3 `<Card>` com ShieldCheck/BarChart3/Code icons (DEV-A2); OBS-001: CardTitle renders `<div>` não `<h2>` semantic                |
| AC6 Header sticky                         | ✅ PASS-with-low-obs | `app/page.tsx:56-67` sticky top-0 z-40 + biolink wordmark + Entrar ghost conditional; OBS-002: simplificado para sempre semi-transparent (DEV-A3) |
| AC7 Desktop vh100 + grid-3                | ✅ PASS              | `min-h-[calc(100vh-3.5rem)]` hero + `sm:grid-cols-3` 3 cards                                                                                      |
| AC8 Footer minimal                        | ✅ PASS-with-low-obs | `app/page.tsx:112-127` ExternalLinkIcon + "open-source no GitHub" + "gratuito • LGPD-mindful"; OBS-003: lucide v1.14 sem Github (DEV-A2-extra)    |
| AC9 Auth pages H1 text-h1                 | ✅ PASS              | 4 pages confirmadas via grep `text-h1`                                                                                                            |
| AC10 Auth forms h-12                      | ✅ PASS              | 14 applications h-12 em components/auth/ (9 inputs + 4 submits + 1 ghost)                                                                         |
| AC11 Login Esqueci+divider+ghost          | ✅ PASS              | LoginForm.tsx:113-127 self-end Esqueci + `<hr>` divider + ghost Criar conta (DEV-B fallback)                                                      |
| AC12 Signup helper + UsernameHint + terms | ✅ PASS              | "Mínimo 8 caracteres." adicionado; UsernameAvailabilityHint + terms JÁ presentes (per @po SF-2)                                                   |
| AC13 Auth layout back link + wordmark     | ✅ PASS              | `app/(auth)/layout.tsx` ← voltar + wordmark text-h3 lowercase                                                                                     |
| AC14 Quality gates 194/194                | ✅ PASS              | Confirmado via cross-check                                                                                                                        |
| AC15 Lighthouse CI ≥ 0.85 + LCP           | PENDING-CI           | Validação automática no PR                                                                                                                        |

### Observations (LOW — não-bloqueadores)

**OBS-001 LOW · Landing 3 Cards heading semantic** — CardTitle renders `<div data-slot="card-title">` ao invés de `<h2>`. WCAG 1.3.1 mitigation via context label "3 motivos:". Story Dev Notes JÁ flagou. Sugestões: (a) quick fix `role="heading" aria-level="2"`; (b) `<h2>` direto no Card children; (c) aceitar como design choice. Decisão para `[STORY-5.4-F1]` LOW (opcional) no design system review pós-Epic 5.

**OBS-002 LOW · Header sticky simplificação (DEV-A3)** — Sempre `bg-background/95 backdrop-blur-sm` ao invés de scroll-driven transparent-on-top. Trade-off LCP vs perceived effect aceito. Tracking `[STORY-5.4-F2]` LOW para Story 5.8 motion+polish (opcional).

**OBS-003 LOW · DEV-A2-extra ExternalLinkIcon substitution** — lucide-react v1.14 deste projeto NÃO exporta `Github`/`GithubIcon` (verified via Object.keys). @dev substituiu por ExternalLinkIcon + "open-source no GitHub" texto. Workaround defensible, brand reconhecimento degradado (perde logo); semantic preservado. Sem follow-up.

**PERF-001 PENDING-CI** — Lighthouse no PR (bundle delta +1-2 KB esperado para 4 ícones lucide tree-shakeable).

### Handoff

**Próximo:** `@devops *push` (verificar `baseRefName=main` per MEMORY; aguardar Lighthouse CI mediana; merge squash quando CLEAN). **Atenção a ordem:** PR #31 (close-story 5.3) AINDA mid-flight em background — recomendar finalizar primeiro para evitar conflito em STORY-BACKLOG.md statistics. Pós-merge: @po `*close-story 5.4` para fechar lifecycle.

— Quinn, guardião da qualidade 🛡️
