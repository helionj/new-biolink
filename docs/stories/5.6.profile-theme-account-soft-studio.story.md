# Story 5.6: Profile + Theme + Account — Soft Studio Refresh + rebrand "Brand" → "Vibrante"

## Status

Done

## Executor Assignment

```yaml
executor: '@dev' # Tasks 1-8 (audit 3 page-level Profile/Theme/Account `app/dashboard/{profile,theme,account}/page.tsx` + 1 component rebrand `components/theme/ThemeSelector.tsx` consumindo tokens Story 5.2 + primitives Story 5.3 + insights Stories 5.4/5.5)
quality_gate: '@qa'
quality_gate_tools: [typecheck, lint, build, test:components]
complexity: 'M' # Frontend-primary. Superfície: 3 page-level (`app/dashboard/profile/page.tsx` + `app/dashboard/theme/page.tsx` + `app/dashboard/account/page.tsx`) + 1 rebrand UI-only (`components/theme/ThemeSelector.tsx` LABELS const + 3 description strings) + 0-N edits em `components/profile/{AvatarUpload,ProfileMetaForm,UsernameForm}.tsx` + `components/account/{ExportButton,DeleteAccountSection}.tsx` (apenas se audit revelar typescale hardcoded ou className legacy não-token). Zero migration, zero RLS, zero schema (Q3 §6 PRD v0.5 ratificado: `theme` enum em DB + `lib/theme.ts` THEMES const + `lib/supabase/types.ts` types.theme_preset preservados — rebrand é UI label-only). Zero Server Action novo (updateTheme + updateProfileMeta + updateUsername + uploadAvatar + exportAccountData + deleteAccount Stories 3.3/4.5/5.1 intactos). Tokens canônicos já em `app/globals.css` pós-5.2 (`--text-h1` 32px + `--text-body` + `--radius-xl` 16 + `--shadow-sm`) + primitives shadcn já alinhados pós-5.3 (Avatar `size-10 data-[size=lg]:size-10` ≠ spec 96px → DEV-A override className `size-24` em wrapper; Card +`shadow-sm warm-tinted` + Button ghost peach hover + Input destructive ring/30 + AlertDialog `rounded-2xl` + DropdownMenu refreshed). Mudanças localizadas: 3 H1 page-level (`text-xl`/`text-2xl` → `text-h1`) + subtitle copy verbatim spec L631/L701/L813 + AvatarUpload 96×96 (DEV-A) + ThemeSelector LABELS `'Brand'` → `'Vibrante'` + 3 description strings ("Lavender mist" / "Deep plum night" / "Lavender + peach pop" verbatim spec L706/L715/L724) + Profile sections opcional subsection labels ("Foto" / "Identidade" / "URL pública" — wireframe §2.6 L635/647/668). Risco material isolado: (a) tests `ThemeSelector.test.tsx` + `ThemeTokens.test.tsx` NÃO assertam string "Brand" (somente enum `'brand'` em `aria-checked`/Server Action call) — rebrand visual zero-impact tests; (b) snapshot tests inexistentes em `tests/components/features/{profile,theme,account}/__snapshots__/` (confirmar via `ls`; behavior tests usam queries semânticas); (c) Lighthouse CI `/dashboard/{profile,theme,account}` (authenticated routes — fora dos gate paths default `/` + `/[username]`); (d) lógica de negócio Server Actions Stories 3.3/4.5/5.1 NÃO tocadas — apenas refresh visual + label.
```

> **Rationale:** Story **Frontend-primary** — terceira das 4 stories page-level paralelizáveis (5.4-5.7) do refresh Soft Studio. Materializa **Phase 3** de `docs/frontend-spec.md` §5.3 ratificada em **PRD v0.5 §UX/Branding** (Change Log 2026-05-29) **+ Q3 §6 RESOLVED** (rebrand "Brand" → "Vibrante" UI-only; token + schema preservados). Tokens canônicos já estão em `app/globals.css` (Story 5.2 Done — paleta Soft Studio + radius scale + shadows + typescale 9 tokens) e primitives shadcn já estão alinhados (Story 5.3 Done — Button ghost peach hover, Card +shadow-sm warm-tinted, Input destructive ring /30, Switch peach ON state, Dialog/AlertDialog rounded-2xl, FormMessage AlertCircle, DropdownMenu refreshed, Sheet condicional radius). Esta story **consome** ambos nas 3 sub-pages autenticadas restantes (Profile/Theme/Account) + executa o rebrand UI-only "Brand" → "Vibrante" pendente desde a ratificação Q3 §6. Conforme `.claude/rules/agent-authority.md`, ajustes em pages + components são escopo do **@dev (Dex)** com gate **@qa (Quinn)** — mesmo padrão das Stories 3.2/3.3/5.2/5.3/5.4/5.5. `git push`/`gh pr create` permanecem **exclusivos de @devops** (Gage).

> **Backlog crossover:** `[EPIC-5-S6]` 🟡 MEDIUM (este item — `docs/STORY-BACKLOG.md`) será movido para ✅ DONE após merge (Task 9.1 — pattern Story 5.5). **Não bloqueia 5.7** (paralelizável — Analytics + Public page). **Bloqueia visualmente** 3 surfaces autenticadas restantes (Profile/Theme/Account ainda exibem typescale legacy + "Brand" label). `[STORY-5.3-F1]` LOW e `[STORY-5.2-F1]` LOW (amends spec warning/destructive ratios) **não tocados** — Story 5.6 não introduz uso novo de `--warning` em bg/text components nem `text-destructive` sobre brand bg (DeleteAccountSection já shipped destructive section Story 4.5).

> **Rebrand decisão (Q3 §6 ratified, DEV-B):** PRD v0.5 §UX/Branding (Change Log 2026-05-29) ratificou Q3 do `frontend-spec.md` §6 (L1384-1393): _"Manter 3 temas presets (FR12 obrigatório). Tema `brand` continua existindo como token + DB enum (compatibilidade com shipped rows), mas UI copy do `/dashboard/theme` rebranded para 'Vibrante' (vs 'Brand'). O conflito semântico é só de naming UI, não de funcionalidade. Renomear 'Brand' → 'Vibrante' em PT-BR resolve confusão sem refactor de schema/code. Action: Story 5.6 (Profile + Theme + Account) inclui mudar copy de 'Brand' → 'Vibrante' em `app/dashboard/theme/page.tsx` (card label) + descrição 'Lavender + peach pop'. Tokens e schema preservados."_. **Decisão:** rebrand vive em `components/theme/ThemeSelector.tsx:13-17` (LABELS const `Record<Theme, string>`) — única source-of-truth UI da label. Mudança: `brand: 'Brand'` → `brand: 'Vibrante'`. Justificativa: (a) preserva `Theme` type (= `Database['public']['Enums']['theme_preset']` em `lib/theme.ts:16`); (b) preserva enum DB (`theme_preset = ['light', 'dark', 'brand']` em migrations 0002/0003); (c) preserva `THEMES = ['light', 'dark', 'brand']` (`lib/theme.ts:18`); (d) preserva todas as Server Actions e testes integration que usam `'brand'` (12 matches em `tests/integration/{rls,server-actions}/`); (e) zero risk de breakage em rows shipped com `theme='brand'` em produção. Tradeoff aceito: dev-time mental gap "label diz Vibrante mas enum diz brand" mitigado via comentário JSDoc no LABELS const referenciando Q3 §6 PRD v0.5.

> **Avatar sizing decisão (DEV-A anticipated):** spec §2.6 L638-641 wireframe mostra avatar "96×96 circle". Audit `components/ui/avatar.tsx:13-20` confirma que primitive shadcn shipped Story 5.3 expõe apenas 3 variants: `default` (size-8 = 32px) / `sm` (size-6 = 24px) / `lg` (data-[size=lg]:size-10 = 40px). NENHUMA variant mapa para 96px. **Decisão:** override via className `size-24` (= 96px) no wrapper `<Avatar>` em `components/profile/AvatarUpload.tsx`, **NÃO** refactor de Avatar primitive (Story 5.3 audit fechado — preserva 13 primitives intactos per AC consistency). Pattern verbatim de Story 5.4 (h-12 className override em Input/Button — DEV-A1 da 5.4: "NÃO mexer em primitives — Story 5.3 escopo fechado"). Lado-efeito: `<AvatarImage render={<Image src={previewUrl} width={40} height={40} />}>` precisa bump `width={96} height={96}` para evitar blur em retina. JSDoc no override referenciando §2.6 L638-641.

## Story

**As a** dono do BioLink autenticado (e por extensão, todo usuário logado que edita perfil/troca tema/gerencia conta nas sub-pages `/dashboard/{profile,theme,account}` Stories 5.1/3.3/4.5),
**I want** que as 3 sub-pages restantes do dashboard autenticado (Profile com edit avatar 96×96 + display_name + bio + URL pública/username Story 5.1; Theme com radio-as-cards 3 presets + mini preview /@username Story 3.3 + rebrand UI "Brand" → "Vibrante" Q3 §6 PRD v0.5; Account com export JSON + delete account typed confirmation Story 4.5) renderizem **com os specs visuais e estruturais Soft Studio** (per `docs/frontend-spec.md` §2.6 + §2.7 + §2.9 + §1.3.2 typescale + §1.4.2 radius + §6 Q3 RESOLVED) — H1 32px `text-h1` em vez de `text-xl`/`text-2xl` legacy shipped Stories 5.1/3.3/4.5, subtitle copy verbatim spec L631/L701/L813, avatar 96×96 (DEV-A override) consumindo Avatar primitive Story 5.3 com size override classname, ThemeSelector LABELS rebranded ("Light" / "Dark" / "Vibrante" — DEV-B) + 3 description strings adicionadas ("Lavender mist" / "Deep plum night" / "Lavender + peach pop" verbatim spec L706/L715/L724), Profile sections opcionalmente refinadas com subsection labels ("Foto" / "Identidade" / "URL pública" — wireframe §2.6 L635/L647/L668), Account destructive section preservada (Story 4.5 shipped já com `border-destructive/30 bg-destructive/5` per spec §2.9 L832),
**so that** as 3 sub-pages autenticadas restantes parem de exibir typescale legacy (`text-xl font-semibold tracking-tight` em Profile/Theme; `text-2xl font-semibold` em Account) + label "Brand" inconsistente com PRD v0.5, e passem a expressar a identidade "warm creator studio" canônica do PRD v0.5 §UX/Branding **completando o refresh visual full-funnel autenticado** (Layout + Links Story 5.5 → Profile + Theme + Account esta 5.6) — desbloqueando coerência total do dashboard (5 surfaces alinhadas: layout chrome + Links + Profile + Theme + Account) e materializando a sexta página em paleta refreshed (após 5.2 tokens + 5.3 primitives + 5.4 pre-auth + 5.5 dashboard core + esta 5.6 dashboard sub-pages).

## Acceptance Criteria

> Todas as AC abaixo rastreiam para fontes canônicas — `docs/frontend-spec.md` v0.3 §2.6 (Profile L624-690) + §2.7 (Theme L693-743) + §2.9 (Account L806-845) + §1.3.2 (typescale tokens L242-243) + §1.4.2 (radius scale Card 16) + §6 Q3 RESOLVED (L1384-1393 rebrand "Brand" → "Vibrante") + §4.2 (a11y keyboard nav) + §4.6 (touch targets ≥44px). Cada AC cita seção exata. NENHUMA invenção (Constitution Art. IV). Tokens canônicos estão em `app/globals.css` pós-5.2 + primitives refreshed em `components/ui/` pós-5.3 — esta story **apenas consome em pages + components/{profile,theme,account}**. Estado pré-existente, deviations descobertas durante audit e racional de cada AC estão isolados em **Dev Notes** — leia antes de implementar.

1. **Profile page H1 + subtitle — "Seu perfil" + typescale `text-h1` 32px + body subtitle** (per `docs/frontend-spec.md` §2.6 L631-633 _"Seu perfil ← H1 / Edite as informações que aparecem em /@demo ← Body muted"_ + §1.3.2 typescale `--text-h1` 32px). Auditar `app/dashboard/profile/page.tsx:30-33`. Mudar L30 H1 de **`<h1 className="text-xl font-semibold tracking-tight">Seu perfil</h1>`** (shipped Story 5.1) para **`<h1 className="text-h1">Seu perfil</h1>`** (spec verbatim §2.6 L631 — copy preservada). Aplicar classe `text-h1` (= 32px + line-height 40 + weight 500 + tracking -0.02em per Story 5.2 typescale tokens). Remover classes Tailwind redundantes `text-xl font-semibold tracking-tight`. Subtitle (L31-33) preservar copy mas refresh typescale: **`<p className="text-sm text-muted-foreground">Edite as informações que aparecem na sua página pública <code>/@{username}</code>.</p>`** (shipped) → **`<p className="text-body text-muted-foreground">Edite as informações que aparecem na sua página pública <code>/@{username}</code>.</p>`** (text-body = 16px line-height 24 per spec §1.3.2; código `<code>` preservado).

2. **Theme page H1 + subtitle — "Tema da página" + typescale `text-h1` 32px + body subtitle** (per `docs/frontend-spec.md` §2.7 L701-703 _"Tema da página ← H1 / Escolha o visual que aparece em /@demo ← Body muted"_ + §1.3.2 typescale). Auditar `app/dashboard/theme/page.tsx:47-50`. Mudar L47 H1 de **`<h1 className="text-xl font-semibold tracking-tight">Tema da página</h1>`** (shipped Story 3.3) para **`<h1 className="text-h1">Tema da página</h1>`** (spec verbatim §2.7 L701). Subtitle (L48-50) refresh typescale: **`<p className="text-sm text-muted-foreground">Escolha como sua página pública em <code>/@{profile?.username ?? ''}</code> aparece.</p>`** (shipped) → **`<p className="text-body text-muted-foreground">Escolha como sua página pública em <code>/@{profile?.username ?? ''}</code> aparece.</p>`** (text-body = 16px line-height 24).

3. **Account page H1 + subtitle — "Conta" + typescale `text-h1` 32px + body subtitle** (per `docs/frontend-spec.md` §2.9 L813-814 _"Conta ← H1 / Gerencie seus dados ← Body muted"_ + §1.3.2 typescale). Auditar `app/dashboard/account/page.tsx:35-39`. Mudar L36 H1 de **`<h1 className="text-2xl font-semibold">Conta</h1>`** (shipped Story 4.5) para **`<h1 className="text-h1">Conta</h1>`** (spec verbatim §2.9 L813). Subtitle (L37-39) refresh typescale + simplify copy per spec L814 "Gerencie seus dados": **`<p className="mt-1 text-sm text-muted-foreground">Gerencie seus dados pessoais — exporte tudo em JSON ou exclua sua conta permanentemente.</p>`** (shipped) → **`<p className="text-body text-muted-foreground">Gerencie seus dados pessoais — exporte tudo em JSON ou exclua sua conta permanentemente.</p>`** (text-body + remove `mt-1` redundante já que header parent tem `space-y-1` ou similar via `<header>` semantic + add `space-y-1` no `<header>` para tightness). Copy preservada (não recortar para "Gerencie seus dados" minimal por completude UX — usuário precisa de contexto antes de hit destructive).

4. **AvatarUpload visual — 96×96 size override (DEV-A) + Card outer wrapper (opcional)** (per `docs/frontend-spec.md` §2.6 L638-641 _"AVATAR / 96x96 circle"_ + L637-645 _"Card with avatar + Trocar foto button + JPG/PNG/WebP até 1MB"_ + §1.4.2 radius scale). Auditar `components/profile/AvatarUpload.tsx:56-67`. Mudança principal (DEV-A): substituir `<Avatar size="lg">` (= size-10 = 40px) por `<Avatar size="lg" className="size-24">` (96px override) + bump `<Image src={previewUrl} alt={altText} width={40} height={40} unoptimized />` para `width={96} height={96}` (L61) — preserva retina sharp, evita blur. **JSDoc no className override** referenciando `[Source: docs/frontend-spec.md §2.6 L638-641]` + nota explicando porque primitive não foi refactored (Story 5.3 audit fechado). Helper text shipped `"jpg, png ou webp — máx 1 MB"` (L86) — copy spec L644 diz "JPG/PNG/WebP até 1MB"; reconciliar como `"JPG, PNG ou WebP — máx 1 MB"` (caps + dash em vez de vírgula, spec verbatim). **NÃO** wrap em Card primitive — wrapper atual `<div className="flex items-center gap-4">` é correto (spec mostra Card mas é decorativo wireframe; AvatarUpload é compositional, não standalone section). DEV-C anticipated: se Task 1 audit decidir adicionar subsection labels Profile (AC8), o `<AvatarUpload>` ficará dentro de uma seção labeled "Foto" — opcional, ratificado durante implementação.

5. **ThemeSelector LABELS rebrand "Brand" → "Vibrante" + 3 descriptions (DEV-B)** (per `docs/frontend-spec.md` §6 Q3 RESOLVED L1384-1393 _"3 temas mantidos, 'Brand' → 'Vibrante' em UI copy"_ + §2.7 L706/L715/L724 _"Lavender mist" / "Deep plum night" / "Lavender + peach pop"_). Auditar `components/theme/ThemeSelector.tsx:13-17`. Mudanças: (a) **LABELS const rebrand** — `brand: 'Brand'` → `brand: 'Vibrante'` (manter `light: 'Light'` + `dark: 'Dark'`; futura PT-BR full rebrand fora desta story); adicionar JSDoc comment acima do const: `// LABELS UI-only — enum 'brand' preservado em DB + Theme type (Q3 §6 PRD v0.5 ratified)`; (b) **DESCRIPTIONS const novo** — adicionar `const DESCRIPTIONS: Record<Theme, string> = { light: 'Lavender mist', dark: 'Deep plum night', brand: 'Lavender + peach pop' };` (verbatim spec L706/L715/L724); (c) **Render** — dentro do `<button role="radio">` (L72-103), adicionar `<span className="text-body-sm text-muted-foreground">{DESCRIPTIONS[theme]}</span>` em `<div className="flex items-center justify-between">` (L91-101) OU em linha separada acima — preferir linha separada abaixo do LABEL para preservar legibilidade. Estrutura final: `<div className="flex flex-col gap-0.5"><span className="font-medium">{LABELS[theme]}</span><span className="text-body-sm text-muted-foreground">{DESCRIPTIONS[theme]}</span></div>` substituindo o `<span className="font-medium">{LABELS[theme]}</span>` simples (L92). aria-label preservado em `aria-label={LABELS[theme]}` (L77) — agora rotula "Vibrante" para SR (UX consistency).

6. **Profile page sections opcionalmente refinadas com subsection labels** (per `docs/frontend-spec.md` §2.6 L635/L647/L668 _"─── Foto ───" / "─── Identidade ───" / "─── URL pública ───"_). Auditar `app/dashboard/profile/page.tsx:27-46`. Mudança opcional (DEV-C condicional — ratificar durante audit): adicionar 3 subsection labels antes de cada bloco (`<AvatarUpload>` / `<ProfileMetaForm>` / `<UsernameForm>`). Pattern sugerido: `<section className="space-y-3"><h2 className="text-h3 font-medium">Foto</h2><AvatarUpload ... /></section>` (text-h3 = 20px line-height 28 — subsection title; gap-3 entre label e content). Repetir para "Identidade" (wrap ProfileMetaForm) e "URL pública" (wrap UsernameForm). **CONDITIONAL** task — se @dev julgar que wireframe spec L635-680 mostra Cards visuais ao redor de cada seção MAS implementação atual usa `space-y-6` no parent (já dá separação visual via spacing), pode ESKIP labels + manter estrutura plana documentado em DEV-C. Trade-off: labels aumentam coerência com wireframe mas adicionam ruído visual em mobile (3 H2 antes de cada form). **NÃO** wrap em Card primitive cada seção (over-engineering — ProfileMetaForm + UsernameForm já consomem Form + Input primitives refreshed Story 5.3; adicionar Card outer faria nested card visual confuso). NÃO regredir `space-y-6` parent (preserva rhythm).

7. **Account page sections — preserve destructive Card visual + Card outer wrapper opcional em export section** (per `docs/frontend-spec.md` §2.9 L815-844 _"─── Dados ─── Card" + "─── Permanente ─── Card com border destructive sutil bg destructive/5"_). Auditar `app/dashboard/account/page.tsx:42-65`. Mudanças: (a) **Export section** (L42-51) — atualmente `<section aria-labelledby="export-heading" className="space-y-3">` com `<h2 id="export-heading" className="text-lg font-medium">Exportar dados</h2>`. Refresh: `text-lg font-medium` → `text-h3 font-medium` (= 20px subsection title per typescale §1.3.2). Wrapper opcional Card visual per spec L817-828: substituir `<section className="space-y-3">` por `<section className="space-y-3 rounded-xl border border-border bg-card p-6">` (rounded-xl 16 + bg-card + p-6 generoso) — DEV-D anticipated. Body text `text-sm` → `text-body` para subscription consistency. (b) **Delete section** (L53-65) — preservar Card destructive shipped Story 4.5 (`rounded-lg border border-destructive/30 bg-destructive/5 p-6`); mudar `rounded-lg` → `rounded-xl` (= 16px alinhamento radius scale §1.4.2); H2 `text-lg font-medium text-destructive` → `text-h3 font-medium text-destructive`; body `text-sm` → `text-body`. Copy preservada. ExportButton e DeleteAccountSection componentes intactos (consumem Button primitive Story 5.3 — destructive variant ratified).

8. **Subsection labels Profile + Account audit reconciliation** (DEV-C + DEV-D anticipated reconciliation) — auditar consistência: se Profile page (AC6) opta por subsection labels "Foto"/"Identidade"/"URL pública" (`<h2 className="text-h3 font-medium">`), Account page (AC7) também deve usar mesmo pattern (`<h2 className="text-h3 font-medium">Exportar dados/Excluir conta</h2>`) — Story consistente. Se Profile opta por NO-OP (DEV-C ESKIP), Account preserva `text-lg font-medium` shipped (Story 4.5) com bump apenas para `text-h3 font-medium` por consistência typescale (AC7 já especifica). **Decisão de @dev durante audit:** se subsection labels ADD em Profile, Account também usa `text-h3 font-medium`; se Profile SKIP, Account ainda muda `text-lg` → `text-h3 font-medium` per AC7 (typescale alignment mínimo). Documentar a decisão em Dev Agent Record.

9. **ThemeSelector tests — NO regression em behavior + add label assertion opcional** (per `tests/components/features/theme/ThemeSelector.test.tsx` + `ThemeTokens.test.tsx`). Tests existentes Story 3.3 cobrem: (a) clicking light/dark/brand fires `updateTheme({ theme })` Server Action; (b) optimistic UI + rollback on error; (c) `role="radiogroup"` + `aria-label="Tema da página"` container; (d) `role="radio"` + `aria-checked` per card. NENHUM teste assertam string literal "Brand" — usam apenas enum `'brand'` em props + Server Action calls. **Mudança rebrand é zero-impact** em tests behavior. Opcional (DEV-E anticipated): adicionar 1 teste assertion novo no `ThemeSelector.test.tsx` — `it('exibe label "Vibrante" para tema brand (Q3 §6 PRD v0.5)', () => { render(<ThemeSelector currentTheme="light" previewData={...} />); expect(screen.getByRole('radio', { name: 'Vibrante' })).toBeInTheDocument(); })`. Não-bloqueia (rebrand é UI label; existing tests query por aria-label via `aria-label={LABELS[theme]}` na L77 — mudando LABELS, tests que usam `getByRole('radio', { name: ... })` automaticamente mudam expectativa). **Validação:** rodar `pnpm test:components -t ThemeSelector` antes E depois da mudança — count PASS deve manter 194/194 (sem regressão).

10. **ProfileMetaForm + UsernameForm + AvatarUpload behaviors preservados** (per `docs/frontend-spec.md` §2.6 + Stories 5.1/3.4 Done). Components shipped Story 5.1/3.4 já implementam: (a) `ProfileMetaForm` RHF + Zod validator `UpdateProfileMetaInput` (display_name ≤ 50, bio ≤ 280) + `updateProfileMeta` Server Action + char counters `{displayNameValue.length}/50` + `{bioValue.length}/280` (`text-xs text-muted-foreground`); (b) `UsernameForm` RHF + Zod `UpdateUsernameInput` + `updateUsername` Server Action + `useUsernameAvailability` hook + amber warning copy "A URL pública mudará para /@<novo>"; (c) `AvatarUpload` file input + optimistic preview + `uploadAvatar` Server Action + rollback on error + URL.revokeObjectURL memory cleanup. **Esta AC é negativa: NÃO regredir nenhum desses comportamentos.** Verificação: rodar `pnpm test:components -t ProfileMetaForm` + `-t UsernameForm` + `-t AvatarUpload` — todos behavior tests devem continuar passing pós-AC1/AC4/AC6 mudanças visuais. Char counter `text-xs` shipped Story 5.1 preservar como está (spec §2.6 L654/L661 mostra "10/50" / "18/280" — sem prescrever typescale específica para counter). Helper text AvatarUpload "JPG, PNG ou WebP — máx 1 MB" reconciliado (AC4) — test que verifica presença de helper deve continuar PASS (queries por regex `/jpg.*png.*webp/i` toleram).

11. **DeleteAccountSection + ExportButton behaviors preservados** (per Story 4.5 Done). Components shipped Story 4.5 já implementam: (a) `DeleteAccountSection` AlertDialog + typed confirmation case-insensitive match server-side + RHF `DeleteAccountInput` Zod + `deleteAccount` Server Action + cascade delete 5 tabelas + Storage cleanup `avatars/{uid}/`; (b) `ExportButton` `useTransition` + `exportAccountData` Server Action + JSON serialization + Blob + `<a download>` + filename `biolink-export-{username}-{YYYY-MM-DD}.json`. **Esta AC é negativa: NÃO regredir nenhum desses comportamentos.** Verificação: `pnpm test:components -t DeleteAccountSection` + `-t ExportButton` — todos behavior tests devem PASS. AlertDialog refreshed Story 5.3 (`rounded-2xl` + `shadow-lg`) consumido automatic — sem mudança em DeleteAccountSection.tsx necessária.

12. **Suíte completa de qualidade local PASS sem regressão funcional**: `pnpm typecheck` exit 0, `pnpm lint` zero warnings, `pnpm build` (Next 16.2.5 / Turbopack) 15/15 static pages PASS (`/dashboard/profile`, `/dashboard/theme`, `/dashboard/account` são dynamic routes, não contam para static — mas build deve completar sem erro), `pnpm test:components` mantém **194/194 verde por contagem** — snapshots em `tests/components/features/{profile,theme,account}/__snapshots__/` esperados inexistentes pré-5.6 (test files usam `screen.getByRole`/`getByText` queries, não `toMatchSnapshot()` — confirmar via `ls` ou `find tests -name "*.snap" | grep -E "profile|theme|account"`). **Behavior tests** (não-snapshot): ThemeSelector tests verificam radio interactions + optimistic UI; ProfileMetaForm + UsernameForm tests verificam RHF + Zod validation + submit; AvatarUpload tests verificam file input + optimistic preview; DeleteAccountSection tests verificam typed confirmation + AlertDialog; ExportButton tests verificam download flow. Todos devem CONTINUAR PASSING — quebra = regressão funcional.

13. **Lighthouse CI mantido ≥ 0.85 em paths gated + bundle delta neutral**: `.lighthouserc.json` threshold (≥ 0.85 Performance/Accessibility/Best Practices/SEO em `/` + `/[username]` per Story 3.5 + estabilizado [STORY-3.5-F3]). `/dashboard/{profile,theme,account}` são authenticated routes — **fora do gate paths default** (Lighthouse não navega para sub-pages dashboard em CI). Mas paths trigger inclui `app/**` e `components/**` — mudanças aqui podem afetar `/` (landing também consome typescale tokens 5.2) via shared bundle. Esperado: bundle delta **neutral** (mudanças são className + 3 description strings + 1 const rename — zero novo import lucide-react, zero novo dependency). LCP `/` baseline pós-5.5 ~180-185 KB First Load JS. Lighthouse audit pode flagar A11y issues novos se: (a) ThemeSelector LABELS rebrand quebra contrast — não aplicável (label muda string, não classNames); (b) Avatar 96×96 override sem `width`/`height` Image bump quebra LCP — AC4 obriga bump. Validar via Lighthouse no PR. Qualquer queda > 5 pontos exige investigação antes do merge (mesmo gate Story 5.2/5.3/5.4/5.5 — runs:3 + median absorve flake per [STORY-3.5-F3]).

14. **Constitution Art. IV compliance + housekeeping**: cada AC1-AC13 cita `[Source: docs/frontend-spec.md §{n}.{m} L{linha}]` em Dev Notes. Zero invenção de glyphs/copies/comportamentos não-spec'd. Rebrand "Brand" → "Vibrante" rastreado para Q3 §6 RESOLVED L1384-1393 PRD v0.5 ratified 2026-05-29. Descriptions "Lavender mist" / "Deep plum night" / "Lavender + peach pop" verbatim spec L706/L715/L724. Backlog crossover: `docs/STORY-BACKLOG.md` entrada `[EPIC-5-S6]` MEDIUM movida para Change Log DONE post-merge (Task 9.1 — pattern Story 5.2/5.3/5.4/5.5). Story file mantém estrutura template-v2 (Status / Executor Assignment / Story / AC / 🤖 CodeRabbit Integration / Tasks / Dev Notes / Change Log / Dev Agent Record). Frontmatter ClickUp pulado (`mcp.enabled: false` per `core-config.yaml`). QA gate manual via QA-001 known-issues (CodeRabbit/WSL incompat. macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`).

> **No-Invention reconciliation:** O backlog `[EPIC-5-S6]` (`docs/STORY-BACKLOG.md` L29-38) lista como acceptance line: _"token interno `brand` preservado em `globals.css` + DB enum + `lib/theme.ts`; tests que assertam 'Brand' atualizados para 'Vibrante'; smoke `/dashboard/theme` cycle 3 temas"_. Reconcilio: (a) **token interno `brand`** — preservado per DEV-B + AC5 (LABELS UI-only); ZERO mudança em `globals.css` ou `lib/theme.ts`; (b) **DB enum** — preservado per AC5 + DEV-B (`theme_preset` em migrations 0002*profiles + 0003_pages intactas); (c) **tests que assertam "Brand"** — NENHUM existe (grep confirma: tests usam apenas enum `'brand'` em `aria-checked`/Server Action calls; LABELS são lidas via `aria-label={LABELS[theme]}` que automaticamente reflete rebrand); AC9 documenta verificação + sugere teste opcional de label "Vibrante"; (d) **smoke /dashboard/theme cycle 3 temas** — Task condicional 8 (não AC formal) — per MEMORY `feedback_skip_manual_smoke.md`, @dev pergunta antes e se skip pode marcar `[x]` com nota `*(SKIPPED ...)\_` + risco residual. AC1-AC8 derivam **verbatim** de spec §2.6 + §2.7 + §2.9 + Q3 §6 (1 AC por elemento estrutural ou rebrand). AC9-AC11 cobrem preservação behavior. AC12-AC14 gates locais/CI/housekeeping.

> **Out-of-scope (explicitamente deferido):**
>
> - **Refactor `Avatar` primitive para suportar variant `xl` (96px)** — Story 5.3 audit fechado; AC4 usa className override pattern (DEV-A) consistente com Story 5.4 h-12 override.
> - **Rebrand "Light" → "Claro" + "Dark" → "Escuro" PT-BR full** — spec §2.7 L705/L714/L723 mostra `☀ Claro` / `☾ Escuro` / `✦ Brand` em wireframe; PT-BR full rebrand fora desta story (escopo Q3 §6 só explicitamente menciona "Brand" → "Vibrante"). Glyphs ☀/☾/✦ também deferidos (deviation acceptable — radio buttons ◉/○ shipped Story 3.3 cumprem mesma função semântica). Sugerir backlog item `[STORY-5.6-F1]` LOW se @dev julgar valor adicional.
> - **Spring transitions / motion polish** (avatar upload spring scale, theme card flip) → Story 5.8 (Motion + polish).
> - **Wordmark `biolink ★` component + favicon ★** → Story 5.9.
> - **Mudança em /dashboard (Links) ou /dashboard/analytics** → Stories 5.5 Done + 5.7.
> - **Mudança em /@username (PublicPage)** → Story 5.7.
> - **Refactor avatar upload para drag-drop / crop** → fora de escopo (Server Action upload simples Story 3.4 preservado).
> - **Email/username live validation refresh visual** → Story 5.1 implementou + 5.4 ratificou; out desta story.
> - **Account export resume / pagination** → fora de escopo (LGPD JSON export single-shot Story 4.5 preservado).
> - **DeleteAccountSection multi-step confirmation flow** → fora de escopo (typed username AlertDialog Story 4.5 preservado).

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

> **Nota (@sm):** `coderabbit_integration` ausente em `.aiox-core/core-config.yaml` **e** `mcp.enabled: false` → skip notice é o comportamento correto do template-v2 (idêntico às Stories 1.4-5.5, todas Done). ClickUp sync pulado (`mcp.enabled: false`). Gate de qualidade segue o **workaround manual QA-001** (CodeRabbit/WSL incompat. em macOS — `docs/qa/known-issues.md`; MEMORY `project_qa_review_hang.md`), padrão validado em 1.4-5.5.

## Tasks / Subtasks

- [x] **Task 1 — Audit Profile Page (`app/dashboard/profile/page.tsx`)** (AC: 1, 6) — _executor: @dev_
  - [x] 1.1 Abrir `app/dashboard/profile/page.tsx`. Mudar L30 H1 `text-xl font-semibold tracking-tight` → `text-h1` (copy "Seu perfil" preservada per spec §2.6 L631).
  - [x] 1.2 Mudar L31-33 subtitle `text-sm` → `text-body` (copy preservada com `<code>/@{username}</code>` em linha).
  - [x] 1.3 (CONDITIONAL DEV-C) Adicionar subsection labels: `<section className="space-y-3"><h2 className="text-h3 font-medium">Foto</h2><AvatarUpload ... /></section>` + repetir para "Identidade" (wrap ProfileMetaForm) + "URL pública" (wrap UsernameForm). OU ESKIP (manter `space-y-6` parent) + documentar em Dev Agent Record.
  - [x] 1.4 Rodar `pnpm typecheck` + `pnpm test:components -t Profile` (sanity).

- [x] **Task 2 — Audit AvatarUpload (`components/profile/AvatarUpload.tsx`)** (AC: 4) — _executor: @dev_
  - [x] 2.1 Abrir `components/profile/AvatarUpload.tsx`. Override `<Avatar size="lg">` (L58) → `<Avatar size="lg" className="size-24">` (96px per spec §2.6 L638-641). Adicionar JSDoc comment acima referenciando spec line.
  - [x] 2.2 Bump `<AvatarImage render={<Image src={previewUrl} alt={altText} width={40} height={40} unoptimized />}>` (L60-62) para `width={96} height={96}` (preserva retina sharp).
  - [x] 2.3 Reconciliar helper text L86 `"jpg, png ou webp — máx 1 MB"` → `"JPG, PNG ou WebP — máx 1 MB"` (caps + dash per spec L644 verbatim).
  - [x] 2.4 Rodar `pnpm test:components -t AvatarUpload` — confirmar 0 regressões (queries por regex `/jpg.*png.*webp/i` toleram caps change; size className adição não afeta behavior tests).

- [x] **Task 3 — Audit Theme Page (`app/dashboard/theme/page.tsx`)** (AC: 2) — _executor: @dev_
  - [x] 3.1 Abrir `app/dashboard/theme/page.tsx`. Mudar L47 H1 `text-xl font-semibold tracking-tight` → `text-h1` (copy "Tema da página" preservada per spec §2.7 L701).
  - [x] 3.2 Mudar L48-50 subtitle `text-sm` → `text-body` (copy preservada com `<code>/@{profile?.username ?? ''}</code>` em linha).
  - [x] 3.3 Rodar `pnpm typecheck` (sanity).

- [x] **Task 4 — Rebrand ThemeSelector (`components/theme/ThemeSelector.tsx`)** (AC: 5, 9) — _executor: @dev_
  - [x] 4.1 Abrir `components/theme/ThemeSelector.tsx`. Mudar L13-17 LABELS const: `brand: 'Brand'` → `brand: 'Vibrante'` (preservar `light: 'Light'` + `dark: 'Dark'`). Adicionar JSDoc acima do LABELS: `/** LABELS UI-only — enum 'brand' preservado em DB + Theme type (Q3 §6 PRD v0.5 ratified 2026-05-29). */`.
  - [x] 4.2 Adicionar DESCRIPTIONS const novo após LABELS: `const DESCRIPTIONS: Record<Theme, string> = { light: 'Lavender mist', dark: 'Deep plum night', brand: 'Lavender + peach pop' };` (verbatim spec §2.7 L706/L715/L724).
  - [x] 4.3 Render — substituir L92 `<span className="font-medium">{LABELS[theme]}</span>` por estrutura nested: `<div className="flex flex-col gap-0.5"><span className="font-medium">{LABELS[theme]}</span><span className="text-body-sm text-muted-foreground">{DESCRIPTIONS[theme]}</span></div>`.
  - [x] 4.4 (OPCIONAL DEV-E) Adicionar teste em `tests/components/features/theme/ThemeSelector.test.tsx`: `it('exibe label "Vibrante" + descrição "Lavender + peach pop" para tema brand (Q3 §6 PRD v0.5)', () => { ... })`. ESKIP se redundante com tests existentes.
  - [x] 4.5 Rodar `pnpm test:components -t ThemeSelector` — confirmar 0 regressões + opcional novo teste PASS.

- [x] **Task 5 — Audit Account Page (`app/dashboard/account/page.tsx`)** (AC: 3, 7, 8) — _executor: @dev_
  - [x] 5.1 Abrir `app/dashboard/account/page.tsx`. Mudar L36 H1 `text-2xl font-semibold` → `text-h1` (copy "Conta" preservada per spec §2.9 L813).
  - [x] 5.2 Mudar L37-39 subtitle `mt-1 text-sm` → `text-body` (remove `mt-1` redundante; copy preservada). Adicionar `space-y-1` ao `<header>` parent se necessário para tightness visual.
  - [x] 5.3 Export section (L42-51) — mudar `<h2 id="export-heading" className="text-lg font-medium">` → `text-h3 font-medium` (consistência subsection labels per AC8). (CONDITIONAL DEV-D) Wrap em Card visual: `<section className="space-y-3 rounded-xl border border-border bg-card p-6">` (mantém aria-labelledby).
  - [x] 5.4 Delete section (L53-65) — `rounded-lg` → `rounded-xl` (radius scale alignment §1.4.2); mudar `<h2 id="delete-heading" className="text-lg font-medium text-destructive">` → `text-h3 font-medium text-destructive`; body `text-sm` → `text-body`.
  - [x] 5.5 Rodar `pnpm typecheck` + `pnpm test:components -t Account` (sanity).

- [x] **Task 6 — Audit ExportButton + DeleteAccountSection (no-op esperado)** (AC: 11) — _executor: @dev_
  - [x] 6.1 Auditar `components/account/ExportButton.tsx` — confirma consume Button outline + Download lucide; sem typescale legacy hardcoded. NO-OP esperado.
  - [x] 6.2 Auditar `components/account/DeleteAccountSection.tsx` — confirma consume AlertDialog (Story 5.3 rounded-2xl refreshed) + Button destructive + Input + Label; sem typescale legacy. NO-OP esperado.
  - [x] 6.3 Rodar `pnpm test:components -t ExportButton` + `-t DeleteAccountSection` — confirmar 0 regressões (changes em Account page restructure não afetam isolated component render tests).

- [x] **Task 7 — Quality Gates Locais** (AC: 12) — _executor: @dev_
  - [x] 7.1 Rodar `pnpm typecheck` — exit 0 esperado.
  - [x] 7.2 Rodar `pnpm lint` — zero warnings esperado.
  - [x] 7.3 Rodar `pnpm build` — Next 16.2.5 Turbopack; 15/15 static pages PASS; 16 routes intactos esperado.
  - [x] 7.4 Rodar `pnpm test:components` — 194/194 PASS esperado (zero snapshot updates; behavior tests intactos).
  - [x] 7.5 Confirmar `find tests/components/features/{profile,theme,account} -name "*.snap"` retorna empty (sem snapshot files pré-5.6 esperado).

- [x] **Task 8 — Smoke Runtime Manual × 3 temas (CONDITIONAL)** (AC: 12, 13) — _executor: @dev_
  - [x] 8.1 _(SKIPPED user opt-in — cobertura indireta via behavior tests Tasks 1-6 + Lighthouse CI no PR; risco residual documentado em Dev Agent Record. Pattern Stories 5.4/5.5 per MEMORY `feedback_skip_manual_smoke.md`.)_
  - [x] 8.2 _(SKIPPED — n/a se 8.1 SKIPPED.)_

- [x] **Task 9 — Housekeeping `[EPIC-5-S6]` MEDIUM → DONE** (AC: 14) — _executor: @dev_
  - [x] 9.1 Editar `docs/STORY-BACKLOG.md`: remover bloco `#### [EPIC-5-S6] Story 5.6 — Profile + Theme + Account + rebrand "Brand" → "Vibrante"` da seção 🟡 MEDIUM (L29-38). Atualizar stats (Open MEDIUM count -1; DONE count +1).
  - [x] 9.2 Adicionar entrada Change Log linha após Story 5.5 MERGE (L315): `| 2026-06-02 | DONE | [EPIC-5-S6] → Story 5.6 implementada (Profile + Theme + Account Soft Studio refresh + rebrand 'Brand' → 'Vibrante')... | Dex (dev) |`.
  - [x] 9.3 Atualizar frontmatter `last_updated:` com nota Story 5.6 implementada.
  - [x] 9.4 Atualizar narrative Epic 5 (L27) incluindo `+ ✅ 5.6 DONE (profile + theme + account page-level refresh + rebrand Vibrante)`.

## Dev Notes

### Previous Story Insights

**Story 5.5 (Dashboard Core — Layout + Links Soft Studio):**

- ✅ Pattern H1 refresh `text-xl font-semibold tracking-tight` → `text-h1` consolidado (Dashboard Links page L56). **REUSE direto** em Profile/Theme (AC1, AC2). Account (AC3) usa pattern leve variante: `text-2xl font-semibold` → `text-h1`.
- ✅ Subtitle refresh `text-sm` → `text-body` consolidado (Dashboard PublicUrlCopy). **REUSE direto** em Profile/Theme/Account (AC1, AC2, AC3).
- ✅ DEV-A1 anticipated (não-trivial UI decision) → ratified via doc + comment. Pattern repetido para DEV-A (96×96 avatar override) + DEV-B (rebrand decisão) nesta story.
- ✅ DEV-H test rewrite (`LinkList.test.tsx` describe `<EmptyState>` regex + button name reconciliado) — pattern aplicável em DEV-E desta story (ThemeSelector teste opcional novo label "Vibrante").
- ✅ Sidebar/UserMenu/MobileDrawer audit NO-OP — pattern repetido para Task 6 (ExportButton + DeleteAccountSection NO-OP esperado; consumem primitives Story 5.3 já refreshed).
- ✅ Smoke runtime manual SKIPPED user opt-in per MEMORY `feedback_skip_manual_smoke.md` — pattern repetido em Task 8.
- ✅ Backlog housekeeping `[EPIC-5-S5]` MEDIUM → DONE entry + stats update + narrative — pattern repetido em Task 9.

**Story 5.4 (Landing + Auth Soft Studio):**

- ✅ Pattern h-12 className override em primitives (Input/Button) — **DEV-A1 da 5.4 "NÃO mexer em primitives Story 5.3 escopo fechado"** → DEV-A desta story (`<Avatar size="lg" className="size-24">` override 96×96; primitive Avatar não refactored).
- ✅ Wordmark text-h3 lowercase consistency landing + auth + dashboard layout — esta story não toca wordmark (Story 5.9 escopo).
- ✅ H1 `text-2xl` → `text-h1` consolidado em 4 auth pages — **REUSE direto** em Account (AC3).

**Story 5.3 (Primitives Audit Soft Studio):**

- ✅ Avatar primitive auditado: `data-[size=lg]:size-10` (40px max) — variant `xl` não adicionada (escopo fechado). Confirma decisão DEV-A desta story (className override em vez de refactor).
- ✅ Card primitive `+shadow-sm` warm-tinted ratified — DEV-D opcional desta story (Account export section wrap em Card) consome essa decision automaticamente.
- ✅ AlertDialog `rounded-2xl` + `shadow-lg` refreshed — DeleteAccountSection (Task 6) consome automatic via primitive composition; NO-OP esperado em DeleteAccountSection.tsx.
- ✅ Button destructive variant ratificado (Input destructive ring/30; Button destructive bg) — DeleteAccountSection AlertDialogAction `variant="destructive"` shipped Story 4.5 consome automatic.

**Story 5.1 (Profile display_name + bio):**

- ✅ `ProfileMetaForm` shipped com RHF + Zod `UpdateProfileMetaInput` + `updateProfileMeta` Server Action + char counters `{value.length}/{max}` em `text-xs text-muted-foreground`. Preservar 100% — AC10 negative.
- ✅ `UsernameForm` shipped (Story 3.5-F2/5.1) com amber warning `text-amber-600 dark:text-amber-500` "A URL pública mudará para /@<novo>". Spec §2.6 L675 mostra "⚠ A URL pública mudará para /@<novo>" — copy alinhada. Preservar.
- ✅ `AvatarUpload` shipped Story 3.4 com optimistic preview + rollback. Preservar lógica; apenas size className override + Image width/height bump (AC4).

**Story 3.3 (Theme Selector):**

- ✅ `ThemeSelector` Client Component shipped com optimistic UI + rollback (pattern de `LinkList.tsx:48-62`) + `useTransition` + `role="radiogroup"` + `aria-label="Tema da página"` + `role="radio"` + `aria-checked` per card. Preservar 100% — AC9/AC10 negative.
- ✅ `ThemePreview` Server Component sem state — wireframe spec L709/L718/L727 mostra "/preview small" embed mini /@username; já implementado. Aria-hidden="true" para SR — preservar.
- ✅ LABELS const `Record<Theme, string>` em ThemeSelector L13-17 — única source-of-truth UI da label. Mudança rebrand vive AQUI (AC5).

**Story 4.5 (Account Module — Export + Delete):**

- ✅ `ExportButton` `useTransition` + Blob + `<a download>` filename `biolink-export-{username}-{YYYY-MM-DD}.json` + LGPD-mindful pseudonimização preservada. Preservar 100% — AC11 negative.
- ✅ `DeleteAccountSection` AlertDialog typed confirmation case-insensitive match server-side + cascade delete 5 tabelas via `auth.users` ON DELETE CASCADE + Storage cleanup `avatars/{uid}/`. Preservar 100% — AC11 negative.
- ✅ Destructive section visual `rounded-lg border border-destructive/30 bg-destructive/5 p-6` shipped per spec §2.9 L832 — apenas mudança `rounded-lg` → `rounded-xl` (radius scale alignment §1.4.2) + typescale `text-lg` → `text-h3` (AC7).

### Data Models

`[Source: lib/supabase/types.ts, lib/theme.ts, supabase/migrations/0002_profiles.sql, supabase/migrations/0003_pages.sql]`

**Theme enum (DB + types — preserved Q3 §6 PRD v0.5):**

```typescript
// lib/theme.ts:14-18 — source of truth via DB enum
export type Theme = Database['public']['Enums']['theme_preset'];
export const THEMES = ['light', 'dark', 'brand'] as const satisfies readonly Theme[];
export const DEFAULT_THEME: Theme = 'light';

// lib/supabase/types.ts:269 + L395 — typegen artifact
theme_preset: 'light' | 'dark' | 'brand';
```

**Rebrand "Brand" → "Vibrante" (AC5, DEV-B):**

```typescript
// components/theme/ThemeSelector.tsx:13-17 — UI label ONLY
const LABELS: Record<Theme, string> = {
  light: 'Light',
  dark: 'Dark',
  brand: 'Vibrante', // ← MUDANÇA (era 'Brand'; Q3 §6 PRD v0.5 ratified)
};
const DESCRIPTIONS: Record<Theme, string> = {
  // ← NEW
  light: 'Lavender mist',
  dark: 'Deep plum night',
  brand: 'Lavender + peach pop',
};
```

**Profile fields (preserved Story 5.1):**

```typescript
// lib/validators/profile.ts (shipped Story 5.1)
UpdateProfileMetaInput = z.object({
  display_name: z.string().max(50).nullable().optional(),
  bio: z.string().max(280).nullable().optional(),
});
UpdateUsernameInput = z.object({
  username: z
    .string()
    .min(3)
    .max(30)
    .regex(/^[a-z0-9-]+$/),
});
```

### API Specifications

N/A — Story frontend-only, **zero mudança de API/Server Actions**. Server Actions consumidas intactas:

- `updateProfileMeta` (Story 5.1) — display_name + bio
- `updateUsername` (Story 2.2/5.1) — username + RLS check
- `uploadAvatar` (Story 3.4) — file upload Supabase Storage
- `updateTheme` (Story 3.3) — pages.theme enum update
- `exportAccountData` (Story 4.5) — JSON export LGPD-mindful
- `deleteAccount` (Story 4.5) — cascade delete + Storage cleanup

### Component Specifications

Já documentadas exaustivamente em **Acceptance Criteria 1-14** + **Tasks 1-9**. Não repetir aqui — fonte canônica é a AC + Task combinada.

`[Source: docs/frontend-spec.md §2.6 L624-690, §2.7 L693-743, §2.9 L806-845, §1.3.2 typescale L242-243, §1.4.2 radius, §6 Q3 RESOLVED L1384-1393]`

### File Locations

**Modificados (esperado 3-5):**

```
app/dashboard/profile/page.tsx              # Task 1 — H1 text-h1 + subtitle text-body + (CONDITIONAL DEV-C) subsection labels
app/dashboard/theme/page.tsx                # Task 3 — H1 text-h1 + subtitle text-body
app/dashboard/account/page.tsx              # Task 5 — H1 text-h1 + subtitle + section restructure + Card wrap export (DEV-D)
components/theme/ThemeSelector.tsx          # Task 4 — LABELS rebrand brand→'Vibrante' + DESCRIPTIONS const new + render nested label/description
components/profile/AvatarUpload.tsx         # Task 2 — Avatar size-24 className override + Image 96×96 + helper text caps reconcile
```

**Auditados sem mudança esperada (NO-OP):**

```
components/profile/ProfileMetaForm.tsx      # AC10 — Story 5.1 consumed primitives; char counters preservar
components/profile/UsernameForm.tsx         # AC10 — Story 5.1 consumed primitives + amber warning preservar
components/profile/UsernameAvailabilityHint.tsx  # AC10 — Story 1.7/5.1 consumed; preservar
components/account/ExportButton.tsx         # AC11 — Story 4.5 Button outline + Download lucide; preservar
components/account/DeleteAccountSection.tsx # AC11 — Story 4.5 AlertDialog (5.3 refreshed) + RHF; preservar
```

**Opcional novo teste:**

```
tests/components/features/theme/ThemeSelector.test.tsx  # Task 4.4 — opcional adicionar 1 it() para label "Vibrante" + descrição "Lavender + peach pop" (DEV-E)
```

**Snapshot files (potencialmente afetados — review obrigatório se existirem):**

```
tests/components/features/profile/__snapshots__/     # (inexistente esperado — confirmar via ls)
tests/components/features/theme/__snapshots__/       # (inexistente esperado — confirmar via ls)
tests/components/features/account/__snapshots__/     # (inexistente esperado — confirmar via ls)
```

**Apenas leitura (referência durante implementação):**

```
app/globals.css                                                    # Tokens canônicos pós-5.2 + status tokens pós-5.3
components/ui/{avatar,button,card,input,textarea,alert-dialog,form}.tsx  # Primitives refreshed pós-5.3 (consumidos)
lib/theme.ts                                                       # Theme type + THEMES const + DEFAULT_THEME (preservados AC5)
lib/supabase/types.ts                                              # theme_preset enum (typegen artifact — preservado)
docs/frontend-spec.md (§2.6 L624-690, §2.7 L693-743, §2.9 L806-845, §1.3.2 typescale, §1.4.2 radius, §6 Q3 RESOLVED L1384-1393) # Source of truth
docs/stories/5.5.dashboard-core-soft-studio.story.md               # Insights anteriores (text-h1 + subtitle pattern)
docs/stories/5.4.landing-auth-soft-studio.story.md                 # Insights primitives override pattern (h-12 → size-24)
docs/stories/5.1.perfil-display-name-bio.story.md                  # Insights ProfileMetaForm shipped
docs/stories/3.3.ui-selecao-tema.story.md                          # Insights ThemeSelector shipped
docs/stories/4.5.conta-exportar-excluir.story.md                   # Insights Account Module shipped
docs/qa/gates/5.5-dashboard-core-soft-studio.yml                   # Gate 5.5 PASS — confirma primitives + tokens ready
docs/STORY-BACKLOG.md                                              # Entrada [EPIC-5-S6] (housekeeping Task 9.1)
```

**NÃO tocar:**

- `app/globals.css` (Story 5.2 + Story 5.3 escopo — esta story apenas consome tokens em pages/components)
- `components/ui/*` (Story 5.3 audit fechado — esta story consome primitives via composition/className override)
- `lib/theme.ts` (DEV-B — `Theme` type + THEMES const + DEFAULT_THEME preservados; rebrand vive em ThemeSelector LABELS)
- `lib/supabase/types.ts` (typegen artifact — auto-generated; rebrand UI-only)
- `supabase/migrations/*.sql` (DB enum `theme_preset` preservado; zero migration esta story)
- `server/page/actions.ts` + `server/profile/actions.ts` + `server/account/actions.ts` (Server Actions Stories 3.3/3.4/4.5/5.1 intactos — Story 5.6 é apenas refresh visual + label)
- `app/layout.tsx` (root layout — Story 5.2 escopo)
- `app/(auth)/**` (Story 5.4 escopo Done)
- `app/page.tsx` (Story 5.4 escopo Done)
- `app/[username]/**` (Story 5.7 escopo)
- `app/dashboard/page.tsx` + `app/dashboard/layout.tsx` + `app/dashboard/analytics/` (Stories 5.5 Done + 5.7)
- `components/dashboard/*` (Story 2.4/5.5 Done — chrome shipped)
- `components/links/*` (Story 2.5/2.6/5.5 Done — links shipped)
- `components/profile/UsernameAvailabilityHint.tsx` (Story 1.7/5.1 — sem mudança visual esperada)
- `scripts/check-contrast.mjs` ([STORY-3.2-F1] LOW)
- `tests/integration/**` (RLS tests + Server Action tests — não afetam UI refresh)
- `tests/unit/theme.test.ts` (DEV-B — THEMES enum preservado; teste assert `'brand'` enum sempre PASS)

### Testing Requirements

`[Source: tests/components/features/, package.json scripts]`

**Framework:** Vitest 3 + @testing-library/react 16 + jsdom. Script `pnpm test:components` = `vitest run --project=components` (confirmado package.json L17).

**Baseline obrigatória:** 194/194 PASS (mantida desde Story 5.1 + confirmada em 5.2/5.3/5.4/5.5 Dev Agent Records).

**Tipos de teste relevantes:**

- **theme/ThemeSelector.test.tsx** — radio interactions + optimistic UI + rollback + aria-label/aria-checked. **NÃO esperado quebrar** (rebrand UI-only; `aria-label={LABELS[theme]}` automaticamente reflete "Vibrante" — queries por `getByRole('radio', { name: ... })` na L120 já tolerante via aria-label dinâmico). Opcional teste novo (Task 4.4) adicionar assertion para "Vibrante" presença.
- **theme/ThemeTokens.test.tsx** — verifica `themes` array contém `'brand'` (L63). **NÃO esperado quebrar** (enum preservado per DEV-B).
- **profile/ProfileMetaForm.test.tsx** — RHF + Zod validation + char counters + submit happy path + error. **NÃO esperado quebrar** (Story 5.6 não modifica ProfileMetaForm; AC10 negative).
- **profile/UsernameForm.test.tsx** — RHF + Zod + useUsernameAvailability + amber warning. **NÃO esperado quebrar** (Story 5.6 não modifica UsernameForm; AC10 negative).
- **profile/AvatarUpload.test.tsx** — file input + optimistic preview + rollback + helper text presence. **Pode precisar update** se assert exato em helper text "jpg, png ou webp" (mudou para "JPG, PNG ou WebP" — caps + dash). Behavior tests (file change + preview + upload) **NÃO devem quebrar** (lógica preserved per AC10).
- **account/ExportButton.test.tsx** — Button render + click + transition pending state. **NÃO esperado quebrar** (Story 5.6 não modifica ExportButton; AC11 negative).
- **account/DeleteAccountSection.test.tsx** — AlertDialog open + typed confirmation + submit + cancel. **NÃO esperado quebrar** (Story 5.6 não modifica DeleteAccountSection; AC11 negative).

**Behavior tests** (não-snapshot) — testam render condicional, event handlers, ARIA. Estes **NÃO devem quebrar** por mudanças visuais. Se quebrar → investigar regressão.

**Test rewrite anticipated (DEV-E opcional):** `tests/components/features/theme/ThemeSelector.test.tsx` adicionar 1 `it()` test verificando label "Vibrante" + descrição "Lavender + peach pop" presença (CONDITIONAL — pode ser redundante com existing tests via aria-label query).

**Test rewrite mandatory (AC4 reconcile):** `tests/components/features/profile/AvatarUpload.test.tsx` se assert exato em helper text "jpg, png ou webp" precisa update para regex `/JPG.*PNG.*WebP/i` ou substring match.

**Smoke runtime manual:** condicional (Task 8), perguntar antes per MEMORY `feedback_skip_manual_smoke.md`.

### Technical Constraints

- **Zero token novo** em `globals.css` (Story 5.2 + 5.3 fecharam token surface — `text-h1`/`text-h2`/`text-h3`/`text-body`/`text-body-sm` + `rounded-xl` + `shadow-sm` todos consumidos).
- **Zero mudança em primitives** (`components/ui/*` — Story 5.3 audit fechado; usar composition + className override pattern em consumers per DEV-A pattern Story 5.4 h-12 override).
- **Zero mudança de API pública** — Server Actions Stories 3.3/3.4/4.5/5.1 consumidas intactas. Nenhuma query nova em RSC (page.tsx files consomem mesmas queries shipped).
- **Zero nova dependência** — Avatar/Button/AlertDialog/Form/Input/Textarea/Image/lucide já no bundle pós-5.3 + Stories anteriores; rebrand é string change zero-impact bundle.
- **Constitution Art. IV (No Invention):** todas as mudanças derivam de `frontend-spec.md` §2.6 + §2.7 + §2.9 + §1.3.2 + §1.4.2 + §6 Q3 RESOLVED verbatim. Qualquer choice arquitetural (DEV-A..E) deve ser documentada no Dev Agent Record.
- **Constitution Art. III (Story-Driven):** mudanças fora dos 3 page-level + 2 components/{profile,theme} + `STORY-BACKLOG.md` (housekeeping) requerem amend desta story.
- **Constitution Art. II (Agent Authority):** `git push`/`gh pr create` exclusivos de @devops (Gage); `@dev` faz commit local + delega push per pattern Stories 5.2-5.5.
- **Backward compat:**
  - Theme enum `'brand'` preservado em DB + types + lib/theme.ts → zero impact em rows shipped com `theme='brand'` em produção.
  - LABELS UI rebrand → consumidores externos do componente (NENHUM no codebase per grep) — zero ripple effect.
  - AvatarUpload props (`currentAvatarUrl`/`displayName`/`username`) inchanged.
  - ProfileMetaForm + UsernameForm + ExportButton + DeleteAccountSection props inchanged.
  - Server Actions response shapes inchanged.
- **a11y WCAG 2.1 AA** mantida (§4) — focus rings preservados via Story 5.3 primitives, aria-label dinâmico em ThemeSelector reflect "Vibrante" automatic (queries por nome em tests automaticamente atualizam), Avatar 96×96 mantém contrast `bg-muted` + `text-muted-foreground` fallback, AlertDialog typed confirmation Story 4.5 preservada (autoComplete="off" + spellCheck=false).
- **Performance:** mudanças são className + 3 description strings + 1 LABELS const rename + 1 DESCRIPTIONS const new (~5 linhas) + JSDoc comments (~5 linhas) + Image width/height bump (zero JS runtime cost). Bundle delta esperado **neutral** (zero novo import, zero novo dependency, zero novo lucide icon). LCP `/` mantido per NFR1 (`<` 2.0s baseline pós-5.5). `/dashboard/{profile,theme,account}` não têm Lighthouse gate (authenticated), mas paths trigger `app/**` + `components/**` em CI workflow.
- **Avatar 96×96 override (AC4 DEV-A):** `<Avatar size="lg" className="size-24">` aplica size-24 (= 96px) via Tailwind className final-wins; `<Image width={96} height={96}>` preserva retina sharp. Bundle/perf impact zero (Tailwind atomic classes; Image params Next-optimized).
- **Theme rebrand "Brand" → "Vibrante" (AC5 DEV-B):** strict UI label change; consumidores Server-side (Server Actions + RLS + DB queries) inchanged. Risk de breakage em prod rows shipped com `theme='brand'`: ZERO (enum + type + Server Actions preservados).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Author     |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 2026-06-02 | 2.0     | **`@po *close-story 5.6` post-merge closure.** Story 5.6 mergeada em main via **PR #36** (squash commit `32949d8`). CI 11/11 SUCCESS (install + lint + typecheck + test-unit + test-components 195/195 + test-integration + audit + build + lighthouse + gitleaks + Vercel preview). Branch `feat/story-5.6-profile-theme-account-soft-studio` deletada (remote + local). Status `Ready for Review → Done` per `.claude/rules/story-lifecycle.md` (@devops merge → @po close). Working tree limpo post-reset (next-env.d.ts regenerado pelo Next descartado). `STORY-BACKLOG.md` atualizado: MERGE entry adicionada (Change Log) + frontmatter `last_updated` reconciliado (remove "pendente push @devops"). Pattern Stories 5.2/5.3/5.4/5.5 (close imediatamente após squash merge). **Phase 3 Epic 5 continua destravada** — Stories 5.7 (Analytics + Public page, paralelizável MEDIUM) / 5.8 (Motion + polish) / 5.9 (Logo + favicon) prontas para `@sm *draft`. Próximo: `@sm *draft 5.7`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Pax (po)   |
| 2026-06-02 | 0.2     | **`@po *validate-story-draft 5.6` → GO incondicional.** **10-point validation: 10/10 PASS.** **Implementation Readiness Score: 9.5/10.** **Confidence: HIGH.** Cross-validation técnica (anti-hallucination Section 9): (a) **Spec line refs verbatim** — L706 "Lavender mist" / L715 "Deep plum night" / L724 "Lavender + peach pop" cross-checked via `sed -n '715,725p'`; L631-633 (Profile H1+subtitle wireframe) / L701-703 (Theme H1+subtitle) / L813-814 (Account H1+subtitle) cross-checked via grep §2.6/§2.7/§2.9 anchors; L1384-1393 (Q3 §6 RESOLVED rebrand decision) cross-checked com PRD v0.5 §UX/Branding Change Log 2026-05-29; L242-243 (typescale §1.3.2) ratified; (b) **Typescale tokens CONFIRMADOS** present em `app/globals.css` L232-243 (`--text-h1: 2rem` line-height `2.5rem` + `--text-h2: 1.5rem` / `--text-h3: 1.25rem` / `--text-body: 1rem` / `--text-body-sm: 0.875rem`) + @theme inline L316-333 (font-weight 500 + letter-spacing -0.02em/-0.01em); AC1/AC2/AC3/AC6/AC7 podem consumir sem fallback; (c) **Avatar primitive** confirmado em `components/ui/avatar.tsx:20` apenas 3 variants (`size-8` default / `size-6 data-[size=sm]:size-6` / `size-10 data-[size=lg]:size-10`); `<Avatar size="lg" className="size-24">` override via `cn()` + tailwind-merge final-wins — DEV-A pattern correto (mesmo Story 5.4 h-12 override em Input/Button); (d) **Theme rebrand zero-impact tests** — `grep "Brand" tests/` retorna VAZIO (tests usam apenas enum `'brand'` em `aria-checked`/Server Action calls + `aria-label={LABELS[theme]}` query-by-name automatic reflect); `tests/unit/theme.test.ts:16` `THEMES === ['light','dark','brand']` enum preservado per DEV-B; 12 integration tests `tests/integration/{rls,server-actions}/` usando `'brand'` enum intactos; (e) **Server Actions intactos** — `updateProfileMeta`/`updateUsername`/`uploadAvatar`/`updateTheme`/`exportAccountData`/`deleteAccount` Stories 3.3/3.4/4.5/5.1 zero-touch (apenas page.tsx + ThemeSelector LABELS); (f) **DB enum + lib/theme.ts preservados** — `theme_preset = ['light', 'dark', 'brand']` em migrations 0002_profiles/0003_pages + `lib/theme.ts:18 THEMES` const + `lib/supabase/types.ts:269,395` types.theme_preset zero-touch; zero risk rows shipped `theme='brand'` em prod; (g) **DESCRIPTIONS const new** defensible per Art. IV — text strings ("Lavender mist" / "Deep plum night" / "Lavender + peach pop") são verbatim do spec wireframe; const structure é detail de implementação não invention; (h) **`size="icon-sm"` Button variant** confirmado em `components/ui/button.tsx:31` (irrelevante para 5.6 mas valida primitives Story 5.3 estáveis). **Executor Assignment** (Section 1.1): `executor: @dev` + `quality_gate: @qa` + `quality_gate_tools: [typecheck, lint, build, test:components]` — convenção do projeto (Stories 5.2-5.5 todas ratificadas GO+PASS com mesmo pattern). **Should-Fix minor (non-blocker):** SF-1 — Story AC10 documenta char counter typescale `text-xs` shipped Story 5.1 como "preservar como está"; spec §2.6 L654/L661 mostra "10/50"/"18/280" sem prescrever typescale específica — defensible (não-regressão); SF-2 — AC4 DEV-A `size-24` className depende de tailwind-merge correct precedence; pattern verbatim Story 5.4 já validado em produção (h-12 override Input/Button shipped + Lighthouse PASS) — defensible. **Nice-to-Have:** NH-1 mencionar `cn()` import já presente em ThemeSelector L10 (zero overhead refresh); NH-2 DEV-C/DEV-D condicional opt-in @dev (Profile subsection labels + Account Card wrap) — pattern consistente com Story 5.5 DEV-E (MobileDrawer wordmark CONDITIONAL ratified SKIPPED); NH-3 PT-BR full rebrand "Light"/"Dark" → "Claro"/"Escuro" deferido (out-of-scope explícito) — pode abrir `[STORY-5.6-F1]` LOW se @dev julgar valor; NH-4 ThemeSelector `<AvatarFallback>` size — wrapper `size-24` cascade via `size-full` em fallback container (avatar.tsx:43); cobertura defense-in-depth. **CodeRabbit Integration** (Section 8): `mcp.enabled: false` em core-config.yaml — skip notice renderizado corretamente (mesmo pattern Stories 5.2-5.5 Done). **Tasks/Subtasks Sequence** (Section 7): 9 tasks logicamente ordenadas — page audit (Tasks 1, 3, 5) podem ser paralelas; component refactors (Tasks 2, 4) independentes; audit no-op (Task 6) último antes de gates; housekeeping (Task 9) final post-Done. Granularidade adequada (3-5 subtasks por task médio). **Status transition: `Draft → Ready`** per `.claude/rules/story-lifecycle.md` (responsabilidade @po post-GO). Próximo: `@dev *develop 5.6` (modo Interactive recomendado dada complexity M com 5 DEV-A..E condicionais — esp. DEV-C/DEV-D condicional add subsection labels/Card wrap precisa decisão durante audit). | Pax (po)   |
| 2026-06-02 | 1.0     | **@dev `*develop 5.6` Interactive mode → Status `Ready → Ready for Review`.** Implementação completa de 9 Tasks + 14 ACs verbatim. **DEV decisões ratificadas (4 condicionais opt-in):** DEV-C ADD subsection labels Profile ("Foto"/"Identidade"/"URL pública" `<h2 text-h3 font-medium>`) + DEV-D ADD Card wrap Account export section (`rounded-xl border border-border bg-card p-6`) + DEV-E ADD novo it() teste "exibe label Vibrante + descrição Lavender + peach pop" + Task 8 SKIP smoke runtime manual (user opt-in per MEMORY `feedback_skip_manual_smoke.md`). **Mudanças mandatórias:** Profile/Theme/Account H1 typescale `text-xl`/`text-2xl` → `text-h1`; subtitles `text-sm`/`mt-1 text-sm` → `text-body`; Account header add `space-y-1`; Account export section H2 `text-lg` → `text-h3 font-medium` + body `text-sm` → `text-body`; Account delete section `rounded-lg` → `rounded-xl` (radius scale §1.4.2) + H2 `text-lg` → `text-h3 font-medium text-destructive` + body `text-body`. **AvatarUpload (DEV-A):** `<Avatar size="lg" className="size-24">` 96×96 override via `cn()` + tailwind-merge final-wins (primitive Story 5.3 escopo fechado — pattern Story 5.4 h-12 override) + `<Image width={96} height={96}>` retina sharp + helper "JPG, PNG ou WebP — máx 1 MB" caps reconcile per spec §2.6 L644 verbatim. **ThemeSelector (DEV-B):** LABELS `brand: 'Brand'` → `brand: 'Vibrante'` + JSDoc rastreio Q3 §6 + DESCRIPTIONS const new ("Lavender mist" / "Deep plum night" / "Lavender + peach pop" verbatim spec L706/L715/L724) + render nested `<div flex-col gap-0.5>` label `font-medium` + descrição `text-body-sm text-muted-foreground`; token + DB enum + lib/theme.ts preservados (zero risk em rows shipped `theme='brand'`). **Test rewrite mandatório (correção da story que afirmava "zero-impact"):** `ThemeSelector.test.tsx` 5× `name: /brand/i` → `/vibrante/i` (regex case-insensitive `/brand/` não match 'Vibrante' — descoberta durante implementação) + 1 novo it() DEV-E. **Gates locais:** `pnpm typecheck` exit 0 ✓ + `pnpm lint` zero warnings ✓ + `pnpm build` Next 16.2.5 Turbopack 15/15 static pages PASS (16 routes intactos) ✓ + `pnpm test:components` 194 baseline + 1 novo = **195/195 effectively** (1ª run 16min flaked LoginForm timeout 15s @env macOS slow; re-run isolated `LoginForm` 6/6 PASS confirmado flake — NÃO regressão de 5.6) ✓. **Bundle delta neutral esperado** (sem novo import; rebrand é string + className changes). **Backlog housekeeping:** `[EPIC-5-S6]` MEDIUM block removido + Change Log DONE entry + stats 13/0/4/9/5 → 12/0/3/9/6 + narrative Epic 5 update ✅ 5.6 DONE. **Pendente push @devops** (Constitution Art. II — `git push`/`gh pr create` exclusivos de @devops).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Dex (dev)  |
| 2026-06-02 | 0.1     | Story draft criada pela `*draft 5.6` do @sm (River). Cobertura: **14 ACs** (8 visuais Profile/Theme/Account page-level + subsection labels + rebrand "Brand" → "Vibrante" + descriptions + 3 behavior preservation + gates locais/CI + housekeeping); **9 Tasks** (1 Profile page + 1 AvatarUpload + 1 Theme page + 1 ThemeSelector rebrand + 1 Account page + 1 ExportButton/DeleteAccountSection audit no-op + 1 quality gates + 1 smoke condicional + 1 housekeeping); **Dev Notes** completos com tokens 5.2 + primitives 5.3 + insights 5.1/3.3/3.4/4.5/5.4/5.5 + tabela arquivos + **DEV-A..E antecipadas** (Avatar 96×96 size-24 className override DEV-A; rebrand LABELS UI-only Q3 §6 PRD v0.5 DEV-B; Profile subsection labels condicional DEV-C; Account export Card wrap condicional DEV-D; ThemeSelector teste opcional novo label "Vibrante" DEV-E); Source-of-truth: `frontend-spec.md` §2.6 L624-690 (Profile wireframe + states + spec verbatim) + §2.7 L693-743 (Theme wireframe + 3 cards + mini preview) + §2.9 L806-845 (Account export + delete sections) + §1.3.2 typescale L242-243 + §1.4.2 radius + §6 Q3 RESOLVED L1384-1393 (rebrand decision ratified) + insights Stories 5.1/3.3/3.4/4.5/5.2/5.3/5.4/5.5 Done. Status: Draft. Próximo: `@po *validate-story-draft 5.6` antes do handoff para `@dev *develop`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | River (sm) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (`claude-opus-4-7`) via Claude Code — `@dev` (Dex/Builder persona) ativado por `*develop 5.6` em modo Interactive (default).

### Debug Log References

- 2026-06-02 — Confirmado via grep+ls que os 5 arquivos-alvo batem exatamente com spec da story (typescale legacy + `LABELS.brand: \047Brand\047` + Avatar `size="lg"` 40px + helper text lowercase + radius-lg destructive).
- 2026-06-02 — Tokens typescale confirmados em `app/globals.css` L232-243 (`--text-h1: 2rem` lh `2.5rem`; `--text-h3: 1.25rem` lh `1.75rem`; `--text-body: 1rem` lh `1.5rem`; `--text-body-sm: 0.875rem` lh `1.25rem`) + @theme inline L316-333 (font-weight 500 + letter-spacing -0.02em/-0.01em). Tailwind v4 gera utility classes automaticamente (sem typecheck regression).
- 2026-06-02 — **Descoberta durante implementação:** story afirmava "rebrand é zero-impact tests" em AC9/Dev Notes, mas `tests/components/features/theme/ThemeSelector.test.tsx` usa `screen.getByRole(\047radio\047, { name: /brand/i })` em 5 queries (L67/L75/L83/L89/L114) — regex `/brand/i` testa contra `aria-label="Vibrante"` retorna NO MATCH (substring brand não consecutiva em Vibrante). **Mudança mandatória:** 5× `name: /brand/i` → `/vibrante/i` para preservar baseline. `currentTheme="brand"` prop (L112) mantido (enum, não label).
- 2026-06-02 — Primeira run de `pnpm test:components` levou **961.25s** (anormal — environment macOS lento) e flaked `LoginForm > "renderiza email + senha + link esqueci a senha + botão entrar"` com Test timed out 15000ms. Re-run isolado `pnpm test:components -t LoginForm` (589s) — **6/6 PASS**, confirmado flake do environment. LoginForm é Story 5.4 (zero relação com 5.6). Baseline 194 + 1 novo teste = 195/195 effectively.
- 2026-06-02 — `pnpm build` Next 16.2.5 Turbopack — Compiled 97s + TypeScript 107s + Static pages 7.9s (15/15) + 16 routes intactos. Warning pré-existente "middleware deprecated → use proxy" não-bloqueante (não-introduzido por 5.6).

### Completion Notes List

- **Task 1 (Profile page)** — H1 `text-xl font-semibold tracking-tight` → `text-h1` (line-height 40 + font-weight 500 + tracking -0.02em via @theme inline); subtitle `text-sm` → `text-body` (16px lh 24); ADD 3 `<section className="space-y-3">` wrapping AvatarUpload/ProfileMetaForm/UsernameForm com `<h2 className="text-h3 font-medium">` "Foto" / "Identidade" / "URL pública" (DEV-C ratified). Source verbatim: `frontend-spec.md` §2.6 L631-633 + L635/647/668. `space-y-6` parent preservado (rhythm).
- **Task 2 (AvatarUpload)** — `<Avatar size="lg" className="size-24">` (DEV-A) — `size-24` (96px) sobrescreve `data-[size=lg]:size-10` (40px) via `cn()` + tailwind-merge final-wins; 3 linhas de JSDoc inline explicando porque primitive não foi refactored (Story 5.3 audit fechado). `<Image width={96} height={96}>` em `<AvatarImage render={...}>` preserva retina sharp. Helper text "jpg, png ou webp — máx 1 MB" → "JPG, PNG ou WebP — máx 1 MB" caps + dash per spec §2.6 L644 verbatim.
- **Task 3 (Theme page)** — H1 `text-xl font-semibold tracking-tight` → `text-h1` + subtitle `text-sm` → `text-body`. NO-OP no `<ThemeSelector>` consumer (props inchanged).
- **Task 4 (ThemeSelector)** — LABELS `brand: \047Brand\047` → `brand: \047Vibrante\047` (DEV-B) + JSDoc acima do const rastreando Q3 §6 PRD v0.5 ratified 2026-05-29. DESCRIPTIONS const new — light "Lavender mist" / dark "Deep plum night" / brand "Lavender + peach pop" verbatim spec §2.7 L706/L715/L724 + JSDoc com source ref. Render — substituído L92 `<span className="font-medium">` por `<div className="flex flex-col gap-0.5">` nested com label `font-medium` + descrição `text-body-sm text-muted-foreground`. Tests — 5× regex update mandatório + 1 novo it() (DEV-E) com 4 assertions (Vibrante presence + 3 descriptions verbatim).
- **Task 5 (Account page)** — Header `space-y-1` adicionado para tightness (subtitle removed `mt-1`); H1 `text-2xl font-semibold` → `text-h1`; subtitle `mt-1 text-sm` → `text-body`. Export section — ADD Card wrap `rounded-xl border border-border bg-card p-6` (DEV-D ratified) + H2 `text-lg font-medium` → `text-h3 font-medium` + body `text-sm` → `text-body`. Delete section — `rounded-lg` → `rounded-xl` (radius scale §1.4.2 alignment com export Card) + H2 `text-lg font-medium text-destructive` → `text-h3 font-medium text-destructive` + body `text-sm` → `text-body`. `border-destructive/30 bg-destructive/5` preserved (Story 4.5 destructive visual).
- **Task 6 (ExportButton + DeleteAccountSection)** — NO-OP confirmado per AC11. Components consomem Button outline/destructive + Download/Trash2 lucide + AlertDialog (Story 5.3 `rounded-2xl` refreshed) + Form/Input/Label primitives. Sem typescale legacy hardcoded em ambos. AlertDialog refresh propagado automatic via primitive composition.
- **Task 7 (Quality Gates)** — typecheck/lint/build/test:components todos PASS. Baseline 194/194 + 1 novo (Task 4.4) = 195/195 effectively (LoginForm flake isolado).
- **Task 8 (Smoke Manual)** — SKIPPED user opt-in. Cobertura indireta: (a) typecheck + lint enforce token usage; (b) build PASS valida render time; (c) test:components valida component behavior + ARIA + char counters preserved; (d) Lighthouse CI runs no PR para /` + /[username]` (authenticated paths fora do gate default mas paths trigger `app/**` + `components/**` em CI). **Risco residual aceito:** visual delta entre wireframe spec e prod só validado em Vercel preview (PR) + Lighthouse score delta < 5 pontos esperado neutral.
- **Task 9 (Housekeeping)** — `docs/STORY-BACKLOG.md` atualizado: bloco `[EPIC-5-S6]` MEDIUM removido (L29-38) + frontmatter `last_updated` → 2026-06-02 + narrative Epic 5 (L27) +✅ 5.6 DONE + stats `13/0/4/9/5` → `12/0/3/9/6` + Change Log entry DONE post-MERGE 5.5. Pattern Stories 5.2/5.3/5.4/5.5.

### File List

**Modificados (4 files de produção + 1 file de teste + 1 file de housekeeping):**

- `app/dashboard/profile/page.tsx` — H1 + subtitle + 3 subsection labels (Task 1)
- `app/dashboard/theme/page.tsx` — H1 + subtitle (Task 3)
- `app/dashboard/account/page.tsx` — H1 + subtitle + sections refresh + Card wrap export (Task 5)
- `components/profile/AvatarUpload.tsx` — Avatar size-24 override + Image 96×96 + helper caps (Task 2)
- `components/theme/ThemeSelector.tsx` — LABELS rebrand + DESCRIPTIONS const + render nested (Task 4)
- `tests/components/features/theme/ThemeSelector.test.tsx` — 5× regex update + 1 novo it() (Task 4)
- `docs/STORY-BACKLOG.md` — housekeeping `[EPIC-5-S6]` MEDIUM → DONE (Task 9)
- `docs/stories/5.6.profile-theme-account-soft-studio.story.md` — Status + Tasks checkboxes + Change Log + Dev Agent Record (esta seção)

**Auditados NO-OP (confirmado sem mudança esperada):**

- `components/profile/ProfileMetaForm.tsx`
- `components/profile/UsernameForm.tsx`
- `components/profile/UsernameAvailabilityHint.tsx`
- `components/account/ExportButton.tsx`
- `components/account/DeleteAccountSection.tsx`

**NÃO tocados** (Story 5.3 escopo fechado / preservados per AC):

- `app/globals.css` (tokens canônicos Story 5.2 + 5.3 consumidos via class)
- `components/ui/*` (primitives Story 5.3 escopo fechado — composition + className override)
- `lib/theme.ts` (Theme type + THEMES const + DEFAULT_THEME preservados DEV-B)
- `lib/supabase/types.ts` (typegen artifact — `theme_preset` enum preservado)
- `supabase/migrations/*.sql` (DB enum preservado per Q3 §6)
- `server/{profile,page,account}/actions.ts` (Server Actions Stories 3.3/3.4/4.5/5.1 intactos)
