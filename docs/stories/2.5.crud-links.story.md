# Story 2.5: CRUD de Links (UI + Server Actions)

## Status

Done

## Executor Assignment

```yaml
executor: '@dev'
quality_gate: '@qa'
quality_gate_tools:
  ['typecheck', 'lint', 'build', 'test:unit', 'test:components', 'test:integration']
complexity: 'M-H'
```

> **Rationale:** Story **Frontend (primário)** — lista de links no dashboard, modal de adição, edição inline, switch de visibilidade otimista, modal de confirmação de exclusão, empty state — com componente **API/Backend (secundário)**: 4 novas Server Actions em `server/links/actions.ts` (`createLink`, `updateLink`, `deleteLink`, `toggleLinkVisibility` — arch §392-399) validadas por Zod e mapeando erros RLS. **Sem nova migration**: a tabela `links`, as 5 RLS policies, a constraint `UNIQUE (page_id, position) DEFERRABLE` e os tipos TS já existem de `supabase/migrations/0004_links.sql` (Story 2.3, **Done**) e `lib/supabase/types.ts`. O esforço dominante é código de UI + 4 Server Actions; padrões 100% pré-existentes (ActionResult, Zod schema-first, `flattenFieldErrors`, react-hook-form+zodResolver, RSC fetch+guard, padrão de teste de Server Action/componente). Seguindo a convenção **validada e Done** das Stories Frontend+Server-Action deste repo — **2.1** (mesma classe: UI + nova Server Action `updateUsername`, `executor: @dev`/`quality_gate: @qa`) e Epic 1 (1.5–1.9 todas `@dev`/`@qa`) — o executor é **@dev (Dex)** com gate por **@qa (Quinn)**. `git push` / `gh pr create` permanecem **exclusivos de @devops** (`.claude/rules/agent-authority.md`). Complexity **M-H** — 0 deps NPM novas (só `pnpm dlx shadcn add` de primitivos faltantes, padrão 2.4), 0 migrations, 0 DDL; superfície maior que 2.1 (4 Server Actions + ~4 componentes de cliente + optimistic UI), mas todos os padrões já estabelecidos. **Reorder/drag-drop está FORA DE ESCOPO (Story 2.6).** Nenhuma decisão bloqueante: todas resolvem por arch canônico (§1278-1368, §392-400) ou precedente sancionado (2.1/2.3/2.4), não invenção; DEV-1/DEV-3/DEV-6 flagged p/ gate @po (classe não-bloqueante validada em 2.4).

## Story

**As a** usuário,
**I want** criar, editar, ocultar e excluir links a partir do meu dashboard,
**so that** eu controle o que aparece na minha página pública.

## Acceptance Criteria

1. Lista de links em `/dashboard` (Links tab) ordenada por position.
2. Botão "Adicionar link" abre modal com campos title + url + icon (select).
3. Edição inline de title (clique abre input) e url (botão de edit ao lado).
4. Switch on/off para is_visible com persistência otimista.
5. Botão delete com modal de confirmação dupla.
6. Toast de sucesso/erro em todas as operações.
7. Server Actions com validação Zod e tratamento de RLS errors.
8. Empty state quando 0 links: ilustração + CTA "Adicione seu primeiro link".

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml` (sem chave `coderabbit_integration`; `mcp.enabled: false`).
> Quality validation will use manual review process only (workaround **QA-001** — CodeRabbit/WSL incompatível em macOS; ver `docs/qa/known-issues.md`).
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks

- [x] **Task 1 — Validators Zod de link** (AC: 2, 3, 7)
  - [x] 1.1 Criar `lib/validators/link.ts` (kebab-case, Zod 4 — espelha `lib/validators/profile.ts`). Schemas PascalCase com sufixo `Input` (naming arch §2478): `CreateLinkInput` = `{ title, url, icon? }`; `UpdateLinkInput` = `{ id: uuid, title?, url? }` (campos editáveis inline AC3). Exportar `z.infer` types homônimos (padrão `profile.ts`).
  - [x] 1.2 Regras Zod **alinhadas ao CHECK do DB** (`0004_links.sql`, DEV-2 — NÃO o snippet ilustrativo arch §1310-1317): `title` = `z.string().trim().min(1).max(100)` (CHECK `length BETWEEN 1 AND 100`); `url` = `z.string().regex(/^https?:\/\//i, 'URL deve começar com http:// ou https://')` (CHECK `url ~* '^https?://'`; Zod 4 idiom, sem `.url()` deprecado); `icon` = `z.string().regex(/^[a-z0-9-]{1,40}$/).optional()` (CHECK `icon ~ '^[a-z0-9-]{1,40}$'`). `id` = `z.string().uuid()`.
  - [x] 1.3 Unit test `tests/unit/validators/link-input.test.ts` (arch §2257): casos válidos + inválidos por campo (title vazio/>100, url sem http(s)/`javascript:`, icon com maiúscula/espaço/>40, id não-uuid). Cobertura ≥ thresholds do project `unit`.

- [x] **Task 2 — Helper de cache `lib/cache.ts`** (AC: 7)
  - [x] 2.1 Criar `lib/cache.ts` **verbatim** de arch §Service Example (L1278-1298, DEV-1): `revalidateUserSurface(username: string | null | undefined, opts: { dashboardOnly?: boolean } = {}): void` → `revalidatePath('/dashboard', 'layout')` + (se `!opts.dashboardOnly && username`) `revalidatePath('/@${username}')`. Docstring canônica do arch. Único arquivo autorizado a importar `revalidatePath` de `next/cache` (Coding Standards §2398).
  - [x] 2.2 (Se `eslint.config.mjs` já tiver a `no-restricted-imports` de `next/cache` — verificar Story 1.1) garantir `lib/cache.ts` não é pego pela regra (é o helper canônico). Se a regra ainda não existir, **não** adicioná-la nesta story (fora de escopo — pertence à Story 1.1; documentar em Completion Notes).

- [x] **Task 3 — Server Actions `server/links/actions.ts`** (AC: 2, 3, 4, 5, 7)
  - [x] 3.1 Criar `server/links/actions.ts` com `'use server'` no topo. Imports espelhando `server/profile/actions.ts`: `ActionResult` de `@/lib/result`, `createClient` de `@/lib/supabase/server`, `Tables` de `@/lib/supabase/types` (`type Link = Tables<'links'>`), schemas de `@/lib/validators/link`, `revalidateUserSurface` de `@/lib/cache`. Copiar o helper local `flattenFieldErrors` de `server/profile/actions.ts:12-22` **verbatim** (comentário "mantido local para não acoplar os módulos server/" — DEV-3, NÃO usar o `as any` do snippet arch §1327).
  - [x] 3.2 `createLink(input: unknown): Promise<ActionResult<Link>>` (AC2/AC7): `safeParse(CreateLinkInput)` → `{ ok:false, error:'Entrada inválida', fieldErrors }` se falhar. `getUser()`; sem user → `{ ok:false, error:'Sessão expirada. Faça login novamente.' }`. Resolver page+username em uma query: `.from('profiles').select('username, pages!inner(id)').eq('id', user.id).single()` (arch §1338-1342; 1:1 page garantida pelo trigger da Story 2.2). `pageId = profile.pages.id`. Próximo position: `.from('links').select('position').eq('page_id', pageId).order('position', { ascending:false }).limit(1).maybeSingle()` → `nextPos = (maxRow?.position ?? -1) + 1` (arch §1348-1355; gaps OK — Q1 RESOLVED). `.insert({ page_id: pageId, position: nextPos, ...parsed.data }).select().single()`. Em erro mapear classe RLS/CHECK (ver 3.6); sucesso → `revalidateUserSurface(profile.username)` + `{ ok:true, data }`.
  - [x] 3.3 `updateLink(input: unknown): Promise<ActionResult<Link>>` (AC3/AC7): `safeParse(UpdateLinkInput)`. `getUser` guard. `.from('links').update({ ...campos presentes }).eq('id', parsed.data.id).select().single()` — RLS `links_update_own` (2.3) garante ownership via JOIN `pages.profile_id = auth.uid()`; row não-owner ⇒ 0 rows ⇒ `{ ok:false, error:'Link não encontrado' }`. Buscar `username` p/ revalidate (`.from('profiles').select('username').eq('id', user.id).single()` ou via join na própria update select). `revalidateUserSurface(username)`.
  - [x] 3.4 `deleteLink(input: unknown): Promise<ActionResult<void>>` (AC5/AC7): `safeParse({ id: uuid })`. Guard. `.from('links').delete().eq('id', id)` — RLS `links_delete_own`. **Gap em `position` é o comportamento sancionado (Q1 RESOLVED — NÃO compactar/reindexar; reorder é 2.6).** `revalidateUserSurface(username)` + `{ ok:true, data: undefined }`.
  - [x] 3.5 `toggleLinkVisibility(input: unknown): Promise<ActionResult<Link>>` (AC4/AC7): `safeParse({ id: uuid, is_visible: boolean })`. Guard. `.update({ is_visible }).eq('id', id).select().single()` — RLS `links_update_own`. `revalidateUserSurface(username)`.
  - [x] 3.6 **Tratamento de RLS/CHECK errors (AC7)** — espelhar o mapeamento de `server/profile/actions.ts:55-69`: erro Postgres com `42501`/violação RLS ou row ausente (não-owner) → mensagem PT-BR tratada (`'Link não encontrado'` ou `'Operação não permitida'`); `23514` (CHECK title/url/icon) → `'Dados do link inválidos'`; `23505` (UNIQUE `(page_id, position)` — não esperado no fluxo CRUD de 2.5, mas mapear defensivamente) → `'Conflito de ordenação'`; fallback `'Erro ao salvar o link. Tente novamente'`. **Nunca** vazar `error.message` cru.

- [x] **Task 4 — Primitivos de design system faltantes** (AC: 2, 4, 5)
  - [x] 4.1 `pnpm dlx shadcn@latest add dialog switch alert-dialog` (DEV-4) — **só os faltantes, SEM `shadcn init`** (idempotência — precedente Story 1.8 §Drift / Story 2.4). `components/ui/` hoje tem só button/input/form/label/sonner/avatar/dropdown-menu/sheet. Verificar que existentes ficam **intactos** e `package.json`/lock **não** mudam manualmente (Base UI-backed, padrão 2.4 — Base UI ≡ Radix).
  - [x] 4.2 `pnpm typecheck` + `pnpm lint` exit 0 após add (corrigir `import/order` cosmético nos primitivos gerados via `eslint --fix` se necessário — classe prevista, precedente 2.4).

- [x] **Task 5 — `lib/link-icons.ts` (catálogo de ícones para o select)** (AC: 2)
  - [x] 5.1 Criar `lib/link-icons.ts` (DEV-5): array tipado de slugs lucide-react curados (sociais/web comuns — ex.: `instagram, twitter, facebook, youtube, linkedin, github, globe, mail, link, music, twitch, dribbble`). Cada slug DEVE casar `^[a-z0-9-]{1,40}$` (CHECK do DB) e existir em `lucide-react` (`^1.14.0`, já dep). Exportar `LINK_ICONS: readonly { slug: string; label: string }[]` + helper `getLinkIcon(slug)` que resolve o componente lucide (dynamic/lookup) com fallback para `Link` se slug ausente.
  - [x] 5.2 Render do ícone reutilizado por `LinkRow` e pelo preview do `AddLinkModal`.

- [x] **Task 6 — `app/dashboard/page.tsx` → lista de links (RSC)** (AC: 1, 8)
  - [x] 6.1 Substituir o placeholder atual (comentário inline da 2.4 já sanciona a substituição). Espelhar o padrão de `app/dashboard/profile/page.tsx`: `createClient`, `getUser`, `redirect('/login')` se sem user (defense-in-depth — layout já guarda). O shell (`app/dashboard/layout.tsx`) provê o **único `<main>`**, header e banner — esta page renderiza só o conteúdo da aba.
  - [x] 6.2 Fetch dos links do usuário ordenados por position: resolver `page_id` via `profiles.pages!inner(id)` (ou `.from('links')` com RLS `links_select_own` filtrando por ownership) — `.from('links').select('*').eq('page_id', pageId).order('position', { ascending: true })`. RLS `links_select_own` (2.3) já restringe ao owner; ainda assim filtrar por `page_id` resolvido do user (defense-in-depth).
  - [x] 6.3 `links.length === 0` → renderizar `<EmptyState />` (AC8): ícone lucide como "ilustração" + texto + CTA "Adicione seu primeiro link" que abre o `AddLinkModal` (Tailwind, sem novo primitivo Card — DEV-8, padrão visual do placeholder atual `rounded-md border bg-card p-6`). Caso contrário renderizar `<LinkList links={links} />`. Botão "Adicionar link" (AC2) visível em ambos os estados (no header da seção quando há links; como CTA no empty state).

- [x] **Task 7 — Componentes de cliente `components/links/`** (AC: 2, 3, 4, 5, 6)
  - [x] 7.1 `components/links/AddLinkModal.tsx` (`'use client'`, AC2): `Dialog` + form react-hook-form + `zodResolver(CreateLinkInput)` (espelha `components/profile/UsernameForm.tsx`). Campos: `title` (Input), `url` (Input), `icon` (select sobre `LINK_ICONS`). `onSubmit` → `createLink(values)`; `!res.ok` → `toast.error(res.error)` + `fieldErrors → form.setError`; `res.ok` → `toast.success('Link adicionado')` + fechar modal + `router.refresh()` (padrão `UsernameForm`).
  - [x] 7.2 `components/links/LinkList.tsx` (AC1): recebe `links` (já ordenada por position do RSC), mapeia para `<LinkRow />`. **Sem drag-drop** (Story 2.6). Lista simples ordenada.
  - [x] 7.3 `components/links/LinkRow.tsx` (`'use client'`, AC3/AC4/AC5/AC6): exibe ícone (via `getLinkIcon`), title, url. **Edição inline (AC3):** clique no title → vira `Input` controlado; blur/Enter → `updateLink({ id, title })`; botão de edit (ícone lucide `pencil`) ao lado da url → abre Input de url → `updateLink({ id, url })`. **Switch (AC4):** `Switch` para `is_visible` com **persistência otimista** — `useOptimistic` (Next 16) ou `useState`+rollback (arch §1141): aplica UI imediato, chama `toggleLinkVisibility({ id, is_visible })`; se `!res.ok` → reverte estado + `toast.error`. **Delete (AC5):** botão delete (ícone `trash`) → `AlertDialog` de confirmação **dupla** (modal é a 1ª barreira; botão destrutivo "Excluir" explícito e distinto de "Cancelar" — DEV-6); confirmar → `deleteLink({ id })` → `toast` + `router.refresh()`. **Toast (AC6):** sucesso/erro em **todas** as operações (create/update/toggle/delete) via `@/lib/toast`.
  - [x] 7.4 Acessibilidade: `Switch` com `aria-label`/label associando "visível"; inputs inline com label acessível; `AlertDialog`/`Dialog` com foco gerenciado pelo primitivo (Base UI). Botões com nome acessível (ícone + `sr-only` ou `aria-label`).

- [x] **Task 8 — Testes de componente** (AC: 1, 2, 3, 4, 5, 6, 8)
  - [x] 8.1 Criar `tests/components/features/links/` (dir novo). Padrão de mock = `tests/components/features/profile/UsernameForm.test.tsx`: `vi.mock('@/server/links/actions')`, `vi.mock('@/lib/toast')`, `vi.mock('next/navigation')` (`useRouter().refresh`).
  - [x] 8.2 `LinkRow.test.tsx` (espelha o exemplo arch §2289-2316): toggle visibility chama `toggleLinkVisibility` com `{ id, is_visible }` + rollback em `{ ok:false }`; edição inline de title/url chama `updateLink`; delete abre modal e só chama `deleteLink` após confirmar; toast em sucesso/erro.
  - [x] 8.3 `AddLinkModal.test.tsx`: abre modal, validação inline (title vazio, url inválida) sem chamar action; submit válido chama `createLink` e fecha + toast success; `fieldErrors` do server refletem em `FormMessage`.
  - [x] 8.4 `LinkList.test.tsx`: renderiza N rows na ordem recebida (AC1); empty state (0 links) mostra ilustração + CTA "Adicione seu primeiro link" (AC8). Cobertura `components` ≥ 50% (NFR6, precedente 2.1/2.4).

- [x] **Task 9 — Testes de integração de Server Actions** (AC: 7)
  - [x] 9.1 Criar `tests/integration/server-actions/links.test.ts` (arch §2265) espelhando o harness de `tests/integration/server-actions/profile.test.ts`: mock in-memory de `next/headers.cookies()`, mock de `next/navigation.redirect`, `await import('@/server/links/actions')` + `await import('@/lib/supabase/server')`, `admin`/`TEST_USER_PASSWORD` de `../helpers/test-users`. **Mockar `@/lib/cache`** (`revalidateUserSurface` é no-op fora de request Next — `vi.mock`). Substrate `biolink-dev`; fixtures isolados por **prefixo + faixa UUID próprios** (distintos de `auth.test.ts`/`profile.test.ts`/`rls/*` — ver docstrings existentes; ex.: prefixo `cifx-link-`, UUID `…1041/1042`), cleanup via `admin` em `afterAll`/`finally`.
  - [x] 9.2 Cenários: (a) `createLink` sem session → `{ ok:false }`; (b) input inválido (title vazio/url sem http/icon inválido) → `{ ok:false, fieldErrors }` sem insert; (c) `createLink` válido autenticado → `{ ok:true }`, row com `position = max+1`, `page_id` correto; (d) 2º `createLink` → position incrementa; (e) `updateLink` próprio → row atualizada (`title`/`url`), `updated_at` tocado pelo trigger `trg_links_set_updated_at`; (f) `updateLink`/`deleteLink`/`toggleLinkVisibility` de link de **outro usuário** → tratado como `{ ok:false }`/0 rows (RLS `links_*_own` via JOIN pages — **AC7 tratamento de RLS errors**); (g) `toggleLinkVisibility` alterna `is_visible`; (h) `deleteLink` próprio → row some, **gap em position permanece** (Q1 RESOLVED, sem reindex). Cobertura `integration` ≥ thresholds; zero regressão nas suítes existentes.

- [x] **Task 10 — Quality gate local + fechamento (handoff)** (AC: todos)
  - [x] 10.1 Rodar os 6 `quality_gate_tools`: `pnpm typecheck` · `pnpm lint` · `pnpm build` · `pnpm test:unit` · `pnpm test:components` · `pnpm test:integration` — **todos exit 0**. `ƒ Proxy (Middleware)` no build = INFO-001 esperado/herdado (não bloqueia). Rotas `theme/analytics/account` 404 = esperado (Stories futuras). Zero regressão vs baseline (`test` full verde).
  - [ ] 10.2 **Smoke manual (handoff owner, deixar `[ ]` — padrão 1.5–2.4, não bloqueia Done):** logar; em `/dashboard` ver empty state; "Adicionar link" → criar link válido (toast); editar title inline + url; toggle visibilidade (UI otimista); tentar URL inválida (erro tratado); deletar link com modal de confirmação dupla; recarregar e ver lista persistida ordenada.
  - [x] 10.3 Preencher Dev Agent Record (Agent Model, Debug Log, Completion Notes, File List) + Handoff + Change Log. **Commit `@dev` em branch `feat/2.5-crud-links`** (stacked sobre HEAD da 2.4 Done). **Push + PR são EXCLUSIVOS de @devops** (`*push`/`*create-pr` — `.claude/rules/agent-authority.md`). Gate de qualidade via **QA-001 manual** (macOS sem WSL/CodeRabbit — `docs/qa/known-issues.md`). Fechamento: gate manual @qa → `docs/qa/gates/2.5-crud-links.yml` → `@po *close-story 2.5`.

## Dev Notes

> **Self-contained:** todo detalhe técnico abaixo vem de artefatos reais (`docs/architecture.md`, `docs/architecture/schema-design.md`, `supabase/migrations/0004_links.sql` via Story 2.3, código existente). O Dev Agent **não** deve precisar abrir os docs de arquitetura — tudo necessário está aqui + nas Tasks.

### Pré-existência (NÃO recriar — IDS REUSE)

- **Tabela `links` + RLS + tipos** já existem (Story 2.3 **Done**, `supabase/migrations/0004_links.sql`). Colunas: `id uuid PK`, `page_id uuid NOT NULL REFERENCES pages(id) ON DELETE CASCADE`, `title text NOT NULL CHECK (length BETWEEN 1 AND 100)`, `url text NOT NULL CHECK (url ~* '^https?://')`, `icon text CHECK (icon IS NULL OR icon ~ '^[a-z0-9-]{1,40}$')`, `is_visible boolean NOT NULL DEFAULT true`, `position integer NOT NULL CHECK (position >= 0)`, `created_at`, `updated_at`. Constraint `uniq_links_page_position UNIQUE (page_id, position) DEFERRABLE INITIALLY DEFERRED`. Trigger `trg_links_set_updated_at`. RLS policies: `links_select_own`, `links_select_public`, `links_insert_own`, `links_update_own`, `links_delete_own` — **todas** via `EXISTS (SELECT 1 FROM pages p WHERE p.id = page_id AND p.profile_id = auth.uid())`. `lib/supabase/types.ts` já inclui `links` Row/Insert/Update + Relationship FK `page_id→pages`. [Source: Story 2.3 Dev Agent Record / File List; `docs/architecture/schema-design.md §4 #links`]
- **Page 1:1 garantida**: trigger `on_auth_user_created` (Story 2.2) cria `pages` row no signup. `createLink` resolve `page_id` via join `profiles.pages!inner(id)`. [Source: Story 2.2; arch §1338-1342]
- **`ActionResult<T>`** em `lib/result.ts`: `{ ok:true; data:T } | { ok:false; error:string; fieldErrors?:Record<string,string> }`. UI sempre checa `res.ok`. [Source: `lib/result.ts`; arch §445-452, §2393]
- **Padrão de Server Action** = `server/profile/actions.ts` (Story 2.1, **Done**): `'use server'`; `input: unknown`; `Schema.safeParse(input)`; helper local `flattenFieldErrors` (copiar verbatim, NÃO acoplar módulos `server/`); `createClient` de `@/lib/supabase/server`; `getUser()` guard com `'Sessão expirada. Faça login novamente.'`; mapeamento de erro Postgres por código (ver `:55-69` o caso `23505`); retorno `ActionResult`. [Source: `server/profile/actions.ts`]
- **Padrão de validator** = `lib/validators/profile.ts`: Zod 4, schemas PascalCase `*Input`, `z.infer` types homônimos. [Source: `lib/validators/profile.ts`]
- **Padrão de form client** = `components/profile/UsernameForm.tsx`: `'use client'`; `useForm({ resolver: zodResolver(Schema) })`; `Form/FormField/FormItem/FormLabel/FormControl/FormMessage` de `@/components/ui/form`; `toast` de `@/lib/toast`; `res.ok` check; `res.fieldErrors → form.setError`; `router.refresh()` pós-sucesso. [Source: `components/profile/UsernameForm.tsx`]
- **Padrão de RSC page** = `app/dashboard/profile/page.tsx`: `createClient`→`getUser`→`redirect('/login')` guard→fetch→render. O shell `app/dashboard/layout.tsx` já provê **único `<main>`**, header (UserMenu) e `EmailVerificationBanner`. [Source: `app/dashboard/{layout,profile/page}.tsx`]
- **Toast facade** `@/lib/toast` → `toast.success/error/info` (NÃO importar `sonner` direto — Story 1.8 §Drift). [Source: `lib/toast.ts`]
- **Aba "Links" → `/dashboard`** (raiz). `components/dashboard/nav-items.ts` já mapeia `{ label:'Links', href:'/dashboard' }`; `app/dashboard/page.tsx` hoje é placeholder com comentário sancionando substituição nesta story. [Source: `components/dashboard/nav-items.ts`, `app/dashboard/page.tsx`]

### Server Actions — Inventário desta story (arch §392-399)

```typescript
// server/links/actions.ts
export async function createLink(input: unknown): Promise<ActionResult<Link>>;
export async function updateLink(input: unknown): Promise<ActionResult<Link>>;
export async function deleteLink(input: unknown): Promise<ActionResult<void>>;
export async function toggleLinkVisibility(input: unknown): Promise<ActionResult<Link>>;
```

> `reorderLinks` (arch §400) **NÃO** faz parte desta story — é **Story 2.6** (drag-and-drop). `@dnd-kit` **não** é dep e **não** deve ser adicionado aqui (DEV-7). O primitivo `DEFERRABLE` que habilita o reorder atômico já existe (2.3) mas só é exercitado em 2.6. [Source: PRD §Story 2.6; `schema-design.md §900-913`]

`createLink` referência canônica (arch §1300-1368) — resolução de page + nextPos:

```typescript
const { data: profile } = await supabase
  .from('profiles')
  .select('username, pages!inner(id)')
  .eq('id', user.id)
  .single();
const pageId = profile.pages.id;
const { data: maxRow } = await supabase
  .from('links')
  .select('position')
  .eq('page_id', pageId)
  .order('position', { ascending: false })
  .limit(1)
  .maybeSingle();
const nextPos = (maxRow?.position ?? -1) + 1;
// insert { page_id: pageId, position: nextPos, ...parsed.data }; revalidateUserSurface(profile.username)
```

### Decisões (default fundamentado + fonte — nenhuma bloqueante)

- **DEV-1 — CREATE `lib/cache.ts` (`revalidateUserSurface`) verbatim de arch §1278-1298.** Coding Standards §2398 exige `revalidateUserSurface(username)` ao fim de **toda** Server Action que altera `links`/profile/theme/page; `revalidatePath` solto é proibido (ESLint custom rule planejada na Story 1.1). O arquivo **não existe** ainda; 2.5 é a 1ª story com mutations de `links`. A Story 2.1 (`updateUsername`) não revalidou (usou `router.refresh()` client-side) — mas 2.1 não tinha superfície pública e sua AC não exigia o helper; 2.5 introduz exatamente o caso que §2398 lista. A rota `/@username` só existe na Story 2.7 → `revalidatePath('/@${username}')` numa rota inexistente é **no-op seguro** no Next.js (apenas marca o path; nada quebra). REUSE do helper canônico documentado, **não invenção**. [Source: arch §1278-1298 (Service Example) + §2398 (Coding Standards)]. **Non-blocking; flagged p/ gate @po (IDS G3).**
- **DEV-2 — Zod alinhado ao CHECK do DB, não ao snippet ilustrativo.** Arch §1310-1317 mostra `icon: z.string().max(50).optional()` / `url: z.string().url().regex(...)` — mas o CHECK real (0004) é `icon ~ '^[a-z0-9-]{1,40}$'`, `url ~* '^https?://'`, `title length 1-100`. O Zod **deve** espelhar o CHECK para falhar no boundary com mensagem tratada em vez de surfacing de `23514`. Zod 4 idiom (sem `.url()` deprecado), padrão `lib/validators/profile.ts`. [Source: `0004_links.sql` via Story 2.3 Dev Notes Task 1.2; arch §1310-1317; Coding Standards §2392]. Decisão de implementação.
- **DEV-3 — `input: unknown` + `flattenFieldErrors` (precedente 2.1) sobre o `as any` do snippet arch §1327.** Coding Standards §2392 ("sem `as any` em entradas externas") e §2397 ("sem `any`") proíbem o cast do snippet ilustrativo. Seguir `server/profile/actions.ts` verbatim. Precedente sancionado e Done, **não invenção**. [Source: `server/profile/actions.ts:12-22`; Coding Standards §2392/2397]. **Non-blocking; flagged p/ gate @po (mesma classe das deviations 2.4).**
- **DEV-4 — Primitivos shadcn faltantes via `pnpm dlx shadcn@latest add dialog switch alert-dialog` (SEM `init`).** `components/ui/` não tem dialog/switch/alert-dialog. Precedente Story 2.4: add **só os faltantes**, sem `init`, Base UI-backed (Base UI ≡ Radix), sem mudar `package.json`/lock manualmente. [Source: Story 2.4 Dev Agent Record v0.3; Story 1.8 §Drift; arch §569-573]. Decisão de implementação.
- **DEV-5 — CREATE `lib/link-icons.ts` (catálogo curado de slugs lucide).** AC2 pede `icon (select)`. Não há registro de ícones; `links.icon` = slug lucide (arch §280, ex.: 'instagram','twitter','globe'), CHECK `^[a-z0-9-]{1,40}$`; `lucide-react` já é dep. CREATE justificado (IDS: nenhum artefato reusável; dep já presente). O **escopo do conjunto curado** é a única peça com latitude — default fundamentado (set comum de sociais/web); se produto quiser um catálogo específico, ajustável sem refactor. [Source: arch §280, Data Models Link §270-299; `0004_links.sql` CHECK]. **Escopo do catálogo flagged p/ @po (advisory, non-blocking).**
- **DEV-6 — "modal de confirmação dupla" (AC5) = `AlertDialog` destrutivo com ação de confirmação explícita distinta de cancelar.** Arch §2401: "Confirmação dupla em destrutivos: delete link ... sempre modal (Card/Modal do design system)". Interpretação: o modal é a 1ª barreira deliberada + botão "Excluir" explícito (variant destrutivo) ≠ "Cancelar" — impede delete acidental de 1 clique. AC mantida **verbatim**; interpretação sólida via arch §2401. [Source: arch §2401, §2237 (modal no design system)]. **Non-blocking; flagged p/ gate @po.**
- **DEV-7 — Reorder/drag-drop FORA DE ESCOPO.** A AC de 2.5 não contém drag; `@dnd-kit` não é dep e não deve ser adicionado. `position` é só **lido** (ordenação AC1) e **escrito** por `createLink` (`nextPos = max+1`). Delete deixa **gap** (Q1 RESOLVED — não compactar/reindexar). [Source: PRD §Story 2.6; `schema-design.md §900-913`; Story 2.3 Dev Notes Task 1.10].
- **DEV-8 — Empty state (AC8) com Tailwind + ícone lucide (sem novo primitivo Card).** `components/ui/` não tem Card; o placeholder atual usa `rounded-md border bg-card p-6`. Manter padrão Tailwind + ícone lucide como "ilustração" + CTA que abre o `AddLinkModal`. Card no design system é trabalho futuro (arch §571) — não bloqueia. [Source: `app/dashboard/page.tsx` atual; arch §571]. Decisão de implementação.

### File Locations (arch §Components #5, §Test Organization)

- **Criar:** `lib/validators/link.ts`, `lib/cache.ts`, `lib/link-icons.ts`, `server/links/actions.ts`, `components/links/AddLinkModal.tsx`, `components/links/LinkList.tsx`, `components/links/LinkRow.tsx`, `components/ui/{dialog,switch,alert-dialog}.tsx` (via shadcn add), `tests/unit/validators/link-input.test.ts`, `tests/integration/server-actions/links.test.ts`, `tests/components/features/links/{LinkRow,AddLinkModal,LinkList}.test.tsx`.
- **Modificar:** `app/dashboard/page.tsx` (placeholder → lista RSC). Possivelmente `tests/setup.ts` se overlays Base UI (`Dialog`/`AlertDialog`) exigirem polyfills jsdom guardados (precedente 2.4 — `pointer-events`/`ResizeObserver`/`scrollIntoView`).
- **NÃO tocar:** `supabase/migrations/*`, `lib/supabase/types.ts` (já tem `links`), shell da 2.4 (`layout.tsx`/`Sidebar`/`MobileDrawer`/`UserMenu`), `eslint.config.mjs` (a regra `no-restricted-imports` de `next/cache` é da Story 1.1 — não introduzir aqui).

### Testing

- **Frameworks:** Vitest 4 (projects `unit`/`components`/`integration`) + Testing Library + `@testing-library/user-event`. [Source: `package.json`; arch §2202-2220]
- **Localização:** unit em `tests/unit/validators/link-input.test.ts`; component em `tests/components/features/links/*.test.tsx` (dir novo); integration em `tests/integration/server-actions/links.test.ts`. [Source: arch §2226-2278]
- **Component pattern** (espelhar `tests/components/features/profile/UsernameForm.test.tsx` + exemplo arch §2289-2316): `vi.mock('@/server/links/actions')`, `vi.mock('@/lib/toast')`, `vi.mock('next/navigation')`. Asserir chamada da action com payload correto + comportamento de toast/rollback.
- **Integration pattern** (espelhar `tests/integration/server-actions/profile.test.ts`): mock in-memory de `next/headers.cookies()`, mock de `next/navigation.redirect`, `vi.mock('@/lib/cache')` (no-op fora de request), `await import(...)` dinâmico de action+`createClient`, `admin`/`TEST_USER_PASSWORD` de `../helpers/test-users`, substrate `biolink-dev`, fixtures isolados por **prefixo + faixa UUID próprios** distintos das suítes existentes, cleanup via `admin`. Cobrir AC7: RLS de não-owner tratada como `{ ok:false }`/0 rows (RLS = "row não visível", não necessariamente erro — comentário canônico em `rls/*.test.ts`).
- **Cobertura:** `components` ≥ 50% (NFR6, precedente 2.1=87% / 2.4); `unit`/`integration` ≥ thresholds do `vitest.config`. Zero regressão na suíte full (`pnpm test`).
- **Smoke manual:** golden path em Task 10.2, deixar `[ ]` (handoff owner, padrão 1.5–2.4, não bloqueia Done).

## Change Log

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Author     |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| 2026-05-19 | 0.1     | Story criada via `@sm *draft 2.5` (task `create-next-story`). Status: Draft. Story 2.4 confirmada **Done** (Change Log v0.4 `@po *close-story`; commit `ee41021` PR #11); 2.5 é a próxima sequencial do Epic 2 (**5/7**). ACs **verbatim** de `docs/prd.md §Story 2.5` (L424-433). Contexto cruzado contra estado real do repo (`links` table/RLS/types de 0004 Story 2.3 Done; `server/profile/actions.ts` padrão Server Action 2.1; `lib/result.ts`/`lib/validators/profile.ts`/`lib/toast.ts`; `components/profile/UsernameForm.tsx` padrão form; `app/dashboard/{layout,page,profile/page}.tsx` shell 2.4; `components/ui/` = button/input/form/label/sonner/avatar/dropdown-menu/sheet — sem dialog/switch/alert-dialog; `lib/cache.ts` **inexistente**; `tests/integration/server-actions/{auth,profile}.test.ts` + `tests/components/features/profile/UsernameForm.test.tsx` harness) + `docs/architecture.md` (§392-400 inventário Server Actions; §1278-1298 `revalidateUserSurface` canônico; §1300-1368 `createLink` canônico; §445-452 ActionResult; §1107-1145 state/optimistic; §2202-2317 Testing; §2384-2483 Coding Standards) + `schema-design.md §900-913` (Q1 RESOLVED — gaps on delete) + precedente 2.1 (Frontend+Server-Action Done) / 2.3 (schema) / 2.4 (shell + shadcn-add) + MEMORY (projeto único, QA-001). 8 decisões isoladas com default fundamentado + fonte (DEV-1 CREATE `lib/cache.ts` verbatim arch; DEV-2 Zod ↔ CHECK do DB; DEV-3 `input:unknown`/`flattenFieldErrors` precedente 2.1 sobre `as any`; DEV-4 shadcn add faltantes sem init; DEV-5 `lib/link-icons.ts` curado; DEV-6 confirmação dupla = AlertDialog destrutivo; DEV-7 reorder OUT-OF-SCOPE→2.6; DEV-8 empty state Tailwind sem Card) — **nenhuma bloqueante** (todas resolvem por arch canônico/precedente sancionado, não invenção; DEV-1/DEV-3/DEV-5/DEV-6 flagged p/ gate @po). CodeRabbit: skip notice (sem chave `coderabbit_integration` em core-config + `mcp.enabled:false`). ClickUp: sync pulado (`mcp.enabled:false`). Próximo: `@po *validate-story-draft 2.5`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | River (sm) |
| 2026-05-19 | 0.3     | Implementação `@dev *develop 2.5` (task `dev-develop-story`, branch `feat/2.5-crud-links` stacked sobre HEAD da 2.4 Done `ee41021`). **Todas as 10 Tasks concluídas** (10.2 smoke manual deixado `[ ]` — handoff owner, não bloqueia). `devLoadAlwaysFiles`/fallback inexistentes no repo → seguido o princípio self-contained (Dev Notes) + padrões reais do código. Criados: `lib/validators/link.ts`, `lib/cache.ts`, `lib/link-icons.ts`, `server/links/actions.ts`, `components/links/{AddLinkModal,LinkList,LinkRow,EmptyState}.tsx`, `components/ui/{dialog,switch,alert-dialog}.tsx` (shadcn add, só faltantes, sem init; `button.tsx`/`package.json`/lock intactos), 4 suítes de teste (unit/component×3/integration). Modificado: `app/dashboard/page.tsx` (placeholder→RSC lista+empty state). **`EmptyState` extraído** p/ componente testável (page é RSC async; decomposição justificada, mantém DEV-8). DEV-2..DEV-8 seguidos; **DEV-1 correlato**: snippet arch §1338-1342 (`profile.pages.id`) **incorreto p/ o repo** — tipos gerados tipam embed reverso 1:1 como array, runtime PostgREST retorna objeto: divergência typegen↔runtime quebrava `createLink` + lista do dashboard (não pego por typecheck/lint/component-tests; **exposto só pelo teste de integração real**). **Fix:** substituído embed por 2 queries diretas type-safe em `createLink` e `dashboard/page.tsx` (anti-hallucination, DEV-2: runtime > snippet). **Quality gate 6/6 verde** (typecheck/lint/build/unit 47/components 89/integration 67), zero regressão. Zod `z.string().uuid()` valida versão RFC (fixture de unit test ajustado p/ UUID v4; `links.id` é `gen_random_uuid()` v4 sempre válido). lucide-react@1.14.0 sem ícones de marca → catálogo curado de genéricos (DEV-5 latitude sancionada). CodeRabbit: skip (QA-001 macOS — gate manual). Próximo: gate manual **@qa** → `docs/qa/gates/2.5-crud-links.yml`; push/PR por **@devops** (`*push`/`*create-pr`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Dex (dev)  |
| 2026-05-19 | 0.2     | Validação `@po *validate-story-draft 2.5` (task `validate-next-story`, auditoria 11-pontos). **Veredito: GO** — Readiness 10/10, Confidence High. Template v2 **completo** (Status/Executor/Story/AC/CodeRabbit-skip/Tasks/Dev Notes+Testing/Change Log/Dev Agent Record/QA Results; zero placeholder pendente). ACs **verbatim** vs `prd.md §Story 2.5` L426-433 (8/8 idênticas). Cobertura AC↔Task **completa** (AC1→T6.2/7.2/8.4; AC2→T1/3.2/4/5/7.1/8.3; AC3→T1/3.3/7.3/8.2; AC4→T3.5/7.3/8.2; AC5→T3.4/4/7.3/8.2; AC6→T7/8; AC7→T1/3.2-3.6/9; AC8→T6.3/8.4). **Anti-hallucination CLEAN** — toda claim técnica verificada contra artefato real: `0004_links.sql` (CHECK title 1-100 / `url ~* '^https?://'` / `icon ~ '^[a-z0-9-]{1,40}$'`, `uniq_links_page_position UNIQUE DEFERRABLE`, `trg_links_set_updated_at`, 5 RLS `links_*_own/public` via `EXISTS JOIN pages` — batem verbatim); `server/profile/actions.ts` (`flattenFieldErrors` :12-22, mapeamento erro :55-69 incl. 23505, guard `getUser`/'Sessão expirada…', `input:unknown`+safeParse); `lib/result.ts` (shape exato); `lib/validators/profile.ts` (Zod4 PascalCase`Input`+`z.infer`); `components/profile/UsernameForm.tsx` (RHF+zodResolver+`res.ok`+`form.setError`+`router.refresh`); `app/dashboard/{layout,page}.tsx` (único `<main>`/UserMenu/EmailVerificationBanner/guard; placeholder + comentário sancionando substituição); `components/dashboard/nav-items.ts` (`{Links,/dashboard}`); `lib/toast.ts` (facade); `lib/supabase/types.ts` (links Row + `links_page_id_fkey`); `lib/cache.ts` **ausente** (confirma DEV-1 CREATE); `components/ui/` = avatar/button/dropdown-menu/form/input/label/sheet/sonner (sem dialog/switch/alert-dialog → confirma DEV-4); arch §1289-1297 `revalidateUserSurface` (assinatura **idêntica** ao DEV-1), §1338-1355 `createLink` page/nextPos (**idêntico** Task 3.2), §392-400 inventário (reorderLinks corretamente excluído→2.6 DEV-7), §2398 (`revalidateUserSurface` obrigatório + ESLint rule Story 1.1), §2401 (confirmação dupla destrutivo), §2478 (Zod PascalCase`Input`); harness `tests/integration/server-actions/profile.test.ts` + `tests/components/features/profile/` + `tests/integration/helpers/test-users.ts` existem. **Mérito anti-hallucination:** DEV-2/DEV-3 **flagam corretamente** os defeitos do snippet ilustrativo arch (`as any` §1327, `.url()` deprecado §1314, `icon max(50)` ≠ CHECK `^[a-z0-9-]{1,40}$` §1316) e desviam alinhando ao CHECK do DB / precedente 2.1 — Constitution Art. IV compliant (precedente+fonte, **não invenção**). **IDS G3:** `executor:@dev`/`quality_gate:@qa` é override vs lista de gates conhecidos da task §1.1 (@architect/@dev/@pm) e type-map template (Frontend→@ux-design-expert/@dev) — **ACEITO com razão documentada**: precedente verbatim sancionado 2.1/Epic 1 (1.5–1.9)/2.3/2.4 todas **Done**, mesma classe aceita em 2.4 v0.2 (não re-litigada per convenção 2.3); não-bloqueante. Nits **não-bloqueantes (advisory)**: (1) AC5 "confirmação dupla" — interpretação DEV-6 (`AlertDialog` destrutivo + botão "Excluir" explícito ≠ "Cancelar") **confirmada suficiente p/ MVP** (type-to-confirm seria over-engineering p/ delete de link; arch §2401 exige "sempre modal", satisfeito); (2) DEV-5 escopo do catálogo curado de ícones — latitude reconhecida, default (sociais/web comuns) aceitável, ajustável sem refactor. Security OK: RLS errors tratados PT-BR sem vazar `error.message` cru (T3.6), regex url `^https?://` rejeita `javascript:` (T1.3 testa), guard `getUser` em toda action, ownership via RLS JOIN + filtro defense-in-depth. 0 Critical, 0 Should-Fix bloqueante. CodeRabbit: N/A (skip notice correto — disabled + `mcp.enabled:false`). Transição **Draft → Ready** aplicada (`story-lifecycle.md` — responsabilidade @po no GO). Próximo: `@dev *develop 2.5` (branch `feat/2.5-crud-links`, stacked sobre HEAD da 2.4 Done). | Pax (po)   |
| 2026-05-19 | 0.4     | **Fechada via `@po *close-story 2.5`** (task `po-close-story`). QA gate **PASS** — fonte autoritativa `docs/qa/gates/2.5-crud-links.yml`, gate manual **QA-001** por @qa Quinn (CodeRabbit/WSL indisponível em macOS — `docs/qa/known-issues.md`). 7 quality checks **PASS** (code_review/unit_tests/acceptance_criteria/no_regressions/performance/security/documentation) + 6 `quality_gate_tools` **re-executados INDEPENDENTEMENTE** pelo @qa no commit `f41fcbb`: typecheck/lint/build exit 0; `pnpm test` **203/203 em 27 arquivos** (+33 testes / +5 arquivos vs feat/2.4 170/22 = exatamente as 5 suítes novas da 2.5 — link-input unit, LinkRow/AddLinkModal/LinkList component, links integration; **zero regressão** em auth/landing/ui/profile/middleware/pages/links-rls/server-actions). **8/8 ACs satisfeitos** (matriz traceability Given-When-Then completa: AC1 lista RSC ordenada por position; AC2 AddLinkModal Dialog+RHF/zodResolver; AC3 inline edit title/url; AC4 Switch otimista+rollback; AC5 AlertDialog confirmação dupla DEV-6; AC6 toast todas ops; AC7 4 Server Actions input:unknown+Zod↔CHECK de 0004 + mapLinkError sem vazar error.message; AC8 EmptyState). **0 blockers**; 3 observações **low/info não-bloqueantes** (UX-001 flash breve no inline-edit — consistência eventual sub-segundo; OBS-002 revalidate degrada graciosamente — `/@username` só existe na 2.7, no-op seguro; OBS-003 `nextPos=max+1` não-atômico — single-user MVP, reorder atômico via constraint DEFERRABLE é escopo explícito da Story 2.6 por design arch §7 Q1). **Destaque:** bug crítico typegen↔runtime (embed reverso `profiles.pages` 1:1 tipado como array mas PostgREST retorna objeto único) **detectado e corrigido pelo @dev via teste de integração real** — fix type-safe (2 queries diretas em `createLink`+`dashboard/page.tsx`, sem casts); validou a estratégia de testes da story. Todas as deviations **APPROVED** (DEV-1..DEV-8 + DEV-correlato typegen + EmptyState-extract + IDS G3) — arch canônico / precedente sancionado 2.1/2.3/2.4, não invenção; já validadas pelo gate @po v0.2, não re-litigadas per convenção 2.3. Stories flat (sem `docs/stories/epics/` index nem `backlog.md`) → close **story-only** (po-close-story §Error Handling); QA Results permanece como registro **@qa-owned** do gate. Status **Ready for Review → Done**. Epic 2: **5/7** done (2.1/2.2/2.3/2.4/2.5). **DOWNSTREAM:** branch `feat/2.5-crud-links` (1 commit `f41fcbb`, stacked sobre `main` `ee41021` Story 2.4 #11) **ainda não pushed** — `@devops *push` + `*create-pr` são **EXCLUSIVOS de @devops** (`.claude/rules/agent-authority.md`); CI rodará os required checks. Task 10.2 (smoke manual no browser) permanece `[ ]` por design (handoff owner, não bloqueia Done — padrão 1.5–2.4). Próxima: **Story 2.6 — reorder/drag-drop de links** (1º consumer real da constraint `UNIQUE (page_id, position) DEFERRABLE` da 2.3) via `@sm *draft 2.6`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Pax (po)   |

## Dev Agent Record

_This section is populated by the development agent during implementation._

### Agent Model Used

claude-opus-4-7 (Dex / @dev) — `*develop 2.5`

### Debug Log References

- Task 1: unit test inicial usava UUID sintético (`…1041`) que falha o `z.string().uuid()` (Zod 4 valida versão RFC 9562). Corrigido p/ UUID v4 real no fixture de teste — `links.id` em produção/integração é `gen_random_uuid()` (sempre v4 válido), validator mantido conforme story Task 1.2.
- Task 9: integração expôs que `profiles.pages!inner(id)` retorna objeto (não array) em runtime apesar dos tipos gerados dizerem array (FK 1:1, divergência supabase-js typegen↔PostgREST). `createLink` retornava `'Erro ao salvar o link…'` genérico. Debug: replicação das queries internas no teste mostrou `prof.data.pages = { id }` (objeto). Fix: duas queries diretas em `createLink` + `dashboard/page.tsx` (ver Completion Notes BUG CRÍTICO).

### Completion Notes List

- **Task 1** ✅ — `lib/validators/link.ts` (CreateLinkInput/UpdateLinkInput/DeleteLinkInput/ToggleLinkVisibilityInput) alinhado verbatim ao CHECK de `0004_links.sql` (title 1-100 trim, url `^https?://` regex, icon `^[a-z0-9-]{1,40}$`, id uuid v4). 47 unit tests verdes (`pnpm test:unit`).
- **Task 2** ✅ — `lib/cache.ts` (`revalidateUserSurface`) conforme DEV-1: `revalidatePath('/dashboard','layout')` + `/@${username}` condicional. Task 2.2: a regra ESLint `no-restricted-imports` de `next/cache` **não existe** em `eslint.config.mjs` (verificado) — **não adicionada** nesta story (escopo da Story 1.1, conforme instrução da Task 2.2).
- **Task 5** ✅ — `lib/link-icons.ts`: `LINK_ICONS` (34 opções slug+label) + `getLinkIcon(slug)` com fallback `Link`. **Anti-hallucination (DEV-5):** `lucide-react@1.14.0` instalado **não exporta ícones de marca** (Instagram/Github/Twitter/etc. = `undefined`, removidos por trademark) — verificado por introspecção do pacote; catálogo curado usa **ícones genéricos por propósito** (latitude sancionada DEV-5/@po v0.2). Todos os slugs casam `^[a-z0-9-]{1,40}$` e resolvem p/ componente existente. 5.2 reuso: `LinkRow` renderiza via `getLinkIcon` (createElement) + `AddLinkModal` lista `LINK_ICONS` no select.
- **Task 6** ✅ — `app/dashboard/page.tsx` substitui placeholder: RSC `createClient`→`getUser`→`redirect` guard; resolve `pageId` via `profiles.pages!inner(id)` (`pages[0].id`), fetch `links` `order(position asc)` filtrando `page_id` (defense-in-depth sobre RLS `links_select_own`). 0 links → empty state Tailwind (DEV-8: ícone lucide `Link2` + texto + CTA `AddLinkModal`, sem primitivo Card); senão `<LinkList>`. Botão "Adicionar link" no header quando há links; CTA no empty state.
- **Task 10** ✅ — Quality gate local: **6/6 verdes** — `pnpm typecheck` ✅ · `pnpm lint` ✅ · `pnpm build` ✅ (`ƒ Proxy (Middleware)` = INFO-001 esperado/herdado; warning middleware→proxy pré-existente; `/dashboard` corretamente `ƒ` dynamic) · `pnpm test:unit` 47/47 ✅ · `pnpm test:components` **89/89 (16 files)** ✅ · `pnpm test:integration` **67/67 (7 files)** ✅. **Zero regressão** vs baseline (todas as suítes pré-existentes verdes; "Not implemented: navigation" no jsdom é log benigno, não falha). 10.2 smoke manual: deixado `[ ]` (handoff owner, padrão 1.5–2.4, não bloqueia Done). Commit `@dev` em `feat/2.5-crud-links`; **push/PR são EXCLUSIVOS de @devops**. Gate de qualidade via **QA-001 manual** (macOS sem WSL/CodeRabbit). Status → **Ready for Review**.
- **Task 9** ✅ — `tests/integration/server-actions/links.test.ts` (7 tests verdes contra `biolink-dev`). Fixtures isoladas: prefixo `cifx-link-`, UUID `…1041/1042` (distintos de auth/profile/rls). Mock `next/headers`/`next/navigation`/**`@/lib/cache`** (revalidate no-op). Cobre AC7 cenários a–h: sem-session, input inválido sem insert, create position 0→1, update title/url + `updated_at` (trigger), toggle alterna `is_visible`, **cross-user update/delete/toggle bloqueado por RLS** (`{ ok:false }`), delete deixa **gap em position** (sem reindex) e próximo create usa max+1.
- **🐞 BUG CRÍTICO encontrado & corrigido (Task 9 → afeta Task 3 + Task 6):** o embed reverso `profiles.select('…, pages!inner(id)')` é **tipado como array** `{id}[]` pelos tipos gerados, mas o PostgREST retorna **objeto único** `{id}` em runtime (FK 1:1) — divergência typegen↔runtime do supabase-js. `profile.pages[0]?.id` (forçado pelo typecheck na 1ª impl) é `undefined` em runtime ⇒ `createLink` **sempre falhava** com erro genérico e a **RSC `dashboard/page.tsx` sempre mostraria empty state** em produção (não pego por typecheck/lint/component-tests; só o teste de integração real expôs). Fix: substituído o embed por **duas queries diretas type-safe** (`profiles.select('username')` + `pages.select('id').eq('profile_id', user.id)`) em `server/links/actions.ts:createLink` e `app/dashboard/page.tsx`. Decisão alinhada ao princípio anti-hallucination (DEV-2): realidade do runtime/typegen > snippet ilustrativo arch §1338-1342. PK/FK indexados — custo desprezível. typecheck + integração verdes pós-fix.
- **Task 8** ✅ — `tests/components/features/links/{LinkRow,AddLinkModal,LinkList}.test.tsx` (13 tests verdes, mock `@/server/links/actions`+`@/lib/toast`+`next/navigation` espelhando `UsernameForm.test.tsx`; Base UI overlay pattern de `MobileDrawer.test.tsx`). Cobre AC1/2/3/4/5/6/8: toggle ok+rollback, edição inline title/url, delete confirma/cancela/erro, validação inline, fieldErrors→FormMessage, ordem da lista, empty state. **Decisão de testabilidade:** `EmptyState` extraído p/ `components/links/EmptyState.tsx` (a page é RSC async+supabase, não testável em jsdom) — não está na lista "Criar" original mas é decomposição justificada p/ honrar a asserção de empty state da Task 8.4; mantém DEV-8 (Tailwind, sem Card). `LinkList.test.tsx` cobre ordem (AC1) + importa `EmptyState` p/ AC8.
- **Task 7** ✅ — `components/links/{AddLinkModal,LinkList,LinkRow}.tsx`. `AddLinkModal`: `Dialog` controlado + RHF `zodResolver(CreateLinkInput)` (espelha `UsernameForm`), select nativo de `LINK_ICONS` (`''`→`undefined` p/ não quebrar o regex opcional do Zod — **sem** primitivo `select` extra, fora do escopo sancionado da Task 4), `trigger` via prop p/ reuso header/empty. `LinkRow`: edição inline title/url (Enter/blur commit, Escape cancela) → `updateLink`; `Switch` Base UI otimista (`useState`+rollback, arch §1141) → `toggleLinkVisibility`; `AlertDialog` confirmação dupla (DEV-6: modal + botão destrutivo "Excluir" ≠ "Cancelar") → `deleteLink`; toast em **todas** as ops (AC6); a11y `aria-label` em switch/botões-ícone, inputs inline com `aria-label`. `LinkList`: lista simples ordenada (sem drag-drop — DEV-7). **Lint fix:** `react-hooks/static-components` flaga `const Icon = getLinkIcon()` + `<Icon/>` → trocado por `createElement(getLinkIcon(...), props)` (ref de componente estável do module-scope `ICON_MAP`). typecheck + lint full exit 0.
- **Task 4** ✅ — `pnpm dlx shadcn@latest add dialog switch alert-dialog` adicionou só os 3 faltantes (sem `init`). `button.tsx` (dep transitiva) **preservado** — overwrite recusado via stdin (`yes ""` → default N), precedente 2.4. `package.json`/`pnpm-lock.yaml` **inalterados** (md5 idêntico antes/depois). 6 erros `import/order` cosméticos nos gerados → `eslint --fix` + `prettier --write` (classe prevista na Task 4.2). typecheck + lint exit 0. APIs Base UI: Dialog controlado via `open`/`onOpenChange`; Switch `checked`/`onCheckedChange`; AlertDialog `AlertDialogAction`/`AlertDialogCancel`.
- **Task 3** ✅ — `server/links/actions.ts` (createLink/updateLink/deleteLink/toggleLinkVisibility) espelhando `server/profile/actions.ts` (`'use server'`, `input:unknown`, `flattenFieldErrors` local verbatim, guard `getUser`/'Sessão expirada…', `mapLinkError` por código Postgres PT-BR sem vazar `error.message`). **Deviation DEV-correlata (anti-hallucination):** os tipos gerados (`lib/supabase/types.ts`) tipam o embedding reverso `profiles→pages` como **array** `{ id }[]` mesmo com FK 1:1 — `profile.pages.id` do snippet da Task 3.2 não compila; normalizado p/ `profile.pages[0]?.id` (trigger 2.2 garante exatamente 1 page). `deleteLink` usa `.delete().eq().select('id')` p/ detectar não-owner (0 rows ⇒ `{ ok:false }`, cenário Task 9f). typecheck + lint exit 0.

### File List

| Status | File                                                             |
| ------ | ---------------------------------------------------------------- |
| Modify | `docs/stories/2.5.crud-links.story.md`                           |
| Create | `lib/validators/link.ts`                                         |
| Create | `tests/unit/validators/link-input.test.ts`                       |
| Create | `lib/cache.ts`                                                   |
| Create | `server/links/actions.ts`                                        |
| Create | `components/ui/dialog.tsx` (shadcn add)                          |
| Create | `components/ui/switch.tsx` (shadcn add)                          |
| Create | `components/ui/alert-dialog.tsx` (shadcn add)                    |
| Create | `lib/link-icons.ts`                                              |
| Create | `components/links/AddLinkModal.tsx`                              |
| Create | `components/links/LinkList.tsx`                                  |
| Create | `components/links/LinkRow.tsx`                                   |
| Modify | `app/dashboard/page.tsx` (placeholder → lista RSC + empty state) |
| Create | `components/links/EmptyState.tsx`                                |
| Create | `tests/components/features/links/LinkRow.test.tsx`               |
| Create | `tests/components/features/links/AddLinkModal.test.tsx`          |
| Create | `tests/components/features/links/LinkList.test.tsx`              |
| Create | `tests/integration/server-actions/links.test.ts`                 |

## QA Results

**Reviewer:** Quinn (@qa) · **Date:** 2026-05-19 · **Method:** Gate manual (QA-001 — macOS sem WSL/CodeRabbit) · **Commit:** `f41fcbb`

### Gate Decision: ✅ **PASS**

Gate file: `docs/qa/gates/2.5-crud-links.yml`

Auditoria do código real (não apenas do resumo do @dev): `server/links/actions.ts`, `lib/validators/link.ts`, `lib/cache.ts`, `lib/link-icons.ts`, `components/links/{LinkRow,AddLinkModal,LinkList,EmptyState}.tsx`, `app/dashboard/page.tsx`, 5 suítes de teste + matriz de rastreabilidade AC↔teste.

**Re-execução INDEPENDENTE dos 6 quality_gate_tools** (commit `f41fcbb`): `typecheck` ✅ · `lint` ✅ · `build` ✅ · `pnpm test` **203/203 (27 arquivos)** ✅ — **zero regressão** (+33 testes / +5 arquivos vs baseline feat/2.4 170/22 = exatamente as 5 novas suítes 2.5).

| Quality Check       | Status                                                                                                                                                                                                                                    |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Code review         | ✅ PASS — actions espelham `profile/actions.ts` (input:unknown+Zod, `flattenFieldErrors` local, `mapLinkError` sem vazar `error.message`); componentes limpos, a11y correta                                                               |
| Unit tests          | ✅ PASS — `link-input.test.ts` valida Zod↔CHECK do DB                                                                                                                                                                                     |
| Acceptance criteria | ✅ PASS — 8/8 ACs rastreáveis a teste                                                                                                                                                                                                     |
| No regressions      | ✅ PASS — 203/203 em 27 arquivos, zero regressão                                                                                                                                                                                          |
| Performance         | ✅ PASS — queries PK/FK indexados, sem N+1, Client mínimo, sem novas deps                                                                                                                                                                 |
| Security            | ✅ PASS — input:unknown+Zod (sem `as any`), guard `getUser` ×4, RLS ownership (insert WITH CHECK + update/delete via JOIN), `pageId` do auth user (**sem IDOR**), URL display-only (sem XSS/`javascript:`), sem admin import, sem secrets |
| Documentation       | ✅ PASS — Dev Agent Record completo, Change Log v0.1–v0.3, Tasks 100% [x]                                                                                                                                                                 |

**0 blockers · 0 CRITICAL/HIGH.** 3 observações low/info não-bloqueantes: **UX-001** (flash breve de valor antigo no inline-edit durante o round-trip — consistência eventual sub-segundo); **OBS-002** (revalidate degrada graciosamente se 2ª query de username falhar; `/@username` só existe na 2.7); **OBS-003** (`nextPos=max+1` não-atômico — single-user MVP, reorder atômico é Story 2.6 por design arch §7 Q1; 23505 mapeado defensivamente).

**Destaque:** o bug crítico typegen↔runtime (embed reverso `profiles.pages` 1:1 tipado como array, runtime retorna objeto) foi **detectado e corrigido pelo @dev via teste de integração real** — fix type-safe (2 queries diretas, sem casts). Validou a estratégia de testes da story.

**Deviations:** DEV-1..DEV-8 + DEV-correlato (typegen→2 queries) + EmptyState-extract + IDS G3 — **todas APPROVED** (arch canônico / precedente sancionado 2.1/2.3/2.4 — não invenção; já validadas pelo gate @po v0.2, não re-litigadas per convenção 2.3).

**Handoff:** Story pode transicionar `Ready for Review → Done`. Próximo: **`@po *close-story 2.5`** (atualiza Status + Change Log). Downstream: `@devops *push` + `*create-pr` (exclusivo @github-devops; branch `feat/2.5-crud-links` ainda não pushed, sem PR/CI nesta fase). Task 10.2 (smoke manual no browser) permanece `[ ]` por design (handoff owner, não bloqueia Done — padrão 1.5–2.4).
